# Oracle cell — codegraphcontext 0.6.13 on spring-data-elasticsearch, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@2` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/spring-projects/spring-data-elasticsearch, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/spring-data-elasticsearch`, commit cc7bd2b729efbe05092fff16e91ef94feccdd189 — **the same commit and the same answer key as the Hobbes cell [spring-data-elasticsearch-java-2026-08-29.md](spring-data-elasticsearch-java-2026-08-29.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/spring-data-elasticsearch-java/oracle.json` (`cell   oracle javac 21.0.12.1+1-LTS (resolution)  sha cc7bd2b7`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 1, wall 154.8 s; no network observed, no repo build invoked. The tool's own log carries 1 error/warning line(s), e.g. `An error occurred during indexing: Binder exception: Cannot find property source`. Conversion: 10557 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}. Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-spring-data-elasticsearch/edges.json spring-data-elasticsearch-java/oracle.json codegraphcontext-spring-data-elasticsearch --lang java`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-spring-data-elasticsearch` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle javac 21.0.12.1+1-LTS (resolution)  sha cc7bd2b7
hobbes edges 10520: confirmed 8673  contradicted 1846  abstract 0  silent 1 map[not-loaded:1]
precision-against-oracle 82.5% (8673/10519)
recall 35.8% (8743/24452 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 16251; misses map[interface→anonymous-member:510 interface→method:11061 static→anonymous-member:4 static→constructor:2554 static→method:1580]
  recall[interface→anonymous-member]   0.6% (3/513)  misses 510 = 3.2% of all misses
  recall[interface→method  ]  36.9% (6472/17533)  misses 11061 = 70.4% of all misses
  recall[static→anonymous-member]  20.0% (1/5)  misses 4 = 0.0% of all misses
  recall[static→constructor]   0.1% (2/2556)  misses 2554 = 16.3% of all misses
  recall[static→method     ]  58.9% (2265/3845)  misses 1580 = 10.1% of all misses
  tier codegraphcontext:EXTRACTED confirmed 2922  contradicted 563  abstract 0  silent 1
  tier codegraphcontext:INFERRED confirmed 5751  contradicted 1283  abstract 0  silent 0
  line-grain tolerance used on 5084 edge(s) (several oracle sites on one line)
poison check: PASS — 10520 seeded wrong edges: 10519 refused, 1 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 10,520 |
| confirmed | 8,673 |
| contradicted | 1,846 |
| abstract | 0 |
| silent | 1 {"not-loaded": 1} |
| precision-against-oracle (lower bound) | **82.5%** (8,673/10,519) |
| recall | 35.8% (8,743/24,452) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 2922 / contradicted 563 / abstract 0 / silent 1; `codegraphcontext:INFERRED` confirmed 5751 / contradicted 1283 / abstract 0 / silent 0.

## Contradicted (1846 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 1094; same file as an oracle target, other line 752. By label: `codegraphcontext:INFERRED` 1283, `codegraphcontext:EXTRACTED` 563.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/main/java/org/springframework/data/elasticsearch/RestStatusException.java:47` → `src/main/java/org/springframework/data/elasticsearch/RestStatusException.java:46` [codegraphcontext:EXTRACTED] — oracle: toString @ :0
- `src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:224` → `src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:202` [codegraphcontext:EXTRACTED] — oracle: connectedTo @ src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:193
- `src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:224` → `src/main/java/org/springframework/data/elasticsearch/client/ClientConfigurationBuilder.java:68` [codegraphcontext:INFERRED] — oracle: connectedTo @ src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:193
- `src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:224` → `src/main/java/org/springframework/data/elasticsearch/client/ClientConfigurationBuilder.java:81` [codegraphcontext:INFERRED] — oracle: connectedTo @ src/main/java/org/springframework/data/elasticsearch/client/ClientConfiguration.java:193
- `src/main/java/org/springframework/data/elasticsearch/client/elc/AutoCloseableElasticsearchClient.java:45` → `src/main/java/org/springframework/data/elasticsearch/client/elc/AutoCloseableElasticsearchClient.java:41` [codegraphcontext:EXTRACTED] — oracle: close @ :0
- `src/main/java/org/springframework/data/elasticsearch/client/elc/ClusterTemplate.java:44` → `src/main/java/org/springframework/data/elasticsearch/client/elc/ClusterTemplate.java:41` [codegraphcontext:EXTRACTED] — oracle: execute @ src/main/java/org/springframework/data/elasticsearch/client/elc/ChildTemplate.java:66, health @ :0

**Triage ratio (A-8), sampled:** over the tool's Java cells, 5 contradicted rows drawn at random (five contradicted rows per tool per language, drawn at random from the language's cells after the converter@2 regrade; each read against the source by hand) read `oracle-grain 0 : tool-wrong 5 : converter-defect 0`; the rest of this cell's 1846 rows are untriaged. The rows drawn from this cell:
- `src/main/java/org/springframework/data/elasticsearch/core/index/MappingParameters.java:171` → `src/main/java/org/springframework/data/elasticsearch/core/index/MappingParameters.java:550` [codegraphcontext:EXTRACTED]: **tool-wrong** — an annotation element `Field.termVector()` drawn to the caller's own `termVector()`

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `interface→method` | 6,472 / 17,533 | 11,061 |
| `static→constructor` | 2 / 2,556 | 2,554 |
| `static→method` | 2,265 / 3,845 | 1,580 |
| `interface→anonymous-member` | 3 / 513 | 510 |
| `static→anonymous-member` | 1 / 5 | 4 |

**Poison check:** PASS — 10,520 seeded wrong edges: 10,519 refused, 1 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (converter@1 → converter@2, 2026-09-09, signed):** the converter now advances a declaration line past leading annotation / decorator lines to the identifier's (C-94, found by the triage sample: Java `@Override` lines were charged to the tool). graded edges 10,520 → 10,520 (+0); confirmed 6,315 → 8,673 (+2,358); contradicted 4,204 → 1,846 (-2,358); precision-against-oracle 60.0% → 82.5% (+22.4); recall 26.0% → 35.8% (+9.7). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

