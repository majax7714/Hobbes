# Oracle cell — codegraphcontext 0.6.13 on toml, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@1` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/BurntSushi/toml, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/toml`, commit d733fc535e4a9f3c454e421a87b90ade5d49bf31 — **the same commit and the same answer key as the Hobbes cell [toml-go-2026-08-27.md](toml-go-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/toml-go/oracle.json` (`cell .  oracle go-rta (reachability)  sha d733fc53`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 25.1 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 888 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 191 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line (identifier line for Go/Java/Rust/undecorated TS; the decorator line for decorated TS and Python).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-toml/edges.json toml-go/oracle.json codegraphcontext-toml --lang go`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-toml` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle go-rta (reachability)  sha d733fc53
hobbes edges 888: confirmed 812  contradicted 60  abstract 0  silent 16 map[not-loaded:7 unreachable:9]
precision-against-oracle 93.1% (812/872)
recall 56.4% (832/1475 in-repo oracle pairs) at 4 roots; external oracle pairs 2700; misses map[func-value→closure:57 func-value→named:74 interface→named:213 static→closure:58 static→named:241]
  recall[func-value→closure]   0.0% (0/57)  misses 57 = 8.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/74)  misses 74 = 11.5% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]   5.8% (13/226)  misses 213 = 33.1% of all misses
  recall[static→closure    ]   0.0% (0/58)  misses 58 = 9.0% of all misses
  recall[static→named      ]  77.3% (819/1060)  misses 241 = 37.5% of all misses
  tier codegraphcontext:EXTRACTED confirmed 750  contradicted 4  abstract 0  silent 5
  tier codegraphcontext:INFERRED confirmed 62  contradicted 56  abstract 0  silent 11
  line-grain tolerance used on 259 edge(s) (several oracle sites on one line)
poison check: PASS — 888 seeded wrong edges: 872 refused, 16 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 888 |
| confirmed | 812 |
| contradicted | 60 |
| abstract | 0 |
| silent | 16 {"not-loaded": 7, "unreachable": 9} |
| precision-against-oracle (lower bound) | **93.1%** (812/872) |
| recall | 56.4% (832/1,475) at 4 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 750 / contradicted 4 / abstract 0 / silent 5; `codegraphcontext:INFERRED` confirmed 62 / contradicted 56 / abstract 0 / silent 11.

## Contradicted (60 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 59; same file as an oracle target, other line 1. By label: `codegraphcontext:INFERRED` 56, `codegraphcontext:EXTRACTED` 4.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `bench_test.go:116` → `internal/toml-test/runner.go:221` [codegraphcontext:INFERRED] — oracle: (*testing.B).Run @ /home/mmarrujo/.local/go/src/testing/benchmark.go:808
- `bench_test.go:149` → `internal/toml-test/runner.go:221` [codegraphcontext:INFERRED] — oracle: (*testing.B).Run @ /home/mmarrujo/.local/go/src/testing/benchmark.go:808
- `bench_test.go:156` → `internal/toml-test/runner.go:221` [codegraphcontext:INFERRED] — oracle: (*testing.B).Run @ /home/mmarrujo/.local/go/src/testing/benchmark.go:808
- `bench_test.go:43` → `internal/toml-test/runner.go:221` [codegraphcontext:INFERRED] — oracle: (*testing.B).Run @ /home/mmarrujo/.local/go/src/testing/benchmark.go:808
- `bench_test.go:54` → `internal/toml-test/runner.go:221` [codegraphcontext:INFERRED] — oracle: (*testing.B).Run @ /home/mmarrujo/.local/go/src/testing/benchmark.go:808
- `cmd/toml-test-decoder/main.go:38` → `encode.go:136` [codegraphcontext:INFERRED] — oracle: encoding/json.NewEncoder @ /home/mmarrujo/.local/go/src/encoding/json/stream.go:194

**Triage ratio (A-8):** `oracle-wrong 0 : tool-wrong 0 : untriaged 60` — the rows are not read; the number above is a lower bound on the tool's precision exactly as it is for Hobbes (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→named` | 819 / 1,060 | 241 |
| `interface→named` | 13 / 226 | 213 |
| `func-value→named` | 0 / 74 | 74 |
| `static→closure` | 0 / 58 | 58 |
| `func-value→closure` | 0 / 57 | 57 |

**Poison check:** PASS — 888 seeded wrong edges: 872 refused, 16 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

