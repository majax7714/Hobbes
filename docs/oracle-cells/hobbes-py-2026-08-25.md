# Oracle cell — hobbes repo, Python zone `pipeline/`, 2026-08-25 (O6)

Produced by `bench/oracle/run-cell.sh . pipeline <out> --lang py --runs 2 -- -q -p no:cacheprovider` at sha 74939a4b; oracle `py-trace` (CPython 3.12.13 `sys.monitoring`, the pipeline venv, the repo's own suite under its default `HOBBES_SCIP=0`; both runs exit 0 — 907 tests). Union over 2 runs (identical site sets). Cell runtime 129 s without ingest (the trace is ~65 s per suite run against ~50 s untraced); the six-language ingest before it ~10 min. Subprocesses the suite spawns (the CLI tests' `hobbes` invocations, the fake policy binary) are not traced. Triage verdicts below and in `docs/extraction-evidence.md`. The first pass (before H-12, H-14) is kept beside it as `report.first-pass.*`.

```
cell pipeline  oracle py-trace 3.12.13 sys.monitoring (trace)  sha 74939a4b
hobbes edges 3490: confirmed 3291  suspect 10  unobserved 189 map[line-mixed:3 line-not-called:147 not-loaded:39]
confirmation rate 94.3% (3291/3490 hobbes edges; coverage-limited, not precision)
suspect rate 0.3% (10/3301 executed hobbes edges; triage queue, never contradicted)
recall-against-executed 86.2% (3291/3816 observed in-repo pairs) over 2 run(s) of [uv run --project /home/mmarrujo/hobbes_public/pipeline python -m pytest -q -p no:cacheprovider]; external python targets 2649; misses map[observed→closure:348 observed→function:88 observed→lambda:76 observed→method:13]
coverage: hobbes sites observed 2971/3149 (94.3%); files loaded 113/129; declarations started 1668/2048; c-callee calls 1001830; subprocesses traced 0
  recall[observed→class    ] 100.0% (307/307)  misses 0 = 0.0% of all misses
  recall[observed→closure  ]  21.1% (93/441)  misses 348 = 66.3% of all misses
  recall[observed→function ]  96.8% (2657/2745)  misses 88 = 16.8% of all misses
  recall[observed→lambda   ]   0.0% (0/76)  misses 76 = 14.5% of all misses
  recall[observed→method   ]  94.7% (234/247)  misses 13 = 2.5% of all misses
  tier semantic   confirmed 3291  suspect 4  unobserved 189
  tier syntactic  confirmed 0  suspect 6  unobserved 0
  suspect      pipeline/src/hobbes/extract/rustsource.py:383  hobbes pipeline/src/hobbes/extract/rustsource.py:394 (hobbes.extract.rustsource._path_segments)  observed pipeline/src/hobbes/extract/rustsource.py:174 (_text)
  suspect      pipeline/src/hobbes/extract/scipsource.py:1066  hobbes pipeline/src/hobbes/extract/scipsource.py:1089 (hobbes.extract.scipsource._index_cargo_root)  observed pipeline/tests/test_rustsource.py:251 (TestCargoGrouping.test_orphan_directories_get_a_degradation_record.<locals>.<lambda>), pipeline/tests/test_scipsource.py:207 (TestOneUnitFailsAlone.test_a_broken_cargo_root_degrades_alone.<locals>.index)
  suspect      pipeline/src/hobbes/extract/scipsource.py:691  hobbes pipeline/src/hobbes/extract/scipsource.py:1192 (hobbes.extract.scipsource._index_ts_zone)  observed pipeline/tests/test_scipsource.py:170 (TestOneUnitFailsAlone.test_a_broken_ts_zone_degrades_alone.<locals>.index)
  suspect      pipeline/src/hobbes/extract/rustsource.py:383  hobbes pipeline/src/hobbes/extract/rustsource.py:394 (hobbes.extract.rustsource._path_segments)  observed pipeline/src/hobbes/extract/rustsource.py:174 (_text)
  suspect      pipeline/src/hobbes/extract/scipsource.py:1066  hobbes pipeline/src/hobbes/extract/scipsource.py:1089 (hobbes.extract.scipsource._index_cargo_root)  observed pipeline/tests/test_rustsource.py:251 (TestCargoGrouping.test_orphan_directories_get_a_degradation_record.<locals>.<lambda>), pipeline/tests/test_scipsource.py:207 (TestOneUnitFailsAlone.test_a_broken_cargo_root_degrades_alone.<locals>.index)
  suspect      pipeline/src/hobbes/extract/scipsource.py:691  hobbes pipeline/src/hobbes/extract/scipsource.py:1192 (hobbes.extract.scipsource._index_ts_zone)  observed pipeline/tests/test_scipsource.py:170 (TestOneUnitFailsAlone.test_a_broken_ts_zone_degrades_alone.<locals>.index)
  suspect      pipeline/src/hobbes/extract/scipsource.py:869  hobbes pipeline/src/hobbes/extract/scipsource.py:1137 (hobbes.extract.scipsource._index_go_module)  observed pipeline/tests/test_gosource.py:285 (TestGoModuleZoning.test_orphan_directories_get_a_degradation_record.<locals>.<lambda>), pipeline/tests/test_scipsource.py:189 (TestOneUnitFailsAlone.test_a_broken_go_module_degrades_alone.<locals>.index)
  suspect      pipeline/tests/test_graph.py:115  hobbes pipeline/tests/test_graph.py:29 (tests.test_graph.symbol_at)  observed pipeline/tests/test_graph.py:39 (symbol_at.<locals>.look_up)
  suspect      pipeline/tests/test_graph.py:117  hobbes pipeline/tests/test_graph.py:29 (tests.test_graph.symbol_at)  observed pipeline/tests/test_graph.py:39 (symbol_at.<locals>.look_up)
  suspect      pipeline/tests/test_graph.py:119  hobbes pipeline/tests/test_graph.py:29 (tests.test_graph.symbol_at)  observed pipeline/tests/test_graph.py:39 (symbol_at.<locals>.look_up)
  suspect      pipeline/tests/test_graph.py:121  hobbes pipeline/tests/test_graph.py:29 (tests.test_graph.symbol_at)  observed pipeline/tests/test_graph.py:39 (symbol_at.<locals>.look_up)
  suspect      pipeline/tests/test_graph.py:123  hobbes pipeline/tests/test_graph.py:29 (tests.test_graph.symbol_at)  observed pipeline/tests/test_graph.py:39 (symbol_at.<locals>.look_up)
  suspect      pipeline/tests/test_graph.py:129  hobbes pipeline/tests/test_graph.py:29 (tests.test_graph.symbol_at)  observed pipeline/tests/test_graph.py:39 (symbol_at.<locals>.look_up)
  (525 missed rows in report.json: 348 observed→closure, 76 observed→lambda, 88 observed→function, 13 observed→method)
```

**Suspect triage (10).** *Semantic, 4 — all not-exercised:*
`rustsource.py:383` is a one-line conditional (`_path_segments(head) if … else [_text(head)]`) whose `_text` branch alone ran; `scipsource.py:1066/691/869` call `_index_cargo_root` / `_index_ts_zone` / `_index_go_module`, which every test that reaches those lines monkeypatches with a local closure — the observed callees are the test doubles, and Hobbes' target is the real function on the untaken path. *Syntactic (lane A tree-sitter), 6 — all hobbes-wrong:* `test_graph.py:115–129` call `symbol_at(...)`, a **pytest fixture parameter** whose value is the closure the fixture returned; the fallback name-matched the parameter to the fixture function `symbol_at` at line 29, which pytest calls once and this line never does. The C-7 floor, priced on the executed slice: every syntactic Python edge the suite reached was wrong (6/6), every semantic one held (3,291 confirmed, 0 wrong).

**Unobserved (189, charged to nobody).** 147 `line-not-called` (branches the suite never takes), 39 `not-loaded` (`pipeline/scripts/*` — the bench and DeepSWE scripts have no tests — and the miniapp fixture's own files), 3 `line-mixed` (a C or out-of-repo callee was the only thing seen on the line).

**Misses (525, by class; the register is `docs/oracle-misses.md`).**
`observed→closure` 348 and `observed→lambda` 76 — 80.8% — are nested functions and lambdas, which are not graph symbols (C-58's Python face; the top targets are test helpers such as `build_units`, `fake_policy_bin`, `symbol_at`'s `look_up`). `observed→function` 88: 50 are the enrichment packs' `_applies`/`_run` called through the `Pack` record's function-valued fields (`pack.applies(ctx)`), 14 are `cli.main` dispatching `_cmd_*` through argparse's `func` default, the rest smaller function-value tables (`compile_all`, `repeat_verdict`) and calls inside decorator expressions (`helper_available` in a `skipif`). `observed→method` 13: 6 callable instances (`ClaudeRunner.__call__`, `ReviewerSessionRunner.__call__`, a test's `FakeRunner`), 7 receivers scip could not type (an untyped `runtime` parameter, a chain on a constructor, a comprehension over a typed dict's values). Classes: 307/307.

**Coverage line.** The suite spoke about 2,971 of Hobbes' 3,149 Python sites (94.3%); 113/129 module files loaded; 1,668/2,048 declared functions started (81.4%); 1,001,830 C-callee calls; 2,649 out-of-repo Python targets (pytest, stdlib); N = 2.
