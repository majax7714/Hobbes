# Oracle cell — codegraphcontext 0.6.13 on rust_proj), module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@1` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: (local crate, ~/rust_proj), clone `/home/mmarrujo/rust_proj`, commit 64acba88c69f804ea712937d4db2c4ad90456af0 — **the same commit and the same answer key as the Hobbes cell [rust_proj-2026-08-28.md](rust_proj-2026-08-28.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/rust_proj-contained/oracle.json` (`cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 1.7 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 1 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}. Grain note from the adapter: declaration line = the tool's node start line (identifier line for Go/Java/Rust/undecorated TS; the decorator line for decorated TS and Python).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-rust_proj/edges.json rust_proj-contained/oracle.json codegraphcontext-rust_proj --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-rust_proj` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88
hobbes edges 1: confirmed 1  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle 100.0% (1/1)
recall 4.8% (1/21 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 59; misses map[macro→function:4 static→function:16]
  recall[macro→function    ]   0.0% (0/4)  misses 4 = 20.0% of all misses
  recall[static→function   ]   5.9% (1/17)  misses 16 = 80.0% of all misses
  tier codegraphcontext:INFERRED confirmed 1  contradicted 0  abstract 0  silent 0
poison check: PASS — 1 seeded wrong edges: 1 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1 |
| confirmed | 1 |
| contradicted | 0 |
| abstract | 0 |
| silent | 0 {} |
| precision-against-oracle (lower bound) | **100.0%** (1/1) |
| recall | 4.8% (1/21) at 12 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:INFERRED` confirmed 1 / contradicted 0 / abstract 0 / silent 0.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 1 / 17 | 16 |
| `macro→function` | 0 / 4 | 4 |

**Poison check:** PASS — 1 seeded wrong edges: 1 refused, 0 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

