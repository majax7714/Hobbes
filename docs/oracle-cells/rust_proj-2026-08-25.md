# Oracle cell — rust_proj, the whole crate, 2026-08-25 (O7)

Produced by `bench/oracle/run-cell.sh ~/rust_proj . <out> --lang rust` at sha 64acba88 (the crate's only commit); oracle `rustc-mir` on rustc 1.100.0-nightly (e7769602a 2026-08-24) via `cargo check --all-targets` — 8 crate targets (lib `mylib`, `main`, `emulator`, two examples, two benches, two integration tests, plus their `--test` builds). Cell runtime 10 s with ingest (rust-analyzer 1.97.1 `scip`), 4 s regrade.

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88
hobbes edges 17: confirmed 17  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle 100.0% (17/17)
recall 81.0% (17/21 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 59; misses map[macro→function:4]
  recall[macro→function    ]   0.0% (0/4)  misses 4 = 100.0% of all misses
  recall[static→function   ] 100.0% (17/17)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 17  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 9 edge(s) (several oracle sites on one line)
  missed       benches/multi-file-bench/main.rs:11  benches -> benches/multi-file-bench/main.rs:4 (criterion_benchmark) [macro→function]
  missed       benches/multi-file-bench/main.rs:12  main -> benches/multi-file-bench/main.rs:11 (benches) [macro→function]
  missed       benches/simple_bench.rs:8  benches -> benches/simple_bench.rs:3 (criterion_benchmark) [macro→function]
  missed       benches/simple_bench.rs:9  main -> benches/simple_bench.rs:8 (benches) [macro→function]
```

**ADR-040's 33/33.** The hand-check counted symbol edges: the graph has **17 `calls` + 16 `uses`**. The oracle grades calls; all 17 are confirmed by the compiler (P15 met on every call edge of the set; the `uses` edges are not calls and were never in scope).

**Misses (4), all `macro→function`.** `criterion_group!(benches, criterion_benchmark)` expands to a `fn benches()` that calls `criterion_benchmark`, and `criterion_main!(benches)` to a `main` that calls `benches` — calls the dependency's macro body makes at the invocation line; the author wrote a function *name* as a macro argument. Hobbes draws no edge (rust-analyzer emits the reference, not a call) and has no symbol for the macro-made `benches`. Registered as its own class so it is never read as a lost direct call.

**Poison check:** PASS — 17 seeded wrong edges: 17 refused, 0 unjudged, 0 falsely confirmed.
