# Oracle cell — repowise 0.49.0 on mux, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@1` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/gorilla/mux, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/mux`, commit db9d1d0073d27a0a2d9a8c1bc52aa0af4374d265 — **the same commit and the same answer key as the Hobbes cell [mux-go-2026-08-27.md](mux-go-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/mux-go-r2/oracle.json` (`cell .  oracle go-rta (reachability)  sha db9d1d00`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 4.4 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 963 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line (the symbol's first line as parsed).

Command: `bench/oracle/grade-foreign.sh repowise-mux/edges.json mux-go-r2/oracle.json repowise-mux --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-mux` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle go-rta (reachability)  sha db9d1d00
hobbes edges 963: confirmed 665  contradicted 250  abstract 3  silent 45 map[unreachable:45]
precision-against-oracle 72.7% (665/915)
recall 45.3% (676/1492 in-repo oracle pairs) at 1 roots; external oracle pairs 1270; misses map[func-value→closure:89 func-value→named:5 interface→named:120 static→closure:45 static→named:557]
  recall[func-value→closure]   0.0% (0/89)  misses 89 = 10.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/5)  misses 5 = 0.6% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]   0.0% (0/120)  misses 120 = 14.7% of all misses
  recall[static→closure    ]   0.0% (0/45)  misses 45 = 5.5% of all misses
  recall[static→named      ]  54.8% (676/1233)  misses 557 = 68.3% of all misses
  tier repowise:package_alias confirmed 4  contradicted 0  abstract 0  silent 2
  tier repowise:receiver_typed_import confirmed 28  contradicted 1  abstract 0  silent 0
  tier repowise:receiver_typed_same_file confirmed 35  contradicted 0  abstract 0  silent 1
  tier repowise:same_file confirmed 267  contradicted 6  abstract 1  silent 0
  tier repowise:same_package confirmed 331  contradicted 243  abstract 2  silent 42
  line-grain tolerance used on 397 edge(s) (several oracle sites on one line)
poison check: PASS — 963 seeded wrong edges: 918 refused, 45 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 963 |
| confirmed | 665 |
| contradicted | 250 |
| abstract | 3 |
| silent | 45 {"unreachable": 45} |
| precision-against-oracle (lower bound) | **72.7%** (665/915) |
| recall | 45.3% (676/1,492) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:package_alias` confirmed 4 / contradicted 0 / abstract 0 / silent 2; `repowise:receiver_typed_import` confirmed 28 / contradicted 1 / abstract 0 / silent 0; `repowise:receiver_typed_same_file` confirmed 35 / contradicted 0 / abstract 0 / silent 1; `repowise:same_file` confirmed 267 / contradicted 6 / abstract 1 / silent 0; `repowise:same_package` confirmed 331 / contradicted 243 / abstract 2 / silent 42.

## Contradicted (250 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 129; same file as an oracle target, other line 121. By label: `repowise:same_package` 243, `repowise:same_file` 6, `repowise:receiver_typed_import` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `example_cors_method_middleware_test.go:14` → `mux.go:360` [repowise:same_package] — oracle: (*github.com/gorilla/mux.Router).HandleFunc @ mux.go:335
- `example_cors_method_middleware_test.go:17` → `mux.go:360` [repowise:same_package] — oracle: (*github.com/gorilla/mux.Router).HandleFunc @ mux.go:335
- `example_cors_method_middleware_test.go:32` → `mux.go:232` [repowise:same_package] — oracle: fmt.Println @ /home/mmarrujo/.local/go/src/fmt/print.go:314, (*net/http/httptest.ResponseRecorder).Header @ /home/mmarrujo/.local/go/src/net/http/httptest/recorder.go:67, (net/http.Header).Get @ /home/mmarrujo/.local/go/src/net/http/header.go:49
- `example_cors_method_middleware_test.go:33` → `mux.go:232` [repowise:same_package] — oracle: fmt.Println @ /home/mmarrujo/.local/go/src/fmt/print.go:314, (*net/http/httptest.ResponseRecorder).Header @ /home/mmarrujo/.local/go/src/net/http/httptest/recorder.go:67, (net/http.Header).Get @ /home/mmarrujo/.local/go/src/net/http/header.go:49
- `middleware_test.go:122` → `mux.go:360` [repowise:same_package] — oracle: (*github.com/gorilla/mux.Router).HandleFunc @ mux.go:335, (*github.com/gorilla/mux.Route).Methods @ route.go:398
- `middleware_test.go:125` → `mux.go:360` [repowise:same_package] — oracle: (*github.com/gorilla/mux.Router).HandleFunc @ mux.go:335, (*github.com/gorilla/mux.Route).Methods @ route.go:398

**Triage ratio (A-8):** `oracle-wrong 0 : tool-wrong 0 : untriaged 250` — the rows are not read; the number above is a lower bound on the tool's precision exactly as it is for Hobbes (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→named` | 676 / 1,233 | 557 |
| `interface→named` | 0 / 120 | 120 |
| `func-value→closure` | 0 / 89 | 89 |
| `static→closure` | 0 / 45 | 45 |
| `func-value→named` | 0 / 5 | 5 |

**Poison check:** PASS — 963 seeded wrong edges: 918 refused, 45 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

