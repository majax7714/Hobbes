# Oracle cell — BurntSushi/toml, module `.`, 2026-08-27

Repo: https://github.com/BurntSushi/toml (MIT), clone at `~/.hobbes/bench/oracle/repos/toml`, commit d733fc535e4a9f3c454e421a87b90ade5d49bf31 (shallow). Picked because it is a well-known, dependency-free Go library of ~10.5k lines in one `go.mod`, with three `cmd/` binaries and a test main giving RTA roots, and it builds with plain `go build ./...` on this box. Ingest SHA d733fc53 (the ingest reports a dirty tree: `hobbes ingest` appends `.hobbes/` to the clone's `.gitignore`, ADR-012 — nothing else changed). Hobbes at 9c474c6, edges from `.hobbes/derived/graph.json` (lane B on, `HOBBES_SCIP=1`). Oracle go-rta (x/tools v0.49.0), default build tags, roots = 4 (`toml.test`, `cmd/toml-test-decoder`, `cmd/toml-test-encoder`, `cmd/tomlv`).

Command: `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/toml . ~/.hobbes/bench/oracle/toml-go --lang go`. Runtime 69 s (ingest + RTA + grade, one language). Outputs in `~/.hobbes/bench/oracle/toml-go/` (`hobbes.json`, `oracle.json`, `report.json`, `report.txt`).

## Numbers (report.txt, verbatim)

```
cell .  oracle go-rta (reachability)  sha d733fc53
hobbes edges 1047: confirmed 1039  contradicted 0  abstract 0  silent 8 map[unreachable:8]
precision-against-oracle 100.0% (1039/1039)
recall 71.9% (1060/1475 in-repo oracle pairs) at 4 roots; external oracle pairs 2700; misses map[func-value→closure:57 func-value→named:74 interface→named:226 static→closure:58]
  recall[func-value→closure]   0.0% (0/57)  misses 57 = 13.7% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/74)  misses 74 = 17.8% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]   0.0% (0/226)  misses 226 = 54.5% of all misses
  recall[static→closure    ]   0.0% (0/58)  misses 58 = 14.0% of all misses
  recall[static→named      ] 100.0% (1060/1060)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 1039  contradicted 0  abstract 0  silent 8
  line-grain tolerance used on 308 edge(s) (several oracle sites on one line)
poison check: PASS — 1047 seeded wrong edges: 1039 refused, 8 unjudged (oracle silent there), 0 falsely confirmed
cell . of /home/mmarrujo/.hobbes/bench/oracle/repos/toml: 69s
```

| bucket | count |
|---|---|
| hobbes edges | 1047 |
| confirmed | 1039 |
| contradicted | 0 |
| silent (unreachable from the 4 roots) | 8 |
| precision-against-oracle | 100.0% (1039/1039) |
| in-repo oracle pairs | 1475 |
| recall at 4 roots | 71.9% (1060/1475) |
| external oracle pairs (not graded) | 2700 |
| tiers | semantic only (1039 / 0 / 8); no syntactic or dynamic edges in this cell |

## Misses by class (415 rows, all in report.txt / report.json)

- **interface→named, 226 (54.5%).** Method calls through an interface value, resolved by RTA to every reachable concrete implementation. Concentrated by site file: `internal/toml-test/runner.go` 64, `decode.go` 46, `toml_test.go` 30, `decode_test.go` 30, `parse.go` 20, `encode.go` 17. The dominant callees are `Error()` on the error types in `error.go` (`ParseError`, `errParseDate`, `errLexUTF8`, `errParseRange`, … — 15 sites each) and `internal/toml-test.timeoutError.Error` (15), plus `UnmarshalTOML` / `UnmarshalText` implementations reached from `(*MetaData).unify` at `decode.go:248` (e.g. `decode_test.go:1013 (*InnerInt).UnmarshalTOML`).
- **static→closure, 58 (14.0%).** Direct calls of a locally bound `func` literal, e.g. `decode.go:202 (*MetaData).PrimitiveDecode -> PrimitiveDecode$1`, `decode_test.go:1155 TestDecodeParallel -> TestDecodeParallel$1`, `decode_test.go:1257 TestMaxDepth -> TestMaxDepth$1`. 40 of the 58 sites are in `decode_test.go`.
- **func-value→named, 74 (17.8%; upper bound, oracle over-approximates).** Calls through a function-typed value to a named function. 42 are the lexer's state-function loop: `lex.go:85 (*lexer).nextItem -> lexDecimalNumber / lexDecimalNumberStart / …` and `lex.go:228 (*lexer).skip -> isWhitespace`; the rest are `internal/toml-test/runner.go` (16) and the two `cmd/toml-test-*` mains (8 each).
- **func-value→closure, 57 (13.7%; upper bound, oracle over-approximates).** Calls through a function-typed value to every reachable closure of matching signature — `internal/toml-test/runner.go` 26, `cmd/toml-test-decoder` 13, `cmd/toml-test-encoder` 13 (e.g. `cmd/toml-test-decoder/main.go:30 main -> encode_test.go:623 TestEncodeTextMarshaler$1`), and `encode.go:492/504 eStruct -> eStruct$1`.
- **static→named, 0 misses** (1060/1060).

**Poison check:** PASS — 1047 seeded wrong edges: 1039 refused, 8 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (which side would need to change; no proposals):** `interface→named` — Hobbes (the oracle's targets are concrete implementations the compiler knows; Hobbes emits nothing at these sites). `static→closure` — Hobbes (a direct call to a func literal is statically resolvable; Hobbes has no callee). `func-value→named` and `func-value→closure` — the oracle first (the reachability oracle over-approximates function values, so the count is an upper bound), with any residue Hobbes. Contradicted 0, silent 8 — nothing to fix on either side.

**Not graded:** the 2700 external oracle pairs (stdlib / out-of-repo callees, by design); the 8 `unreachable` silent edges (RTA did not reach them from the 4 roots). No repo was abandoned.
