# Oracle cell — repowise 0.49.0 on rust_proj), module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@1` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: (local crate, ~/rust_proj), clone `/home/mmarrujo/rust_proj`, commit 64acba88c69f804ea712937d4db2c4ad90456af0 — **the same commit and the same answer key as the Hobbes cell [rust_proj-2026-08-28.md](rust_proj-2026-08-28.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/rust_proj-contained/oracle.json` (`cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 3.3 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 0 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line (the symbol's first line as parsed).

Command: `bench/oracle/grade-foreign.sh repowise-rust_proj/edges.json rust_proj-contained/oracle.json repowise-rust_proj --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-rust_proj` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88
hobbes edges 0: confirmed 0  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle: undefined (no confirmed or contradicted edges)
recall 0.0% (0/21 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 59; misses map[macro→function:4 static→function:17]
  recall[macro→function    ]   0.0% (0/4)  misses 4 = 19.0% of all misses
  recall[static→function   ]   0.0% (0/17)  misses 17 = 81.0% of all misses
poison check: FAIL — 0 seeded wrong edges: 0 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 0 |
| confirmed | 0 |
| contradicted | 0 |
| silent | 0 |
| precision-against-oracle | **undefined** — nothing confirmed or contradicted (the tool stored 0 call edge(s) in this cell) |
| recall | 0.0% (0/21) |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): .

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 0 / 17 | 17 |
| `macro→function` | 0 / 4 | 4 |

**Poison check:** FAIL — 0 seeded wrong edges: 0 refused, 0 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

