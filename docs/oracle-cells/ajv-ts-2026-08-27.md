# Oracle cell — ajv-validator/ajv, zone `.`, 2026-08-27

Repo: https://github.com/ajv-validator/ajv (MIT), clone at `~/.hobbes/bench/oracle/repos/ajv`, commit f177fe323420ccb23e1a79445fd470cbf80aee7c. Picked because it is a well-known, mid-popularity TypeScript library of ~9.9k lines across 106 `.ts` files under `lib/`, one root `tsconfig.json` (`include: ["lib"]`, extends `@ajv-validator/config`), pinning its own `typescript` 5.3.3, and `tsc --noEmit` passes on this box. This is the repo on which the TS oracle hung (H-17, `docs/oracle-defects.md`); this record is the regrade after that fix. Ingest SHA f177fe32 (ingest at Hobbes 9c474c6 with lane B on, `HOBBES_SCIP=1`; the ingest appends `.hobbes/` to the clone's `.gitignore`, ADR-012 — nothing else in the clone changed). Oracle = the zone's own `tsc` 5.3.3 (`node_modules/typescript`), resolution oracle, no roots; 123 files loaded, 2,607 call sites (2,467 static, 140 dynamic). Oracle run at Hobbes HEAD 0197636f (after the H-17 fix).

Command: `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/ajv . ~/.hobbes/bench/oracle/ajv-ts --lang ts --no-ingest`. Runtime 2 s (export + `tsc` oracle + grade; ingest not included). Outputs in `~/.hobbes/bench/oracle/ajv-ts/` (`hobbes.json`, `oracle.json`, `report.json`, `report.txt`).

## Numbers (report.txt, verbatim)

```
cell .  oracle tsc 5.3.3 (the zone's own) (resolution)  sha f177fe32
hobbes edges 1543: confirmed 1375  contradicted 3  abstract 0  silent 165 map[not-loaded:165]
precision-against-oracle 99.8% (1375/1378)
recall 62.0% (1383/2232 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 482; misses map[func-value→local-binding:13 interface→type-member:12 static→class:107 static→closure:174 static→function:491 static→method:35 static→type-member:17]
  recall[func-value→local-binding]   0.0% (0/13)  misses 13 = 1.5% of all misses
  recall[func-value→variable] 100.0% (1/1)  misses 0 = 0.0% of all misses
  recall[interface→type-member]   0.0% (0/12)  misses 12 = 1.4% of all misses
  recall[static→class      ]   0.0% (0/107)  misses 107 = 12.6% of all misses
  recall[static→closure    ]   0.0% (0/174)  misses 174 = 20.5% of all misses
  recall[static→function   ]  50.3% (496/987)  misses 491 = 57.8% of all misses
  recall[static→method     ]  96.1% (873/908)  misses 35 = 4.1% of all misses
  recall[static→type-member]   0.0% (0/17)  misses 17 = 2.0% of all misses
  recall[static→variable   ] 100.0% (13/13)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 1373  contradicted 3  abstract 0  silent 153
  tier syntactic  confirmed 2  contradicted 0  abstract 0  silent 12
  line-grain tolerance used on 518 edge(s) (several oracle sites on one line)
  contradicted lib/compile/codegen/index.ts:175  hobbes lib/compile/codegen/index.ts:234 (lib/compile/codegen/index.If.render)  oracle .../node_modules/typescript/lib/lib.es5.d.ts:1480 (Array.reduce), .../lib.es5.d.ts:1481 (Array.reduce), .../lib.es5.d.ts:1487 (Array.reduce), lib/compile/codegen/index.ts:52 (Def.render)
  contradicted lib/compile/codegen/index.ts:182  hobbes lib/compile/codegen/index.ts:240 (lib/compile/codegen/index.If.optimizeNodes)  oracle lib/compile/codegen/index.ts:30 (Node.optimizeNodes)
  contradicted lib/compile/codegen/index.ts:196  hobbes lib/compile/codegen/index.ts:258 (lib/compile/codegen/index.If.optimizeNames)  oracle lib/compile/codegen/index.ts:58 (Def.optimizeNames)
poison check: PASS — 1543 seeded wrong edges: 1378 refused, 165 unjudged (oracle silent there), 0 falsely confirmed
cell . of /home/mmarrujo/.hobbes/bench/oracle/repos/ajv: 2s
```

(The three `contradicted` rows are quoted with the absolute `node_modules` prefix elided; report.txt carries the full paths.)

| bucket | count |
|---|---|
| hobbes edges | 1543 |
| confirmed | 1375 |
| contradicted | 3 |
| silent (`not-loaded`) | 165 |
| precision-against-oracle (lower bound, A-8) | 99.8% (1375/1378) |
| in-repo oracle pairs | 2232 |
| recall (resolution oracle, no roots) | 62.0% (1383/2232) |
| external oracle pairs (not graded) | 482 |
| tiers | semantic 1373 / 3 / 153; syntactic 2 / 0 / 12 |
| line-grain tolerance | 518 edges |
| oracle-silent sites | 1 `computed-key` |

**Triage ratio (contradicted rows) oracle-wrong : hobbes-wrong : untriaged = 0 : 0 : 3.** The three rows are all in `lib/compile/codegen/index.ts` (sites 175, 182, 196): Hobbes names the `If` subclass's own `render` / `optimizeNodes` / `optimizeNames` (lines 234 / 240 / 258); the oracle names `Def.render` + `Array.reduce`, `Node.optimizeNodes` (line 30) and `Def.optimizeNames` (line 58). Not triaged here.

**Computed-key site: 1** — `lib/core.ts:770`, caller `checkOptions`, `this.logger[log](...)`; oracle mode `dynamic`, no targets, silent as `computed-key` (D-O4). This is the H-17 shape; it is graded as silent, not as a miss or a contradiction. Hobbes emits no edge there (C-63).

## Misses by class (849 rows, all in report.txt / report.json)

Every miss sits under `lib/`; every confirmed edge sits under `lib/` too (the zone's `tsconfig.json` includes only `lib`).

- **static→function, 491 (57.8%).** Direct calls to a named function, 7 distinct targets. By callee: `lib/compile/codegen/code.ts` `_` 372, `lib/compile/codegen/index.ts` `not` 58, `code.ts` `str` 48, `index.ts` `optimizeExpr` 12 (the oracle reports both lines 791 and 792 for the overloaded `optimizeExpr`), `lib/compile/errors.ts` `reportError` 1. By site file: `lib/compile/jtd/parse.ts` 54, `lib/compile/validate/index.ts` 44, `lib/compile/validate/dataType.ts` 35, `lib/compile/jtd/serialize.ts` 33, `lib/compile/errors.ts` 25, `lib/vocabularies/code.ts` 21, `lib/compile/codegen/index.ts` 20, `lib/compile/util.ts` 18. The class's other half (496/987) is confirmed.
- **static→closure, 174 (20.5%).** Direct calls of a function declared inside another function, 114 distinct targets. Largest: `lib/runtime/parseJson.ts` `errorMessage` 8, `lib/compile/codegen/index.ts` `forBody` 4, `lib/standalone/index.ts` `validateCode` 4, `lib/vocabularies/applicator/if.ts` `validateClause` 4, `lib/compile/validate/keyword.ts` `assignValid` 3, `parseJson.ts` `parseDigits` 3, `lib/vocabularies/dynamic/dynamicRef.ts` `_callRef` 3, `lib/vocabularies/unevaluated/unevaluatedProperties.ts` `unevaluatedPropCode` 3. By site file: `lib/runtime/parseJson.ts` 11, `lib/standalone/index.ts` 11, `lib/compile/jtd/serialize.ts` 10, `lib/vocabularies/jtd/properties.ts` 10, then 9 each in `codegen/index.ts`, `validate/index.ts`, `core/ref.ts`, `format/format.ts`.
- **static→class, 107 (12.6%).** `new X(...)` sites resolved by the oracle to the class, 25 distinct targets: `lib/compile/codegen/code.ts` `Name` 29 and `_Code` 22, `lib/compile/codegen/index.ts` `ParentNode` 12 (the `super(...)` calls of its subclasses at 231/283/308/330/355/412/467 and the `new ParentNode` at 247/586), `lib/compile/index.ts` `SchemaEnv` 9, `lib/compile/ref_error.ts` `MissingRefError` 5, `lib/core.ts` `Ajv` 3 (`lib/2019.ts:15`, `lib/2020.ts:12`), `If` / `CodeGen` 3 each, `Assign` / `Scope` 2 each. By site file: `lib/compile/codegen/index.ts` 42, `lib/compile/names.ts` 16, `lib/compile/codegen/scope.ts` 9, `lib/compile/codegen/code.ts` 8, `lib/compile/errors.ts` 7. Recall for the class is 0/107.
- **static→method, 35 (4.1%).** 17 distinct targets: `lib/core.ts` `compile` 24 and `validate` 8 (site files `lib/core.ts` 20, `lib/standalone/index.ts` 6, `lib/standalone/instance.ts` 6), plus the three `codegen/index.ts` sites (175 / 182 / 196) that are also the contradicted rows above, where the oracle's target is `Def.render`, `Node.optimizeNodes`, `Def.optimizeNames`. The rest of the class (873/908) is confirmed.
- **static→type-member, 17 (2.0%).** Calls whose oracle target is an interface/type member, 5 distinct: `lib/core.ts` `Logger.warn` 7 and `Logger.error` 5, `lib/types/index.ts` `parse` 2, `resolve` 2, `serialize` 1. Site files: `lib/core.ts` 5, `lib/compile/resolve.ts` 4, `lib/compile/index.ts` 2, one each in five others.
- **func-value→local-binding, 13 (1.5%).** Calls through a locally bound function value, 11 distinct targets: `lib/compile/util.ts` `mergeToName` 2, `mergeNames`, `mergeValues`, `resultToName` 1 each; `lib/compile/errors.ts` `params` / `message`; `lib/compile/index.ts` `makeValidate`; `lib/compile/jtd/parse.ts` `makeParse`; `lib/compile/jtd/serialize.ts` `makeSerialize`; `lib/core.ts` `v`.
- **interface→type-member, 12 (1.4%).** 5 distinct: `lib/compile/util.ts` `props` 4 and `items` 4, `lib/types/index.ts` `code` 2, `validateSchema` 1, `lib/core.ts` `process` 1. Site files: `lib/vocabularies/core/ref.ts` 4, `lib/compile/validate/index.ts` 3, `lib/vocabularies/applicator/properties.ts` 2.
- **func-value→variable 0 misses** (1/1); **static→variable 0 misses** (13/13).

**Silent, 165 (`not-loaded`).** Hobbes edges whose site file the oracle did not load under the zone's `tsconfig.json` (`include: ["lib"]`): `spec/` 156 (`spec/codegen.spec.ts` 58, `spec/async_validate.spec.ts` 28, `spec/jtd-schema.spec.ts` 14, `spec/json-schema.spec.ts` 13, `spec/options/strict.spec.ts` 8, …), `benchmark/jtd.js` 4, `rollup.config.js` 4, `scripts/get-contributors.js` 1. The 12 syntactic-tier silent edges are all in the `.js` files plus three `spec/async_*.spec.ts` sites; `spec/` has its own `spec/tsconfig.json` and was not a zone in this cell. Charged to nobody.

**Poison check:** PASS — 1543 seeded wrong edges: 1378 refused, 165 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (which side would need to change; no proposals):** `static→function`, `static→closure`, `static→class`, `static→method` (the 32 `compile` / `validate` rows), `static→type-member`, `interface→type-member`, `func-value→local-binding` — Hobbes (the compiler resolves each site to a definition in the repo; Hobbes emits nothing there). Contradicted 3 — untriaged; whichever side is wrong at its own grain (the oracle's target is the base-class member, Hobbes' is the subclass override), it is the same site on both, so one side changes. Computed-key 1 — neither (silent by rule D-O4). Silent 165 — neither (files outside the zone).

**Not graded:** the 482 external oracle pairs (`lib.es5.d.ts`, `node_modules` callees, by design); the 165 `not-loaded` silent edges (`spec/`, `benchmark/`, `scripts/`, `rollup.config.js`); the one `computed-key` site. No repo was abandoned; this cell was fixed to ajv by assignment.
