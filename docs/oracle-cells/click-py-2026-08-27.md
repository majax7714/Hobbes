# Oracle cell — pallets/click, Python zone `.`, 2026-08-27

**Repo:** `pallets/click` (BSD-3-Clause) at `36baa15ff831b939a22bc527cd76ce653ef6f66d` (shallow clone, `~/.hobbes/bench/oracle/repos/click`). Picked because it is a well-known mid-size library (12.7k lines under `src/click`, 14.8k of tests), a single pytest-run package with no runtime dependencies, and its suite runs in ~3 s untraced under `uv sync --group tests`. **Hobbes at** `22379be`; ingest reported `36baa15ff831 (dirty tree)` — the only dirt is the `.gitignore` line the ingest itself adds (ADR-012). Oracle `py-trace` (CPython 3.12.13 `sys.monitoring`, the clone's own uv venv on 3.12, the repo's own suite). Both runs exit 0 — `1991 passed, 24 skipped, 31000 deselected, 1 xfailed`. Union over 2 runs.

**Command:** `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/click . ~/.hobbes/bench/oracle/click-py --lang py --runs 2 -- -q -p no:cacheprovider`. **Runtime:** 47 s for the whole cell including the single-language lane B ingest (each traced suite run ~14.4 s against ~3 s untraced). Subprocesses the suite spawns are not traced (0). Output dir `~/.hobbes/bench/oracle/click-py/` (`hobbes.json`, `oracle.json`, `oracle.run1.json`, `oracle.run2.json`, `report.json`, `report.txt`).

Ingest capture line, for the record: `graph.json: 148 nodes, 597 module edges, 1896 symbols, 3750 call edges`; `capture [python]: 72.3% of 4713 detected call sites accounted` — cannot resolve 958 (attr-call 814, import-binding 107, fallback-resolved 24, unclassified 13); `tests` directory 66.9% of 2341 sites (attr-call 629), `src/click` 78.6% of 1825.

```
cell   oracle py-trace 3.12.13 sys.monitoring (trace)  sha 36baa15f
hobbes edges 2003: confirmed 1623  suspect 85  unobserved 295 map[line-mixed:11 line-not-called:117 not-loaded:167]
confirmation rate 81.0% (1623/2003 hobbes edges; coverage-limited, not precision)
suspect rate 5.0% (85/1708 executed hobbes edges; triage queue, never contradicted)
recall-against-executed 35.3% (1623/4595 observed in-repo pairs) over 2 run(s) of [uv run --project /home/mmarrujo/.hobbes/bench/oracle/repos/click/. python -m pytest -q -p no:cacheprovider]; external python targets 1524; misses map[observed→class:149 observed→closure:1196 observed→function:804 observed→lambda:81 observed→method:742]
coverage: hobbes sites observed 1603/1875 (85.5%); files loaded 51/79; declarations started 1402/1949; c-callee calls 518748; subprocesses traced 0
  recall[observed→class    ]  76.0% (472/621)  misses 149 = 5.0% of all misses
  recall[observed→closure  ]   2.5% (31/1227)  misses 1196 = 40.2% of all misses
  recall[observed→function ]  45.7% (678/1482)  misses 804 = 27.1% of all misses
  recall[observed→lambda   ]   0.0% (0/81)  misses 81 = 2.7% of all misses
  recall[observed→method   ]  37.3% (442/1184)  misses 742 = 25.0% of all misses
  tier semantic   confirmed 1617  suspect 85  unobserved 277
  tier syntactic  confirmed 6  suspect 0  unobserved 18
  (85 suspect rows and 2972 missed rows in report.json: 1196 observed→closure, 804 observed→function, 742 observed→method, 149 observed→class, 81 observed→lambda)
```

| | edges | confirmed | suspect | unobserved | rate |
|---|---|---|---|---|---|
| all | 2003 | 1623 | 85 | 295 | confirmation 81.0%, suspect 5.0% of 1708 executed |
| semantic | 1979 | 1617 | 85 | 277 | |
| syntactic | 24 | 6 | 0 | 18 | |
| recall-against-executed | | 1623 / 4595 observed in-repo pairs | | | 35.3% |

**Poison check:** PASS — 2003 seeded wrong edges: 1535 refused (suspect), 468 unjudged (oracle silent there), 0 falsely confirmed.

**Suspects (85; triage queue, never contradicted).** By site file: `src/click/core.py` 18, `tests/test_context.py` 13, `tests/test_termui.py` 9, `src/click/decorators.py` 8, the rest spread over 15 files. By shape, counted from the rows: **47** name the same function as the observed callee but at a different line in the same file — the Hobbes target is the first `@t.overload` stub and the trace reports the implementation body (`Context.invoke` 850 vs 857, `convert_type` 1332 vs 1341, `termui.prompt` 139 vs 168, `get_current_context` 13 vs 20, `Group.command` 1792 vs 1799, `Group.group` 1841 vs 1848, `progressbar` 403 vs 443, `decorators.command` 138 vs 168, `decorators.group` 263 vs 293); **17** name a base-class method where the trace observed an override (`Parameter.to_info_dict / add_to_parser / get_help_record / _parse_decls / get_default` → `Option.*` / `Argument.*` / a test's `CustomOption`; `Context.lookup_default` → a test's `CustomContext`); **21** mix the two shapes or are monkeypatched at the observed line (`BashComplete._check_version` → `tests/test_shell_completion.py:345 _patch_for_completion`, `termui.getchar` → a test lambda, `termui.prompt` → a test closure). Not triaged further here.

**Unobserved (295, charged to nobody).** 167 `not-loaded`: 122 in `examples/*` (termui 39, imagepipe 23, repo 16, aliases 13, colors 10, completion 6, validation 6, naval 5, complex 4), 30 in `tests/typing/` (type-check-only files the suite does not import), 15 in `src/click`. 117 `line-not-called`: 56 in `src/click`, 61 across the test files. 11 `line-mixed` (5 in `tests/test_termui.py`).

**Misses (2972, by class; the register is `docs/oracle-misses.md`).** Caller side: 2238 in `tests/`, 734 in `src/`.
- `observed→closure` 1196 (40.2%): nested functions, overwhelmingly the decorator factories' inner `decorator` — `command.<locals>.decorator` 342, `option.<locals>.decorator` 221, `argument.<locals>.decorator` 101, `Group.command.<locals>.decorator` 60, `Group.group.<locals>.decorator` 8, `make_pass_decorator.<locals>.decorator` 7; plus `_make_cached_stream_func.<locals>.func` 4, `Command.get_params.<locals>.<genexpr>` 4.
- `observed→function` 804 (27.1%): the decorator factories themselves called at decoration sites — `command` 308, `option` 207, `argument` 98, `group` 46, `pass_context` 42 — then `get_current_context` 19, `prompt` 15, `progressbar` 11.
- `observed→method` 742 (25.0%): `CliRunner.invoke` 374, `Group.command` 63, `ProgressBar.__exit__` 18, `_PagerWriter.write` 17, `Context.__exit__` 14, `Context.invoke` 11, `BytesIOCopy.write` 9, `ParamType.to_info_dict` 9.
- `observed→class` 149 (5.0%): `IntRange` 21, `Choice` 20, `File` 17, `Path` 12, `FloatRange` 8, `Option` 4, `DateTime` 4, `Tuple` 3.
- `observed→lambda` 81 (2.7%): bare `<lambda>` 29, the rest test-local lambdas (pager/echo tests).

**Coverage line.** The suite spoke about 1,603 of Hobbes' 1,875 Python sites (85.5%); 51/79 module files loaded; 1,402/1,949 declared functions started (71.9%); 518,748 C-callee calls; 1,524 out-of-repo Python targets; N = 2 (identical pass counts both runs).

**Direction of fix (which side would move; no proposals).**
- `observed→closure` and `observed→lambda` (1277): Hobbes — nested functions and lambdas are not graph symbols (C-58's Python face); the oracle reports them as it sees them.
- `observed→function` (804) and `observed→method` (742): Hobbes — the observed pairs are real executions at sites the ingest's own capture line lists as unresolved (attr-call / import-binding).
- `observed→class` (149): Hobbes — constructor calls the trace saw and the graph carries no edge for.
- Suspects, overload-stub shape (47): the oracle lane's matcher (the trace names the implementation line; Hobbes names the first declaration of the same symbol) — or Hobbes' target line, whichever side the lane decides owns overload resolution; not both.
- Suspects, base-vs-override and monkeypatch shapes (38): neither charged — trace-asymmetry; stays in the triage queue.
- Unobserved (295): nobody — coverage, not a defect on either side.

**Not graded:** none abandoned; `click` was the first pick and graded on the first run.
