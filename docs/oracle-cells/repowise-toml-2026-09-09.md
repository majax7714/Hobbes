# Oracle cell — repowise 0.49.0 on toml, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@1` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/BurntSushi/toml, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/toml`, commit d733fc535e4a9f3c454e421a87b90ade5d49bf31 — **the same commit and the same answer key as the Hobbes cell [toml-go-2026-08-27.md](toml-go-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/toml-go/oracle.json` (`cell .  oracle go-rta (reachability)  sha d733fc53`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 7.7 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 990 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line (the symbol's first line as parsed).

Command: `bench/oracle/grade-foreign.sh repowise-toml/edges.json toml-go/oracle.json repowise-toml --lang go`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-toml` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle go-rta (reachability)  sha d733fc53
hobbes edges 990: confirmed 915  contradicted 66  abstract 0  silent 9 map[not-loaded:1 unreachable:8]
precision-against-oracle 93.3% (915/981)
recall 63.5% (936/1475 in-repo oracle pairs) at 4 roots; external oracle pairs 2700; misses map[func-value→closure:57 func-value→named:74 interface→named:226 static→closure:58 static→named:124]
  recall[func-value→closure]   0.0% (0/57)  misses 57 = 10.6% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/74)  misses 74 = 13.7% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]   0.0% (0/226)  misses 226 = 41.9% of all misses
  recall[static→closure    ]   0.0% (0/58)  misses 58 = 10.8% of all misses
  recall[static→named      ]  88.3% (936/1060)  misses 124 = 23.0% of all misses
  tier repowise:import_merged confirmed 2  contradicted 1  abstract 0  silent 1
  tier repowise:package_alias confirmed 18  contradicted 21  abstract 0  silent 5
  tier repowise:receiver_typed_global confirmed 8  contradicted 0  abstract 0  silent 0
  tier repowise:receiver_typed_import confirmed 62  contradicted 0  abstract 0  silent 1
  tier repowise:receiver_typed_same_file confirmed 551  contradicted 0  abstract 0  silent 0
  tier repowise:same_file confirmed 201  contradicted 4  abstract 0  silent 1
  tier repowise:same_package confirmed 73  contradicted 40  abstract 0  silent 1
  line-grain tolerance used on 287 edge(s) (several oracle sites on one line)
poison check: PASS — 990 seeded wrong edges: 981 refused, 9 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 990 |
| confirmed | 915 |
| contradicted | 66 |
| abstract | 0 |
| silent | 9 {"not-loaded": 1, "unreachable": 8} |
| precision-against-oracle (lower bound) | **93.3%** (915/981) |
| recall | 63.5% (936/1,475) at 4 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:import_merged` confirmed 2 / contradicted 1 / abstract 0 / silent 1; `repowise:package_alias` confirmed 18 / contradicted 21 / abstract 0 / silent 5; `repowise:receiver_typed_global` confirmed 8 / contradicted 0 / abstract 0 / silent 0; `repowise:receiver_typed_import` confirmed 62 / contradicted 0 / abstract 0 / silent 1; `repowise:receiver_typed_same_file` confirmed 551 / contradicted 0 / abstract 0 / silent 0; `repowise:same_file` confirmed 201 / contradicted 4 / abstract 0 / silent 1; `repowise:same_package` confirmed 73 / contradicted 40 / abstract 0 / silent 1.

## Contradicted (66 rows; all in report.json)

Mechanical shape (no reading): same file as an oracle target, other line 50; another file than every oracle target 16. By label: `repowise:same_package` 40, `repowise:package_alias` 21, `repowise:same_file` 4, `repowise:import_merged` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `bench_test.go:137` → `decode.go:143` [repowise:package_alias] — oracle: github.com/BurntSushi/toml.Decode @ decode.go:35
- `bench_test.go:152` → `decode.go:143` [repowise:package_alias] — oracle: github.com/BurntSushi/toml.Decode @ decode.go:35
- `bench_test.go:48` → `decode.go:143` [repowise:package_alias] — oracle: github.com/BurntSushi/toml.Decode @ decode.go:35
- `bench_test.go:64` → `decode.go:143` [repowise:package_alias] — oracle: github.com/BurntSushi/toml.Decode @ decode.go:35
- `bench_test.go:87` → `decode.go:143` [repowise:package_alias] — oracle: github.com/BurntSushi/toml.Decode @ decode.go:35
- `cmd/toml-test-decoder/main.go:30` → `error.go:233` [repowise:package_alias] — oracle: context.WithCancel$1 @ /home/mmarrujo/.local/go/src/context/context.go:242, (*context.cancelCtx).propagateCancel$1 @ /home/mmarrujo/.local/go/src/context/context.go:510, (*context.cancelCtx).propagateCancel$2 @ /home/mmarrujo/.local/go/src/context/context.go:522

**Triage ratio (A-8):** `oracle-wrong 0 : tool-wrong 0 : untriaged 66` — the rows are not read; the number above is a lower bound on the tool's precision exactly as it is for Hobbes (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `interface→named` | 0 / 226 | 226 |
| `static→named` | 936 / 1,060 | 124 |
| `func-value→named` | 0 / 74 | 74 |
| `static→closure` | 0 / 58 | 58 |
| `func-value→closure` | 0 / 57 | 57 |

**Poison check:** PASS — 990 seeded wrong edges: 981 refused, 9 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

