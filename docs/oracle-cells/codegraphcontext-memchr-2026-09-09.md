# Oracle cell — codegraphcontext 0.6.13 on memchr, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@1` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/BurntSushi/memchr, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/memchr`, commit bd6068c30e9074a90c285e47912fa0b047d07597 — **the same commit and the same answer key as the Hobbes cell [memchr-rust-2026-08-27.md](memchr-rust-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/memchr-rust-r2/oracle.json` (`cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha bd6068c3`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 33.9 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 3823 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 164 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line (identifier line for Go/Java/Rust/undecorated TS; the decorator line for decorated TS and Python).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-memchr/edges.json memchr-rust-r2/oracle.json codegraphcontext-memchr --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-memchr` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha bd6068c3
hobbes edges 3817: confirmed 275  contradicted 164  abstract 0  silent 3378 map[no-targets:1 not-loaded:3285 unreachable:92]
precision-against-oracle 62.6% (275/439)
recall 24.1% (276/1146 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1476; misses map[macro→function:57 macro→method:42 static→closure:1 static→function:158 static→generated:5 static→method:607]
  recall[macro→function    ]   0.0% (0/57)  misses 57 = 6.6% of all misses
  recall[macro→method      ]   0.0% (0/42)  misses 42 = 4.8% of all misses
  recall[static→closure    ]   0.0% (0/1)  misses 1 = 0.1% of all misses
  recall[static→function   ]  27.9% (61/219)  misses 158 = 18.2% of all misses
  recall[static→generated  ]   0.0% (0/5)  misses 5 = 0.6% of all misses
  recall[static→method     ]  26.2% (215/822)  misses 607 = 69.8% of all misses
  tier codegraphcontext:EXTRACTED confirmed 138  contradicted 43  abstract 0  silent 2070
  tier codegraphcontext:INFERRED confirmed 137  contradicted 121  abstract 0  silent 1308
  line-grain tolerance used on 186 edge(s) (several oracle sites on one line)
poison check: PASS — 3817 seeded wrong edges: 439 refused, 3378 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 3,817 |
| confirmed | 275 |
| contradicted | 164 |
| abstract | 0 |
| silent | 3,378 {"no-targets": 1, "not-loaded": 3285, "unreachable": 92} |
| precision-against-oracle (lower bound) | **62.6%** (275/439) |
| recall | 24.1% (276/1,146) at 2 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 138 / contradicted 43 / abstract 0 / silent 2070; `codegraphcontext:INFERRED` confirmed 137 / contradicted 121 / abstract 0 / silent 1308.

## Contradicted (164 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 154; same file as an oracle target, other line 10. By label: `codegraphcontext:INFERRED` 121, `codegraphcontext:EXTRACTED` 43.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/arch/all/memchr.rs:323` → `src/arch/all/memchr.rs:322` [codegraphcontext:EXTRACTED] — oracle: arch::generic::memchr::Iter::<'h>::count @ src/arch/generic/memchr.rs:1064
- `src/arch/all/memchr.rs:378` → `src/arch/aarch64/neon/memchr.rs:400` [codegraphcontext:INFERRED] — oracle: arch::all::memchr::Two::new @ src/arch/all/memchr.rs:363
- `src/arch/all/memchr.rs:56` → `src/arch/aarch64/neon/memchr.rs:40` [codegraphcontext:INFERRED] — oracle: arch::all::memchr::One::new @ src/arch/all/memchr.rs:46
- `src/arch/all/memchr.rs:642` → `src/arch/aarch64/neon/memchr.rs:682` [codegraphcontext:INFERRED] — oracle: arch::all::memchr::Three::new @ src/arch/all/memchr.rs:621
- `src/arch/all/memchr.rs:907` → `src/arch/aarch64/neon/memchr.rs:40` [codegraphcontext:INFERRED] — oracle: arch::all::memchr::One::new @ src/arch/all/memchr.rs:46, arch::all::memchr::One::iter @ src/arch/all/memchr.rs:276, core::iter::Iterator::collect @ :0
- `src/arch/all/memchr.rs:916` → `src/arch/aarch64/neon/memchr.rs:40` [codegraphcontext:INFERRED] — oracle: arch::all::memchr::One::new @ src/arch/all/memchr.rs:46, arch::all::memchr::One::iter @ src/arch/all/memchr.rs:276, core::iter::Iterator::rev @ :0

**Triage ratio (A-8):** `oracle-wrong 0 : tool-wrong 0 : untriaged 164` — the rows are not read; the number above is a lower bound on the tool's precision exactly as it is for Hobbes (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→method` | 215 / 822 | 607 |
| `static→function` | 61 / 219 | 158 |
| `macro→function` | 0 / 57 | 57 |
| `macro→method` | 0 / 42 | 42 |
| `static→generated` | 0 / 5 | 5 |
| `static→closure` | 0 / 1 | 1 |

**Poison check:** PASS — 3,817 seeded wrong edges: 439 refused, 3,378 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

