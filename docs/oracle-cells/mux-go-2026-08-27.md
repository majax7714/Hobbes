# Oracle cell — gorilla/mux, module `.`, 2026-08-27

Repo: https://github.com/gorilla/mux (BSD-3-Clause), clone at `~/.hobbes/bench/oracle/repos/mux`, commit db9d1d0073d27a0a2d9a8c1bc52aa0af4374d265 (shallow). Picked because it is a well-known, dependency-free Go HTTP router of ~7.5k lines in one `go.mod` (no `require` block at all), from an author not yet graded, with a test main giving the RTA root, and it builds with plain `go build ./...` on this box. Ingest SHA db9d1d00 (the ingest reports a dirty tree: `hobbes ingest` appends `.hobbes/` to the clone's `.gitignore`, ADR-012 — nothing else changed). Hobbes at 91e8bdb, edges from `.hobbes/derived/graph.json` (lane B on, `HOBBES_SCIP=1`; the ingest warns `scip-decode extraction degraded`: 2 package namespaces declared in more than one file, references left unattributed, C-28). Oracle go-rta (x/tools v0.49.0), default build tags, roots = 1 (`mux.test`; the repo has no `cmd/`).

Command: `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/mux . ~/.hobbes/bench/oracle/mux-go --lang go`. Runtime 13 s (ingest + RTA + grade, one language). Outputs in `~/.hobbes/bench/oracle/mux-go/` (`hobbes.json`, `oracle.json`, `report.json`, `report.txt`).

## Numbers (report.txt, verbatim)

```
cell .  oracle go-rta (reachability)  sha db9d1d00
hobbes edges 1264: confirmed 1221  contradicted 3  abstract 0  silent 40 map[unreachable:40]
precision-against-oracle 99.8% (1221/1224)
recall 82.6% (1233/1492 in-repo oracle pairs) at 1 roots; external oracle pairs 1270; misses map[func-value→closure:89 func-value→named:5 interface→named:120 static→closure:45]
  recall[func-value→closure]   0.0% (0/89)  misses 89 = 34.4% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/5)  misses 5 = 1.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]   0.0% (0/120)  misses 120 = 46.3% of all misses
  recall[static→closure    ]   0.0% (0/45)  misses 45 = 17.4% of all misses
  recall[static→named      ] 100.0% (1233/1233)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 1221  contradicted 3  abstract 0  silent 40
  line-grain tolerance used on 514 edge(s) (several oracle sites on one line)
  contradicted mux.go:595  hobbes mux.go:26 (mux.RegexpCompileFunc)  oracle /home/mmarrujo/.local/go/src/regexp/regexp.go:130 (regexp.Compile), route_test.go:40 (github.com/gorilla/mux.BenchmarkNewRouterRegexpFunc$1)
  contradicted regexp.go:109  hobbes mux.go:26 (mux.RegexpCompileFunc)  oracle /home/mmarrujo/.local/go/src/regexp/regexp.go:130 (regexp.Compile), route_test.go:40 (github.com/gorilla/mux.BenchmarkNewRouterRegexpFunc$1)
  contradicted regexp.go:132  hobbes mux.go:26 (mux.RegexpCompileFunc)  oracle /home/mmarrujo/.local/go/src/regexp/regexp.go:130 (regexp.Compile), route_test.go:40 (github.com/gorilla/mux.BenchmarkNewRouterRegexpFunc$1)
poison check: PASS — 1264 seeded wrong edges: 1224 refused, 40 unjudged (oracle silent there), 0 falsely confirmed
cell . of /home/mmarrujo/.hobbes/bench/oracle/repos/mux: 13s
```

| bucket | count |
|---|---|
| hobbes edges | 1264 |
| confirmed | 1221 |
| contradicted | 3 |
| silent (unreachable from the 1 root) | 40 |
| precision-against-oracle | 99.8% (1221/1224) |
| in-repo oracle pairs | 1492 |
| recall at 1 root | 82.6% (1233/1492) |
| external oracle pairs (not graded) | 1270 |
| tiers | semantic only (1221 / 3 / 40); no syntactic or dynamic edges in this cell |

## Contradicted (3 rows, all in report.txt)

All three are the same shape: a call through the package-level function variable `RegexpCompileFunc` (`mux.go:26`, `RegexpCompileFunc = regexp.Compile`, documented as overridable) at `mux.go:595`, `regexp.go:109`, `regexp.go:132`. Hobbes resolves the callee to the variable's declaration (`mux.go:26`); the oracle resolves it to the functions the value can hold — `regexp.Compile` (stdlib) and `route_test.go:40 BenchmarkNewRouterRegexpFunc$1`, the closure the test assigns to it.

## Misses by class (259 rows, all in report.txt / report.json)

- **interface→named, 120 (46.3%).** Method calls through an interface value, resolved by RTA to every reachable concrete implementation. Concentrated by site file: `middleware_test.go` 58, `mux_test.go` 26, `route.go` 10, `mux.go` 10, `old_test.go` 4, `mux_httpserver_test.go` 4. The dominant callees are the repo's `http.ResponseWriter` implementations — `(*ResponseRecorder).Write` at `old_test.go:45` (26) and `(*TestA301ResponseWriter).Write` at `mux_test.go:2021` (26), plus their `WriteHeader` (9 each) and `Header` (4 each) — and the `http.Handler` implementations `(*Router).ServeHTTP` at `mux.go:188` (15) and `customMethodNotAllowedHandler.ServeHTTP` at `mux_test.go:2893` (15). The busiest site is `route.go:56 (*Route).Match` (8, the `matcher` interface).
- **static→closure, 45 (17.4%).** Direct calls of a locally bound `func` literal: `old_test.go` 32 (all in `TestRouteMatchers` — `$1` 8, `$2` 8, `$3` 16), `mux_test.go` 13 (`TestWalkNested$1` 6, `TestHeaders$1` 4, one each in `TestContextMiddleware`, `TestPath`, `TestPanicOnCapturingGroups`).
- **func-value→closure, 89 (34.4%; upper bound, oracle over-approximates).** Calls through a function-typed value to every reachable closure of matching signature. Two test sites carry 48 of them: `mux_test.go:2762 TestSubrouterMatching$19$1` (24) and `middleware_test.go:561 TestCORSMethodMiddleware$7` (24). The rest are the library's own function-typed hooks: `route.go:535 (*Route).BuildVarsFunc$1` (10), `middleware.go:20 MiddlewareFunc.Middleware` (10), `mux.go:412 (*Router).walk` (6, the `WalkFunc`), `route.go:825 (*Route).buildVars` (5), `route.go:378 MatcherFunc.Match` (2), `regexp.go` (2).
- **func-value→named, 5 (1.9%; upper bound, oracle over-approximates).** `middleware.go:20 MiddlewareFunc.Middleware -> (*testMiddleware).Middleware` (1) and `mux_test.go:2966 TestContextMiddleware$1$1` calling four `Example*` functions of matching signature (`ExampleSetURLVars`, `ExampleCORSMethodMiddleware`, `ExampleRoute_HeadersRegexp`, `ExampleRoute_HeadersRegexp_exactMatch`).
- **static→named, 0 misses** (1233/1233).

**Poison check:** PASS — 1264 seeded wrong edges: 1224 refused, 40 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (which side would need to change; no proposals):** `contradicted` (3, calls through the `RegexpCompileFunc` variable) — Hobbes (it emits the variable's declaration as the callee; the oracle's targets are the functions the value holds). `interface→named` — Hobbes (the oracle's targets are concrete implementations the compiler knows; Hobbes emits nothing at these sites). `static→closure` — Hobbes (a direct call to a func literal is statically resolvable; Hobbes has no callee). `func-value→named` and `func-value→closure` — the oracle first (the reachability oracle over-approximates function values, so the count is an upper bound), with any residue Hobbes. Silent 40 — nothing to fix on either side.

**Not graded:** the 1270 external oracle pairs (stdlib / out-of-repo callees, by design); the 40 `unreachable` silent edges (RTA did not reach them from the 1 root). No repo was abandoned.
