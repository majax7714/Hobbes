# Oracle cells — dagger, Go modules, 2026-08-25 (O4)

Ingest at f3cc3eb3 (dagger HEAD; 55,764 call edges, six languages).
Oracle go-rta (x/tools v0.49.0), default build tags, one cell per Go
module (`~/.hobbes/bench/oracle/run-dagger.sh`, sites scoped to the
module and nested modules excluded on both sides after H-10). 33
`go.mod` outside integration testdata; 24 with Hobbes edges; **19
graded**, 4 not gradeable (below), the root module by package subtree
(below). Runtimes are the cell lines: 150–500 s each, the RTA pass
dominating; 24 roots across the 19 (binaries + test mains).

## The 19 module cells

| module | Hobbes edges | confirmed / contradicted / silent | precision-against-oracle | recall (in-repo pairs) at roots | misses by class |
|---|---|---|---|---|---|
| `dagql/idtui/viztest` | 1260 | 1219 / 9 / 32 | 99.3% | 95.5% (1220/1277) at 1 | func-value→closure 1, interface→named 9, static→closure 46, static→named 1 |
| `dagql/idtui/viztest/dep` | 409 | 381 / 2 / 26 | 99.5% | 89.9% (383/426) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `dagql/idtui/viztest/dep/nested-dep` | 368 | 341 / 2 / 25 | 99.4% | 88.8% (341/384) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `e2e/helm` | 72 | 72 / 0 / 0 | 100.0% | 100.0% (72/72) at 1 |  |
| `e2e/installers` | 12 | 12 / 0 / 0 | 100.0% | 75.0% (12/16) at 1 | func-value→closure 4 |
| `modules/alpine` | 483 | 446 / 5 / 32 | 98.9% | 91.3% (452/495) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `modules/daggerverse` | 449 | 415 / 2 / 32 | 99.5% | 90.6% (415/458) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `modules/gha` | 985 | 943 / 0 / 42 | 100.0% | 95.2% (962/1010) at 1 | func-value→closure 3, interface→named 9, static→closure 32, static→named 4 |
| `modules/gha/examples/go` | 481 | 450 / 0 / 31 | 100.0% | 90.9% (450/495) at 1 | func-value→closure 1, interface→named 9, static→closure 34, static→named 1 |
| `modules/git-releaser` | 498 | 462 / 3 / 33 | 99.4% | 91.5% (462/505) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `modules/metrics` | 452 | 419 / 2 / 31 | 99.5% | 90.7% (419/462) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `modules/ruff` | 576 | 527 / 0 / 49 | 100.0% | 92.5% (529/572) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `modules/wolfi` | 415 | 384 / 0 / 31 | 100.0% | 90.0% (388/431) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `sdk/elixir/runtime` | 559 | 521 / 2 / 36 | 99.6% | 92.4% (521/564) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `sdk/go` | 603 | 481 / 0 / 122 | 100.0% | 81.4% (481/591) at 3 | func-value→closure 17, func-value→named 13, interface→named 26, static→closure 46, static→named 8 |
| `sdk/java/runtime` | 560 | 524 / 5 / 31 | 99.1% | 91.9% (524/570) at 1 | func-value→closure 1, interface→named 9, static→closure 35, static→named 1 |
| `sdk/php/runtime` | 487 | 452 / 2 / 33 | 99.6% | 91.3% (452/495) at 1 | func-value→closure 1, interface→named 9, static→closure 32, static→named 1 |
| `sdk/python/runtime` | 920 | 882 / 2 / 36 | 99.8% | 94.0% (884/940) at 3 | func-value→closure 2, func-value→named 3, interface→named 10, static→closure 32, static→named 9 |
| `sdk/typescript/runtime` | 923 | 885 / 4 / 34 | 99.6% | 93.3% (888/952) at 2 | func-value→closure 1, interface→named 30, static→closure 32, static→named 1 |
**Aggregate (19 cells, not a pooled recall — each cell's number stands
with its roots; the sum is a size, not a rate to compare):** 10,512
Hobbes edges — **9,816 confirmed, 40 contradicted, 656 silent (all
`unreachable`)**; precision-against-oracle **99.6%**; 9,855 of 10,715
in-repo oracle pairs drawn. Static calls to named declarations
**9,854 / 9,889**. Every silent edge sits in a function no root
reaches — a library's untested exports, a module's helpers only the
engine invokes by name.

**All 40 contradictions are one shape: a type conversion drawn as a
call.** 37 semantic — `dagger.JSON("0")`, `dagger.Platform(p)` — where
tree-sitter sees call syntax, scip resolves the operand to a *type*,
and the join emits `calls` to the type's declaration; 3 syntactic —
`string(b)` name-matched by lane A to a symbol that happens to be
called `string`. A wrong edge, not a silence: the first the lane has
found in the semantic tier. Product defect, handed to W1.

**The 35 misses among static calls to named declarations** (of 9,889):
- **16 recursion** — `return findSingleGQLError(x.Unwrap())`;
  `graph.py:113` drops self-calls by design. Now **C-59**.
- **11 method expressions and generic instantiation calls** —
  `(*Gha).Job(&parent, …)`, `Ref[*File](c, id).Contents(ctx)`,
  `w.Permissions.Permissions()` (a method named like its field).
- **4 chain continuations** — `.File("/hello.txt").` on its own line in
  a multi-line method chain: no edge and **no site** (the call node
  starts on the chain's first line, so the continuation is never a
  tree-sitter site and the capture number does not see it).
- **4 calls on an assignment's left side** —
  `m.Discovery.UserConfig().BaseImage = ref` drawn as `uses`.

The remaining misses are C-58's classes: 577 calls into closures, 192
interface dispatches, 16 function-table calls, 41 inflated `func()` pairs.

## Not gradeable

| module | why |
|---|---|
| `docs` | 1,292 load errors: doc snippets import `dagger.io/dagger` with no module requirement (the same undeclarable residue C-34 records for its TS side) |
| `docs/recorder`, `docs/recorder2` | import a generated `internal/dagger` package that is not in the tree |
| `e2e/helm/dagger` | a library with no `main` and no tests — RTA has nothing to root at (its sites are graded where they are called, in `e2e/helm`) |

## The root module

The whole program does not fit: RTA over the root's dependency closure
(buildkit, containerd, …) with `InstantiateGenerics` — which RTA
requires — was **OOM-killed at 20.5 GB with tests and at 20.7 GB
without**, on a 30 GB box (H-9). Graded instead by package subtree
(`--packages`), each a smaller, stated question:

| subtree cell | packages | result |
|---|---|---|
| `root/integration` | `./core/integration/...` with tests | **OOM-killed at 24.4 GB after 1,204 s** (GOGC=50, GOMEMLIMIT=24GiB) — the integration tests import the engine, so the program is the root's closure again |
| `root/core` | `./core/...` without tests | **nothing to root at**: `core` has no binaries; with tests it is the integration program above |
