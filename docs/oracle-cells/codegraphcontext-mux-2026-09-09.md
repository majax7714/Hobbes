# Oracle cell — codegraphcontext 0.6.13 on mux, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@1` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/gorilla/mux, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/mux`, commit db9d1d0073d27a0a2d9a8c1bc52aa0af4374d265 — **the same commit and the same answer key as the Hobbes cell [mux-go-2026-08-27.md](mux-go-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/mux-go-r2/oracle.json` (`cell .  oracle go-rta (reachability)  sha db9d1d00`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 4.0 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 1193 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 146 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line (identifier line for Go/Java/Rust/undecorated TS; the decorator line for decorated TS and Python).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-mux/edges.json mux-go-r2/oracle.json codegraphcontext-mux --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-mux` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle go-rta (reachability)  sha db9d1d00
hobbes edges 1193: confirmed 920  contradicted 250  abstract 0  silent 23 map[unreachable:23]
precision-against-oracle 78.6% (920/1170)
recall 62.4% (931/1492 in-repo oracle pairs) at 1 roots; external oracle pairs 1270; misses map[func-value→closure:89 func-value→named:5 interface→named:101 static→closure:45 static→named:321]
  recall[func-value→closure]   0.0% (0/89)  misses 89 = 15.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/5)  misses 5 = 0.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]  15.8% (19/120)  misses 101 = 18.0% of all misses
  recall[static→closure    ]   0.0% (0/45)  misses 45 = 8.0% of all misses
  recall[static→named      ]  74.0% (912/1233)  misses 321 = 57.2% of all misses
  tier codegraphcontext:EXTRACTED confirmed 490  contradicted 22  abstract 0  silent 7
  tier codegraphcontext:INFERRED confirmed 430  contradicted 228  abstract 0  silent 16
  line-grain tolerance used on 468 edge(s) (several oracle sites on one line)
poison check: PASS — 1193 seeded wrong edges: 1170 refused, 23 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1,193 |
| confirmed | 920 |
| contradicted | 250 |
| abstract | 0 |
| silent | 23 {"unreachable": 23} |
| precision-against-oracle (lower bound) | **78.6%** (920/1,170) |
| recall | 62.4% (931/1,492) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 490 / contradicted 22 / abstract 0 / silent 7; `codegraphcontext:INFERRED` confirmed 430 / contradicted 228 / abstract 0 / silent 16.

## Contradicted (250 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 154; same file as an oracle target, other line 96. By label: `codegraphcontext:INFERRED` 228, `codegraphcontext:EXTRACTED` 22.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `example_cors_method_middleware_test.go:16` → `mux.go:360` [codegraphcontext:INFERRED] — oracle: (*github.com/gorilla/mux.Route).Methods @ route.go:398
- `example_cors_method_middleware_test.go:20` → `mux.go:360` [codegraphcontext:INFERRED] — oracle: (*github.com/gorilla/mux.Route).Methods @ route.go:398
- `example_cors_method_middleware_test.go:24` → `old_test.go:32` [codegraphcontext:INFERRED] — oracle: net/http/httptest.NewRecorder @ /home/mmarrujo/.local/go/src/net/http/httptest/recorder.go:51
- `example_cors_method_middleware_test.go:32` → `mux.go:232` [codegraphcontext:INFERRED] — oracle: fmt.Println @ /home/mmarrujo/.local/go/src/fmt/print.go:314, (*net/http/httptest.ResponseRecorder).Header @ /home/mmarrujo/.local/go/src/net/http/httptest/recorder.go:67, (net/http.Header).Get @ /home/mmarrujo/.local/go/src/net/http/header.go:49
- `example_cors_method_middleware_test.go:32` → `mux_test.go:2017` [codegraphcontext:INFERRED] — oracle: fmt.Println @ /home/mmarrujo/.local/go/src/fmt/print.go:314, (*net/http/httptest.ResponseRecorder).Header @ /home/mmarrujo/.local/go/src/net/http/httptest/recorder.go:67, (net/http.Header).Get @ /home/mmarrujo/.local/go/src/net/http/header.go:49
- `example_cors_method_middleware_test.go:33` → `mux.go:232` [codegraphcontext:INFERRED] — oracle: fmt.Println @ /home/mmarrujo/.local/go/src/fmt/print.go:314, (*net/http/httptest.ResponseRecorder).Header @ /home/mmarrujo/.local/go/src/net/http/httptest/recorder.go:67, (net/http.Header).Get @ /home/mmarrujo/.local/go/src/net/http/header.go:49

**Triage ratio (A-8):** `oracle-wrong 0 : tool-wrong 0 : untriaged 250` — the rows are not read; the number above is a lower bound on the tool's precision exactly as it is for Hobbes (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→named` | 912 / 1,233 | 321 |
| `interface→named` | 19 / 120 | 101 |
| `func-value→closure` | 0 / 89 | 89 |
| `static→closure` | 0 / 45 | 45 |
| `func-value→named` | 0 / 5 | 5 |

**Poison check:** PASS — 1,193 seeded wrong edges: 1,170 refused, 23 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

## Determinism, observed (the same clone indexed four times, a fresh database each time)

Three indexes stored **767** CALLS rows (byte-identical converted edge files); the one this record grades — the batch's — stored **1,193**, a strict superset of the 767. The tool's own end-of-run summary printed `CALLS edges 2165` on every run, so its counter is not the stored count. The cause is not known to us and is not investigated here (the tool is run as documented, nothing custom). The 767-row graph graded against the same key (`~/.hobbes/bench/comparative/codegraphcontext-mux-det1/report.txt`, kept beside this cell; quoted in prose so the block above stays the record's standing grade for the graphics, ADR-102): 767 edges — confirmed 683, contradicted 65, silent 19 (unreachable); precision-against-oracle 91.3% (683/748); recall 45.8% (684/1,492) at 1 root; poison check PASS — 767 seeded, 748 refused, 19 unjudged, 0 falsely confirmed.

So on this cell the tool reads 91.3% / 45.8% or 78.6% / 62.4% depending on the run; both are its numbers at our grain. The other twelve repos were indexed once each.
