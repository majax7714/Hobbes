# Oracle cell — repowise 0.49.0 on sqlite-vector, module `.`, 2026-09-14 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@2` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/sqliteai/sqlite-vector, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/sqlite-vector`, commit 0c2223ada9dce1fa33248c8835a15f51d9a0f655 — **the same commit and the same answer key as the Hobbes cell [sqlite-vector-c](sqlite-vector-c)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/sqlite-vector-c/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 11 s; no network observed, no repo build invoked. The tool's own log carries 1 error/warning line(s), e.g. `2026-09-14 10:22:18 [warning  ] parser.symbol_bloat            language=cpp path=libs/sqlite3.h symbol_count=844 threshold=500`. Conversion: 1702 (site, callee) pairs; dropped {"unknown-caller": 24, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh repowise-sqlite-vector/edges.json sqlite-vector-c/oracle.json repowise-sqlite-vector --lang c`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-sqlite-vector` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad
hobbes edges 1657: confirmed 780  contradicted 99  abstract 0  silent 778 map[no-targets:231 not-loaded:352 unreachable:195]
precision-against-oracle 88.7% (780/879)
recall 93.3% (1018/1091 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1721; misses map[static→function:73]
recall-collapsed 91.7% (780/851 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→function   ]  93.3% (1018/1091)  misses 73 = 100.0% of all misses
  tier repowise:import_merged confirmed 263  contradicted 0  abstract 0  silent 395
  tier repowise:same_file confirmed 517  contradicted 99  abstract 0  silent 383
  line-grain tolerance used on 394 edge(s) (several oracle sites on one line)
poison check: PASS — 1657 seeded wrong edges: 879 refused, 778 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1,657 |
| confirmed | 780 |
| contradicted | 99 |
| abstract | 0 |
| silent | 778 {"no-targets": 231, "not-loaded": 352, "unreachable": 195} |
| precision-against-oracle (lower bound) | **88.7%** (780/879) |
| recall | 93.3% (1,018/1,091) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:import_merged` confirmed 263 / contradicted 0 / abstract 0 / silent 395; `repowise:same_file` confirmed 517 / contradicted 99 / abstract 0 / silent 383.

## Contradicted (99 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 90; same file as an oracle target, other line 9. By label: `repowise:same_file` 99.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/distance-avx2.c:101` → `src/distance-avx2.c:70` [repowise:same_file] — oracle: _mm256_fmadd_ps @ :0
- `src/distance-avx2.c:134` → `src/distance-avx2.c:21` [repowise:same_file] — oracle: _mm256_add_ps @ :0, _mm256_set1_ps @ :0, _mm256_andnot_ps @ :0
- `src/distance-avx2.c:135` → `src/distance-avx2.c:21` [repowise:same_file] — oracle: _mm256_add_ps @ :0, _mm256_andnot_ps @ :0, _mm256_set1_ps @ :0
- `src/distance-avx2.c:136` → `src/distance-avx2.c:21` [repowise:same_file] — oracle: _mm256_add_ps @ :0, _mm256_andnot_ps @ :0, _mm256_set1_ps @ :0
- `src/distance-avx2.c:137` → `src/distance-avx2.c:21` [repowise:same_file] — oracle: _mm256_add_ps @ :0, _mm256_set1_ps @ :0, _mm256_andnot_ps @ :0
- `src/distance-avx2.c:141` → `src/distance-avx2.c:21` [repowise:same_file] — oracle: _mm256_add_ps @ :0, _mm256_set1_ps @ :0, _mm256_andnot_ps @ :0

**Triage ratio (A-8):** every row read (the paragraph below): `tool-grain (a function-like macro stored as `function`, C-95) 99 : converter-defect 0 : tool-wrong 0` — the number above is a lower bound at our grain (C-62, C-95), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-14):** Read 2026-09-14, every row: all 99 contradicted rows target a `#define` line — `MM256_FMA_PS`, `_mm256_abs_ps`, `S8_TO_BIASED_U8_AVX2` and their AVX-512, SSE2 and generic siblings, function-like macros the tool stores as kind `function`, drawn at sites clang resolves to the intrinsic (external; 86 rows) or to the expansion's callee. The macro exclusion fires on a `macro` kind alone (C-95). With those rows excluded as Hobbes' own are, the tool would read 780/780. Its 73 static misses are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 1,018 / 1,091 | 73 |

**Poison check:** PASS — 1,657 seeded wrong edges: 879 refused, 778 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

