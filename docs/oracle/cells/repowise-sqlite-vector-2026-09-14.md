# Oracle cell — repowise 0.49.0 on sqlite-vector, module `.`, 2026-09-14 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@3` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/sqliteai/sqlite-vector, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/sqlite-vector`, commit 0c2223ada9dce1fa33248c8835a15f51d9a0f655 — **the same commit and the same answer key as the Hobbes cell [sqlite-vector-c](sqlite-vector-c)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/sqlite-vector-c/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 11 s; no network observed, no repo build invoked. The tool's own log carries 1 error/warning line(s), e.g. `2026-09-14 10:22:18 [warning  ] parser.symbol_bloat            language=cpp path=libs/sqlite3.h symbol_count=844 threshold=500`. Conversion: 1702 (site, callee) pairs; dropped {"unknown-caller": 24, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored).

Command: `bench/oracle/grade-foreign.sh repowise-sqlite-vector/edges.json sqlite-vector-c/oracle.json repowise-sqlite-vector --lang c`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-sqlite-vector` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad
hobbes edges 1050: confirmed 780  contradicted 0  abstract 0  silent 270 map[not-loaded:119 unreachable:151]
precision-against-oracle 100.0% (780/780)
recall 93.3% (1018/1091 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1721; misses map[static→function:73]
recall-collapsed 91.7% (780/851 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→function   ]  93.3% (1018/1091)  misses 73 = 100.0% of all misses
  tier repowise:import_merged confirmed 263  contradicted 0  abstract 0  silent 42
  tier repowise:same_file confirmed 517  contradicted 0  abstract 0  silent 228
  line-grain tolerance used on 317 edge(s) (several oracle sites on one line)
poison check: PASS — 1050 seeded wrong edges: 780 refused, 270 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1,050 |
| confirmed | 780 |
| contradicted | 0 |
| abstract | 0 |
| silent | 270 {"not-loaded": 119, "unreachable": 151} |
| precision-against-oracle (lower bound) | **100.0%** (780/780) |
| recall | 93.3% (1,018/1,091) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:import_merged` confirmed 263 / contradicted 0 / abstract 0 / silent 42; `repowise:same_file` confirmed 517 / contradicted 0 / abstract 0 / silent 228.

**Triage (hand-read, 2026-09-14):** Read 2026-09-14, every row, at converter@2 first: all 99 contradicted rows targeted a `#define` line — `MM256_FMA_PS`, `_mm256_abs_ps`, `S8_TO_BIASED_U8_AVX2` and their AVX-512, SSE2 and generic siblings, function-like macros the tool stores as kind `function`, drawn at sites clang resolves to the intrinsic (external; 86 rows) or to the expansion's callee. converter@3 (ADR-101's 2026-09-14 amendment) reads a leading `#define` at the declared line as `macro`: 607 of the tool's edges are now excluded as Hobbes' own are, and nothing is contradicted. Its 73 static misses are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 1,018 / 1,091 | 73 |

**Poison check:** PASS — 1,050 seeded wrong edges: 780 refused, 270 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (repowise-adapter@2 → repowise-adapter@3, 2026-09-14, signed):** the converter now reads a callee whose declared line begins with #define as kind macro, which the lane excludes as it excludes Hobbes' own (C-95; ADR-101's 2026-09-14 amendment: a tool storing a function-like macro as a function was graded against the expansion's callee). graded edges 1,657 → 1,050 (-607); confirmed 780 → 780 (+0); contradicted 99 → 0 (-99); precision-against-oracle 88.7% → 100.0% (+11.3); recall 93.3% → 93.3% (+0.0). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

## Regrade, converter@5 (2026-09-26) — a `__module__` caller is a call site

`repowise-adapter@5` (ADR-101's 2026-09-26 amendment, C-94) reads a call edge whose caller is repowise's per-file `<file>::__module__` node — a call at the file's top level, or inside an anonymous function there — as call sites in that file at the tool's own `call_lines_json`; through @4 the converter dropped every such edge as `unknown-caller`. Re-converted from this cell's stored `raw.json` (no re-index; @4 re-run from the same `raw.json` reproduced the stored `edges.json` row for row first), source lines read from `/home/mmarrujo/.hobbes/bench/oracle/repos/sqlite-vector`, graded by the same key with today's `oracle` binary. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-sqlite-vector/at5`. Conversion: 1,702 → 1,738 (site, callee) pairs; `unknown-caller` 24 → 0 edges; 24 edge(s) read from a `__module__` caller. **Every row graded before keeps its bucket; everything that moved is a row the fix added.**

**Direction of fix (repowise-adapter@3 → repowise-adapter@5, 2026-09-26, signed):** graded edges 1,050 → 1,053 (+3); confirmed 780 → 783 (+3); contradicted 0 → 0 (+0); precision-against-oracle 100.0% (780/780) → 100.0% (783/783) (+0.0); strict = precision before and after (no line-unresolved row); recall 93.3% (1,018/1,091) → 93.6% (1,021/1,091). Nothing on the tool's side moved; the edges were in its store all along.

The 36 added pairs grade as 3 confirmed; the rest are outside the cell or repeats (`other-language`/`duplicate` at import); none contradicted.

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad
hobbes edges 1053: confirmed 783  contradicted 0  abstract 0  silent 270 map[not-loaded:119 unreachable:151]
precision-against-oracle 100.0% (783/783)
recall 93.6% (1021/1091 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1721; misses map[static→function:70]
recall-collapsed 92.0% (783/851 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→function   ]  93.6% (1021/1091)  misses 70 = 100.0% of all misses
  tier repowise:import_merged confirmed 265  contradicted 0  abstract 0  silent 42
  tier repowise:same_file confirmed 518  contradicted 0  abstract 0  silent 228
  line-grain tolerance used on 317 edge(s) (several oracle sites on one line)
poison check: PASS — 1053 seeded wrong edges: 777 refused, 276 unjudged (oracle silent there), 0 falsely confirmed
```
