# Oracle cell — repowise 0.49.0 on cJSON, module `.`, 2026-09-14 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@3` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/DaveGamble/cJSON, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/cJSON`, commit fb16e5cf358798aabb049655975cde8427101056 — **the same commit and the same answer key as the Hobbes cell [cjson-c](cjson-c)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/cjson-c/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 11 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 2139 (site, callee) pairs; dropped {"unknown-caller": 65, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored).

Command: `bench/oracle/grade-foreign.sh repowise-cjson/edges.json cjson-c/oracle.json repowise-cjson --lang c`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-cjson` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf
hobbes edges 1328: confirmed 1073  contradicted 2  abstract 0  silent 253 map[not-loaded:241 unreachable:12]
precision-against-oracle 99.8% (1073/1075)
recall 56.0% (1074/1918 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 259; misses map[macro→function:728 static→function:116]
recall-collapsed 56.2% (1073/1908 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/728)  misses 728 = 86.3% of all misses
  recall[static→function   ]  90.3% (1074/1190)  misses 116 = 13.7% of all misses
  tier repowise:global_unique confirmed 579  contradicted 0  abstract 0  silent 176
  tier repowise:import_merged confirmed 36  contradicted 2  abstract 0  silent 9
  tier repowise:same_file confirmed 458  contradicted 0  abstract 0  silent 68
  line-grain tolerance used on 307 edge(s) (several oracle sites on one line)
poison check: PASS — 1328 seeded wrong edges: 1075 refused, 253 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 1,328 |
| confirmed | 1,073 |
| contradicted | 2 |
| abstract | 0 |
| silent | 253 {"not-loaded": 241, "unreachable": 12} |
| precision-against-oracle (lower bound) | **99.8%** (1,073/1,075) |
| recall | 56.0% (1,074/1,918) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 579 / contradicted 0 / abstract 0 / silent 176; `repowise:import_merged` confirmed 36 / contradicted 2 / abstract 0 / silent 9; `repowise:same_file` confirmed 458 / contradicted 0 / abstract 0 / silent 68.

## Contradicted (2 rows; all in report.json)

Mechanical shape (no reading): same file as an oracle target, other line 2. By label: `repowise:import_merged` 2.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `tests/unity/src/unity.c:1338` → `tests/unity/src/unity.h:45` [repowise:import_merged] — oracle: setUp @ tests/unity/src/unity.h:39
- `tests/unity/src/unity.c:1343` → `tests/unity/src/unity.h:47` [repowise:import_merged] — oracle: tearDown @ tests/unity/src/unity.h:40

**Triage ratio (A-8):** every row read (the paragraph below): `tool-wrong (a dead-`#if`-arm declaration, the C-138 shape) 2 : oracle-grain 0 : converter-defect 0` at converter@3; at @2 the 564 read `tool-grain (a function-like macro stored as `function`, C-95) 562 : tool-wrong 2` — the number above is a lower bound (C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-14):** Read 2026-09-14, every row, at converter@2 first: 562 of the 564 contradicted rows targeted a `#define` line — the tool stores a function-like macro (`can_read`, `buffer_at_offset`, Unity's `TEST_ASSERT_*`) as kind `function`, so the converter could not apply the macro exclusion (it fires on a `macro` kind alone, C-95), and each row graded against the callee clang saw in the expansion (`UnityFail`, `strncmp`, `memcpy`). converter@3 (ADR-101's 2026-09-14 amendment) reads a leading `#define` at the declared line as `macro`: 699 of the tool's edges are now excluded as Hobbes' own are, and the two rows left draw `setUp`/`tearDown` to the `UNITY_WEAK_ATTRIBUTE` definitions at unity.h:45/47 where clang's key declares them at :39/:40, the other `#if` arm (the shape of C-138): tool-wrong at the compiled grain. Its 116 static misses are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `macro→function` | 0 / 728 | 728 |
| `static→function` | 1,074 / 1,190 | 116 |

**Poison check:** PASS — 1,328 seeded wrong edges: 1,075 refused, 253 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (repowise-adapter@2 → repowise-adapter@3, 2026-09-14, signed):** the converter now reads a callee whose declared line begins with #define as kind macro, which the lane excludes as it excludes Hobbes' own (C-95; ADR-101's 2026-09-14 amendment: a tool storing a function-like macro as a function was graded against the expansion's callee). graded edges 2,027 → 1,328 (-699); confirmed 1,073 → 1,073 (+0); contradicted 564 → 2 (-562); precision-against-oracle 65.5% → 99.8% (+34.3); recall 56.0% → 56.0% (+0.0). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

