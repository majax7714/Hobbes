# Oracle cell — repowise 0.49.0 on cJSON, module `.`, 2026-09-14 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@2` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/DaveGamble/cJSON, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/cJSON`, commit fb16e5cf358798aabb049655975cde8427101056 — **the same commit and the same answer key as the Hobbes cell [cjson-c](cjson-c)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/cjson-c/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 11 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 2139 (site, callee) pairs; dropped {"unknown-caller": 65, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh repowise-cjson/edges.json cjson-c/oracle.json repowise-cjson --lang c`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-cjson` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf
hobbes edges 2027: confirmed 1073  contradicted 564  abstract 0  silent 390 map[not-loaded:301 unreachable:89]
precision-against-oracle 65.5% (1073/1637)
recall 56.0% (1074/1918 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 259; misses map[macro→function:728 static→function:116]
recall-collapsed 56.2% (1073/1908 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/728)  misses 728 = 86.3% of all misses
  recall[static→function   ]  90.3% (1074/1190)  misses 116 = 13.7% of all misses
  tier repowise:global_unique confirmed 579  contradicted 0  abstract 0  silent 176
  tier repowise:import_merged confirmed 36  contradicted 502  abstract 0  silent 92
  tier repowise:same_file confirmed 458  contradicted 62  abstract 0  silent 122
  line-grain tolerance used on 599 edge(s) (several oracle sites on one line)
poison check: PASS — 2027 seeded wrong edges: 1637 refused, 390 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 2,027 |
| confirmed | 1,073 |
| contradicted | 564 |
| abstract | 0 |
| silent | 390 {"not-loaded": 301, "unreachable": 89} |
| precision-against-oracle (lower bound) | **65.5%** (1,073/1,637) |
| recall | 56.0% (1,074/1,918) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 579 / contradicted 0 / abstract 0 / silent 176; `repowise:import_merged` confirmed 36 / contradicted 502 / abstract 0 / silent 92; `repowise:same_file` confirmed 458 / contradicted 62 / abstract 0 / silent 122.

## Contradicted (564 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 553; same file as an oracle target, other line 11. By label: `repowise:import_merged` 502, `repowise:same_file` 62.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `cJSON.c:1377` → `cJSON.c:299` [repowise:same_file] — oracle: strncmp @ :0
- `cJSON.c:1377` → `cJSON.c:304` [repowise:same_file] — oracle: strncmp @ :0
- `cJSON.c:1384` → `cJSON.c:299` [repowise:same_file] — oracle: strncmp @ :0
- `cJSON.c:1384` → `cJSON.c:304` [repowise:same_file] — oracle: strncmp @ :0
- `cJSON.c:1391` → `cJSON.c:299` [repowise:same_file] — oracle: strncmp @ :0
- `cJSON.c:1391` → `cJSON.c:304` [repowise:same_file] — oracle: strncmp @ :0

**Triage ratio (A-8):** every row read (the paragraph below): `tool-grain (a function-like macro stored as `function`, C-95) 562 : dead-`#if`-arm declaration (the C-138 shape) 2 : converter-defect 0 : tool-wrong 0` — the number above is a lower bound at our grain (C-62, C-95), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-14):** Read 2026-09-14, every row: 562 of the 564 contradicted rows target a `#define` line — the tool stores a function-like macro (`can_read`, `buffer_at_offset`, Unity's `TEST_ASSERT_*`) as kind `function`, so the converter cannot apply the macro exclusion (it fires on a `macro` kind alone, C-95), and the row grades against the callee clang saw in the expansion (`UnityFail`, `strncmp`, `memcpy`). The two others draw `setUp`/`tearDown` to the `UNITY_WEAK_ATTRIBUTE` definitions at unity.h:45/47 where clang's key declares them at :39/:40, the other `#if` arm (the shape of C-138). With the macro rows excluded as Hobbes' own are, the tool would read 1,073/1,075 (99.8%). Its 116 static misses are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `macro→function` | 0 / 728 | 728 |
| `static→function` | 1,074 / 1,190 | 116 |

**Poison check:** PASS — 2,027 seeded wrong edges: 1,637 refused, 390 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

