# Oracle cell — codegraphcontext 0.6.13 on cue, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/Blueturboguy07/cue, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/Blueturboguy07__cue` — `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-cue/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit a27308ed233503902ee96f6d7c4882a7de1dbd47 — **the same commit and the same answer key as the Hobbes cell [cue-js-2026-09-20.md](cue-js-2026-09-20.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/cells/cue-provisioned/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha a27308ed`), regenerated by the command that cell names.

Index command: `unshare -rn codegraphcontext --db kuzudb --db-path <db> index <copy>  (0.6.13; CGC_RUNTIME_DB_TYPE=kuzudb)` — exit 0, wall 14 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked. 0.6.13 is the version the other 44 foreign cells ran, and PyPI's latest on 2026-09-26. The tool's summary lists the JavaScript files it parsed: .js 76, .cjs 1. It left a `.cgcignore` at the copy's root (its default ignore patterns), nothing else. The key was graded with the ingest's npm tree mounted (the Hobbes cell's provisioned arm), so the copy got the same tree (`~/.hobbes/cache/npm/cd588e5f47a77d71`) at `node_modules`; the tool stored no edge with a `node_modules` path. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. The tool's own log carries 0 error/warning line(s). Conversion: 1079 (site, callee) pairs, of which `oracle import` folded 14 repeated pair(s) (the same site and callee from two of the tool's rows), leaving 1,065 graded; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 576 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-cue/edges.json cue-provisioned/oracle.json codegraphcontext-cue --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-cue` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha a27308ed
hobbes edges 1065: confirmed 1010  contradicted 40  abstract 0  silent 15 map[line-unresolved:4 no-targets:11]
precision-against-oracle 96.2% (1010/1050)
precision-strict 95.8% (1010/1054): the 4 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 62.5% (1028/1644 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 5198; misses map[func-value→local-binding:30 func-value→parameter:23 static→anonymous-function:2 static→class:38 static→closure:124 static→function:245 static→method:154]
recall-collapsed 63.0% (1010/1603 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/30)  misses 30 = 4.9% of all misses
  recall[func-value→parameter]   0.0% (0/23)  misses 23 = 3.7% of all misses
  recall[static→anonymous-function]   0.0% (0/2)  misses 2 = 0.3% of all misses
  recall[static→class      ]   0.0% (0/38)  misses 38 = 6.2% of all misses
  recall[static→closure    ]  76.2% (397/521)  misses 124 = 20.1% of all misses
  recall[static→function   ]  69.4% (556/801)  misses 245 = 39.8% of all misses
  recall[static→method     ]  32.8% (75/229)  misses 154 = 25.0% of all misses
  tier codegraphcontext:EXTRACTED confirmed 1009  contradicted 40  abstract 0  silent 15
  tier codegraphcontext:INFERRED confirmed 1  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 427 edge(s) (several oracle sites on one line)
poison check: PASS — 1065 seeded wrong edges: 940 refused, 125 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1,065 |
| confirmed | 1,010 |
| contradicted | 40 |
| abstract | 0 |
| silent | 15 {"line-unresolved": 4, "no-targets": 11} |
| precision-against-oracle (lower bound) | **96.2%** (1,010/1,050) |
| precision strict (ADR-124: the 4 line-unresolved rows counted as contradicted) | **95.8%** (1,010/1,054) |
| recall | 62.5% (1,028/1,644) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 1009 / contradicted 40 / abstract 0 / silent 15; `codegraphcontext:INFERRED` confirmed 1 / contradicted 0 / abstract 0 / silent 0.

## Contradicted (40 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 39; same file as an oracle target, other line 1. By label: `codegraphcontext:EXTRACTED` 40.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `renderer/renderer.js:10` → `renderer/icons.js:31` [codegraphcontext:EXTRACTED] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:11` → `renderer/icons.js:31` [codegraphcontext:EXTRACTED] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:12` → `renderer/icons.js:31` [codegraphcontext:EXTRACTED] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:13` → `renderer/icons.js:31` [codegraphcontext:EXTRACTED] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:14` → `renderer/icons.js:31` [codegraphcontext:EXTRACTED] — oracle: ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22708, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22709, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22710
- `renderer/renderer.js:15` → `renderer/icons.js:31` [codegraphcontext:EXTRACTED] — oracle: ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22708, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22709, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22710

**Triage ratio (A-8):** 5 of 40 drawn at random and read by hand: `oracle-grain 0 : tool-wrong 5 : converter-defect 0` (below); the other 35 are untriaged. Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** 5 of the 40 drawn at random (seed 20260926) and read against the source: **tool-wrong 5**, oracle-grain 0, converter-defect 0 — `fetchImpl(…)` (a parameter of `timedFetch`) drawn to a test file's local `fetchImpl`; `this.session.stop(…)` drawn to the calling class's own `stop`; `read()` (a local returned by the test's fixture, twice) drawn to `src/vad.js`'s method `read`; `this.utteranceChunks.push(…)` (an array's `push`) drawn to the class's own `push`. The other 35 rows and the 4 line-unresolved rows are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 556 / 801 | 245 |
| `static→method` | 75 / 229 | 154 |
| `static→closure` | 397 / 521 | 124 |
| `static→class` | 0 / 38 | 38 |
| `func-value→local-binding` | 0 / 30 | 30 |
| `func-value→parameter` | 0 / 23 | 23 |
| `static→anonymous-function` | 0 / 2 | 2 |

**Poison check:** PASS — 1,065 seeded wrong edges: 940 refused, 125 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

