# Oracle cell — repowise 0.49.0 on cheerio, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@2` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/cheeriojs/cheerio, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio`, commit 98c7d13131c73163aab022e37d22ccd37f605b2c — **the same commit and the same answer key as the Hobbes cell [cheerio-ts-2026-08-27.md](cheerio-ts-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/cheerio-ts-r2/oracle.json` (`cell .  oracle tsc 6.0.3 (the zone's own) (resolution)  sha 98c7d131`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 6.4 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 208 (site, callee) pairs; dropped {"unknown-caller": 121, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh repowise-cheerio/edges.json cheerio-ts-r2/oracle.json repowise-cheerio --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-cheerio` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 6.0.3 (the zone's own) (resolution)  sha 98c7d131
hobbes edges 208: confirmed 167  contradicted 17  abstract 8  silent 16 map[not-loaded:16]
precision-against-oracle 90.8% (167/184)
recall 2.8% (168/5910 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 5180; misses map[func-value→local-binding:1070 func-value→variable:671 static→anonymous-signature:12 static→class:5 static→closure:173 static→function:3759 static→method:46 static→type-member:6]
  recall[func-value→local-binding]   0.0% (0/1070)  misses 1070 = 18.6% of all misses
  recall[func-value→variable]   1.9% (13/684)  misses 671 = 11.7% of all misses
  recall[static→anonymous-signature]   0.0% (0/12)  misses 12 = 0.2% of all misses
  recall[static→class      ]   0.0% (0/5)  misses 5 = 0.1% of all misses
  recall[static→closure    ]   0.0% (0/173)  misses 173 = 3.0% of all misses
  recall[static→function   ]   4.0% (155/3914)  misses 3759 = 65.5% of all misses
  recall[static→method     ]   0.0% (0/46)  misses 46 = 0.8% of all misses
  recall[static→type-member]   0.0% (0/6)  misses 6 = 0.1% of all misses
  tier repowise:global_unique confirmed 14  contradicted 7  abstract 0  silent 3
  tier repowise:import_merged confirmed 1  contradicted 3  abstract 0  silent 0
  tier repowise:import_scoped confirmed 71  contradicted 0  abstract 0  silent 1
  tier repowise:same_file confirmed 81  contradicted 7  abstract 8  silent 12
  line-grain tolerance used on 53 edge(s) (several oracle sites on one line)
poison check: PASS — 208 seeded wrong edges: 192 refused, 16 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 208 |
| confirmed | 167 |
| contradicted | 17 |
| abstract | 8 |
| silent | 16 {"not-loaded": 16} |
| precision-against-oracle (lower bound) | **90.8%** (167/184) |
| recall | 2.8% (168/5,910) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 14 / contradicted 7 / abstract 0 / silent 3; `repowise:import_merged` confirmed 1 / contradicted 3 / abstract 0 / silent 0; `repowise:import_scoped` confirmed 71 / contradicted 0 / abstract 0 / silent 1; `repowise:same_file` confirmed 81 / contradicted 7 / abstract 8 / silent 12.

## Contradicted (17 rows; all in report.json)

Mechanical shape (no reading): same file as an oracle target, other line 11; another file than every oracle target 6. By label: `repowise:same_file` 7, `repowise:global_unique` 7, `repowise:import_merged` 3.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `benchmark/benchmark.ts:48` → `benchmark/benchmark.ts:122` [repowise:same_file] — oracle: setup @ benchmark/benchmark.ts:44
- `benchmark/benchmark.ts:51` → `benchmark/benchmark.ts:284` [repowise:same_file] — oracle: __type.test @ benchmark/benchmark.ts:27
- `benchmark/benchmark.ts:60` → `benchmark/benchmark.ts:122` [repowise:same_file] — oracle: setup @ benchmark/benchmark.ts:44
- `benchmark/benchmark.ts:62` → `benchmark/benchmark.ts:284` [repowise:same_file] — oracle: Bench.add @ /home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/node_modules/tinybench/dist/index.d.ts:848, __type.test @ benchmark/benchmark.ts:27
- `src/api/attributes.ts:477` → `src/static.ts:72` [repowise:import_merged] — oracle: "/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/src/api/manipulation".clone @ src/api/manipulation.ts:1102, "/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/src/api/manipulation".wrap @ src/api/manipulation.ts:404, "/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/src/api/traversing".parent @ src/api/traversing.ts:239
- `src/api/attributes.ts:816` → `src/api/traversing.ts:684` [repowise:global_unique] — oracle: "/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/src/api/traversing".toArray @ src/api/traversing.ts:1054, Array.map @ /home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/node_modules/typescript/lib/lib.es5.d.ts:1468, "/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio/src/static".text @ src/static.ts:121

**Triage ratio (A-8), sampled:** over the tool's TypeScript cells, 5 contradicted rows drawn at random (five contradicted rows per tool per language, drawn at random from the language's cells after the converter@2 regrade; each read against the source by hand) read `oracle-grain 1 : tool-wrong 4 : converter-defect 0`; the rest of this cell's 17 rows are untriaged. The rows drawn from this cell:
- `src/api/traversing.ts:569` → `src/api/traversing.ts:792` [repowise:same_file]: **tool-wrong** — `Array.filter` drawn to `traversing.filter`
- `benchmark/benchmark.ts:48` → `benchmark/benchmark.ts:122` [repowise:same_file]: **oracle-grain** — the tool names the function the destructured `setup` binding holds; the oracle names the binding (D-O4's binding rule) — both true of the site
- `src/api/forms.ts:61` → `src/api/traversing.ts:1054` [repowise:global_unique]: **tool-wrong** — `this.map(` drawn to `traversing.toArray`
- `src/index.ts:84` → `src/static.ts:252` [repowise:import_merged]: **tool-wrong** — `new Writable(` (node:stream) drawn to a type alias `Writable<T>`
- `src/load.ts:173` → `src/static.ts:21` [repowise:import_merged]: **tool-wrong** — a parameter `render` shadows the module-level `render` the tool chose

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 155 / 3,914 | 3,759 |
| `func-value→local-binding` | 0 / 1,070 | 1,070 |
| `func-value→variable` | 13 / 684 | 671 |
| `static→closure` | 0 / 173 | 173 |
| `static→method` | 0 / 46 | 46 |
| `static→anonymous-signature` | 0 / 12 | 12 |
| `static→type-member` | 0 / 6 | 6 |
| `static→class` | 0 / 5 | 5 |

**Poison check:** PASS — 208 seeded wrong edges: 192 refused, 16 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

## Regrade, converter@5 (2026-09-26) — a `__module__` caller is a call site

`repowise-adapter@5` (ADR-101's 2026-09-26 amendment, C-94) reads a call edge whose caller is repowise's per-file `<file>::__module__` node — a call at the file's top level, or inside an anonymous function there — as call sites in that file at the tool's own `call_lines_json`; through @4 the converter dropped every such edge as `unknown-caller`. Re-converted from this cell's stored `raw.json` (no re-index; @4 re-run from the same `raw.json` reproduced the stored `edges.json` row for row first), source lines read from `/home/mmarrujo/.hobbes/bench/oracle/repos/cheerio`, graded by the same key with today's `oracle` binary. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-cheerio/at5`. Conversion: 208 → 1,496 (site, callee) pairs; `unknown-caller` 121 → 0 edges; 121 edge(s) read from a `__module__` caller. **Every row graded before keeps its bucket; everything that moved is a row the fix added.**

**Direction of fix (repowise-adapter@2 → repowise-adapter@5, 2026-09-26, signed):** graded edges 208 → 1,496 (+1,288); confirmed 167 → 1,348 (+1,181); contradicted 17 → 30 (+13); precision-against-oracle 90.8% (167/184) → 97.8% (1,348/1,378) (+7.1); strict = precision before and after (no line-unresolved row); recall 2.8% (168/5,910) → 23.2% (1,370/5,910). Nothing on the tool's side moved; the edges were in its store all along.

**The 13 new contradictions, read by hand (5 of 13) and counted mechanically (13 of 13): one shape, and not a wrong callee.** Each is a member call in a chain written over several lines — `const more = $(\n  '<li…>',\n).get();`, `$('li')\n  .last()`, `load(…)\n  .root()` — which repowise files at the chain's first line, where the key's resolved target is the `$`/`load` call. The callee the tool names (`get`, `last`, `insertBefore`, `root`) is the method the chain calls, one or two lines further down. This is the tool's site-line grain (its `call_lines_json` holds the call expression's first line), graded against the tool as the lane's rules require; the converter cannot recover the member's line from what the tool stores. The same mechanical count finds 7 of this cell's 17 earlier contradictions in that shape.

```
cell .  oracle tsc 6.0.3 (the zone's own) (resolution)  sha 98c7d131
hobbes edges 1496: confirmed 1348  contradicted 30  abstract 98  silent 20 map[not-loaded:17 unreachable:3]
precision-against-oracle 97.8% (1348/1378)
recall 23.2% (1370/5910 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 5180; misses map[func-value→local-binding:1070 func-value→variable:294 static→anonymous-signature:12 static→class:5 static→closure:173 static→function:2934 static→method:46 static→type-member:6]
recall-collapsed 35.2% (1348/3826 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/1070)  misses 1070 = 23.6% of all misses
  recall[func-value→variable]  57.0% (390/684)  misses 294 = 6.5% of all misses
  recall[static→anonymous-signature]   0.0% (0/12)  misses 12 = 0.3% of all misses
  recall[static→class      ]   0.0% (0/5)  misses 5 = 0.1% of all misses
  recall[static→closure    ]   0.0% (0/173)  misses 173 = 3.8% of all misses
  recall[static→function   ]  25.0% (980/3914)  misses 2934 = 64.6% of all misses
  recall[static→method     ]   0.0% (0/46)  misses 46 = 1.0% of all misses
  recall[static→type-member]   0.0% (0/6)  misses 6 = 0.1% of all misses
  tier repowise:global_unique confirmed 939  contradicted 20  abstract 2  silent 6
  tier repowise:import_merged confirmed 1  contradicted 3  abstract 24  silent 0
  tier repowise:import_scoped confirmed 240  contradicted 0  abstract 23  silent 1
  tier repowise:module_alias confirmed 40  contradicted 0  abstract 5  silent 0
  tier repowise:same_file confirmed 128  contradicted 7  abstract 44  silent 13
  line-grain tolerance used on 1109 edge(s) (several oracle sites on one line)
poison check: PASS — 1496 seeded wrong edges: 1476 refused, 20 unjudged (oracle silent there), 0 falsely confirmed
```
