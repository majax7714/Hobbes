# Oracle cell — codegraphcontext 0.6.13 on args, module `.`, 2026-09-15 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/Taywee/args, clone `/home/mmarrujo/.hobbes/bench/cpp-cells/draw-args`, commit 903b07dfdf5120622c9998a48ce97d64c0066556 — **the same commit and the same answer key as the Hobbes cell [args-cpp-2026-09-15.md](args-cpp-2026-09-15.md)**; the key is `/home/mmarrujo/.hobbes/bench/cpp-cells/args-cell/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 903b07df`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 3 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 6 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}. Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-args/edges.json args-cell/oracle.json codegraphcontext-args --lang cpp`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-args` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 903b07df
hobbes edges 0: confirmed 0  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle: undefined (no confirmed or contradicted edges)
recall 0.0% (0/3561 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1681; misses map[static→constructor:1332 static→function:865 static→method:1364]
recall-collapsed 0.0% (0/3161 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→constructor]   0.0% (0/1332)  misses 1332 = 37.4% of all misses
  recall[static→function   ]   0.0% (0/865)  misses 865 = 24.3% of all misses
  recall[static→method     ]   0.0% (0/1364)  misses 1364 = 38.3% of all misses
poison check: FAIL — 0 seeded wrong edges: 0 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 0 |
| confirmed | 0 |
| contradicted | 0 |
| silent | 0 |
| precision-against-oracle | **undefined** — nothing confirmed or contradicted (the tool stored 6 call edge(s) in this cell) |
| recall | 0.0% (0/3,561) |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): .

**Triage (hand-read, 2026-09-15):** Nothing to read. The tool stored six CALLS rows on args, all in `.ycm_extra_conf.py`, a Python file excluded as another language. Its parser table maps `.cpp`, `.h`, `.hpp` and `.hh` to C++, and args is one `.hxx` header with 101 `.cxx` test programs, so it read neither; its index log carries no error line. Precision is undefined and recall 0.0% (0/3,561). The poison line reads FAIL only because nothing was seeded, as on repowise's rust_proj cell. **Pre-registration (§10.7):** P32, P33 and P34 are undecidable on this cell (nothing graded).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→method` | 0 / 1,364 | 1,364 |
| `static→constructor` | 0 / 1,332 | 1,332 |
| `static→function` | 0 / 865 | 865 |

**Poison check:** FAIL — 0 seeded wrong edges: 0 refused, 0 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

