# Oracle cell — codegraphcontext 0.6.13 on xmpp.js, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/xmppjs/xmpp.js, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/xmppjs__xmpp.js` — `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-xmpp/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 9cce6c14a7f1c186c312f5c64f918ed88c69bca7 — **the same commit and the same answer key as the Hobbes cell [xmpp-js-2026-09-19.md](xmpp-js-2026-09-19.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/regrade/h34/xmpp/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9cce6c14`), regenerated by the command that cell names.

Index command: `unshare -rn codegraphcontext --db kuzudb --db-path <db> index <copy>  (0.6.13; CGC_RUNTIME_DB_TYPE=kuzudb)` — exit 0, wall 20 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked. 0.6.13 is the version the other 44 foreign cells ran, and PyPI's latest on 2026-09-26. The tool's summary lists the JavaScript files it parsed: .js 168, .cjs 3. It left a `.cgcignore` at the copy's root (its default ignore patterns), nothing else. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. The tool's own log carries 0 error/warning line(s). Conversion: 802 (site, callee) pairs, of which `oracle import` folded 5 repeated pair(s) (the same site and callee from two of the tool's rows), leaving 797 graded; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 491 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-xmpp/edges.json xmpp/oracle.json codegraphcontext-xmpp --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-xmpp` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9cce6c14
hobbes edges 797: confirmed 337  contradicted 71  abstract 0  silent 389 map[line-unresolved:34 no-targets:355]
precision-against-oracle 82.6% (337/408)
precision-strict 76.2% (337/442): the 34 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 40.7% (342/840 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 425; misses map[func-value→local-binding:18 func-value→parameter:64 func-value→variable:46 static→class:104 static→closure:6 static→function:127 static→method:133]
recall-collapsed 40.4% (337/834 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/18)  misses 18 = 3.6% of all misses
  recall[func-value→parameter]   0.0% (0/64)  misses 64 = 12.9% of all misses
  recall[func-value→variable]   0.0% (0/46)  misses 46 = 9.2% of all misses
  recall[static→class      ]   0.0% (0/104)  misses 104 = 20.9% of all misses
  recall[static→closure    ]  85.7% (36/42)  misses 6 = 1.2% of all misses
  recall[static→function   ]  66.2% (249/376)  misses 127 = 25.5% of all misses
  recall[static→method     ]  30.0% (57/190)  misses 133 = 26.7% of all misses
  tier codegraphcontext:EXTRACTED confirmed 327  contradicted 60  abstract 0  silent 305
  tier codegraphcontext:INFERRED confirmed 10  contradicted 11  abstract 0  silent 84
  line-grain tolerance used on 272 edge(s) (several oracle sites on one line)
poison check: PASS — 797 seeded wrong edges: 287 refused, 510 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 797 |
| confirmed | 337 |
| contradicted | 71 |
| abstract | 0 |
| silent | 389 {"line-unresolved": 34, "no-targets": 355} |
| precision-against-oracle (lower bound) | **82.6%** (337/408) |
| precision strict (ADR-124: the 34 line-unresolved rows counted as contradicted) | **76.2%** (337/442) |
| recall | 40.7% (342/840) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 327 / contradicted 60 / abstract 0 / silent 305; `codegraphcontext:INFERRED` confirmed 10 / contradicted 11 / abstract 0 / silent 84.

## Contradicted (71 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 66; same file as an oracle target, other line 5. By label: `codegraphcontext:EXTRACTED` 60, `codegraphcontext:INFERRED` 11.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `packages/base64/index.js:25` → `packages/base64/index.js:16` [codegraphcontext:EXTRACTED] — oracle: TextDecoder.decode @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:31769
- `packages/base64/index.js:3` → `packages/base64/index.js:1` [codegraphcontext:EXTRACTED] — oracle: TextEncoder.encode @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:31824
- `packages/connection-tcp/test/Connection.js:65` → `packages/resolve/index.js:59` [codegraphcontext:EXTRACTED] — oracle: fn @ packages/connection-tcp/test/Connection.js:63
- `packages/connection-tcp/test/Connection.js:65` → `packages/resolve/lib/dns.js:81` [codegraphcontext:EXTRACTED] — oracle: fn @ packages/connection-tcp/test/Connection.js:63
- `packages/connection-tcp/test/Connection.js:65` → `packages/resolve/lib/http.js:5` [codegraphcontext:EXTRACTED] — oracle: fn @ packages/connection-tcp/test/Connection.js:63
- `packages/connection-tcp/test/Connection.js:65` → `packages/resolve/resolve.js:4` [codegraphcontext:EXTRACTED] — oracle: fn @ packages/connection-tcp/test/Connection.js:63

**Triage ratio (A-8):** 5 of 71 drawn at random and read by hand: `oracle-grain 0 : tool-wrong 5 : converter-defect 0` (below); the other 66 are untriaged. Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** 5 of the 71 drawn at random (seed 20260926) and read against the source: **tool-wrong 5**, oracle-grain 0, converter-defect 0 — `ConnectionTCP.prototype.socketParameters(…)` in `packages/tcp/test.js`, whose `ConnectionTCP` is imported from `./lib/Connection.js`, drawn to `packages/connection-tcp/index.js`'s method; `http.resolve(…)` drawn to the calling function `resolve` itself; `cb?.()` (a parameter) drawn to `packages/resolve/resolve.js`'s `resolve`; `server.reset()` drawn to `fast.js`'s `reset`; `authenticate(…)` (a parameter) drawn to `packages/sasl2/index.js`'s function. The other 66 rows and the 34 line-unresolved rows are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→method` | 57 / 190 | 133 |
| `static→function` | 249 / 376 | 127 |
| `static→class` | 0 / 104 | 104 |
| `func-value→parameter` | 0 / 64 | 64 |
| `func-value→variable` | 0 / 46 | 46 |
| `func-value→local-binding` | 0 / 18 | 18 |
| `static→closure` | 36 / 42 | 6 |

**Poison check:** PASS — 797 seeded wrong edges: 287 refused, 510 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

