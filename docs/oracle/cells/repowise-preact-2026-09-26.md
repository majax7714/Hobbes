# Oracle cell — repowise 0.49.0 on preact, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@5` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/preactjs/preact, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/preactjs__preact` — `/home/mmarrujo/.hobbes/bench/comparative/repowise-preact/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 8101ff821690817c7786739c317af215c62a0cff — **the same commit and the same answer key as the Hobbes cell [preact-js-2026-09-19.md](preact-js-2026-09-19.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/regrade/h34/preact/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 8101ff82`), regenerated by the command that cell names.

Index command: `unshare -rn repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the copy)` — exit 0, wall 15 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked; 0.49.0 is the version the other published repowise cells ran (PyPI's latest on 2026-09-26 is 0.53.0, not used). It wrote `.repowise/` into the copy, nothing else. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. **Converted at converter@5** (ADR-101's 2026-09-26 amendment): 776 of the tool's call edges have a `<file>::__module__` caller and are read as sites in that file; at @4 they were dropped as `unknown-caller` and this cell graded 470 edges (`report.at4.*` beside the cell keeps that grade; it is not a published figure). The tool's own log carries 0 error/warning line(s). Conversion: 4664 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored); a `<file>::__module__` caller (a top-level call, no wiki_symbols row) is read as call sites in that file at the tool's call lines (converter@5; @4 dropped every such edge as unknown-caller).

Command: `bench/oracle/grade-foreign.sh repowise-preact/edges.json preact/oracle.json repowise-preact --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-preact` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 8101ff82
hobbes edges 4664: confirmed 1212  contradicted 1690  abstract 85  silent 1677 map[line-unresolved:452 no-targets:949 not-loaded:276]
precision-against-oracle 41.8% (1212/2902)
precision-strict 36.1% (1212/3354): the 452 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 15.2% (1398/9211 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 2090; misses map[func-value→local-binding:1588 func-value→parameter:112 func-value→variable:217 interface→type-member:1796 static→anonymous-function:1 static→class:570 static→closure:2023 static→function:1268 static→method:205 static→type-member:33]
recall-collapsed 13.8% (1212/8808 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/1588)  misses 1588 = 20.3% of all misses
  recall[func-value→parameter]   0.0% (0/112)  misses 112 = 1.4% of all misses
  recall[func-value→variable]   2.7% (6/223)  misses 217 = 2.8% of all misses
  recall[interface→type-member]   0.0% (0/1796)  misses 1796 = 23.0% of all misses
  recall[static→anonymous-function]   0.0% (0/1)  misses 1 = 0.0% of all misses
  recall[static→class      ]   1.0% (6/576)  misses 570 = 7.3% of all misses
  recall[static→closure    ]   0.7% (15/2038)  misses 2023 = 25.9% of all misses
  recall[static→function   ]  42.7% (946/2214)  misses 1268 = 16.2% of all misses
  recall[static→method     ]   0.0% (0/205)  misses 205 = 2.6% of all misses
  recall[static→type-member]   0.0% (0/33)  misses 33 = 0.4% of all misses
  recall[static→variable   ] 100.0% (425/425)  misses 0 = 0.0% of all misses
  tier repowise:global_unique confirmed 0  contradicted 263  abstract 57  silent 885
  tier repowise:import_merged confirmed 0  contradicted 210  abstract 0  silent 57
  tier repowise:import_scoped confirmed 1042  contradicted 1191  abstract 18  silent 470
  tier repowise:module_alias confirmed 0  contradicted 0  abstract 0  silent 10
  tier repowise:same_file confirmed 170  contradicted 26  abstract 10  silent 247
  tier repowise:self_scope confirmed 0  contradicted 0  abstract 0  silent 8
  line-grain tolerance used on 1882 edge(s) (several oracle sites on one line)
poison check: PASS — 4664 seeded wrong edges: 2765 refused, 1899 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 4,664 |
| confirmed | 1,212 |
| contradicted | 1,690 |
| abstract | 85 |
| silent | 1,677 {"line-unresolved": 452, "no-targets": 949, "not-loaded": 276} |
| precision-against-oracle (lower bound) | **41.8%** (1,212/2,902) |
| precision strict (ADR-124: the 452 line-unresolved rows counted as contradicted) | **36.1%** (1,212/3,354) |
| recall | 15.2% (1,398/9,211) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 0 / contradicted 263 / abstract 57 / silent 885; `repowise:import_merged` confirmed 0 / contradicted 210 / abstract 0 / silent 57; `repowise:import_scoped` confirmed 1042 / contradicted 1191 / abstract 18 / silent 470; `repowise:module_alias` confirmed 0 / contradicted 0 / abstract 0 / silent 10; `repowise:same_file` confirmed 170 / contradicted 26 / abstract 10 / silent 247; `repowise:self_scope` confirmed 0 / contradicted 0 / abstract 0 / silent 8.

## Contradicted (1690 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 1664; same file as an oracle target, other line 26. By label: `repowise:import_scoped` 1191, `repowise:global_unique` 263, `repowise:import_merged` 210, `repowise:same_file` 26.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `compat/src/index.js:146` → `demo/logger.jsx:163` [repowise:global_unique] — oracle: flush @ compat/src/index.js:140
- `compat/test/browser/Children.test.jsx:188` → `test/_util/helpers.jsx:232` [repowise:import_merged] — oracle: Foo @ compat/test/browser/Children.test.jsx:178
- `compat/test/browser/Children.test.jsx:36` → `test/_util/helpers.jsx:232` [repowise:import_merged] — oracle: Foo @ compat/test/browser/Children.test.jsx:24
- `compat/test/browser/Children.test.jsx:69` → `test/_util/helpers.jsx:232` [repowise:import_merged] — oracle: Foo @ compat/test/browser/Children.test.jsx:48
- `compat/test/browser/Children.test.jsx:89` → `test/_util/helpers.jsx:232` [repowise:import_merged] — oracle: Foo @ compat/test/browser/Children.test.jsx:80
- `compat/test/browser/PureComponent.test.jsx:143` → `test/browser/refs.test.jsx:7` [repowise:global_unique] — oracle: spy @ compat/test/browser/PureComponent.test.jsx:140

**Triage ratio (A-8):** 6 of 1,690 read by hand (below): `oracle-grain 6 : tool-wrong 0 : converter-defect 0`; the other 1,684 are untriaged, and the mechanical split below says where most of them sit. Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** **A mechanical split first, then a reading.** 1,222 of the 1,690 rows are one shape. The key names a declaration in one of the repo's own `.d.ts` files. Most rows are `render`, `createElement`, `Fragment` and `createContext` in `src/index.d.ts`, which the key reaches through the tests' self-import `from 'preact'`, resolved by the package's own `exports`. The tool's callee is a function of the same name in a `.js` file under that `.d.ts` file's directory, such as `src/render.js`'s `render`. The rest: 245 name another declaration in a `.d.ts`, and 223 have no in-repo `.d.ts` target. **6 of the 1,222 were read by hand, drawn at random with seed 20260926, and all 6 are oracle-grain.** `render(…)` (four rows), `<Fragment>` and `act(…)` are drawn to the implementation that runs: `src/index.js` re-exports `render` from `./render`, and the test-utils `act` is `test-utils/src/index.js`'s. The key names the hand-written typing, which scip-typescript also names, so Hobbes' edges there confirm. The other 1,216 rows of the shape, and the 468 outside it, are untriaged. By the lane's rule all of them count as contradicted; read at runtime, a large part of this cell's contradictions are the key's grain, not the tool's error.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→closure` | 15 / 2,038 | 2,023 |
| `interface→type-member` | 0 / 1,796 | 1,796 |
| `func-value→local-binding` | 0 / 1,588 | 1,588 |
| `static→function` | 946 / 2,214 | 1,268 |
| `static→class` | 6 / 576 | 570 |
| `func-value→variable` | 6 / 223 | 217 |
| `static→method` | 0 / 205 | 205 |
| `func-value→parameter` | 0 / 112 | 112 |
| `static→type-member` | 0 / 33 | 33 |
| `static→anonymous-function` | 0 / 1 | 1 |
| `static→variable` | 425 / 425 | 0 |

**Poison check:** PASS — 4,664 seeded wrong edges: 2,765 refused, 1,899 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

