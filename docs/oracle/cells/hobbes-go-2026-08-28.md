# Oracle cell — hobbes, `go` module, 2026-08-28 (O2 regraded contained, after the Go scope veto)

Produced by `bench/oracle/run-cell.sh . go <out>` on the dirty tree at d6cccf42 + the ADR-092 and triage work; oracle go-rta (x/tools), 20 roots (4 binaries + 16 test mains), default build tags. **Ingest and cell run contained** (ADR-092). Cell runtime 501 s with the six-language ingest.

```
hobbes edges 1284: confirmed 1283  contradicted 0  abstract 0  silent 1 map[unreachable:1]
precision-against-oracle 100.0% (1283/1283)
recall 87.5% (1285/1468 in-repo oracle pairs) at 20 roots; external oracle pairs 12022; misses map[func-value→closure:138 func-value→named:6 interface→named:2 static→closure:37]
  tier semantic   confirmed 1283  contradicted 0  abstract 0  silent 1
poison check: PASS — 1284 seeded wrong edges: 1283 refused, 1 unjudged (oracle silent there), 0 falsely confirmed
```

**Against the 2026-08-25 cell** (1,278 confirmed, 3 contradicted, 1 silent, 99.8%): the three syntactic contradictions — the test helper's `run := func(...)` bound to `main.run` (`cmd/hobbes-session/main_test.go:55/57/58`) — are gone; they were the first sighting of the shape the fzf cell showed at scale, and the Go scope veto (ADR-090 extended, 2026-08-28) removes them here too. Confirmed 1,278 → 1,283 is the tree growing (the containment and oracle work), not the veto. Recall and the miss classes unchanged (C-58). No syntactic edges remain in the cell.

**Triage ratio (A-8):** 0 contradicted — `0 : 0 : 0`. **Direction of fix:** the product fix that landed for fzf; this cell is its check on the original sighting. The 2026-08-25 record was a host run; this one is the Go cell re-earned under containment.
