# Oracle cell — hobbes, `pipeline` zone, 2026-08-28 (O6 contained vs host — ADR-092 phase 2)

Produced twice on the same dirty tree (sha d6cccf42 + the ADR-092 work) by `bench/oracle/run-cell.sh . pipeline <out> --lang py --no-ingest --runs 1 -- -q -p no:cacheprovider -m "not lane_b" --ignore=tests/test_containment.py`: once **inside the sandbox image** (the tree as an overlay, the venv's interpreter chain ro, no network) and once on the host with `HOBBES_UNCONTAINED=1`. The tests that *test containment* are deselected in both, because they take different branches by construction (inside the image there is no nested podman, so every containment-sensitive test skips or refuses; on the host the escape hatch is set). Not comparable to the 2026-08-25 cell: the tree moved. Cell runtime 58 s / 59 s.

```
cell pipeline  oracle py-trace 3.12.13 sys.monitoring (trace)  sha d6cccf42
oracle ran contained (ADR-092)
hobbes edges 3633: confirmed 3311  suspect 5  unobserved 317 map[line-mixed:4 line-not-called:216 not-loaded:97]
confirmation rate 91.1% (3311/3633 hobbes edges; coverage-limited, not precision)
suspect rate 0.2% (5/3316 executed hobbes edges; triage queue, never contradicted)
recall-against-executed 86.1% (3311/3846 observed in-repo pairs); external python targets 2660; misses map[observed→closure:361 observed→function:88 observed→lambda:73 observed→method:13]
coverage: hobbes sites observed 2992/3289 (91.0%); files loaded 114/131; declarations started 1677/2137; c-callee calls 504208; subprocesses traced 0
```

Host run: confirmed **3319**, suspect 5, unobserved 309; recall 86.1% (3319/3854); coverage 3000/3289, declarations started 1678; misses identical (361/88/73/13).

**Residue, explained — not oracle drift.** Site sets ignoring hit counts: 7,745 vs 7,752, 4 only-contained, 11 only-host. Every differing site is inside one test, `tests/test_bench.py::TestSoloPolicy::test_bench_box_grants_tests_and_commit_and_keeps_guarantees` (its `shutil.which` probe finds no container runtime inside the image and the test skips at line 603; on the host it runs its closure body, lines 605–615), plus `bench/verdict.py::docker_host_env` and one `orchestrate.run_task` branch that test drives. The eight extra confirmed edges on the host are that body. Hit counts on shared sites differ by a few (`spawn` 142 vs 146, `has_ts_files` 1,994 vs 1,993) for the same reason. The suspect set (5) and every miss class are identical. **The oracle moved nothing; the traced program probes its environment.** This is C-60's shape (a trace grades what ran), noted here rather than as an H-entry: nothing in the harness is wrong, and the rule to carry forward is that a cell's suite must not depend on the host it runs on — or the cell states which tests it deselects, as this one does.

**Triage ratio (A-8):** 0 contradicted (trace oracle: suspects only) — `oracle-wrong 0 : hobbes-wrong 0 : untriaged 5`, the same five suspects on both sides.

**Direction of fix on regrade:** none needed on the oracle side.

**Poison check** (`~/.hobbes/bench/oracle/hobbes-py-contained/report.txt`, the contained run above; quoted 2026-09-09 so the record carries the line the comparative graphics read, ADR-102):

```
poison check: PASS — 3633 seeded wrong edges: 2905 refused, 728 unjudged (oracle silent there), 0 falsely confirmed
```

## Regrade 2026-09-10 (later still; Hobbes 0.1.8-beta — the versioned baseline: same clone, a fresh trace over the tree at ea72308 (the tree moved), one run, contained)

Every Hobbes cell was re-ingested on one build and regraded against its standing key so the comparative graphics carry one version (Max, 2026-09-10; ADR-103). Artifacts `~/.hobbes/bench/v018/hobbes-py/`; a fresh trace, since the tree moved: 5,311 edges (was 3,633 with 3,311 confirmed — the expression receivers C-80 counts since 2026-09-03 and the code written since), 4,741 confirmed, confirmation rate 89.3%, recall-against-executed 84.6%; 10 suspects (was 5), every one a test's mock observed where the graph names the real callee (`containment.run`, `run_helper`, the `_index_*` steps — C-60's asymmetry, the same class as before).

```
cell pipeline  oracle py-trace 3.12.13 sys.monitoring (trace)  sha ea723087
oracle ran contained (ADR-092)
hobbes edges 5311: confirmed 4741  suspect 10  unobserved 560 map[line-mixed:5 line-not-called:383 not-loaded:172]
confirmation rate 89.3% (4741/5311 hobbes edges; coverage-limited, not precision)
suspect rate 0.2% (10/4751 executed hobbes edges; triage queue, never contradicted)
recall-against-executed 84.6% (4741/5604 observed in-repo pairs) over 1 run(s) of [/home/mmarrujo/hobbes_public/pipeline/.venv/bin/python -m pytest -q -p no:cacheprovider -m not lane_b --ignore=tests/test_containment.py]; external python targets 4010; misses map[observed→closure:640 observed→function:118 observed→lambda:96 observed→method:9]
coverage: hobbes sites observed 4315/4837 (89.2%); files loaded 145/169; declarations started 2358/2957; c-callee calls 786411; subprocesses traced 0
  recall[observed→class    ] 100.0% (410/410)  misses 0 = 0.0% of all misses
  recall[observed→closure  ]  20.0% (160/800)  misses 640 = 74.2% of all misses
  recall[observed→function ]  97.0% (3790/3908)  misses 118 = 13.7% of all misses
  recall[observed→lambda   ]   0.0% (0/96)  misses 96 = 11.1% of all misses
  recall[observed→method   ]  97.7% (381/390)  misses 9 = 1.0% of all misses
  tier semantic   confirmed 4741  suspect 10  unobserved 559
  tier syntactic  confirmed 0  suspect 0  unobserved 1
poison check: PASS — 5311 seeded wrong edges: 4040 refused, 1271 unjudged (oracle silent there), 0 falsely confirmed
cell pipeline of /home/mmarrujo/hobbes_public: 127s
```


## Regrade 2026-09-10 (later still; Hobbes 0.1.10-beta — every cell on one build again: same clone, a fresh trace over the tree at 1c65190 (the tree moved), one run, contained)

Every Hobbes cell was re-ingested on 0.1.10-beta and regraded against its standing key so the comparative graphics state one version (Max, 2026-09-10; ADR-103, third amendment). Artifacts `~/.hobbes/bench/v0110/hobbes-py/`; what moved from the 0.1.8-beta grade: `hobbes edges 5311: confirmed 4741  suspect 10  unobserved 560 map[line-mixed:5 line-not-called:383 not-loaded:` → `hobbes edges 5316: confirmed 4743  suspect 10  unobserved 563 map[line-mixed:5 line-not-called:384 not-loaded:`; `confirmation rate 89.3% (4741/5311 hobbes edges; coverage-limited, not precision)` → `confirmation rate 89.2% (4743/5316 hobbes edges; coverage-limited, not precision)`; `suspect rate 0.2% (10/4751 executed hobbes edges; triage queue, never contradicted)` → `suspect rate 0.2% (10/4753 executed hobbes edges; triage queue, never contradicted)`; `recall-against-executed 84.6% (4741/5604 observed in-repo pairs) over 1 run(s) of [/home/mmarrujo/hobbes_publi` → `recall-against-executed 84.6% (4743/5608 observed in-repo pairs) over 1 run(s) of [/home/mmarrujo/hobbes_publi`; `coverage: hobbes sites observed 4315/4837 (89.2%); files loaded 145/169; declarations started 2358/2957; c-cal` → `coverage: hobbes sites observed 4317/4842 (89.2%); files loaded 145/169; declarations started 2359/2958; c-cal`; `recall[observed→closure  ]  20.0% (160/800)  misses 640 = 74.2% of all misses` → `recall[observed→closure  ]  20.0% (160/802)  misses 642 = 74.2% of all misses`.

```
cell pipeline  oracle py-trace 3.12.13 sys.monitoring (trace)  sha 1c651905
oracle ran contained (ADR-092)
hobbes edges 5316: confirmed 4743  suspect 10  unobserved 563 map[line-mixed:5 line-not-called:384 not-loaded:174]
confirmation rate 89.2% (4743/5316 hobbes edges; coverage-limited, not precision)
suspect rate 0.2% (10/4753 executed hobbes edges; triage queue, never contradicted)
recall-against-executed 84.6% (4743/5608 observed in-repo pairs) over 1 run(s) of [/home/mmarrujo/hobbes_public/pipeline/.venv/bin/python -m pytest -q -p no:cacheprovider -m not lane_b --ignore=tests/test_containment.py]; external python targets 4007; misses map[observed→closure:642 observed→function:118 observed→lambda:96 observed→method:9]
coverage: hobbes sites observed 4317/4842 (89.2%); files loaded 145/169; declarations started 2359/2958; c-callee calls 786545; subprocesses traced 0
  recall[observed→class    ] 100.0% (410/410)  misses 0 = 0.0% of all misses
  recall[observed→closure  ]  20.0% (160/802)  misses 642 = 74.2% of all misses
  recall[observed→function ]  97.0% (3791/3909)  misses 118 = 13.6% of all misses
  recall[observed→lambda   ]   0.0% (0/96)  misses 96 = 11.1% of all misses
  recall[observed→method   ]  97.7% (382/391)  misses 9 = 1.0% of all misses
  tier semantic   confirmed 4743  suspect 10  unobserved 562
  tier syntactic  confirmed 0  suspect 0  unobserved 1
poison check: PASS — 5316 seeded wrong edges: 4042 refused, 1274 unjudged (oracle silent there), 0 falsely confirmed
```

**What moved, read:** the tree moved (the session's commits on `scipsource.py`, `containment.py` and their tests), so lines shifted and five edges grew — 4,741 → 4,743 confirmed, 10 suspects both times, every suspect C-60's mock asymmetry as before. Not comparable line-for-line to the 0.1.8-beta grade for that reason; the shape is the same.
