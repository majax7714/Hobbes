# Oracle cell — codegraphcontext 0.6.13 on fzf, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@2` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/junegunn/fzf, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/fzf`, commit f7ae439ff5b2f8298716d39783a546db793b0625 — **the same commit and the same answer key as the Hobbes cell [fzf-go-2026-08-27.md](fzf-go-2026-08-27.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/fzf-go-r2/oracle.json` (`cell .  oracle go-rta (reachability)  sha f7ae439f`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 22.4 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 6503 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 383 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-fzf/edges.json fzf-go-r2/oracle.json codegraphcontext-fzf --lang <lang>`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-fzf` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle go-rta (reachability)  sha f7ae439f
hobbes edges 3517: confirmed 2720  contradicted 423  abstract 0  silent 374 map[no-targets:218 not-loaded:136 unreachable:20]
precision-against-oracle 86.5% (2720/3143)
recall 39.0% (2742/7035 in-repo oracle pairs) at 5 roots; external oracle pairs 8094; misses map[func-value→closure:2457 func-value→named:81 interface→named:106 static→closure:1212 static→named:437]
  recall[func-value→closure]   0.0% (0/2457)  misses 2457 = 57.2% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/81)  misses 81 = 1.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]  74.6% (311/417)  misses 106 = 2.5% of all misses
  recall[static→closure    ]   0.0% (0/1212)  misses 1212 = 28.2% of all misses
  recall[static→named      ]  84.8% (2431/2868)  misses 437 = 10.2% of all misses
  tier codegraphcontext:EXTRACTED confirmed 1617  contradicted 123  abstract 0  silent 355
  tier codegraphcontext:INFERRED confirmed 1103  contradicted 300  abstract 0  silent 19
  line-grain tolerance used on 855 edge(s) (several oracle sites on one line)
poison check: PASS — 3517 seeded wrong edges: 3143 refused, 374 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 3,517 |
| confirmed | 2,720 |
| contradicted | 423 |
| abstract | 0 |
| silent | 374 {"no-targets": 218, "not-loaded": 136, "unreachable": 20} |
| precision-against-oracle (lower bound) | **86.5%** (2,720/3,143) |
| recall | 39.0% (2,742/7,035) at 5 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 1617 / contradicted 123 / abstract 0 / silent 355; `codegraphcontext:INFERRED` confirmed 1103 / contradicted 300 / abstract 0 / silent 19.

## Contradicted (423 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 394; same file as an oracle target, other line 29. By label: `codegraphcontext:INFERRED` 300, `codegraphcontext:EXTRACTED` 123.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `main.go:100` → `src/tui/light.go:1557` [codegraphcontext:INFERRED] — oracle: fmt.Print @ /home/mmarrujo/.local/go/src/fmt/print.go:271
- `main.go:103` → `src/util/concurrent_set.go:26` [codegraphcontext:INFERRED] — oracle: os.Remove @ /home/mmarrujo/.local/go/src/os/file_unix.go:357
- `main.go:107` → `src/core.go:55` [codegraphcontext:INFERRED] — oracle: (*os/exec.Cmd).Run @ /home/mmarrujo/.local/go/src/os/exec/exec.go:628
- `main.go:108` → `src/tui/light.go:1557` [codegraphcontext:INFERRED] — oracle: fmt.Print @ /home/mmarrujo/.local/go/src/fmt/print.go:271
- `main.go:86` → `src/tui/light.go:1557` [codegraphcontext:INFERRED] — oracle: fmt.Print @ /home/mmarrujo/.local/go/src/fmt/print.go:271
- `src/algo/algo.go:1039` → `src/util/atomicbool.go:26` [codegraphcontext:INFERRED] — oracle: (*github.com/junegunn/fzf/src/util.Chars).Get @ src/util/chars.go:239, github.com/junegunn/fzf/src/algo.indexAt @ src/algo/algo.go:97

**Triage ratio (A-8), sampled:** over the tool's Go cells, 5 contradicted rows drawn at random (five contradicted rows per tool per language, drawn at random from the language's cells after the converter@2 regrade; each read against the source by hand) read `oracle-grain 0 : tool-wrong 5 : converter-defect 0`; the rest of this cell's 423 rows are untriaged. The rows drawn from this cell:
- `src/ansi_test.go:380` → `src/tui/tcell_test.go:13` [codegraphcontext:INFERRED]: **tool-wrong** — a local closure `assert := func` shadows; the tool bound the name to a same-named function in another package
- `src/terminal.go:6844` → `src/terminal.go:133` [codegraphcontext:EXTRACTED]: **tool-wrong** — wrong receiver: `(*EventBox).Set` drawn to `(*resumableState).Set`
- `src/terminal.go:1415` → `src/history.go:53` [codegraphcontext:EXTRACTED]: **tool-wrong** — the builtin `append` drawn to a method named `append`

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `func-value→closure` | 0 / 2,457 | 2,457 |
| `static→closure` | 0 / 1,212 | 1,212 |
| `static→named` | 2,431 / 2,868 | 437 |
| `interface→named` | 311 / 417 | 106 |
| `func-value→named` | 0 / 81 | 81 |

**Poison check:** PASS — 3,517 seeded wrong edges: 3,143 refused, 374 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

