# Oracle cell — DaveGamble/cJSON, build root `.`, 2026-09-12 (O9, the first C cell)

**Repo:** https://github.com/DaveGamble/cJSON (MIT), clone at `~/.hobbes/bench/oracle/repos/cJSON`, commit `fb16e5cf358798aabb049655975cde8427101056`.
- **Why this repo:** it is the repo C's lane A and lane B were built on (ADR-108, ADR-109), chosen for shape, not drawn: a small C89 library, its tests, and the Unity test framework vendored under `tests/unity`.
- **Ingest:** SHA `fb16e5cf`. The tree reads dirty only because `hobbes ingest` appends `.hobbes/` to the clone's `.gitignore` (ADR-012). Hobbes 0.2.4-beta at `055d3fe`, lane B on (`HOBBES_SCIP=1`), scip-clang 0.4.0 over CMake's export, contained (`index-c`).
- **Oracle:** `c-clang` (ADR-110), `Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json`. Roots line: `compile database: cmake (23 units)`, CMake's export at its default options, derived in the image with no network, 0 units failed. Kind `resolution`, no roots.

**Command:** `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/cJSON . ~/.hobbes/bench/oracle/cjson-c --lang c` (re-run with `--no-ingest` after the containment fix `17def60`). Runtime 8 s. The outputs are in `~/.hobbes/bench/oracle/cjson-c/` (`hobbes.json`, `oracle.json`, `report.json`, `report.txt`, `clang-shards/`, `cmake-build/`).

## Numbers, Hobbes 0.2.4-beta (report.txt, verbatim; the 728 `missed` rows elided)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf
oracle ran contained (ADR-092)
hobbes edges 1713: confirmed 1188  contradicted 0  abstract 0  silent 525 map[not-loaded:513 unreachable:12]
coverage: map[sites_dynamic:30 sites_external:259 sites_link_ambiguous:0 sites_macro:728 sites_static:1190 sites_tu_split:0 sites_undefined:0 units:23 units_failed:0]
precision-against-oracle 100.0% (1188/1188)
recall 62.0% (1190/1918 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 259; misses map[macro→function:728]
recall-collapsed 62.3% (1188/1908 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/728)  misses 728 = 100.0% of all misses
  recall[static→function   ] 100.0% (1190/1190)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 1188  contradicted 0  abstract 0  silent 0
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 525
  line-grain tolerance used on 319 edge(s) (several oracle sites on one line)
poison check: PASS — 1713 seeded wrong edges: 1188 refused, 525 unjudged (oracle silent there), 0 falsely confirmed
```

| bucket | count |
|---|---|
| hobbes edges (graded) | 1,713 (1,188 semantic, 525 syntactic) |
| excluded before grading | 1,821 edges to `macro` symbols (a macro invocation is expanded, not called) |
| confirmed | 1,188 |
| contradicted | 0 |
| silent | 525 (not-loaded 513, unreachable 12) |
| precision-against-oracle | **100.0%** (1,188/1,188) |
| in-repo oracle pairs | 1,918 |
| recall (no roots; every resolved site) | 62.0% (1,190/1,918); collapsed 62.3% |
| external oracle pairs (not graded) | 259 (libc, math, builtins) |
| lane agreement at the ingest | 1,717 sites, 0 disagreements |

**Triage ratio (A-8):** 0 contradicted, so `oracle-wrong 0 : hobbes-wrong 0 : untriaged 0`.

## Misses by class

**`macro→function`, 728 (100%).** Every one is at a line under `tests/`: a call a macro's expansion makes. 723 go into Unity through its assertion and runner macros, and 5 into cJSON's own `cJSON_SetNumberHelper` through the library's `cJSON_SetNumberValue` API macro (*corrected 2026-09-12:* this record first said every miss was Unity's). The macro's body spells the call; the test line spells only the invocation.

| target | pairs |
|---|---|
| `UnityFail` | 380 |
| `UnityDefaultTestRun` | 153 |
| `UnityAssertEqualNumber` | 51 |
| `UnityAssertEqualString` | 29 |
| `UnityAssertBits` | 27 |
| `UnityBegin`, `UnityEnd` | 18 each |
| `UnityAssertDoublesWithin` | 13 |
| other Unity entry points | 34 |
| `cJSON_SetNumberHelper` (cJSON's own, via `cJSON_SetNumberValue`) | 5 |

Hobbes draws each of those lines to the macro (`TEST_ASSERT_TRUE`, `RUN_TEST`, …), which the export excludes before grading, and never to the function the expansion calls. That is C-131's face on the recall side: the preprocessor's output is the compiler's knowledge, and lane A reads the source as written.

**`static→function`: 1,190 of 1,190.** Every call a cJSON or Unity line spells directly, to a definition the build compiles, is drawn.

## Silent (525, all syntactic, charged to nobody)

- **`not-loaded` 513:** edges in files CMake's default configuration does not compile.
  - `tests/unity` 215: Unity's own tests, examples and extras.
  - `cJSON_Utils.c` 161, `tests/old_utils_tests.c` 65, `tests/json_patch_tests.c` 40, `tests/misc_utils_tests.c` 26: `ENABLE_CJSON_UTILS` is off by default.
  - `fuzzing/afl.c` 6.

  Lane B indexed none of these files either, which is why every edge in them is lane A's fallback.
- **`unreachable` 12:** all in `tests/unity/src/unity.c`, calls on lines inside `#if` arms this build leaves out (`UnityPrint`, `IsStringInBiggerString`). Lane A reads both arms (C-131); clang sees one.

So this cell grades cJSON's **semantic** tier, and only it. No syntactic edge sat in a compiled file where lane B left a site to the fallback.

**The unit count.** The evidence log's lane-B row said CMake's export gave 27 units. That was the spike's number. On a staged copy in the image, CMake's defaults give 23, the same 23 the oracle derived. With `-DENABLE_CJSON_UTILS=On` there are 27: the 23 plus `cJSON_Utils.c` and its three test files. The log is corrected.

**Poison check:** PASS — 1,713 seeded wrong edges: 1,188 refused, 525 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix** (which side would have to change; no proposals):
- `macro→function`: Hobbes, if a C macro's invocation is ever to draw the calls its expansion makes. Lane B's index already holds the expansion's references at the invocation.
- Silent: neither side. The build's configuration decides what is compiled.

**Pre-registration (`oracle-grading.md` §10.5):**
- P18 **met** (semantic 1,188/1,188, 0 hobbes-wrong).
- P19 **undecidable** (0 syntactic edges judged; all 525 silent).
- P20 **met** (`macro→*` is 100% of misses).
- P21 **met** (`static→*` recall 100%).
- P22 **met** (`not-loaded` 513 of 525 silent).
- P24 **met** here.
