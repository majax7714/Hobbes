# Oracle cell — rust_proj, the whole crate, 2026-08-28 (O7 regraded contained — ADR-092 phase 2)

Produced by `bench/oracle/run-cell.sh ~/rust_proj . <out> --lang rust` at sha 64acba88 (unchanged); oracle `rustc-mir` on rustc 1.100.0-nightly (e7769602a 2026-08-24), the same nightly, **run inside the sandbox image**: the crate as an overlay at its host path, the cell dir and the Hobbes cache rw, the nightly sysroot and the driver ro, `cargo fetch` in a networked container first, `cargo check --all-targets` with no network. Ingest contained too (ADR-092 phase 1). Cell runtime 15 s with ingest.

**Numeric no-op, as phase 2 requires.** `oracle.json` is byte-identical to the 2026-08-25 cell's except for the new `containment: "contained"` field; `report.json` identical except that field and the poison block. Every number below is the stored cell's.

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88
oracle ran contained (ADR-092)
hobbes edges 17: confirmed 17  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle 100.0% (17/17)
recall 81.0% (17/21 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 59; misses map[macro→function:4]
  recall[macro→function    ]   0.0% (0/4)  misses 4 = 100.0% of all misses
  recall[static→function   ] 100.0% (17/17)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 17  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 9 edge(s) (several oracle sites on one line)
poison check: PASS — 17 seeded wrong edges: 17 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
```

**Triage ratio (A-8):** 0 contradicted — `oracle-wrong 0 : hobbes-wrong 0 : untriaged 0`.

**Direction of fix on regrade:** none needed — the containment change moved no number. Had it, the drift would have been a new H-entry before anything else landed (ADR-092 phase 2's rule).

## Regrade 2026-09-10 (later still; Hobbes 0.1.8-beta — the versioned baseline: same clone, the 2026-08-28 MIR key, contained)

Every Hobbes cell was re-ingested on one build and regraded against its standing key so the comparative graphics carry one version (Max, 2026-09-10; ADR-103). Artifacts `~/.hobbes/bench/v018/rust_proj/`; unchanged to the digit from the standing grade.

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88
oracle ran contained (ADR-092)
hobbes edges 17: confirmed 17  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle 100.0% (17/17)
recall 81.0% (17/21 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 59; misses map[macro→function:4]
  recall[macro→function    ]   0.0% (0/4)  misses 4 = 100.0% of all misses
  recall[static→function   ] 100.0% (17/17)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 17  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 9 edge(s) (several oracle sites on one line)
poison check: PASS — 17 seeded wrong edges: 17 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
```


## Regrade 2026-09-10 (later still; Hobbes 0.1.10-beta — every cell on one build again: same clone, the 2026-08-28 MIR key, contained)

Every Hobbes cell was re-ingested on 0.1.10-beta and regraded against its standing key so the comparative graphics state one version (Max, 2026-09-10; ADR-103, third amendment). Artifacts `~/.hobbes/bench/v0110/rust_proj/`; **unchanged to the digit from the 0.1.8-beta grade**; the one new line is the grader's `recall-collapsed` (ADR-089 amended).

```
cell   oracle rustc-mir rustc 1.100.0-nightly (e7769602a 2026-08-24) (resolution)  sha 64acba88
oracle ran contained (ADR-092)
hobbes edges 17: confirmed 17  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle 100.0% (17/17)
recall 81.0% (17/21 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 59; misses map[macro→function:4]
recall-collapsed 81.0% (17/21 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/4)  misses 4 = 100.0% of all misses
  recall[static→function   ] 100.0% (17/17)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 17  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 9 edge(s) (several oracle sites on one line)
poison check: PASS — 17 seeded wrong edges: 17 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
```
