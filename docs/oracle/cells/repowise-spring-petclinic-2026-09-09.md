# Oracle cell — repowise 0.49.0 on spring-petclinic, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@2` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/spring-projects/spring-petclinic, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/spring-petclinic`, commit 818c4136ea971c21674525f9053de0d9c7ad8cfe — **the same commit and the same answer key as the Hobbes cell [petclinic-java-2026-08-29.md](petclinic-java-2026-08-29.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/spring-petclinic-java/oracle.json` (`cell   oracle javac 21.0.12.1+1-LTS (resolution)  sha 818c4136`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 4.7 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 365 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh repowise-spring-petclinic/edges.json spring-petclinic-java/oracle.json repowise-spring-petclinic --lang java`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-spring-petclinic` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle javac 21.0.12.1+1-LTS (resolution)  sha 818c4136
hobbes edges 365: confirmed 331  contradicted 34  abstract 0  silent 0 map[]
precision-against-oracle 90.7% (331/365)
recall 91.7% (342/373 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1239; misses map[interface→method:31]
  recall[interface→method  ]  89.6% (266/297)  misses 31 = 100.0% of all misses
  recall[static→constructor] 100.0% (45/45)  misses 0 = 0.0% of all misses
  recall[static→method     ] 100.0% (31/31)  misses 0 = 0.0% of all misses
  tier repowise:global_unique confirmed 96  contradicted 3  abstract 0  silent 0
  tier repowise:import_merged confirmed 9  contradicted 1  abstract 0  silent 0
  tier repowise:import_scoped confirmed 7  contradicted 0  abstract 0  silent 0
  tier repowise:module_alias confirmed 0  contradicted 2  abstract 0  silent 0
  tier repowise:receiver_field_import confirmed 18  contradicted 7  abstract 0  silent 0
  tier repowise:receiver_typed_import confirmed 35  contradicted 8  abstract 0  silent 0
  tier repowise:receiver_typed_same_package confirmed 2  contradicted 0  abstract 0  silent 0
  tier repowise:same_file confirmed 37  contradicted 5  abstract 0  silent 0
  tier repowise:same_package confirmed 127  contradicted 8  abstract 0  silent 0
  line-grain tolerance used on 133 edge(s) (several oracle sites on one line)
poison check: PASS — 365 seeded wrong edges: 365 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 365 |
| confirmed | 331 |
| contradicted | 34 |
| abstract | 0 |
| silent | 0 {} |
| precision-against-oracle (lower bound) | **90.7%** (331/365) |
| recall | 91.7% (342/373) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 96 / contradicted 3 / abstract 0 / silent 0; `repowise:import_merged` confirmed 9 / contradicted 1 / abstract 0 / silent 0; `repowise:import_scoped` confirmed 7 / contradicted 0 / abstract 0 / silent 0; `repowise:module_alias` confirmed 0 / contradicted 2 / abstract 0 / silent 0; `repowise:receiver_field_import` confirmed 18 / contradicted 7 / abstract 0 / silent 0; `repowise:receiver_typed_import` confirmed 35 / contradicted 8 / abstract 0 / silent 0; `repowise:receiver_typed_same_package` confirmed 2 / contradicted 0 / abstract 0 / silent 0; `repowise:same_file` confirmed 37 / contradicted 5 / abstract 0 / silent 0; `repowise:same_package` confirmed 127 / contradicted 8 / abstract 0 / silent 0.

## Contradicted (34 rows; all in report.json)

Mechanical shape (no reading): same file as an oracle target, other line 22; another file than every oracle target 12. By label: `repowise:same_package` 8, `repowise:receiver_typed_import` 8, `repowise:receiver_field_import` 7, `repowise:same_file` 5, `repowise:global_unique` 3, `repowise:module_alias` 2, `repowise:import_merged` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:178` → `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:144` [repowise:same_file] — oracle: getPet @ src/main/java/org/springframework/samples/petclinic/owner/Owner.java:126
- `src/main/java/org/springframework/samples/petclinic/owner/PetController.java:189` → `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:144` [repowise:same_package] — oracle: getPet @ src/main/java/org/springframework/samples/petclinic/owner/Owner.java:126
- `src/main/java/org/springframework/samples/petclinic/owner/PetController.java:86` → `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:144` [repowise:same_package] — oracle: getPet @ src/main/java/org/springframework/samples/petclinic/owner/Owner.java:126
- `src/main/java/org/springframework/samples/petclinic/owner/VisitController.java:70` → `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:144` [repowise:same_package] — oracle: getPet @ src/main/java/org/springframework/samples/petclinic/owner/Owner.java:126
- `src/main/java/org/springframework/samples/petclinic/owner/VisitController.java:79` → `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:173` [repowise:import_merged] — oracle: addVisit @ src/main/java/org/springframework/samples/petclinic/owner/Pet.java:81
- `src/main/java/org/springframework/samples/petclinic/vet/Vet.java:62` → `src/main/java/org/springframework/samples/petclinic/model/NamedEntity.java:37` [repowise:module_alias] — oracle: sorted @ :0, comparing @ :0

**Triage ratio (A-8), sampled:** over the tool's Java cells, 5 contradicted rows drawn at random (five contradicted rows per tool per language, drawn at random from the language's cells after the converter@2 regrade; each read against the source by hand) read `oracle-grain 0 : tool-wrong 5 : converter-defect 0`; none of the draws fell in this cell, whose 34 rows are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `interface→method` | 266 / 297 | 31 |
| `static→constructor` | 45 / 45 | 0 |
| `static→method` | 31 / 31 | 0 |

**Poison check:** PASS — 365 seeded wrong edges: 365 refused, 0 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (converter@1 → converter@2, 2026-09-09, signed):** the converter now advances a declaration line past leading annotation / decorator lines to the identifier's (C-94, found by the triage sample: Java `@Override` lines were charged to the tool). graded edges 365 → 365 (+0); confirmed 271 → 331 (+60); contradicted 94 → 34 (-60); precision-against-oracle 74.2% → 90.7% (+16.4); recall 75.6% → 91.7% (+16.1). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

