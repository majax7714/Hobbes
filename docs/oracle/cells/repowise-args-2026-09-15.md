# Oracle cell — repowise 0.49.0 on args, module `.`, 2026-09-15 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@4` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/Taywee/args, clone `/home/mmarrujo/.hobbes/bench/cpp-cells/draw-args`, commit 903b07dfdf5120622c9998a48ce97d64c0066556 — **the same commit and the same answer key as the Hobbes cell [args-cpp-2026-09-15.md](args-cpp-2026-09-15.md)**; the key is `/home/mmarrujo/.hobbes/bench/cpp-cells/args-cell/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 903b07df`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 7 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 957 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh repowise-args/edges.json args-cell/oracle.json repowise-args --lang cpp`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-args` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 903b07df
hobbes edges 952: confirmed 815  contradicted 89  abstract 0  silent 48 map[line-unresolved:33 no-targets:4 not-loaded:6 unreachable:5]
precision-against-oracle 90.2% (815/904)
recall 23.3% (831/3561 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1681; misses map[static→constructor:1303 static→function:200 static→method:1227]
recall-collapsed 25.8% (815/3161 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→constructor]   2.2% (29/1332)  misses 1303 = 47.7% of all misses
  recall[static→function   ]  76.9% (665/865)  misses 200 = 7.3% of all misses
  recall[static→method     ]  10.0% (137/1364)  misses 1227 = 44.9% of all misses
  tier repowise:enclosing_class confirmed 9  contradicted 1  abstract 0  silent 9
  tier repowise:global_unique confirmed 30  contradicted 6  abstract 0  silent 2
  tier repowise:import_merged confirmed 563  contradicted 1  abstract 0  silent 0
  tier repowise:receiver_typed_global confirmed 16  contradicted 0  abstract 0  silent 3
  tier repowise:receiver_typed_same_file confirmed 33  contradicted 1  abstract 0  silent 1
  tier repowise:same_file confirmed 158  contradicted 80  abstract 0  silent 33
  tier repowise:scoped_name confirmed 6  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 753 edge(s) (several oracle sites on one line)
poison check: PASS — 952 seeded wrong edges: 867 refused, 85 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 952 |
| confirmed | 815 |
| contradicted | 89 |
| abstract | 0 |
| silent | 48 {"line-unresolved": 33, "no-targets": 4, "not-loaded": 6, "unreachable": 5} |
| precision-against-oracle (lower bound) | **90.2%** (815/904) |
| recall | 23.3% (831/3,561) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:enclosing_class` confirmed 9 / contradicted 1 / abstract 0 / silent 9; `repowise:global_unique` confirmed 30 / contradicted 6 / abstract 0 / silent 2; `repowise:import_merged` confirmed 563 / contradicted 1 / abstract 0 / silent 0; `repowise:receiver_typed_global` confirmed 16 / contradicted 0 / abstract 0 / silent 3; `repowise:receiver_typed_same_file` confirmed 33 / contradicted 1 / abstract 0 / silent 1; `repowise:same_file` confirmed 158 / contradicted 80 / abstract 0 / silent 33; `repowise:scoped_name` confirmed 6 / contradicted 0 / abstract 0 / silent 0.

## Contradicted (122 rows at converter@4, read 2026-09-15; 89 after the 2026-09-16 regrade — all in report.json)

*The counts in this section — the mechanical shape, the by-label split and the triage ratio — are **as of the converter@4 grade of 2026-09-15**, when these rows were read. The 2026-09-16 regrade of the oracle's own H-30 defect moved 33 of them to `silent`/`line-unresolved`, leaving 89; it read no row and re-triaged nothing. The signed before → after is in **Direction of fix (the oracle's H-30)** below.*

Mechanical shape (no reading): same file as an oracle target, other line 87; another file than every oracle target 35. By label: `repowise:same_file` 104, `repowise:enclosing_class` 7, `repowise:global_unique` 6, `repowise:receiver_typed_global` 3, `repowise:receiver_typed_same_file` 1, `repowise:import_merged` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `args.hxx:1048` → `args.hxx:2320` [repowise:same_file] — oracle: Matched @ args.hxx:1037
- `args.hxx:1290` → `args.hxx:1276` [repowise:same_file] — oracle: push_back @ :0, ToString @ args.hxx:1267, ToString @ args.hxx:1267
- `args.hxx:1308` → `args.hxx:1452` [repowise:enclosing_class] — oracle: operator== @ args.hxx:996, Nargs @ args.hxx:992, Name @ args.hxx:1233
- `args.hxx:1308` → `args.hxx:5322` [repowise:same_file] — oracle: operator== @ args.hxx:996, Nargs @ args.hxx:992, Name @ args.hxx:1233
- `args.hxx:1334` → `args.hxx:764` [repowise:same_file] — oracle: NamedBase @ args.hxx:1185, Matcher @ args.hxx:682, move @ :0
- `args.hxx:1352` → `args.hxx:612` [repowise:receiver_typed_same_file] — oracle: operator<< @ :0, operator<< @ :0, str @ args.hxx:607

**Triage ratio (A-8), sampled:** over the tool's C++ cells, 40 contradicted rows drawn at random (oracle-grading.md §10.7 (P34): every contradicted row where a cell has 60 or fewer, otherwise 20 drawn by random.Random(20260915).sample over the cell's contradicted rows at the standing grade (converter@4); each read against the source by hand) read `oracle-grain 1 : tool-wrong 39 : converter-defect 0`; the rest of this cell's 122 rows are untriaged. The rows drawn from this cell:
- `args.hxx:3737` → `args.hxx:2508` [repowise:same_file]: **tool-wrong** — `GetProgramLine(helpParams)` inside Subparser::Parse drawn to Command's override (2508); the call is Group's (1915)
- `args.hxx:3686` → `args.hxx:5349` [repowise:same_file]: **tool-wrong** — `std::begin(args)` drawn to an args.hxx class's member `begin()`; the key's target is the standard library's
- `args.hxx:3841` → `args.hxx:2326` [repowise:same_file]: **tool-wrong** — the qualified `FlagBase::Match(arg)` drawn to another class's nullary `Match()` (2326); the call is FlagBase's (1345)
- `args.hxx:1678` → `args.hxx:5349` [repowise:same_file]: **tool-wrong** — row 1's shape: `std::begin(group.Children())` drawn to a member `begin()`
- `args.hxx:438` → `args.hxx:5349` [repowise:same_file]: **tool-wrong** — row 1's shape: `std::begin(second)` in Wrap
- `args.hxx:1927` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — `std::end(names)` drawn to an args.hxx class's member `end()`
- `examples/gitlike.cxx:39` → `args.hxx:5364` [repowise:import_merged]: **tool-wrong** — `std::end(args)` in an example program drawn to args.hxx's member `end()`
- `args.hxx:2465` → `args.hxx:2444` [repowise:same_file]: **tool-wrong** — the qualified `Group::HasCommand()` drawn to Command's override (2444); the call is Group's (1820)
- `args.hxx:437` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — row 5's shape: `std::end(first)` in Wrap
- `args.hxx:1308` → `args.hxx:1452` [repowise:enclosing_class]: **oracle-grain** — H-30: `NumberOfArguments()` inside FlagBase::GetNameString drawn to FlagBase's own pure virtual (1452), the declaration the source names; the key leaves the virtual call dynamic and judged the edge against the line's other calls
- `args.hxx:5270` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — row 5's shape: `std::end(map)`
- `args.hxx:2592` → `args.hxx:5349` [repowise:same_file]: **tool-wrong** — row 1's shape: `std::begin(groupDescriptions)`
- `args.hxx:4541` → `args.hxx:2326` [repowise:same_file]: **tool-wrong** — row 2's shape in ValueFlagList
- `args.hxx:2071` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — `Children().end()` on a std::vector drawn to an args.hxx member `end()`
- `args.hxx:1804` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — row 13's shape in Group::HasFlag
- `args.hxx:3602` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — row 5's shape: `std::end(info)`
- `args.hxx:2898` → `args.hxx:2326` [repowise:same_file]: **tool-wrong** — `Match(arg)` drawn to the nullary `Match()` (2326); the call is Command::Match(const EitherFlag&) (2339), the site where Hobbes' own first grade collapsed to the same line (C-151)
- `args.hxx:4349` → `args.hxx:4406` [repowise:enclosing_class]: **tool-wrong** — `std::end(values)` drawn to the enclosing class's member `end()`
- `args.hxx:1822` → `args.hxx:5364` [repowise:same_file]: **tool-wrong** — row 13's shape in Group::HasCommand
- `args.hxx:1352` → `args.hxx:612` [repowise:receiver_typed_same_file]: **tool-wrong** — `flag.str()` drawn to the two-parameter `str` overload (612); the call is the nullary one (607)

**Triage (hand-read, 2026-09-15):** A seeded sample of 20 of the 122 at converter@4 (oracle-grading.md §10.7, P34): tool-wrong 19, oracle-grain 1, converter-defect 0. The 19 are one shape, a call drawn by its short name to another declaration of that name: the standard library's `begin`/`end` (10 free calls, 3 on a std::vector) drawn to an args.hxx container's member `begin()`/`end()`; another class's override for a qualified or inherited call (`FlagBase::Match`, `Group::HasCommand`, `GetProgramLine`); the other overload (`Match()` for `Match(const EitherFlag&)`, the site where Hobbes' own first grade collapsed to the same line, C-151, and `str()`). The oracle-grain row is H-30: `NumberOfArguments()` drawn to FlagBase's own pure virtual, which the key leaves dynamic. The other 102 are untriaged. **At converter@3** the draw read one row as the converter's: the second `ToString`, stored at its return-type line (`typename std::enable_if<…>::type`, 1275) where the key keys its name (1276). @4 advances a split C/C++ head to the name's line, and the row is confirmed. **Pre-registration (§10.7):** P32 met (poison PASS); P33 met (87.0% against Hobbes' 100%); P34 met (19 of 20).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→constructor` | 29 / 1,332 | 1,303 |
| `static→method` | 137 / 1,364 | 1,227 |
| `static→function` | 665 / 865 | 200 |

**Poison check:** PASS — 952 seeded wrong edges: 867 refused, 85 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (repowise-adapter@3 → repowise-adapter@4, 2026-09-15, signed):** the converter now reads `# define` spelled with spaces as the same directive (kind macro, excluded as Hobbes' own are) and advances a declaration head split over lines to the line holding the name (C-94's C++ face; ADR-101's 2026-09-15 amendment, found by the foreign C++ cells' triage). graded edges 952 → 952 (+0); confirmed 814 → 815 (+1); contradicted 123 → 122 (-1); precision-against-oracle 86.9% → 87.0% (+0.1); recall 23.3% → 23.3% (+0.0). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

**Direction of fix (the oracle's H-30, 2026-09-16, signed):** the grader no longer contradicts on a line the key itself left unresolved — a defect of the oracle, not of this tool (`oracle-grading.md` §7d and D-O4; `oracle-defects.md` H-30). The key was regenerated as well (H-28, H-29 changed what a shard holds), so this cell is graded against the **new** key (§10.8, P45). graded edges 952 → 952 (+0); confirmed 815 → 815 (+0); contradicted 122 → 89 (-33); silent 15 → 48 (+33), of which `line-unresolved` 33; precision-against-oracle 87.0% → 90.2%. Nothing on the tool's side moved and its graph is unchanged: the rule withdraws judgement where the key is silent, which is why every moved foreign cell moves upward. Hobbes' own 38 non-C++ cells did not move at all, because it abstains where lane B is silent. The regrade's outputs are in `~/.hobbes/bench/oracle-defect-drivers/regrade-out/foreign-newkey/repowise-args/`; the `report.json` beside this cell in `~/.hobbes/bench/comparative/` is still the converter@4 grade this line moves from, kept as it was.

