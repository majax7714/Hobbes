# Oracle cell — codegraphcontext 0.6.13 on Severed-Chains, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@2` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/Legend-of-Dragoon-Modding/Severed-Chains, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/Severed-Chains`, commit 3841686e70a15803037359fe1f4545f0beec469f — **the same commit and the same answer key as the Hobbes cell [severed-chains-java-2026-08-29.md](severed-chains-java-2026-08-29.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/Severed-Chains-java/oracle.json` (`cell   oracle javac 25.0.4.1+1-LTS (resolution)  sha 3841686e`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 263.8 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 12065 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}. Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-Severed-Chains/edges.json Severed-Chains-java/oracle.json codegraphcontext-Severed-Chains --lang java`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-Severed-Chains` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle javac 25.0.4.1+1-LTS (resolution)  sha 3841686e
hobbes edges 12065: confirmed 9580  contradicted 2485  abstract 0  silent 0 map[]
precision-against-oracle 79.4% (9580/12065)
recall 17.8% (9726/54520 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 19050; misses map[interface→anonymous-member:78 interface→method:29650 static→constructor:11504 static→method:3562]
  recall[interface→anonymous-member]   0.0% (0/78)  misses 78 = 0.2% of all misses
  recall[interface→method  ]  16.3% (5787/35437)  misses 29650 = 66.2% of all misses
  recall[static→constructor]   0.0% (3/11507)  misses 11504 = 25.7% of all misses
  recall[static→method     ]  52.5% (3936/7498)  misses 3562 = 8.0% of all misses
  tier codegraphcontext:EXTRACTED confirmed 4277  contradicted 689  abstract 0  silent 0
  tier codegraphcontext:INFERRED confirmed 5303  contradicted 1796  abstract 0  silent 0
  line-grain tolerance used on 4073 edge(s) (several oracle sites on one line)
poison check: PASS — 12065 seeded wrong edges: 12065 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 12,065 |
| confirmed | 9,580 |
| contradicted | 2,485 |
| abstract | 0 |
| silent | 0 {} |
| precision-against-oracle (lower bound) | **79.4%** (9,580/12,065) |
| recall | 17.8% (9,726/54,520) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 4277 / contradicted 689 / abstract 0 / silent 0; `codegraphcontext:INFERRED` confirmed 5303 / contradicted 1796 / abstract 0 / silent 0.

## Contradicted (2485 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 2332; same file as an oracle target, other line 153. By label: `codegraphcontext:INFERRED` 1796, `codegraphcontext:EXTRACTED` 689.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/main/java/discord/DiscordRichPresence.java:40` → `src/main/java/discord/DiscordRichPresence.java:37` [codegraphcontext:EXTRACTED] — oracle: activityManager @ :0, updateActivity @ :0
- `src/main/java/legend/core/AddRegistryEvent.java:19` → `src/main/java/legend/core/AddRegistryEvent.java:18` [codegraphcontext:EXTRACTED] — oracle: addRegistry @ src/main/java/legend/core/Registries.java:103
- `src/main/java/legend/core/Config.java:203` → `src/main/java/legend/core/Config.java:202` [codegraphcontext:EXTRACTED] — oracle: exists @ :0
- `src/main/java/legend/core/Config.java:208` → `src/main/java/legend/core/Config.java:206` [codegraphcontext:INFERRED] — oracle: load @ :0
- `src/main/java/legend/core/Config.java:226` → `src/main/java/legend/core/Config.java:224` [codegraphcontext:EXTRACTED] — oracle: addAll @ :0, entrySet @ :0
- `src/main/java/legend/core/Config.java:232` → `src/main/java/legend/core/Config.java:231` [codegraphcontext:EXTRACTED] — oracle: TreeSet @ :0, keySet @ :0

**Triage ratio (A-8), sampled:** over the tool's Java cells, 5 contradicted rows drawn at random (five contradicted rows per tool per language, drawn at random from the language's cells after the converter@2 regrade; each read against the source by hand) read `oracle-grain 0 : tool-wrong 5 : converter-defect 0`; the rest of this cell's 2485 rows are untriaged. The rows drawn from this cell:
- `src/main/java/legend/game/saves/Campaign.java:63` → `src/main/java/legend/game/saves/Campaign.java:62` [codegraphcontext:EXTRACTED]: **tool-wrong** — `Files.exists(` (java.nio) drawn to the enclosing `exists()`

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `interface→method` | 5,787 / 35,437 | 29,650 |
| `static→constructor` | 3 / 11,507 | 11,504 |
| `static→method` | 3,936 / 7,498 | 3,562 |
| `interface→anonymous-member` | 0 / 78 | 78 |

**Poison check:** PASS — 12,065 seeded wrong edges: 12,065 refused, 0 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (converter@1 → converter@2, 2026-09-09, signed):** the converter now advances a declaration line past leading annotation / decorator lines to the identifier's (C-94, found by the triage sample: Java `@Override` lines were charged to the tool). graded edges 12,065 → 12,065 (+0); confirmed 5,575 → 9,580 (+4,005); contradicted 6,490 → 2,485 (-4,005); precision-against-oracle 46.2% → 79.4% (+33.2); recall 10.5% → 17.8% (+7.4). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

