# Oracle cell — codegraphcontext 0.6.13 on cJSON, module `.`, 2026-09-14 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@2` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/DaveGamble/cJSON, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/cJSON`, commit fb16e5cf358798aabb049655975cde8427101056 — **the same commit and the same answer key as the Hobbes cell [cjson-c](cjson-c)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/cjson-c/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 22 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 1663 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 11 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-cjson/edges.json cjson-c/oracle.json codegraphcontext-cjson --lang c`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-cjson` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha fb16e5cf
hobbes edges 1521: confirmed 1179  contradicted 0  abstract 0  silent 342 map[not-loaded:330 unreachable:12]
precision-against-oracle 100.0% (1179/1179)
recall 61.5% (1180/1918 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 259; misses map[macro→function:728 static→function:10]
recall-collapsed 61.8% (1179/1908 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/728)  misses 728 = 98.6% of all misses
  recall[static→function   ]  99.2% (1180/1190)  misses 10 = 1.4% of all misses
  tier codegraphcontext:EXTRACTED confirmed 1179  contradicted 0  abstract 0  silent 342
  line-grain tolerance used on 318 edge(s) (several oracle sites on one line)
poison check: PASS — 1521 seeded wrong edges: 1179 refused, 342 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 1,521 |
| confirmed | 1,179 |
| contradicted | 0 |
| abstract | 0 |
| silent | 342 {"not-loaded": 330, "unreachable": 12} |
| precision-against-oracle (lower bound) | **100.0%** (1,179/1,179) |
| recall | 61.5% (1,180/1,918) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 1179 / contradicted 0 / abstract 0 / silent 342.

**Triage (hand-read, 2026-09-14):** Read 2026-09-14: nothing to read — 0 contradicted of 1,179. C has one namespace per link and cJSON defines each name once, so the tool's name-resolved edges land where clang's do. Its 738 misses are 728 macro-expansion sites (the Unity assertions, exactly Hobbes' own misses on this key, C-131) and ten static sites the tool stored no edge for: two calls of the file-static `print` (cJSON.c:1239), `cJSON_Delete` from tests/common.h:32, three of `read_file` (tests/common.h:47), two of `compare_double`, and Unity's `setUp`/`tearDown`.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `macro→function` | 0 / 728 | 728 |
| `static→function` | 1,180 / 1,190 | 10 |

**Poison check:** PASS — 1,521 seeded wrong edges: 1,179 refused, 342 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

