# Oracle cell — the `minic` fixture, 2026-09-12 (O9, the fixture cell)

**Repo:** `pipeline/tests/fixtures/minic` at `1f2baf1`, copied to `~/.hobbes/bench/oracle/repos/minic` and git-initialised (`b93de47`). This is C lane A's hand-built fixture (ADR-108): a Makefile whose default target builds `minic` from `src/main.c`, `src/util.c` and `src/shapes.c`. `tests/test_util.c` is built only by `make test`, and `src/platform.c` by no target.
- **Hobbes:** 0.2.4-beta at `055d3fe`, lane B on (bear over `make -k`).
- **Oracle:** `c-clang`, clang 18.1.3, `compile database: make (3 units)`.

**Command:** `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/minic . ~/.hobbes/bench/oracle/minic-c --lang c`. Runtime 1 s.
- **The first run refused** with podman's `duplicate mount destination`: `run-cell.sh` builds `oracle` into the cell dir, and the step mounted that dir both rw and ro (H-27).
- It was fixed in `contain.New` (`17def60`) and re-run with `--no-ingest`.

## Numbers, Hobbes 0.2.4-beta (report.txt, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha b93de477
oracle ran contained (ADR-092)
hobbes edges 6: confirmed 5  contradicted 0  abstract 0  silent 1 map[not-loaded:1]
coverage: map[sites_dynamic:3 sites_external:2 sites_link_ambiguous:0 sites_macro:0 sites_static:7 sites_tu_split:0 sites_undefined:0 units:3 units_failed:0]
precision-against-oracle 100.0% (5/5)
recall 100.0% (7/7 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 2; misses map[]
  tier semantic   confirmed 5  contradicted 0  abstract 0  silent 0
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 1
poison check: PASS — 6 seeded wrong edges: 5 refused, 1 unjudged (oracle silent there), 0 falsely confirmed
```

**Reading.** Every in-repo pair is drawn, hand-checkable from the fixture's sources:
- `main.c:10` → `util.c:7`; `main.c:13` → `main.c:5`; `main.c:21` → `include/minic/api.h:17`;
- `util.c:8` → `util.c:3`, twice; `shapes.c:8` → `shapes.c:3`, twice.

The rest:
- the two `printf` calls are external;
- the three dynamic sites (a function-pointer parameter, a struct field, a dereference) have no targets;
- the silent edge is `tests/test_util.c:4` → `util.c:7`, syntactic, in a file the default target does not build.

The two macro invocations (`MINIC_MAX`, `MINIC_CLAMP`) expand to no call and are excluded. **P17 met** as registered: the `cclang` fixture's truth lands exactly (unit tests and `TestOracleCClangEndToEnd`, contained), and minic grades 0 contradicted with `tests/test_util.c` silent as not-loaded.
