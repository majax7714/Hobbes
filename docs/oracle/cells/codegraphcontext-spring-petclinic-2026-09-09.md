# Oracle cell — codegraphcontext 0.6.13 on spring-petclinic, module `.`, 2026-09-09 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@2` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/spring-projects/spring-petclinic, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/spring-petclinic`, commit 818c4136ea971c21674525f9053de0d9c7ad8cfe — **the same commit and the same answer key as the Hobbes cell [petclinic-java-2026-08-29.md](petclinic-java-2026-08-29.md)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/spring-petclinic-java/oracle.json` (`cell   oracle javac 21.0.12.1+1-LTS (resolution)  sha 818c4136`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 1, wall 10.1 s; no network observed, no repo build invoked. The tool's own log carries 1 error/warning line(s), e.g. `An error occurred during indexing: Binder exception: Cannot find property source`. Conversion: 237 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}. Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-spring-petclinic/edges.json spring-petclinic-java/oracle.json codegraphcontext-spring-petclinic --lang java`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-spring-petclinic` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle javac 21.0.12.1+1-LTS (resolution)  sha 818c4136
hobbes edges 237: confirmed 236  contradicted 1  abstract 0  silent 0 map[]
precision-against-oracle 99.6% (236/237)
recall 66.0% (246/373 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1239; misses map[interface→method:82 static→constructor:45]
  recall[interface→method  ]  72.4% (215/297)  misses 82 = 64.6% of all misses
  recall[static→constructor]   0.0% (0/45)  misses 45 = 35.4% of all misses
  recall[static→method     ] 100.0% (31/31)  misses 0 = 0.0% of all misses
  tier codegraphcontext:EXTRACTED confirmed 44  contradicted 1  abstract 0  silent 0
  tier codegraphcontext:INFERRED confirmed 192  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 91 edge(s) (several oracle sites on one line)
poison check: PASS — 237 seeded wrong edges: 237 refused, 0 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 237 |
| confirmed | 236 |
| contradicted | 1 |
| abstract | 0 |
| silent | 0 {} |
| precision-against-oracle (lower bound) | **99.6%** (236/237) |
| recall | 66.0% (246/373) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 44 / contradicted 1 / abstract 0 / silent 0; `codegraphcontext:INFERRED` confirmed 192 / contradicted 0 / abstract 0 / silent 0.

## Contradicted (1 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 1. By label: `codegraphcontext:EXTRACTED` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:165` → `src/main/java/org/springframework/samples/petclinic/owner/Owner.java:157` [codegraphcontext:EXTRACTED] — oracle: toString @ :0

**Triage ratio (A-8), sampled:** over the tool's Java cells, 5 contradicted rows drawn at random (five contradicted rows per tool per language, drawn at random from the language's cells after the converter@2 regrade; each read against the source by hand) read `oracle-grain 0 : tool-wrong 5 : converter-defect 0`; none of the draws fell in this cell, whose 1 rows are untriaged.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `interface→method` | 215 / 297 | 82 |
| `static→constructor` | 0 / 45 | 45 |
| `static→method` | 31 / 31 | 0 |

**Poison check:** PASS — 237 seeded wrong edges: 237 refused, 0 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (converter@1 → converter@2, 2026-09-09, signed):** the converter now advances a declaration line past leading annotation / decorator lines to the identifier's (C-94, found by the triage sample: Java `@Override` lines were charged to the tool). graded edges 237 → 237 (+0); confirmed 213 → 236 (+23); contradicted 24 → 1 (-23); precision-against-oracle 89.9% → 99.6% (+9.7); recall 59.8% → 66.0% (+6.2). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

