# Oracle cell — repowise 0.49.0 on xmpp.js, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@5` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/xmppjs/xmpp.js, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/xmppjs__xmpp.js` — `/home/mmarrujo/.hobbes/bench/comparative/repowise-xmpp/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 9cce6c14a7f1c186c312f5c64f918ed88c69bca7 — **the same commit and the same answer key as the Hobbes cell [xmpp-js-2026-09-19.md](xmpp-js-2026-09-19.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/regrade/h34/xmpp/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9cce6c14`), regenerated by the command that cell names.

Index command: `unshare -rn repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the copy)` — exit 0, wall 7 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked; 0.49.0 is the version the other published repowise cells ran (PyPI's latest on 2026-09-26 is 0.53.0, not used). It wrote `.repowise/` into the copy, nothing else. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. **Converted at converter@5** (ADR-101's 2026-09-26 amendment): 159 of the tool's call edges have a `<file>::__module__` caller and are read as sites in that file; at @4 they were dropped as `unknown-caller` and this cell graded 288 edges (`report.at4.*` beside the cell keeps that grade; it is not a published figure). The tool's own log carries 0 error/warning line(s). Conversion: 898 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored); a `<file>::__module__` caller (a top-level call, no wiki_symbols row) is read as call sites in that file at the tool's call lines (converter@5; @4 dropped every such edge as unknown-caller).

Command: `bench/oracle/grade-foreign.sh repowise-xmpp/edges.json xmpp/oracle.json repowise-xmpp --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-xmpp` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9cce6c14
hobbes edges 898: confirmed 489  contradicted 54  abstract 1  silent 354 map[line-unresolved:20 no-targets:332 unreachable:2]
precision-against-oracle 90.1% (489/543)
precision-strict 86.9% (489/563): the 20 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 58.9% (495/840 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 425; misses map[func-value→local-binding:18 func-value→parameter:64 func-value→variable:43 static→class:26 static→closure:42 static→function:12 static→method:140]
recall-collapsed 58.6% (489/834 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/18)  misses 18 = 5.2% of all misses
  recall[func-value→parameter]   0.0% (0/64)  misses 64 = 18.6% of all misses
  recall[func-value→variable]   6.5% (3/46)  misses 43 = 12.5% of all misses
  recall[static→class      ]  75.0% (78/104)  misses 26 = 7.5% of all misses
  recall[static→closure    ]   0.0% (0/42)  misses 42 = 12.2% of all misses
  recall[static→function   ]  96.8% (364/376)  misses 12 = 3.5% of all misses
  recall[static→method     ]  26.3% (50/190)  misses 140 = 40.6% of all misses
  tier repowise:global_unique confirmed 0  contradicted 9  abstract 1  silent 36
  tier repowise:import_merged confirmed 137  contradicted 45  abstract 0  silent 86
  tier repowise:import_scoped confirmed 150  contradicted 0  abstract 0  silent 227
  tier repowise:module_alias confirmed 49  contradicted 0  abstract 0  silent 4
  tier repowise:same_file confirmed 105  contradicted 0  abstract 0  silent 1
  tier repowise:self_scope confirmed 48  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 262 edge(s) (several oracle sites on one line)
poison check: PASS — 898 seeded wrong edges: 408 refused, 490 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 898 |
| confirmed | 489 |
| contradicted | 54 |
| abstract | 1 |
| silent | 354 {"line-unresolved": 20, "no-targets": 332, "unreachable": 2} |
| precision-against-oracle (lower bound) | **90.1%** (489/543) |
| precision strict (ADR-124: the 20 line-unresolved rows counted as contradicted) | **86.9%** (489/563) |
| recall | 58.9% (495/840) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 0 / contradicted 9 / abstract 1 / silent 36; `repowise:import_merged` confirmed 137 / contradicted 45 / abstract 0 / silent 86; `repowise:import_scoped` confirmed 150 / contradicted 0 / abstract 0 / silent 227; `repowise:module_alias` confirmed 49 / contradicted 0 / abstract 0 / silent 4; `repowise:same_file` confirmed 105 / contradicted 0 / abstract 0 / silent 1; `repowise:self_scope` confirmed 48 / contradicted 0 / abstract 0 / silent 0.

## Contradicted (54 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 33; same file as an oracle target, other line 21. By label: `repowise:import_merged` 45, `repowise:global_unique` 9.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `packages/client-core/src/fast/fast.js:118` → `server/index.js:147` [repowise:global_unique] — oracle: reset @ packages/client-core/src/fast/fast.js:114
- `packages/client-core/src/fast/fast.js:130` → `server/index.js:147` [repowise:global_unique] — oracle: reset @ packages/client-core/src/fast/fast.js:114
- `packages/client-core/src/fast/fast.js:68` → `packages/sasl/index.js:19` [repowise:import_merged] — oracle: authenticate @ packages/client-core/src/fast/fast.js:50
- `packages/id/index.js:4` → `packages/jid/lib/JID.js:30` [repowise:global_unique] — oracle: Math.random @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:746, Number.toString @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:564, String.slice @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:484
- `packages/jid/test/equality.js:10` → `packages/jid/index.js:9` [repowise:import_merged] — oracle: j @ packages/jid/index.js:17
- `packages/jid/test/equality.js:11` → `packages/jid/index.js:9` [repowise:import_merged] — oracle: j @ packages/jid/index.js:17

**Triage ratio (A-8):** all 54 read by shape (below): `oracle-grain 21 : tool-wrong 33 : converter-defect 0`. Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** all 54, by shape; each shape read by hand on at least two rows. **Tool-wrong 33.** 24 are `new Context(…)` drawn to `packages/middleware/lib/Context.js`'s base class: in 23 the test's `Context` is imported from `../lib/IncomingContext.js` or `../lib/OutgoingContext.js`, and in `packages/middleware/index.js:8` it is the listener's parameter. 6 are `Math.random().toString()` drawn to `packages/jid/lib/JID.js`'s `toString`. 3 are `reset()` / `authenticate(…)` in `packages/client-core/src/fast/fast.js`, where the key names the file's own local functions and the tool names other files' functions. **Oracle-grain 21.** Each is `jid(…)` drawn to `packages/jid/index.js`'s `function jid`, where the module's default export is `const j = jid.bind()` and the key names the bound value `j`: the tool names the function that bound value calls. Converter-defect 0.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→method` | 50 / 190 | 140 |
| `func-value→parameter` | 0 / 64 | 64 |
| `func-value→variable` | 3 / 46 | 43 |
| `static→closure` | 0 / 42 | 42 |
| `static→class` | 78 / 104 | 26 |
| `func-value→local-binding` | 0 / 18 | 18 |
| `static→function` | 364 / 376 | 12 |

**Poison check:** PASS — 898 seeded wrong edges: 408 refused, 490 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

