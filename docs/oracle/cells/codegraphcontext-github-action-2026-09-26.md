# Oracle cell — codegraphcontext 0.6.13 on github-action, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/cypress-io/github-action, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/cypress-io__github-action` — `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-github-action/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 01e3b659a495b41649cdd0aa82e1d1624e26520b — **the same commit and the same answer key as the Hobbes cell [github-action-js-2026-09-20.md](github-action-js-2026-09-20.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/cells/c165-provisioned/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 01e3b659`), regenerated by the command that cell names.

Index command: `unshare -rn codegraphcontext --db kuzudb --db-path <db> index <copy>  (0.6.13; CGC_RUNTIME_DB_TYPE=kuzudb)` — exit 0, wall 8 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked. 0.6.13 is the version the other 44 foreign cells ran, and PyPI's latest on 2026-09-26. The tool's summary lists the JavaScript files it parsed: .js 67, .jsx 4, .mjs 3; its default `.cgcignore` skips the tracked `dist/` bundle. It left a `.cgcignore` at the copy's root (its default ignore patterns), nothing else. The key was graded with the ingest's npm tree mounted (the Hobbes cell's provisioned arm), so the copy got the same tree (`~/.hobbes/cache/npm/5f31b8be7cf816e8`) at `node_modules`; the tool stored no edge with a `node_modules` path. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. The tool's own log carries 0 error/warning line(s). Conversion: 69 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 40 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-github-action/edges.json c165-provisioned/oracle.json codegraphcontext-github-action --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-github-action` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 01e3b659
hobbes edges 69: confirmed 63  contradicted 5  abstract 0  silent 1 map[line-unresolved:1]
precision-against-oracle 92.6% (63/68)
precision-strict 91.3% (63/69): the 1 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 36.4% (63/173 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 413; misses map[func-value→local-binding:4 func-value→parameter:3 func-value→variable:86 static→closure:2 static→function:7 static→variable:8]
recall-collapsed 36.4% (63/173 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/4)  misses 4 = 3.6% of all misses
  recall[func-value→parameter]   0.0% (0/3)  misses 3 = 2.7% of all misses
  recall[func-value→variable]   0.0% (0/86)  misses 86 = 78.2% of all misses
  recall[static→closure    ]  50.0% (2/4)  misses 2 = 1.8% of all misses
  recall[static→function   ]  12.5% (1/8)  misses 7 = 6.4% of all misses
  recall[static→variable   ]  88.2% (60/68)  misses 8 = 7.3% of all misses
  tier codegraphcontext:EXTRACTED confirmed 63  contradicted 5  abstract 0  silent 1
  line-grain tolerance used on 5 edge(s) (several oracle sites on one line)
poison check: PASS — 69 seeded wrong edges: 67 refused, 2 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 69 |
| confirmed | 63 |
| contradicted | 5 |
| abstract | 0 |
| silent | 1 {"line-unresolved": 1} |
| precision-against-oracle (lower bound) | **92.6%** (63/68) |
| precision strict (ADR-124: the 1 line-unresolved rows counted as contradicted) | **91.3%** (63/69) |
| recall | 36.4% (63/173) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 63 / contradicted 5 / abstract 0 / silent 1.

## Contradicted (5 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 5. By label: `codegraphcontext:EXTRACTED` 5.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `examples/browser/cypress.config.js:10` → `examples/browser/cypress.config.js:24` [codegraphcontext:EXTRACTED] — oracle: Console.log @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:37915
- `examples/browser/cypress.config.js:11` → `examples/browser/cypress.config.js:24` [codegraphcontext:EXTRACTED] — oracle: Console.log @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:37915
- `examples/browser/cypress.config.js:17` → `examples/browser/cypress.config.js:24` [codegraphcontext:EXTRACTED] — oracle: Console.log @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:37915
- `examples/browser/cypress.config.js:25` → `examples/browser/cypress.config.js:24` [codegraphcontext:EXTRACTED] — oracle: Console.log @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:37915
- `examples/quiet/cypress.config.js:10` → `examples/quiet/cypress.config.js:9` [codegraphcontext:EXTRACTED] — oracle: Console.log @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:37915

**Triage ratio (A-8):** all 5 read by hand in this cell: `oracle-grain 0 : tool-wrong 5 : converter-defect 0` (below). Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** all 5 read against the source: **tool-wrong 5**, oracle-grain 0, converter-defect 0 — every one is `console.log(…)` in a `cypress.config.js` drawn to the `log (message)` method of the object literal passed to `on('task', {…})` in the same file. The one line-unresolved row (`examples/browser/cypress.config.js:18`, `console.log(launchOptions.args.join(os.EOL))` → the same `log`) is the same shape and reads wrong by hand, so on this cell the strict figure is the one the reading supports.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `func-value→variable` | 0 / 86 | 86 |
| `static→variable` | 60 / 68 | 8 |
| `static→function` | 1 / 8 | 7 |
| `func-value→local-binding` | 0 / 4 | 4 |
| `func-value→parameter` | 0 / 3 | 3 |
| `static→closure` | 2 / 4 | 2 |

**Poison check:** PASS — 69 seeded wrong edges: 67 refused, 2 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

