# Oracle cell — Taywee/args, build root `.`, 2026-09-15 (O10, the random draw)

**Repo:** https://github.com/Taywee/args (MIT), clone at `~/.hobbes/bench/cpp-cells/draw-args`, commit `903b07df`.
- **Why this repo:** the random draw (`oracle-grading.md` §7d, seed 20260914, the seventh of the shuffle; the six passed over are recorded there with their reasons). It is a header-only argument parser: one 185 KB `args.hxx` and 97 test programs, built by CMake.
- **Ingest:** SHA `903b07df`. Hobbes 0.2.21-beta at `33920b6`, lane B on, scip-clang 0.4.0 over CMake's export at its defaults, one translation unit per run (99 units, under the 400 bound), contained (`index-c`).
- **Oracle:** `c-clang --lang cpp` (O10), `Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json`. 99 units, all under clang++, 0 failed. Kind `resolution`, no roots.
- **Pre-registered:** `oracle-grading.md` §10.6 (P26–P31), committed `33920b6` before this cell ran. It was written after fmt's first grade, and says so.

**Command:** `bench/oracle/run-cell.sh ~/.hobbes/bench/cpp-cells/draw-args . ~/.hobbes/bench/cpp-cells/args-cell --lang cpp`. Runtime 326 s. The outputs are in `~/.hobbes/bench/cpp-cells/args-cell/`.

## Numbers, Hobbes 0.2.21-beta (report.txt, verbatim; the `missed` rows elided)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 903b07df
oracle ran contained (ADR-092)
hobbes edges 2014: confirmed 2004  contradicted 4  abstract 0  silent 6 map[no-targets:1 not-loaded:5]
coverage: map[lang_cpp:1 sites_constructor:1332 sites_dynamic:225 sites_external:1681 sites_link_ambiguous:0 sites_macro:0 sites_operator:238 sites_static:1591 sites_tu_split:32 sites_undefined:15 sites_virtual:243 units:99 units_cpp:99 units_failed:0]
precision-against-oracle 99.8% (2004/2008)
recall 57.1% (2035/3561 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1681; misses map[static→constructor:889 static→function:40 static→method:597]
recall-collapsed 63.4% (2003/3161 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→constructor]  33.3% (443/1332)  misses 889 = 58.3% of all misses
  recall[static→function   ]  95.4% (825/865)  misses 40 = 2.6% of all misses
  recall[static→method     ]  56.2% (767/1364)  misses 597 = 39.1% of all misses
  tier semantic   confirmed 2004  contradicted 4  abstract 0  silent 1
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 5
  line-grain tolerance used on 1312 edge(s) (several oracle sites on one line)
poison check: PASS — 2014 seeded wrong edges: 2008 refused, 6 unjudged (oracle silent there), 0 falsely confirmed
```

| bucket | count |
|---|---|
| hobbes edges (graded) | 2,014 (2,009 semantic, 5 syntactic) |
| confirmed | 2,004, all semantic |
| contradicted | 4, all semantic |
| silent | 6 (not-loaded 5, all syntactic; no-targets 1) |
| precision-against-oracle | **99.8%** (2,004/2,008) |
| in-repo oracle pairs | 3,561 |
| recall (no roots; every resolved site) | 57.1% (2,035/3,561); collapsed 63.4% |
| external oracle pairs (not graded) | 1,681 |

**Triage ratio (A-8):** 4 contradicted: `hobbes-wrong 4 : oracle-wrong 0 : untriaged 0`.

## Contradictions (4, every row read): all the overload collapse

The helper's probe (the one fmt's triage used) shows each site carrying references to two overloads of one member, 198 each across the units, with ADR-109's one-target rule keeping the smaller line:

| Site | Hobbes' target | The call means | The references |
|---|---|---|---|
| `args.hxx:2898`, `Match(arg)` | `Command::Match()` at 2326 | `Command::Match(const EitherFlag&)` at 2339 | `Command#Match(49f6…).` at 2326, `(c793…).` at 2339 |
| `args.hxx:2970`, `Match(…)` | 2326 | 2339 | the same pair |
| `args.hxx:3164`, `Match(…)` | 2326 | 2339 | the same pair |
| `args.hxx:3192`, `Complete(it, end, terminated)` | `ArgumentParser::Complete(FlagBase&, It, It)` at 2764 | `Complete(It, It, bool)` at 3056 | `ArgumentParser#Complete(c810…).` at 2764, `(b744…).` at 3056 |

Each call sits in a member template (`It`), where scip-clang lists the candidates it cannot choose between. Across args' 14 such sites, the rule's edges were 9 confirmed, 4 contradicted and 1 silent.

## Misses (1,526)

`static→constructor` 889 (58.3%): of 1,332 construct sites, 443 drawn. `static→method` 597 (39.1%): C-143's face (a member call is lane B's, and lane B abstains where it lists several candidates) and C-145's. `static→function` 40. There are no macro misses: `args.hxx` defines no function-like macro its tests call.

## Silent (6)

- `not-loaded` 5: the syntactic edges of files outside the 99 units.
- `no-targets` 1.

**Poison check:** PASS — 2,014 seeded wrong edges: 2,008 refused, 6 unjudged, 0 falsely confirmed.

**Direction of fix:** Hobbes, for all four: ADR-113 §2's third amendment, rule 1 (a site whose references name several monikers abstains). Projected on this grade: 1,995/1,995 (100%), at the cost of the 9 confirmed edges at the collapse sites.

**Pre-registration (`oracle-grading.md` §10.6):**
- P26 recorded, not predicted (the fixture passed at `a848`).
- P27 **met**: semantic precision 99.8%, in [98%, 100%), and 4 of 4 contradictions are a later overload of the same name in the same file.
- P28 **undecidable**: 0 syntactic edges judged (all 5 silent, not loaded).
- P29 **met**: `static→constructor` and `static→method` are 97.4% of misses.
- P30 **met**: poison PASS.
- P31 **missed on args**: its four contradictions are all Hobbes'. The O10 defects of the day (H-28–H-31) came from fmt's triage, which no prediction covered.

The predictions are graded on the first grade above, the one they were written for.

## Regrade, Hobbes 0.2.22-beta (ADR-113 §2's third amendment, `0f1b4d6`)

Re-ingested at `0f1b4d6`, exported, and graded with `oracle grade --poison` against this cell's stored key, which is unchanged. The outputs are in `~/.hobbes/bench/cpp-cells/args-regrade/`.

```
hobbes edges 2000: confirmed 1995  contradicted 0  abstract 0  silent 5 map[not-loaded:5]
precision-against-oracle 100.0% (1995/1995)
recall 56.4% (2010/3561 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1681; misses map[static→constructor:889 static→function:50 static→method:612]
  tier semantic   confirmed 1995  contradicted 0  abstract 0  silent 0
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 5
poison check: PASS — 2000 seeded wrong edges: 1995 refused, 5 unjudged (oracle silent there), 0 falsely confirmed
```

**Direction of fix (signed):**
- **Precision-against-oracle:** 99.8% → 100.0% (+0.2 points). Contradicted 4 → 0; confirmed 2,004 → 1,995 (−9, the collapse sites where the first overload was the one meant).
- **Recall:** 57.1% → 56.4% (2,035 → 2,010 pairs, −25).
- **The new records:** `scip-decode` counts 14 overload sites; nothing withheld (`cpp_withheld` 0), since every syntactic edge args had sat in a file outside the units.

**Lane agreement at the ingest:** 793 sites compared, 2 disagreements. Lane B's target is the key's at both.

## Regrade, Hobbes 0.2.28-beta (O10's defect fixes — H-28, H-29, H-30)

The key was regenerated contained (318 s) over this cell's own compile database, and the **standing** Hobbes export (`args-regrade/hobbes.json`) graded against it — 2,000 edges, identical as a set, so only the key and the matcher moved. Outputs in `~/.hobbes/bench/oracle-defect-drivers/regrade-out/args/`.

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 903b07df
oracle ran contained (ADR-092)
hobbes edges 2000: confirmed 1995  contradicted 0  abstract 0  silent 5 map[not-loaded:5]
precision-against-oracle 100.0% (1995/1995)
recall 56.4% (2010/3561 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1681; misses map[static→constructor:889 static→function:50 static→method:612]
recall-collapsed 63.1% (1994/3161 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  tier semantic   confirmed 1995  contradicted 0  abstract 0  silent 0
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 5
poison check: PASS — 2000 seeded wrong edges: 1938 refused, 62 unjudged (oracle silent there), 0 falsely confirmed
```

**Direction of fix (signed):**
- **Precision-against-oracle:** 100.0% → 100.0% (+0.0). Confirmed 1,995 → 1,995 (+0); contradicted 0 → 0; silent 5 → 5. **Not one graded row moved** — checked bucket by bucket, not by totals.
- **Recall:** 56.4% → 56.4% (+0.0).
- **Collapsed recall did move:** 63.4% (2,003/3,161) → **63.1%** (1,994/3,161), −0.3. That line keys on the *site* line, and H-28 moved member-call sites onto the member's own token, so nine pairs fold differently. The per-signature grade above is unaffected.
- **The poison line moved too, without the grade moving:** 1,995 refused / 5 unjudged → **1,938 / 62**. The regenerated key repositions member-call sites, so the seeded twins land on different lines and 57 more fall where the key is silent. Still PASS, 0 falsely confirmed.
- `line-unresolved` is **0** on this cell: args has no line where a resolved call shares its line with one the key left unresolved, which is why H-30 is inert here.

**Pre-registration (`oracle-grading.md` §10.8):** P39 met (100.0%, no contradiction appears), P40 met, P43 met.

