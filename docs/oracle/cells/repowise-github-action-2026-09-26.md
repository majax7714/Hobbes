# Oracle cell — repowise 0.49.0 on github-action, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@5` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/cypress-io/github-action, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/cypress-io__github-action` — `/home/mmarrujo/.hobbes/bench/comparative/repowise-github-action/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 01e3b659a495b41649cdd0aa82e1d1624e26520b — **the same commit and the same answer key as the Hobbes cell [github-action-js-2026-09-20.md](github-action-js-2026-09-20.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/cells/c165-provisioned/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 01e3b659`), regenerated by the command that cell names.

Index command: `unshare -rn repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the copy)` — exit 0, wall 4 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked; 0.49.0 is the version the other published repowise cells ran (PyPI's latest on 2026-09-26 is 0.53.0, not used). It wrote `.repowise/` into the copy, nothing else. The key was graded with the ingest's npm tree mounted (the Hobbes cell's provisioned arm), so the copy got the same tree (`~/.hobbes/cache/npm/5f31b8be7cf816e8`) at `node_modules`; the tool stored no edge with a `node_modules` path. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. **Converted at converter@5** (ADR-101's 2026-09-26 amendment): 10 of the tool's call edges have a `<file>::__module__` caller and are read as sites in that file; at @4 they were dropped as `unknown-caller` and this cell graded 125 edges (`report.at4.*` beside the cell keeps that grade; it is not a published figure). The tool's own log carries 0 error/warning line(s). Conversion: 154 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored); a `<file>::__module__` caller (a top-level call, no wiki_symbols row) is read as call sites in that file at the tool's call lines (converter@5; @4 dropped every such edge as unknown-caller).

Command: `bench/oracle/grade-foreign.sh repowise-github-action/edges.json c165-provisioned/oracle.json repowise-github-action --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-github-action` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 01e3b659
hobbes edges 154: confirmed 154  contradicted 0  abstract 0  silent 0 map[]
precision-against-oracle 100.0% (154/154)
recall 89.0% (154/173 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 413; misses map[func-value→local-binding:4 func-value→parameter:3 static→closure:4 static→variable:8]
recall-collapsed 89.0% (154/173 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/4)  misses 4 = 21.1% of all misses
  recall[func-value→parameter]   0.0% (0/3)  misses 3 = 15.8% of all misses
  recall[func-value→variable] 100.0% (86/86)  misses 0 = 0.0% of all misses
  recall[static→closure    ]   0.0% (0/4)  misses 4 = 21.1% of all misses
  recall[static→function   ] 100.0% (8/8)  misses 0 = 0.0% of all misses
  recall[static→variable   ]  88.2% (60/68)  misses 8 = 42.1% of all misses
  tier repowise:import_merged confirmed 7  contradicted 0  abstract 0  silent 0
  tier repowise:import_scoped confirmed 3  contradicted 0  abstract 0  silent 0
  tier repowise:same_file confirmed 144  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 17 edge(s) (several oracle sites on one line)
poison check: PASS — 154 seeded wrong edges: 148 refused, 6 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 154 |
| confirmed | 154 |
| contradicted | 0 |
| abstract | 0 |
| silent | 0 {} |
| precision-against-oracle (lower bound) | **100.0%** (154/154) |
| recall | 89.0% (154/173) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:import_merged` confirmed 7 / contradicted 0 / abstract 0 / silent 0; `repowise:import_scoped` confirmed 3 / contradicted 0 / abstract 0 / silent 0; `repowise:same_file` confirmed 144 / contradicted 0 / abstract 0 / silent 0.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→variable` | 60 / 68 | 8 |
| `func-value→local-binding` | 0 / 4 | 4 |
| `static→closure` | 0 / 4 | 4 |
| `func-value→parameter` | 0 / 3 | 3 |
| `func-value→variable` | 86 / 86 | 0 |
| `static→function` | 8 / 8 | 0 |

**Poison check:** PASS — 154 seeded wrong edges: 148 refused, 6 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

