# Oracle cell — repowise 0.49.0 on fmt, module `.`, 2026-09-15 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@4` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/fmtlib/fmt, clone `/home/mmarrujo/.hobbes/bench/cpp-cells/fmt`, commit 3a0661d7c03a81e63acb75836c3139a2e4ecd725 — **the same commit and the same answer key as the Hobbes cell [fmt-cpp-2026-09-15.md](fmt-cpp-2026-09-15.md)**; the key is `/home/mmarrujo/.hobbes/bench/cpp-cells/fmt-cell/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 3a0661d7`), regenerated by the command that cell names.

Index command: `repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the clone)` — exit 0, wall 41 s; no network observed, no repo build invoked. The tool's own log carries 13 error/warning line(s), e.g. `<unknown>:160: SyntaxWarning: invalid escape sequence '\S'`. Conversion: 10055 (site, callee) pairs; dropped {"unknown-caller": 156, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh repowise-fmt/edges.json fmt-cell/oracle.json repowise-fmt --lang cpp`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-fmt` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 3a0661d7
hobbes edges 6366: confirmed 2414  contradicted 2929  abstract 0  silent 1023 map[no-targets:234 not-loaded:118 unreachable:671]
precision-against-oracle 45.2% (2414/5343)
recall 13.9% (7017/50569 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 6985; misses map[macro→function:1631 macro→method:1140 static→constructor:8233 static→function:10302 static→method:22246]
recall-collapsed 9.2% (2414/26333 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/1631)  misses 1631 = 3.7% of all misses
  recall[macro→method      ]   0.0% (0/1140)  misses 1140 = 2.6% of all misses
  recall[static→constructor]   1.2% (100/8333)  misses 8233 = 18.9% of all misses
  recall[static→function   ]  17.9% (2244/12546)  misses 10302 = 23.7% of all misses
  recall[static→method     ]  17.4% (4673/26919)  misses 22246 = 51.1% of all misses
  tier repowise:enclosing_class confirmed 64  contradicted 15  abstract 0  silent 16
  tier repowise:global_unique confirmed 179  contradicted 393  abstract 0  silent 140
  tier repowise:import_merged confirmed 412  contradicted 970  abstract 0  silent 326
  tier repowise:receiver_global confirmed 3  contradicted 5  abstract 0  silent 0
  tier repowise:receiver_typed_global confirmed 48  contradicted 0  abstract 0  silent 7
  tier repowise:receiver_typed_import confirmed 91  contradicted 3  abstract 0  silent 11
  tier repowise:receiver_typed_same_file confirmed 159  contradicted 1  abstract 0  silent 11
  tier repowise:same_file confirmed 1422  contradicted 1509  abstract 0  silent 464
  tier repowise:same_target confirmed 27  contradicted 32  abstract 0  silent 37
  tier repowise:scoped_name confirmed 9  contradicted 1  abstract 0  silent 11
  line-grain tolerance used on 4093 edge(s) (several oracle sites on one line)
poison check: PASS — 6366 seeded wrong edges: 5343 refused, 1023 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 6,366 |
| confirmed | 2,414 |
| contradicted | 2,929 |
| abstract | 0 |
| silent | 1,023 {"no-targets": 234, "not-loaded": 118, "unreachable": 671} |
| precision-against-oracle (lower bound) | **45.2%** (2,414/5,343) |
| recall | 13.9% (7,017/50,569) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:enclosing_class` confirmed 64 / contradicted 15 / abstract 0 / silent 16; `repowise:global_unique` confirmed 179 / contradicted 393 / abstract 0 / silent 140; `repowise:import_merged` confirmed 412 / contradicted 970 / abstract 0 / silent 326; `repowise:receiver_global` confirmed 3 / contradicted 5 / abstract 0 / silent 0; `repowise:receiver_typed_global` confirmed 48 / contradicted 0 / abstract 0 / silent 7; `repowise:receiver_typed_import` confirmed 91 / contradicted 3 / abstract 0 / silent 11; `repowise:receiver_typed_same_file` confirmed 159 / contradicted 1 / abstract 0 / silent 11; `repowise:same_file` confirmed 1422 / contradicted 1509 / abstract 0 / silent 464; `repowise:same_target` confirmed 27 / contradicted 32 / abstract 0 / silent 37; `repowise:scoped_name` confirmed 9 / contradicted 1 / abstract 0 / silent 11.

## Contradicted (2929 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 2285; same file as an oracle target, other line 644. By label: `repowise:same_file` 1509, `repowise:import_merged` 970, `repowise:global_unique` 393, `repowise:same_target` 32, `repowise:enclosing_class` 15, `repowise:receiver_global` 5, `repowise:receiver_typed_import` 3, `repowise:scoped_name` 1, `repowise:receiver_typed_same_file` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `include/fmt/chrono.h:1009` → `include/fmt/format.h:540` [repowise:import_merged] — oracle: operator= @ include/fmt/core.h:2444, operator= @ include/fmt/core.h:2444, fill_n @ include/fmt/format.h:534
- `include/fmt/chrono.h:1035` → `include/fmt/format.h:4574` [repowise:import_merged] — oracle: format_to @ include/fmt/core.h:2828, format_to @ include/fmt/core.h:2828, back_inserter @ :0
- `include/fmt/chrono.h:1159` → `include/fmt/format.h:1310` [repowise:import_merged] — oracle: count_digits @ include/fmt/format.h:1246
- `include/fmt/chrono.h:1162` → `include/fmt/chrono.h:623` [repowise:same_file] — oracle: operator= @ include/fmt/core.h:2444, operator= @ include/fmt/core.h:2444, write_padding @ include/fmt/chrono.h:617
- `include/fmt/chrono.h:1178` → `include/fmt/chrono.h:1138` [repowise:same_file] — oracle: write2 @ include/fmt/chrono.h:1133, write2 @ include/fmt/chrono.h:1133, write2 @ include/fmt/chrono.h:1133
- `include/fmt/chrono.h:1180` → `include/fmt/chrono.h:1138` [repowise:same_file] — oracle: write2 @ include/fmt/chrono.h:1133, write2 @ include/fmt/chrono.h:1133, write2 @ include/fmt/chrono.h:1133

**Triage ratio (A-8), sampled:** over the tool's C++ cells, 40 contradicted rows drawn at random (oracle-grading.md §10.7 (P34): every contradicted row where a cell has 60 or fewer, otherwise 20 drawn by random.Random(20260915).sample over the cell's contradicted rows at the standing grade (converter@4); each read against the source by hand) read `oracle-grain 1 : tool-wrong 39 : converter-defect 0`; the rest of this cell's 2929 rows are untriaged. The rows drawn from this cell:
- `test/ranges-test.cc:721` → `test/ranges-test.cc:711` [repowise:same_file]: **tool-wrong** — `tieable()`, a value-initialised struct with no user-declared constructor, drawn to the struct's line; clang records no call there
- `test/posix-mock-test.cc:116` → `src/os.cc:236` [repowise:import_merged]: **tool-wrong** — libc's `close(fildes)` inside the mock `test::close` drawn to fmt's member `file::close()`
- `test/xchar-test.cc:351` → `include/fmt/color.h:567` [repowise:import_merged]: **tool-wrong** — `fmt::vformat(loc, …)` drawn to the text_style overload in color.h; the call is the locale overload
- `test/chrono-test.cc:406` → `include/fmt/chrono.h:2218` [repowise:import_merged]: **tool-wrong** — the free `fmt::format(…)` drawn to formatter<local_time>'s member `format`
- `test/color-test.cc:99` → `include/fmt/color.h:622` [repowise:import_merged]: **tool-wrong** — `fmt::format(fmt::text_style(), "hi")`, the free overload for a text_style, drawn to a formatter's member `format` (622)
- `test/chrono-test.cc:40` → `test/chrono-test.cc:75` [repowise:same_file]: **tool-wrong** — `make_tm()` drawn to the six-parameter `make_tm` (75); the call is the nullary one (33)
- `test/format-test.cc:1642` → `test/format-test.cc:2728` [repowise:same_file]: **tool-wrong** — the free `fmt::format(…)` drawn to formatter<incomplete_type>'s member `format` in the same file
- `include/fmt/core.h:2513` → `include/fmt/core.h:424` [repowise:same_file]: **tool-wrong** — `map(value_.uint128_value)` drawn to the fallback `map(native_uint128) -> monostate` (424) in the `#if !FMT_USE_INT128` arm; this build has `__int128`, so the call is 415
- `test/format-test.cc:555` → `test/format-test.cc:2728` [repowise:same_file]: **tool-wrong** — row 26's shape
- `test/format-test.cc:1337` → `test/format-test.cc:2728` [repowise:same_file]: **tool-wrong** — row 26's shape
- `test/chrono-test.cc:530` → `include/fmt/chrono.h:2218` [repowise:import_merged]: **tool-wrong** — row 23's shape
- `test/os-test.cc:196` → `test/os-test.cc:370` [repowise:same_file]: **tool-wrong** — `open_buffered_file()` drawn to the file's own one-parameter function of that name (370); the call is util.cc's (14), whose parameter defaults
- `test/format-test.cc:857` → `test/format-test.cc:2728` [repowise:same_file]: **tool-wrong** — row 26's shape
- `test/xchar-test.cc:94` → `test/os-test.cc:19` [repowise:global_unique]: **tool-wrong** — `std::wstring_view(L"{}")` drawn to a `using wstring_view` alias in another test program (os-test.cc:19)
- `test/chrono-test.cc:679` → `include/fmt/chrono.h:2218` [repowise:import_merged]: **tool-wrong** — row 23's shape
- `include/fmt/format.h:624` → `include/fmt/core.h:607` [repowise:import_merged]: **tool-wrong** — the construction `string_view(ptr, …)` drawn to the `using string_view` alias line (607)
- `test/args-test.cc:183` → `test/format-test.cc:2572` [repowise:global_unique]: **tool-wrong** — `std::string("foo")` drawn to a test struct named `string` in another program (format-test.cc:2572)
- `test/chrono-test.cc:472` → `include/fmt/chrono.h:1623` [repowise:import_merged]: **tool-wrong** — `std::chrono::seconds(0)` drawn to a class-local `using seconds` alias in chrono.h (1623)
- `test/chrono-test.cc:647` → `test/chrono-test.cc:577` [repowise:same_file]: **tool-wrong** — `dms(12.56)` drawn to the `using dms` alias line (577); the constructor called is std::chrono::duration's, outside the repo
- `test/color-test.cc:15` → `include/fmt/color.h:336` [repowise:import_merged]: **tool-wrong** — `fmt::text_style()` drawn to the `text_style(uint64_t)` constructor (336); the call is the default one

**Triage (hand-read, 2026-09-15):** A seeded sample of 20 of the 2,929 at converter@4 (oracle-grading.md §10.7, P34): tool-wrong 20, oracle-grain 0, converter-defect 0. 14 are the name-match shape: the free `fmt::format` drawn to a formatter's member `format` (8 of them); the other overload (`make_tm`, `vformat`, `text_style`'s constructor, `open_buffered_file`); libc's `close` drawn to fmt's `file::close()`; and a `map` overload in the `#if !FMT_USE_INT128` arm this build does not compile. 6 are constructions drawn to a type or a `using` alias (`dms(12.56)`, `std::chrono::seconds(0)`, `string_view(…)`, a struct with no user-declared constructor), which the key holds as the constructor clang called or as no call (C-95's C++ face). The other 2,909 are untriaged. **At converter@3** the first draw read 3 of 20 as the converter's: gtest's and fmt's nested macros written `#  define` (`GTEST_LOG_`, `EXPECT_WRITE`, `FMT_ASSERT`), which @3's literal `#define` test did not read as macros. That was 713 of the cell's graded edges, 365 of them contradicted. @4 reads the directive with its spaces and excludes those edges as Hobbes' own macro edges are, and advances two split heads (gtest functions under an attribute macro) to the name's line. **Pre-registration (§10.7):** P32 met (poison PASS); P33 met (45.2% against Hobbes' 99.1%); P34 met (14 of 20); P35 not met (13.9% against Hobbes' 14.5%).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→method` | 4,673 / 26,919 | 22,246 |
| `static→function` | 2,244 / 12,546 | 10,302 |
| `static→constructor` | 100 / 8,333 | 8,233 |
| `macro→function` | 0 / 1,631 | 1,631 |
| `macro→method` | 0 / 1,140 | 1,140 |

**Poison check:** PASS — 6,366 seeded wrong edges: 5,343 refused, 1,023 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (repowise-adapter@3 → repowise-adapter@4, 2026-09-15, signed):** the converter now reads `# define` spelled with spaces as the same directive (kind macro, excluded as Hobbes' own are) and advances a declaration head split over lines to the line holding the name (C-94's C++ face; ADR-101's 2026-09-15 amendment, found by the foreign C++ cells' triage). graded edges 7,079 → 6,366 (-713); confirmed 2,412 → 2,414 (+2); contradicted 3,296 → 2,929 (-367); precision-against-oracle 42.3% → 45.2% (+2.9); recall 13.9% → 13.9% (+0.0). Nothing on the tool's side moved; `report.v1.*` beside the cell keeps the first grade.

**What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

