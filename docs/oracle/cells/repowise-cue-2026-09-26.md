# Oracle cell — repowise 0.49.0 on cue, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@5` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/Blueturboguy07/cue, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/Blueturboguy07__cue` — `/home/mmarrujo/.hobbes/bench/comparative/repowise-cue/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit a27308ed233503902ee96f6d7c4882a7de1dbd47 — **the same commit and the same answer key as the Hobbes cell [cue-js-2026-09-20.md](cue-js-2026-09-20.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/cells/cue-provisioned/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha a27308ed`), regenerated by the command that cell names.

Index command: `unshare -rn repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the copy)` — exit 0, wall 7 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked; 0.49.0 is the version the other published repowise cells ran (PyPI's latest on 2026-09-26 is 0.53.0, not used). It wrote `.repowise/` into the copy, nothing else. The key was graded with the ingest's npm tree mounted (the Hobbes cell's provisioned arm), so the copy got the same tree (`~/.hobbes/cache/npm/cd588e5f47a77d71`) at `node_modules`; the tool stored no edge with a `node_modules` path. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. **Converted at converter@5** (ADR-101's 2026-09-26 amendment): 182 of the tool's call edges have a `<file>::__module__` caller and are read as sites in that file; at @4 they were dropped as `unknown-caller` and this cell graded 598 edges (`report.at4.*` beside the cell keeps that grade; it is not a published figure). The tool's own log carries 0 error/warning line(s). Conversion: 1258 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored); a `<file>::__module__` caller (a top-level call, no wiki_symbols row) is read as call sites in that file at the tool's call lines (converter@5; @4 dropped every such edge as unknown-caller).

Command: `bench/oracle/grade-foreign.sh repowise-cue/edges.json cue-provisioned/oracle.json repowise-cue --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-cue` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha a27308ed
hobbes edges 1258: confirmed 1235  contradicted 19  abstract 4  silent 0 map[]
precision-against-oracle 98.5% (1235/1254)
recall 76.1% (1251/1644 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 5198; misses map[func-value→local-binding:30 func-value→parameter:23 static→anonymous-function:2 static→class:7 static→closure:140 static→function:63 static→method:128]
recall-collapsed 77.0% (1235/1603 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/30)  misses 30 = 7.6% of all misses
  recall[func-value→parameter]   0.0% (0/23)  misses 23 = 5.9% of all misses
  recall[static→anonymous-function]   0.0% (0/2)  misses 2 = 0.5% of all misses
  recall[static→class      ]  81.6% (31/38)  misses 7 = 1.8% of all misses
  recall[static→closure    ]  73.1% (381/521)  misses 140 = 35.6% of all misses
  recall[static→function   ]  92.1% (738/801)  misses 63 = 16.0% of all misses
  recall[static→method     ]  44.1% (101/229)  misses 128 = 32.6% of all misses
  tier repowise:global_unique confirmed 0  contradicted 19  abstract 4  silent 0
  tier repowise:import_scoped confirmed 226  contradicted 0  abstract 0  silent 0
  tier repowise:module_alias confirmed 159  contradicted 0  abstract 0  silent 0
  tier repowise:same_file confirmed 786  contradicted 0  abstract 0  silent 0
  tier repowise:self_scope confirmed 64  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 529 edge(s) (several oracle sites on one line)
poison check: PASS — 1258 seeded wrong edges: 1142 refused, 116 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1,258 |
| confirmed | 1,235 |
| contradicted | 19 |
| abstract | 4 |
| silent | 0 {} |
| precision-against-oracle (lower bound) | **98.5%** (1,235/1,254) |
| recall | 76.1% (1,251/1,644) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 0 / contradicted 19 / abstract 4 / silent 0; `repowise:import_scoped` confirmed 226 / contradicted 0 / abstract 0 / silent 0; `repowise:module_alias` confirmed 159 / contradicted 0 / abstract 0 / silent 0; `repowise:same_file` confirmed 786 / contradicted 0 / abstract 0 / silent 0; `repowise:self_scope` confirmed 64 / contradicted 0 / abstract 0 / silent 0.

## Contradicted (19 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 19. By label: `repowise:global_unique` 19.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `renderer/renderer.js:10` → `renderer/icons.js:31` [repowise:global_unique] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:11` → `renderer/icons.js:31` [repowise:global_unique] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:12` → `renderer/icons.js:31` [repowise:global_unique] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:13` → `renderer/icons.js:31` [repowise:global_unique] — oracle: $ @ renderer/renderer.js:5, icon @ renderer/renderer.js:3
- `renderer/renderer.js:14` → `renderer/icons.js:31` [repowise:global_unique] — oracle: ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22708, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22709, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22710
- `renderer/renderer.js:15` → `renderer/icons.js:31` [repowise:global_unique] — oracle: ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22708, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22709, ParentNode.querySelector @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:22710

**Triage ratio (A-8):** all 19 read by shape (below): `oracle-grain 13 : tool-wrong 6 : converter-defect 0`. Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** all 19, by shape; each shape read by hand on at least two rows. **Oracle-grain 13.** In `renderer/renderer.js`, `icon(…)` is called through `const { icon } = window.ICONS`, and `renderer/icons.js` sets `window.ICONS = { icon }`. The key names the local destructured binding at `renderer.js:3`. The tool names `icons.js`'s `function icon`, the function that runs; it filed these rows `global_unique`, a name guess that is right here. **Tool-wrong 6.** Each is `read()` in `test/store-publik.test.js`, a local returned by the test's fixture, drawn to `src/vad.js`'s method `read`. Converter-defect 0. CodeGraphContext's cue cell has the same 13 icon rows.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→closure` | 381 / 521 | 140 |
| `static→method` | 101 / 229 | 128 |
| `static→function` | 738 / 801 | 63 |
| `func-value→local-binding` | 0 / 30 | 30 |
| `func-value→parameter` | 0 / 23 | 23 |
| `static→class` | 31 / 38 | 7 |
| `static→anonymous-function` | 0 / 2 | 2 |

**Poison check:** PASS — 1,258 seeded wrong edges: 1,142 refused, 116 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

