# Oracle cell — codegraphcontext 0.6.13 on preact, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/preactjs/preact, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/preactjs__preact` — `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-preact/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 8101ff821690817c7786739c317af215c62a0cff — **the same commit and the same answer key as the Hobbes cell [preact-js-2026-09-19.md](preact-js-2026-09-19.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/regrade/h34/preact/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 8101ff82`), regenerated by the command that cell names.

Index command: `unshare -rn codegraphcontext --db kuzudb --db-path <db> index <copy>  (0.6.13; CGC_RUNTIME_DB_TYPE=kuzudb)` — exit 0, wall 51 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked. 0.6.13 is the version the other 44 foreign cells ran, and PyPI's latest on 2026-09-26. The tool's summary lists the JavaScript files it parsed: .js 64, .jsx 128, .mjs 9 (and .ts 21, .tsx 19). As on the Hobbes cell, the tool ran over the whole clone while the key's program is the ingest's root zone (a copy without `demo/`, `test/ts/` and `compat/test/ts/`, the three `tsconfig.json` zones), so the tool's edges there read `silent`/`not-loaded` (143). It left a `.cgcignore` at the copy's root (its default ignore patterns), nothing else. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. The tool's own log carries 0 error/warning line(s). Conversion: 5712 (site, callee) pairs, of which `oracle import` folded 207 repeated pair(s) (the same site and callee from two of the tool's rows), leaving 5,505 graded; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 249 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-preact/edges.json preact/oracle.json codegraphcontext-preact --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-preact` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 8101ff82
hobbes edges 5505: confirmed 1484  contradicted 2202  abstract 0  silent 1819 map[line-unresolved:611 no-targets:1065 not-loaded:143]
precision-against-oracle 40.3% (1484/3686)
precision-strict 34.5% (1484/4297): the 611 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 18.4% (1693/9211 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 2090; misses map[func-value→local-binding:1588 func-value→parameter:112 func-value→variable:223 interface→type-member:1796 static→anonymous-function:1 static→class:576 static→closure:1526 static→function:1458 static→method:203 static→type-member:33 static→variable:2]
recall-collapsed 16.8% (1484/8808 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/1588)  misses 1588 = 21.1% of all misses
  recall[func-value→parameter]   0.0% (0/112)  misses 112 = 1.5% of all misses
  recall[func-value→variable]   0.0% (0/223)  misses 223 = 3.0% of all misses
  recall[interface→type-member]   0.0% (0/1796)  misses 1796 = 23.9% of all misses
  recall[static→anonymous-function]   0.0% (0/1)  misses 1 = 0.0% of all misses
  recall[static→class      ]   0.0% (0/576)  misses 576 = 7.7% of all misses
  recall[static→closure    ]  25.1% (512/2038)  misses 1526 = 20.3% of all misses
  recall[static→function   ]  34.1% (756/2214)  misses 1458 = 19.4% of all misses
  recall[static→method     ]   1.0% (2/205)  misses 203 = 2.7% of all misses
  recall[static→type-member]   0.0% (0/33)  misses 33 = 0.4% of all misses
  recall[static→variable   ]  99.5% (423/425)  misses 2 = 0.0% of all misses
  tier codegraphcontext:EXTRACTED confirmed 1484  contradicted 2127  abstract 0  silent 1758
  tier codegraphcontext:INFERRED confirmed 0  contradicted 75  abstract 0  silent 61
  line-grain tolerance used on 1956 edge(s) (several oracle sites on one line)
poison check: PASS — 5505 seeded wrong edges: 3405 refused, 2100 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 5,505 |
| confirmed | 1,484 |
| contradicted | 2,202 |
| abstract | 0 |
| silent | 1,819 {"line-unresolved": 611, "no-targets": 1065, "not-loaded": 143} |
| precision-against-oracle (lower bound) | **40.3%** (1,484/3,686) |
| precision strict (ADR-124: the 611 line-unresolved rows counted as contradicted) | **34.5%** (1,484/4,297) |
| recall | 18.4% (1,693/9,211) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 1484 / contradicted 2127 / abstract 0 / silent 1758; `codegraphcontext:INFERRED` confirmed 0 / contradicted 75 / abstract 0 / silent 61.

## Contradicted (2202 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 2167; same file as an oracle target, other line 35. By label: `codegraphcontext:EXTRACTED` 2127, `codegraphcontext:INFERRED` 75.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `compat/scheduler.js:4` → `compat/test/ts/scheduler.ts:11` [codegraphcontext:EXTRACTED] — oracle: callback @ compat/scheduler.js:3
- `compat/scheduler.js:4` → `test/browser/refs.test.jsx:7` [codegraphcontext:EXTRACTED] — oracle: callback @ compat/scheduler.js:3
- `compat/scheduler.mjs:20` → `compat/test/ts/scheduler.ts:11` [codegraphcontext:EXTRACTED] — oracle: callback @ compat/scheduler.mjs:19
- `compat/scheduler.mjs:20` → `test/browser/refs.test.jsx:7` [codegraphcontext:EXTRACTED] — oracle: callback @ compat/scheduler.mjs:19
- `compat/src/forwardRef.js:14` → `test/_util/helpers.jsx:15` [codegraphcontext:EXTRACTED] — oracle: ObjectConstructor.assign @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es2015.core.d.ts:286, ObjectConstructor.assign @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es2015.core.d.ts:295, ObjectConstructor.assign @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es2015.core.d.ts:305
- `compat/src/index.js:145` → `test/browser/refs.test.jsx:7` [codegraphcontext:EXTRACTED] — oracle: callback @ compat/src/index.js:134

**Triage ratio (A-8):** 15 of 2,202 read by hand (8 drawn at random, 1 picked from the `.d.ts` rows, 6 drawn from the 86 implementation rows below): `oracle-grain 6 : tool-wrong 9 : converter-defect 0`; the other 2,187 are untriaged. Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** 8 of the 2,202 drawn at random (seed 7) and read against the source: **tool-wrong 8**, oracle-grain 0, converter-defect 0 — `rerender()` (a test's local binding, 4 rows) drawn to `test/fixtures/preact.js`'s `rerender`; `render(<Foo …/>, scratch)` in `test/browser/` (which imports `render` from the repo's `src`) drawn to `compat/src/render.js`'s `render` (3); `createEvent(…)` drawn to another test file's local `createEvent` (1). **A mechanical split, then a reading:** on 1,023 of the 2,202 rows the key names a declaration in one of the repo's own `.d.ts` files (preact types its JavaScript by `src/index.d.ts`, `hooks/src/index.d.ts` and siblings; H-34), and the tool's callee declares a function of the same name. For **86** of them the callee sits in a `.js` file under that `.d.ts` file's own directory, i.e. it is the implementation the typing describes. Six of those 86 were drawn at random (seed 20260926) and read by hand, and **all six are oracle-grain**: `createRef()` (twice), `createElement(…)` and `createContext(…)` (twice) are drawn to `src/create-element.js` and `src/create-context.js`, and `setupRerender()` to `test-utils/src/index.js`. Each is the function that runs, where the key names the hand-written typing. The other **937** name a same-named function elsewhere, like the three `render` rows above that go to `compat/src/render.js`, which is tool-wrong. The one row picked from the 1,023 before this split (`hooks/src/index.js:397`, `options.useDebugValue(…)` drawn to the hook `useDebugValue` itself, where the key names the `options` member) is tool-wrong. The rest are untriaged, and the 611 line-unresolved rows are counted against the tool only in the strict figure.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `interface→type-member` | 0 / 1,796 | 1,796 |
| `func-value→local-binding` | 0 / 1,588 | 1,588 |
| `static→closure` | 512 / 2,038 | 1,526 |
| `static→function` | 756 / 2,214 | 1,458 |
| `static→class` | 0 / 576 | 576 |
| `func-value→variable` | 0 / 223 | 223 |
| `static→method` | 2 / 205 | 203 |
| `func-value→parameter` | 0 / 112 | 112 |
| `static→type-member` | 0 / 33 | 33 |
| `static→variable` | 423 / 425 | 2 |
| `static→anonymous-function` | 0 / 1 | 1 |

**Poison check:** PASS — 5,505 seeded wrong edges: 3,405 refused, 2,100 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

