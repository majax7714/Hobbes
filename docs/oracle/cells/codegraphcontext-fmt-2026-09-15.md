# Oracle cell — codegraphcontext 0.6.13 on fmt, module `.`, 2026-09-15 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/fmtlib/fmt, clone `/home/mmarrujo/.hobbes/bench/cpp-cells/fmt`, commit 3a0661d7c03a81e63acb75836c3139a2e4ecd725 — **the same commit and the same answer key as the Hobbes cell [fmt-cpp-2026-09-15.md](fmt-cpp-2026-09-15.md)**; the key is `/home/mmarrujo/.hobbes/bench/cpp-cells/fmt-cell/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 3a0661d7`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 36 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 1391 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 8 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-fmt/edges.json fmt-cell/oracle.json codegraphcontext-fmt --lang cpp`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-fmt` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 3a0661d7
hobbes edges 1249: confirmed 844  contradicted 131  abstract 0  silent 274 map[no-targets:218 not-loaded:1 unreachable:55]
precision-against-oracle 86.6% (844/975)
recall 11.8% (5952/50569 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 6985; misses map[macro→function:1631 macro→method:1140 static→constructor:8310 static→function:10673 static→method:22863]
recall-collapsed 3.2% (844/26333 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.0% (0/1631)  misses 1631 = 3.7% of all misses
  recall[macro→method      ]   0.0% (0/1140)  misses 1140 = 2.6% of all misses
  recall[static→constructor]   0.3% (23/8333)  misses 8310 = 18.6% of all misses
  recall[static→function   ]  14.9% (1873/12546)  misses 10673 = 23.9% of all misses
  recall[static→method     ]  15.1% (4056/26919)  misses 22863 = 51.2% of all misses
  tier codegraphcontext:EXTRACTED confirmed 817  contradicted 98  abstract 0  silent 252
  tier codegraphcontext:INFERRED confirmed 27  contradicted 33  abstract 0  silent 22
  line-grain tolerance used on 750 edge(s) (several oracle sites on one line)
poison check: PASS — 1249 seeded wrong edges: 975 refused, 274 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 1,249 |
| confirmed | 844 |
| contradicted | 131 |
| abstract | 0 |
| silent | 274 {"no-targets": 218, "not-loaded": 1, "unreachable": 55} |
| precision-against-oracle (lower bound) | **86.6%** (844/975) |
| recall | 11.8% (5,952/50,569) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 817 / contradicted 98 / abstract 0 / silent 252; `codegraphcontext:INFERRED` confirmed 27 / contradicted 33 / abstract 0 / silent 22.

## Contradicted (131 rows; all in report.json)

Mechanical shape (no reading): same file as an oracle target, other line 71; another file than every oracle target 60. By label: `codegraphcontext:EXTRACTED` 98, `codegraphcontext:INFERRED` 33.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `include/fmt/args.h:177` → `include/fmt/core.h:2742` [codegraphcontext:INFERRED] — oracle: emplace_arg @ include/fmt/args.h:112, emplace_arg @ include/fmt/args.h:112, emplace_arg @ include/fmt/args.h:112
- `include/fmt/chrono.h:1223` → `include/fmt/chrono.h:1688` [codegraphcontext:EXTRACTED] — oracle: operator= @ include/fmt/core.h:2444, operator= @ include/fmt/core.h:2444, write @ include/fmt/format.h:3849
- `include/fmt/chrono.h:1229` → `include/fmt/chrono.h:1688` [codegraphcontext:EXTRACTED] — oracle: operator= @ include/fmt/core.h:2444, operator= @ include/fmt/core.h:2444, write @ include/fmt/format.h:3849
- `include/fmt/chrono.h:1248` → `include/fmt/chrono.h:1688` [codegraphcontext:EXTRACTED] — oracle: operator= @ include/fmt/core.h:2444, operator= @ include/fmt/core.h:2444, write @ include/fmt/format.h:3849
- `include/fmt/chrono.h:1254` → `include/fmt/chrono.h:1688` [codegraphcontext:EXTRACTED] — oracle: operator= @ include/fmt/core.h:2444, operator= @ include/fmt/core.h:2444, write @ include/fmt/format.h:3849
- `include/fmt/chrono.h:1310` → `include/fmt/chrono.h:1188` [codegraphcontext:EXTRACTED] — oracle: format_utc_offset @ include/fmt/chrono.h:1184, format_utc_offset @ include/fmt/chrono.h:1184, format_utc_offset @ include/fmt/chrono.h:1184

**Triage ratio (A-8), sampled:** over the tool's C++ cells, 20 contradicted rows drawn at random (oracle-grading.md §10.7 (P34): every contradicted row where a cell has 60 or fewer, otherwise 20 drawn by random.Random(20260915).sample over the cell's contradicted rows at the standing grade (converter@4); each read against the source by hand) read `oracle-grain 3 : tool-wrong 17 : converter-defect 0`; the rest of this cell's 131 rows are untriaged. The rows drawn from this cell:
- `include/fmt/core.h:861` → `include/fmt/core.h:861` [codegraphcontext:EXTRACTED]: **tool-wrong** — `fmt_.begin()` drawn to the enclosing `begin()` itself; the call is basic_string_view::begin (core.h:559)
- `include/fmt/printf.h:559` → `include/fmt/printf.h:245` [codegraphcontext:EXTRACTED]: **tool-wrong** — the free `write(out, basic_string_view<Char>(…))` drawn to a member template `write(T)` of another class; the key's is format.h:3810
- `test/gtest/gmock/gmock.h:9615` → `test/gtest/gmock/gmock.h:9651` [codegraphcontext:EXTRACTED]: **tool-wrong** — `func_mocker->PerformDefaultAction(…)` drawn to ActionResultHolder's static of the same name; the call is FunctionMocker's (9735)
- `include/fmt/chrono.h:1254` → `include/fmt/chrono.h:1688` [codegraphcontext:EXTRACTED]: **tool-wrong** — the free `write(out_, …)` drawn to another class's member `write(Rep, int, pad_type)`; the key's is format.h:3849
- `test/gtest/gtest/gtest.h:6054` → `test/gtest/gtest/gtest.h:5830` [codegraphcontext:INFERRED]: **tool-wrong** — `UniversalPrinter<T1>::Print` drawn to the Variant partial specialisation's Print; the key names the primary's (5756)
- `test/gtest/gmock/gmock.h:6667` → `test/gtest/gmock/gmock.h:6679` [codegraphcontext:EXTRACTED]: **oracle-grain** — H-29: `GetTypeName()` in VariantMatcher calls its own static (6679), which the tool drew; the key merges the pattern's member by bare name and names another matcher's (6740)
- `include/fmt/format.h:1542` → `include/fmt/format.h:1542` [codegraphcontext:EXTRACTED]: **tool-wrong** — `buffer_.size()` drawn to the enclosing `size()` itself; the call is buffer::size (core.h:1802)
- `test/gtest/gtest/gtest.h:8199` → `test/gtest/gtest/gtest.h:8185` [codegraphcontext:EXTRACTED]: **oracle-grain** — H-30: `BaseGenerator()` inside Iterator::Equals calls the Iterator's own override (8185), which the tool drew; the key leaves the virtual call dynamic and judged the edge against the line's other calls (GTEST_CHECK_'s expansion)
- `include/fmt/format.h:3845` → `include/fmt/format.h:520` [codegraphcontext:EXTRACTED]: **tool-wrong** — `base_iterator(out, it)` drawn to the other overload (520); the instantiations call 527
- `include/fmt/chrono.h:2014` → `include/fmt/core.h:2372` [codegraphcontext:INFERRED]: **tool-wrong** — `formatter<std::tm, Char>::parse(ctx)` drawn to another formatter's parse; the qualified call names chrono.h:2153
- `include/fmt/color.h:468` → `include/fmt/color.h:426` [codegraphcontext:INFERRED]: **tool-wrong** — `reset_color.end()` on a string_view drawn to a color.h class's member `end()`; the call is core.h:560
- `include/fmt/format-inl.h:140` → `include/fmt/format-inl.h:69` [codegraphcontext:INFERRED]: **tool-wrong** — std's `numpunct<Char>::decimal_point()` drawn to fmt's own `decimal_point()` at 69; the key's target is the standard library's
- `include/fmt/format.h:3792` → `include/fmt/format.h:520` [codegraphcontext:EXTRACTED]: **tool-wrong** — `base_iterator(out, it)`, the other overload again (520 for 527)
- `include/fmt/printf.h:420` → `include/fmt/core.h:2663` [codegraphcontext:INFERRED]: **tool-wrong** — `context.arg(arg_index)` on a printf context drawn to basic_format_context::arg(string_view); the call is printf.h:45
- `test/gtest/gmock/gmock.h:3486` → `test/gtest/gmock/gmock.h:3483` [codegraphcontext:EXTRACTED]: **tool-wrong** — `std::get<N - 1>(matcher_tuple).Matches(…)` drawn to the enclosing TuplePrefix::Matches; the call is Matcher's (gtest.h:6361)
- `include/fmt/chrono.h:1248` → `include/fmt/chrono.h:1688` [codegraphcontext:EXTRACTED]: **tool-wrong** — row 3's shape at on_abbr_month
- `test/gtest/gmock/gmock.h:5974` → `test/gtest/gmock/gmock.h:5945` [codegraphcontext:EXTRACTED]: **oracle-grain** — H-30: `VariadicExpand({…})` drawn to its one declaration (5945); the key leaves the site dynamic with no targets and judged the edge against the line's other calls (fmt's record lists the same row for Hobbes)
- `test/scan.h:508` → `test/scan.h:450` [codegraphcontext:EXTRACTED]: **tool-wrong** — `read(it, temp, specs)` with a double drawn to the template overload (450); the call is the double overload (472)
- `test/gtest/gtest/gtest.h:8547` → `test/gtest/gtest/gtest.h:3134` [codegraphcontext:INFERRED]: **tool-wrong** — std::string's `c_str()` drawn to FilePath::c_str (3134)
- `include/fmt/format.h:4232` → `include/fmt/format.h:4264` [codegraphcontext:EXTRACTED]: **tool-wrong** — `format_as(value)` drawn to fmt's enum overload (4264); the key's targets are the tests' own ADL overloads

**Triage (hand-read, 2026-09-15):** A seeded sample of 20 of the 131 read (oracle-grading.md §10.7, P34): tool-wrong 17, oracle-grain 3, converter-defect 0. The 17 are one shape, a call drawn by its short name to another declaration of that name: the enclosing function itself (`fmt_.begin()` inside `begin()`), another class's member, a template's partial specialisation for the primary, the other overload. The 3 oracle-grain rows are the key's open defects on fmt, and at each the tool's target is the one the source names: H-29 (VariantMatcher's own `GetTypeName`) and H-30 twice (`BaseGenerator()` and `VariadicExpand`, dynamic sites judged against their line's other calls). The other 111 are untriaged. **The tool reads no `.cc` file**: its parser table maps `.cpp`, `.h`, `.hpp` and `.hh` to C++, so fmt's 47 `.cc` sources (`src/`, the tests) are unseen, and its edges are the headers' own. converter@4 moved nothing here. **Pre-registration (§10.7):** P32 met (poison PASS); P33 met (86.6% against Hobbes' 99.1%); P34 met (17 of 20); P35 not met by this tool (11.8% against Hobbes' 14.5%).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→method` | 4,056 / 26,919 | 22,863 |
| `static→function` | 1,873 / 12,546 | 10,673 |
| `static→constructor` | 23 / 8,333 | 8,310 |
| `macro→function` | 0 / 1,631 | 1,631 |
| `macro→method` | 0 / 1,140 | 1,140 |

**Poison check:** PASS — 1,249 seeded wrong edges: 975 refused, 274 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

