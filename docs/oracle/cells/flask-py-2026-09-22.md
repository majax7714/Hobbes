# Oracle cell — pallets/flask, Python zone `.`, 2026-09-22

**Repo:** `pallets/flask` (BSD-3-Clause) at `d73fa1c` — the clone ADR-137 held out
(`~/.hobbes/bench/c4-fixtures/heldout/flask`), cloned again for the cell to
`~/.hobbes/bench/oracle/repos/flask` so the held-out drivers keep their tree. Keyed to
grade ADR-145's amendment (the value through a local, an inherited method), which it
was drawn for: flask's `app` fixture is the shape. Pre-registered in
`~/.hobbes/bench/c4-local-value/PREREG.md` (P3) before the key existed. **Hobbes at**
0.2.68-beta (`5d23358`); the ingest reported the tree dirty — the only dirt is the
`.gitignore` line the ingest adds (ADR-012). Oracle `py-trace` (CPython 3.12.13
`sys.monitoring`, the clone's own uv venv from `uv sync --group tests --python 3.12`,
the repo's own suite, `testpaths = ["tests"]`). Both runs exit 0 — `494 passed`. Union
over 2 runs. Untraced, the suite runs in about 1 s.

**Command:** `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/flask .
~/.hobbes/bench/oracle/flask-py --lang py --runs 2 -- -q -p no:cacheprovider`.
**Runtime:** 18 s for the cell after the ingest. Output dir
`~/.hobbes/bench/oracle/flask-py/` (`hobbes.json`, `oracle.json`, `oracle.run1.json`,
`oracle.run2.json`, `report.json`, `report.txt`); the log `flask-py.log` beside it.

Ingest line: `graph.json: 159 nodes, 634 module edges, 1580 symbols, 1016 call edges,
2184 uses edges, 121 implements edges`; `capture [python]: 65.4% of 4185 detected call
sites accounted`.

```
cell   oracle py-trace 3.12.13 sys.monitoring (trace)  sha d73fa1cd
oracle ran contained (ADR-092)
hobbes edges 1330: confirmed 1121  suspect 18  unobserved 191 map[line-mixed:14 line-not-called:27 not-loaded:150]
confirmation rate 84.3% (1121/1330 hobbes edges; coverage-limited, not precision)
suspect rate 1.6% (18/1139 executed hobbes edges; triage queue, never contradicted)
recall-against-executed 41.5% (1121/2698 observed in-repo pairs) over 2 run(s); external python targets 1411
  recall[observed→class    ]  84.3% (268/318)   misses 50
  recall[observed→closure  ]   4.2% (36/865)    misses 829
  recall[observed→function ]  96.8% (306/316)   misses 10
  recall[observed→lambda   ]   0.0% (0/25)      misses 25
  recall[observed→method   ]  43.5% (511/1174)  misses 663
  tier semantic   confirmed 1117  suspect 18  unobserved 180
  tier syntactic  confirmed 4  suspect 0  unobserved 11
poison check: PASS — 1330 seeded wrong edges: 1061 refused, 269 unjudged, 0 falsely confirmed
```

**Suspects (18), read row by row.**
- **15, trace asymmetry (C-60), neither side wrong:** a base-class method where the
  trace observed an override — `JSONProvider.dumps/loads/response` →
  `DefaultJSONProvider.*` (5), `SessionInterface.open_session/save_session` →
  `SecureCookieSessionInterface.*` or a test's subclass (4), `JSONTag.check/to_json/
  to_python` → the `Tag*` subclasses (3), `App.create_jinja_environment` →
  `Flask.create_jinja_environment`, `Scaffold.add_url_rule` → `App.add_url_rule` /
  `Blueprint.add_url_rule`, `Scaffold._check_setup_finished` → `App.*` / `Blueprint.*`.
- **3, Hobbes-wrong:** in `tests/test_helpers.py`, `TestStreaming`'s methods each
  define a nested `index` with a nested `generate` (or `gen`). Each is its own symbol
  (`…test_streaming_with_context.index.generate`,
  `…test_streaming_with_context_as_decorator.index.generate`, …), but three `semantic`
  edges name a **sibling method's** def: line 251 (`…_as_decorator.index`) → `…
  test_streaming_with_context.index.generate` (232), not its own at 246; line 281
  (`…_and_custom_close.index`) → the same 232, not its own at 276; line 310
  (`test_async_view.index`) → `test_stream_keeps_session.index.gen` (293), not its own at
  307. The trace saw each test's own def. **The cause is not yet read** — the join picks
  the first of several same-suffix nested defs, and whether scip-python's symbol for a
  def nested in a method's nested def drops the method is the question to ask in the
  image. Owned as ours either way (P9).

**Misses (1,577).** `observed→closure` 829: flask's tests define views, handlers and
hooks as nested functions (C-58's Python face). `observed→method` 663: most sit on the
`app` fixture's value — `app.route`, `app.add_url_rule`, `app.test_request_context`, …
— which is ADR-145's amendment (below); then `with app.app_context():` →
`AppContext.__exit__` (54), `FlaskClient.__exit__` (26).

**ADR-145's amendment, simulated on this key** (`~/.hobbes/bench/c4-local-value/`,
`probe_worded.py` over this cell's graph, `PREREG-worded.md` first): 400 drawn — 71
direct, 329 inherited along `Flask(App(Scaffold))` — **400 confirmed at the exact target
line, 0 contradicted**, all 400 missed pairs today; recall would read 1,121 → 1,521 of
2,698 (41.5% → 56.4%). The built cell is graded in `oracle-grading.md` when it lands.

**Coverage line.** The suite spoke about 1,115 of Hobbes' 1,285 Python sites (86.8%);
54/83 module files loaded (the unloaded are mostly `examples/`, outside `testpaths`);
1,273/1,622 declared functions started; 65,705 C-callee calls; 1,411 out-of-repo Python
targets; N = 2.

**Direction of fix (which side would move; no proposals).**
- The 3 Hobbes-wrong suspects: Hobbes — a nested def resolved to a sibling method's
  same-named nested def.
- `observed→method` on the `app` fixture: Hobbes — ADR-145's amendment.
- `observed→closure` and `observed→lambda`: Hobbes — C-58.
- The 15 base-vs-override suspects: neither — C-60.

**Not graded:** none abandoned; flask was the first pick.
