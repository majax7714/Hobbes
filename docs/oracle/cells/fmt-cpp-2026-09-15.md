# Oracle cell — fmtlib/fmt, build root `.`, 2026-09-15 (O10, the first C++ cell)

**Repo:** https://github.com/fmtlib/fmt (MIT), clone at `~/.hobbes/bench/cpp-cells/fmt`, commit `3a0661d7c03a81e63acb75836c3139a2e4ecd725`.
- **Why this repo:** chosen, not drawn (ADR-113 §4). It is a header-heavy library whose code lives in `.h` files, which is the claim rule's hardest case, with a CMake build and its bundled gtest and gmock.
- **Ingest:** SHA `3a0661d7`. The tree reads dirty only because `hobbes ingest` appends `.hobbes/` to the clone's `.gitignore` (ADR-012). Hobbes 0.2.21-beta at `594fd32`, lane B on, scip-clang 0.4.0 over CMake's export, one translation unit per run (52 units, under the 400 bound), contained (`index-c`).
- **Oracle:** `c-clang --lang cpp` (O10, ADR-113 §3), `Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json`. Roots line: `compile database: cmake (52 units, 51 c++)`, 0 units failed. Kind `resolution`, no roots.
- **Not pre-registered.** This cell was graded before `oracle-grading.md` §10.6 existed, so its numbers are measured, never predictions met or missed.

**Command:** `bench/oracle/run-cell.sh ~/.hobbes/bench/cpp-cells/fmt . ~/.hobbes/bench/cpp-cells/fmt-cell --lang cpp` (the driver's `cpp` case, `77cbd44`). Runtime 331 s. The outputs are in `~/.hobbes/bench/cpp-cells/fmt-cell/` (`hobbes.json`, `oracle.json`, `report.json`, `report.txt`, `contradicted.tsv`).

## Numbers, Hobbes 0.2.21-beta (report.txt, verbatim; the 42,708 `missed` and 138 `contradicted` rows elided)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 3a0661d7
oracle ran contained (ADR-092)
hobbes edges 4092: confirmed 3439  contradicted 138  abstract 0  silent 515 map[no-targets:192 not-loaded:54 unreachable:269]
coverage: map[lang_cpp:1 sites_constructor:8331 sites_dynamic:3760 sites_external:6983 sites_link_ambiguous:35 sites_macro:2771 sites_operator:8627 sites_static:30809 sites_tu_split:6 sites_undefined:499 sites_virtual:19 units:52 units_cpp:51 units_failed:0]
precision-against-oracle 96.1% (3439/3577)
recall 15.5% (7861/50569 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 6985; misses map[macro→function:1622 macro→method:1140 static→constructor:8205 static→function:9953 static→method:21788]
recall-collapsed 13.1% (3439/26333 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[macro→function    ]   0.6% (9/1631)  misses 1622 = 3.8% of all misses
  recall[macro→method      ]   0.0% (0/1140)  misses 1140 = 2.7% of all misses
  recall[static→constructor]   1.5% (128/8333)  misses 8205 = 19.2% of all misses
  recall[static→function   ]  20.7% (2593/12546)  misses 9953 = 23.3% of all misses
  recall[static→method     ]  19.1% (5131/26919)  misses 21788 = 51.0% of all misses
  tier semantic   confirmed 3331  contradicted 64  abstract 0  silent 262
  tier syntactic  confirmed 108  contradicted 74  abstract 0  silent 253
  line-grain tolerance used on 2188 edge(s) (several oracle sites on one line)
poison check: PASS — 4092 seeded wrong edges: 3577 refused, 515 unjudged (oracle silent there), 0 falsely confirmed
```

| bucket | count |
|---|---|
| hobbes edges (graded) | 4,092 (3,657 semantic, 435 syntactic) |
| confirmed | 3,439 (3,331 semantic, 108 syntactic) |
| contradicted | 138 (64 semantic, 74 syntactic) |
| silent | 515 (no-targets 192, not-loaded 54, unreachable 269) |
| precision-against-oracle | **96.1%** (3,439/3,577); semantic 98.1% (3,331/3,395), syntactic 59.3% (108/182) |
| in-repo oracle pairs | 50,569 |
| recall (no roots; every resolved site) | 15.5% (7,861/50,569); collapsed 13.1% |
| external oracle pairs (not graded) | 6,985 |

**Triage ratio (A-8):** 138 contradicted: `hobbes-wrong 120 : oracle-wrong 18 : untriaged 0`. Of the 120, 110 come from two Hobbes rules and 10 from the provider (P9).

## Contradictions by mechanism (138, every row read)

The rows were read at their cited lines. For the lane B rows, the join's inputs were captured by wrapping `evidence.join` during an ingest, and the helper's choices were logged by a probe running a patched copy of the helper. No tracked file was touched. Scripts and logs are in the session's scratchpad.

| Mechanism | Tier | Rows | Charged to |
|---|---|---|---|
| **The overload collapse.** ADR-109's one-target rule keeps the smallest line where a site's references name several overloads of one file | semantic | 36 | Hobbes, the helper's decode (C-151) |
| **The name fallback in a file lane B indexed**, at a site where lane B has no occurrence of the name | syntactic | 74 | Hobbes, lane A (C-152) |
| **scip-clang's single wrong candidate** for a dependent or qualified call | semantic | 10 | the provider (C-153, P9) |
| A member call keyed at its object's start (H-28) | semantic | 1 | oracle |
| A class-template pattern's member merged by bare name across classes (H-29) | semantic | 5 | oracle |
| An edge judged against other calls on its line where the key left the call `dynamic` (H-30) | semantic | 5 | oracle |
| A call the key does not hold at all (H-31) | semantic | 7 | oracle |

**The overload collapse (36).** The probe logged 634 sites where the one-target rule met references at several lines of one file, and every one names more than one moniker. At `chrono.h:1348`, `write2(tm_mon() + 1, pad)` inside the class template `tm_writer` carries 18 references to `tm_writer#write2(d4f7…).` (line 1133, one parameter) and `(fc4f…).` (1138, two). scip-clang references the candidates of a call in a template it cannot resolve there, and the rule keeps 1133. Of the edges drawn at the 634 sites, 75 were confirmed (the first overload was the one meant), 36 contradicted and 73 silent. The rows by callee:
- `write2` ×10;
- `write_padding` and `check_arg_id`, ×3 each;
- `gmtime`, `set_fill`, `range_begin`, `for_each` and `for_each2`, ×2 each;
- once each: `get_buffer`, `get_iterator`, `vprint`, `data`, `print`, `range_end`, `read` (`scan.h:551`), `scan_to`, gmock's `Throws` and `GetCharWidthPrefix`.

**The name fallback (74).** Every judged syntactic edge (182) sits in a file lane B indexed, and 70 of the 74 contradicted ones sit where lane B has no occurrence of the name at all. The other 4 are C-148's abstentions (`count<…>()` at `core.h:1076`, 1080, 1083, 2715). Two shapes:
- **Reach into a namespace (50).** C's ranks are namespace-blind. A libc call scip-clang records no occurrence for in a C++ unit is drawn by name to a namespaced function of the repo. 43 go to fmt's POSIX mocks in `test/posix-mock-test.cc` (`test::close`, `test::fopen`, `test::dup2`, …). Ten of those are the mocks' own bodies: `::FMT_POSIX(close(fildes))` at `posix-mock-test.cc:116` is drawn to `test::close` itself. The other 7 are `detail::allocate` ×5 (gmock's `EXPECT_CALL(alloc, allocate(size))`), `detail::gmtime_r` (the overload set with libc's, `chrono.h:508`) and `getpagesize`.
- **A "unique" name that was not (24).** Lane A lost the real definition to a parse error (C-145), so rank 3 found one definition of the name and took it. `runtime` ×11 draws `xchar.h:122` where the call means `core.h:2692` (not a symbol). The others: `vformat_to` ×5, `count` ×4 (a member `count() const` at `core.h:1971`, read as a free function because its class was lost), `parse_arg_id` ×2 (drawn to a test's function at `test/core-test.cc:490`), `vprintln`, `basic_string_view`.

The external veto (ADR-111) fired 0 times on this cell. C++ units give it almost nothing to fire on: across fmt's C++ files, lane B's external occurrences of libc names number 141 in all.

**The provider's single wrong candidate (10).** Lane B carries one reference, and it names the wrong declaration:
- the caller's own specialisation for another's member: `formatter<int>::format(42, ctx)` inside `formatter<type_with_get>::format` (`compile-test.cc:37`), and the same shape at `:62`, `format-test.cc:1910` and `std.h:698`;
- the explicit specialisation for the primary: `test_format<20>::format("{19}")` drawn to `test_format<0>::format` (683), where the call names the primary template (676), at `format-test.cc:689`, 690, 692 and 696;
- one overload of two: `isfinite(from.count())` at `chrono.h:459` drawn to the integral overload (`chrono.h:1497`) where the instantiation calls the floating one (`format.h:2854`); `read(it, abs_value, specs)` at `scan.h:466` drawn to the two-argument `read` (394) where the call has three (450).

**The oracle's (18), all read from source with Hobbes' target the callee the source names:**
- H-28 (1): `.GetString()` at `gmock-gtest-all.cc:4635`, which the key holds at 4631, the start of `(Message() << …`.
- H-29 (5): `format_as(x)` at `std.h:714` and 726 calls the enclosing `formatter<bitint<N>>`'s own static member (709, 721), and the key names `formatter<std::byte>`'s (747). `GetTypeName()` at `gmock.h:6653`, 6667 and 6673 calls `VariantMatcher`'s own (6679), and the key names another matcher's (6740).
- H-30 (5): `ExplainSuccess` (`gmock.h:5798`), `VariadicExpand` (5974), `count()` (6068, 6086) and `PushTrace` (`gtest.h:12144`). Each is a `dynamic` site with no targets in the key, judged against the targets of the line's other calls.
- H-31 (7): `fmt::arg(…)` inside `decltype(…)` at `compile-test.cc:127`, and six `os.cc` calls written `FMT_SYSTEM(fopen(…))` inside `FMT_RETRY_VAL(…)` (176, 222, 274, 282, 300, 310). The key holds no site for any of them. `posix-mock-test.cc:20` includes `../src/os.cc` with `FMT_SYSTEM(call)` defined as `test::call` (`posix-mock.h:73`), so in that unit the call is the mock Hobbes draws, and in the library's own unit it is libc's.

## Misses (42,708)

`static→method` 21,788 (51.0%), `static→function` 9,953 (23.3%), `static→constructor` 8,205 (19.2%), `macro→function` 1,622, `macro→method` 1,140. Recall at 15.5% is fmt's shape rather than one hole:
- **C-145:** every library header parsed with error nodes, and 6,896 of 7,376 below-floor facts point into such files (measured at 0.2.19-beta).
- **Operators are recall only (C-146):** 8,627 operator sites in the key.
- **Constructions:** 8,331 construct sites, 128 of them drawn. The implicit-construction guard and the class-over-constructor rule leave most to the key.
- **C-148:** 240 monikers abstained, 29,556 references counted (each unit's copy of a header counts since per-unit indexing).
- **Macros:** gtest's assertion macros (C-131).

## Silent (515, charged to nobody)

- `unreachable` 269: lines in `#if` arms or code clang's configuration leaves out.
- `no-targets` 192: a site the key holds as `dynamic` or `undefined`.
- `not-loaded` 54: files outside the 52 units.

**Poison check:** PASS — 4,092 seeded wrong edges: 3,577 refused, 515 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix** (which side would have to change):
- The collapse (36) and the fallback (74): Hobbes. ADR-113 §2's third amendment decides both (Max's route, "Fix both, then row"). Projected on this grade: 3,256/3,284 (99.1%), at the cost of 183 confirmed edges (75 collapse, 108 syntactic).
- The provider's 10: neither side the lane controls. Registered as C-153.
- The oracle's 18: the oracle. H-28–H-31 are open (Max: recorded open, triage carries them).

## Regrade, Hobbes 0.2.22-beta (ADR-113 §2's third amendment, `0f1b4d6`)

Re-ingested at `0f1b4d6`, exported, and graded with `oracle grade --poison` against this cell's stored key, which is unchanged. The outputs are in `~/.hobbes/bench/cpp-cells/fmt-regrade/`.

```
hobbes edges 3525: confirmed 3254  contradicted 28  abstract 0  silent 243 map[no-targets:119 not-loaded:54 unreachable:70]
precision-against-oracle 99.1% (3254/3282)
recall 14.5% (7319/50569 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 6985; misses map[macro→function:1622 macro→method:1140 static→constructor:8205 static→function:10320 static→method:21963]
  tier semantic   confirmed 3254  contradicted 28  abstract 0  silent 189
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 54
poison check: PASS — 3525 seeded wrong edges: 3282 refused, 243 unjudged (oracle silent there), 0 falsely confirmed
```

**Direction of fix (signed):**
- **Precision-against-oracle:** 96.1% → 99.1% (+3.0 points). Contradicted 138 → 28 (−110); confirmed 3,439 → 3,254 (−185).
- **Semantic tier:** 3,331/3,395 → 3,254/3,282. Contradicted 64 → 28 (−36, the collapse); confirmed −77.
- **Syntactic tier:** 108/182 → none judged (−108 confirmed, −74 contradicted). The 54 left are silent, in files outside the 52 units, which keep their fallback.
- **Recall:** 15.5% → 14.5% (7,861 → 7,319 pairs, −542). A withheld or abstained edge can cover several of the key's pairs on its line, so recall fell further than the edge count projected (15.1%).
- **The 28 left** are 28 of the triaged 138, row for row: 10 the provider's (C-153) and 18 the oracle's (H-28–H-31). Triage ratio `hobbes-wrong 10 : oracle-wrong 18 : untriaged 0`.
- **The new records:** `scip-decode` counts 658 overload sites (the probe logged 634, the sites whose lines differ); `cpp-fallback` counts 527 guesses withheld.

**Lane agreement at the ingest:** 2,239 sites compared and 316 disagreements, none of which draws a graded edge. Lane B's answer wins the join, and in a compiled file lane A draws nothing. Read against the key:
- 66: lane B's target is the key's.
- 24: lane B names the `using testing::Contains;` declaration, a grain below the floor.
- 205: neither lane's target is in the key. In 189 of them lane B's answer is a definition lane A holds no symbol for (C-145; `runtime` in `core.h` alone is 149).
- 21: the key has no target at the line.

`hobbes lanes` therefore exits 1 on fmt, as it did before this patch. This repo's own graph has 0 C++ disagreements.

## Regrade, Hobbes 0.2.28-beta (O10's defect fixes — H-28, H-29, H-30)

**The key itself was regenerated this time, not only the grade.** H-28 and H-29 change what a shard holds, so `oracle c-clang --lang cpp` re-ran contained (305 s) over this cell's own compile database, and the **standing** Hobbes export (`fmt-regrade/hobbes.json`) was graded against the new key. The Hobbes side is identical row for row — 3,525 edges, checked as a set and not as a count — so only the key and the matcher moved. Outputs in `~/.hobbes/bench/oracle-defect-drivers/regrade-out/fmt/`.

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 3a0661d7
oracle ran contained (ADR-092)
hobbes edges 3525: confirmed 3263  contradicted 11  abstract 0  silent 251 map[line-unresolved:13 no-targets:119 not-loaded:54 unreachable:65]
precision-against-oracle 99.7% (3263/3274)
recall 14.5% (7328/50524 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 6987; misses map[macro→function:1622 macro→method:1140 static→constructor:8205 static→function:10321 static→method:21908]
recall-collapsed 12.4% (3263/26281 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  tier semantic   confirmed 3263  contradicted 11  abstract 0  silent 197
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 54
poison check: PASS — 3525 seeded wrong edges: 2643 refused, 882 unjudged (oracle silent there), 0 falsely confirmed
```

**Direction of fix (signed):**
- **Precision-against-oracle:** 99.1% → 99.7% (+0.6 points). Contradicted 28 → 11 (−17); confirmed 3,254 → 3,263 (+9); silent 243 → 251 (+8).
- **Recall:** 14.5% → 14.5% (7,319 → 7,328 pairs, +9). Collapsed 13.1% → 12.4%.
- **Every movement, row for row — there are exactly three kinds and no others:** contradicted → `silent`/`line-unresolved` **13**; `silent`/`unreachable` → confirmed **5**; contradicted → confirmed **4**. No confirmed edge was lost.
- **The 4 newly confirmed** are H-28's one member call (`gmock-gtest-all.cc:4635`, now keyed at its member token) and 3 of H-29's five (`gmock.h:6653`, 6667, 6673, now keyed to `VariantMatcher`'s own member). The other two H-29 rows (`std.h:714`, 726) went silent instead: their line also carries a call the key left unresolved.
- **The 5 that went `unreachable` → confirmed** are all in `gmock-gtest-all.cc` (1083, 1907, 2012, 4193, 12349): H-28's repositioning moved the key's member-call site onto the line Hobbes had drawn all along, which the prediction did not anticipate.
- **The 11 left** are H-31's 7 — the six `os.cc` rows and `compile-test.cc:127`, open by design — and **4 of C-153's 10**.

**What the 99.7% does not say, stated because it runs against our own interest.** Six of C-153's ten rows are now `silent`/`line-unresolved`: scip-clang names the wrong declaration in a template, Hobbes' edge there is genuinely **wrong**, and the matcher now declines to judge it because the same line carries a dependent call the key left unresolved. They are unjudged, not fixed. Judging the rows the standing grade judged, precision is **99.48%** (3,263/3,280); the 99.7% above is the number the lane's own rule produces. C-153 therefore now reads **4 judged : 6 unjudged**, not 10 judged. Triage ratio (A-8) on the 11: `hobbes-wrong 4 : oracle-wrong 7 : untriaged 0`.

**The poison instrument covers less than it did.** On a line the key left unresolved the matcher declines to judge, so it also declines to *refuse* a poisoned edge there: 3,282 refused / 243 unjudged → **2,643 / 882**. Still PASS, still 0 falsely confirmed, which is how §10.8's P43 is worded — but the instrument now exercises 639 fewer sites on this cell.

**Pre-registration (`oracle-grading.md` §10.8):** P36 **missed** (11 left, not 17), P37 **missed** (+9 confirmed, not +6), P38 **missed** (99.7%, above the 99.4–99.6% range), P40 met, P43 met. All three misses are in the flattering direction and are recorded as misses; §10.8 carries the causes.

