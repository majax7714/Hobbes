# Oracle cell — junegunn/fzf, module `.`, 2026-08-27

Repo: https://github.com/junegunn/fzf (MIT), clone at `~/.hobbes/bench/oracle/repos/fzf`, commit f7ae439ff5b2f8298716d39783a546db793b0625 (shallow). Picked because it is a well-known Go CLI tool (not a library) of ~33k lines in one `go.mod` with four small deps (`x/sys`, `x/term`, `go-shellwords`, `fastwalk`), from an author not yet graded, with a real `main` plus four package test mains as RTA roots, and it builds with plain `go build ./...` on this box. Ingest SHA f7ae439f (the ingest reports a dirty tree: `hobbes ingest` appends `.hobbes/` to the clone's `.gitignore`, ADR-012 — nothing else changed). Hobbes at 53fabff, edges from `.hobbes/derived/graph.json` (lane B on, `HOBBES_SCIP=1`; the ingest warns `scip-decode extraction degraded`: 6 symbols defined in more than one file — package namespaces of `src/algo`, `src/tui` and an `init()` — references left unattributed, C-28). Oracle go-rta (x/tools v0.49.0), default build tags, roots = 5 (`fzf` main, `src.test`, `src/algo.test`, `src/tui.test`, `src/util.test`).

Command: `bench/oracle/run-cell.sh ~/.hobbes/bench/oracle/repos/fzf . ~/.hobbes/bench/oracle/fzf-go --lang go`. Runtime 33 s (ingest + RTA + grade, one language). Outputs in `~/.hobbes/bench/oracle/fzf-go/` (`hobbes.json`, `oracle.json`, `report.json`, `report.txt`; full log `fzf-go.log`).

## Numbers (report.txt, verbatim; the 87 contradicted and 4167 missed rows are in report.txt / report.json)

```
cell .  oracle go-rta (reachability)  sha f7ae439f
hobbes edges 2973: confirmed 2832  contradicted 87  abstract 0  silent 54 map[not-loaded:51 unreachable:3]
precision-against-oracle 97.0% (2832/2919)
recall 40.8% (2868/7035 in-repo oracle pairs) at 5 roots; external oracle pairs 8094; misses map[func-value→closure:2457 func-value→named:81 interface→named:417 static→closure:1212]
  recall[func-value→closure]   0.0% (0/2457)  misses 2457 = 59.0% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[func-value→named  ]   0.0% (0/81)  misses 81 = 1.9% of all misses  (inflated: reachability oracle over-approximates function values; upper bound)
  recall[interface→named   ]   0.0% (0/417)  misses 417 = 10.0% of all misses
  recall[static→closure    ]   0.0% (0/1212)  misses 1212 = 29.1% of all misses
  recall[static→named      ] 100.0% (2868/2868)  misses 0 = 0.0% of all misses
  tier semantic   confirmed 2832  contradicted 0  abstract 0  silent 3
  tier syntactic  confirmed 0  contradicted 87  abstract 0  silent 51
  line-grain tolerance used on 700 edge(s) (several oracle sites on one line)
poison check: PASS — 2973 seeded wrong edges: 2919 refused, 54 unjudged (oracle silent there), 0 falsely confirmed
cell . of /home/mmarrujo/.hobbes/bench/oracle/repos/fzf: 33s
```

| bucket | count |
|---|---|
| hobbes edges | 2973 |
| confirmed | 2832 |
| contradicted | 87 |
| silent | 54 (not-loaded 51, unreachable 3) |
| precision-against-oracle | 97.0% (2832/2919) |
| in-repo oracle pairs | 7035 |
| recall at 5 roots | 40.8% (2868/7035) |
| external oracle pairs (not graded) | 8094 |
| tiers | semantic 2832 / 0 / 3; syntactic 0 / 87 / 51; no dynamic edges in this cell |

## Contradicted (87 rows, all syntactic tier)

Every contradicted edge is a syntactic-fallback edge, and every one has the same shape: a call to a name that is bound to a **local `func` literal shadowing a package-level function of the same name**. Hobbes' fallback resolved the name to the package-level function in another file; the oracle resolves it to the local closure.

- 81 sites (`src/ansi_test.go` 43, `src/result_test.go` 38) call a local `assert := func(...)` (declared at `ansi_test.go:191 TestExtractColor$1`, `ansi_test.go:363 TestAnsiCodeStringConversion$1`, `result_test.go:137 TestColorOffset$1`); Hobbes emits `src/merger_test.go:12 (src/merger_test.assert)`, the package-level test helper. 26 of these rows also list `src/tui/tui.go:398 NewColorPair` and 2 `tui.go:456 ColorPair.WithAttr` as oracle targets on the same line (line-grain tolerance).
- 4 sites (`src/tui/tui_test.go:56–59`) call a local `assert` (`tui_test.go:46 TestHexToColor$1`); Hobbes emits `src/tui/tcell_test.go:13 (src/tui/tcell_test.assert)`, a helper in a file behind `//go:build tcell || windows`.
- 2 sites (`src/terminal.go:5556`, `:5562`) call a local `atoi := func(s string) int` (`terminal.go:5541 evaluateScrollOffset$1`); Hobbes emits `src/options.go:824 (src/options.atoi)`.

## Misses by class (4167 rows)

- **func-value→closure, 2457 (59.0%; upper bound, oracle over-approximates).** Calls through a function-typed value to every reachable closure of matching signature. By site file: `src/terminal.go` 1400, `src/core.go` 327, `src/reader.go` 186, `src/options.go` 174, `src/chunklist.go` 63, `src/util/atexit.go` 58, `src/tui/light.go` 58, `src/terminal_test.go` 54. Six sites carry 58 each — the `func()` slots with 58 candidate closures: `util/atexit.go:25 RunAtExitFuncs`, `tui/light.go:515 (*LightRenderer).CancelGetChar`, `terminal.go:7078 Loop$17`, `terminal.go:6768 Loop$8`, `terminal.go:6337 Loop`, `terminal.go:5863 executeCommand`. The top callee is `terminal.go:6820 Loop$10` (87), then a run of `Loop$17$n` closures and `terminal_test.go:46/:52 TestReplacePlaceholder$1/$2` at 26 each.
- **static→closure, 1212 (29.1%).** Direct calls of a locally bound `func` literal. By site file: `src/options.go` 384, `src/tui/light_test.go` 276, `src/terminal.go` 134, `src/tui/tui.go` 95, `src/options_test.go` 73, `src/result_test.go` 58, `src/ansi_test.go` 43, `src/terminal_test.go` 26. Top callees: `tui/light_test.go:48 TestLightRenderer$4` (217), `options.go:1023 parseKeyChords$1` (117), `options.go:1753 parseActionList$1` (104), `options.go:2571 parseOptions$4` (60), `terminal.go:854 defaultKeymap$1` (55), `options.go:1452 parseTheme$2` (47), `tui/tui.go:1286 InitTheme$2` (46), `tui/tui.go:1425 initPalette$1` (43). (The 87 contradicted `assert`/`atoi` sites above are the same call shape, seen from the precision side.)
- **interface→named, 417 (10.0%).** All 417 sites are in `src/terminal.go`, calling the `tui.Renderer` / `tui.Window` interfaces; RTA resolves them to the concrete implementations in `src/tui/light.go` (362), `src/tui/dummy.go` (52) and `src/tui/light_unix.go` (3). Top callees: `(*LightWindow).Height` (59), `.Width` (56), `.Top` (51), `.CPrint` (30), `.Left` (21), `(*LightRenderer).NewWindow` (18), `(*FullscreenRenderer).NewWindow` (18), `(*LightWindow).Print` (17). Sites concentrate in `Terminal.Loop$17` and `resizeWindows`.
- **func-value→named, 81 (1.9%; upper bound, oracle over-approximates).** `src/proxy.go:48 (*labelUpdater).update` (26, reached from `terminal.go` / `core.go` function-typed fields); the six `algo.*Match*` functions (7 each, 42 — from `pattern.go:346 matchChunk`, `pattern.go:489 iter`, and the `algo` tests' function tables); `algo/indexbyte2_test.go:196 refLastIndexByteTwo` and the `runeprefilter_test.go` reference functions for the rest.
- **static→named, 0 misses** (2868/2868).

**Poison check:** PASS — 2973 seeded wrong edges: 2919 refused, 54 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (which side would need to change; no proposals):** `contradicted` (87, syntactic fallback binding a shadowed name to a package-level function instead of the enclosing local closure) — Hobbes. `interface→named` — Hobbes (the oracle's targets are concrete implementations the compiler knows; Hobbes emits nothing at these sites). `static→closure` — Hobbes (a direct call to a func literal is statically resolvable; Hobbes has no callee). `func-value→named` and `func-value→closure` — the oracle first (the reachability oracle over-approximates function values, so the count is an upper bound), with any residue Hobbes. Silent 54 — nothing to fix on either side (51 `not-loaded` are syntactic edges in build-tagged files — `tcell.go`, `tcell_test.go`, `*_windows.go`, `options_pprof*.go`, `runeindex_others.go` — outside the default-tag build; 3 `unreachable`).

**Not graded:** the 8094 external oracle pairs (stdlib / dependency callees, by design); the 51 `not-loaded` silent edges (files excluded by default build tags) and 3 `unreachable` silent edges (RTA did not reach them from the 5 roots). No repo was abandoned.

## Regrade 2026-08-28 (triage; ADR-090's scope veto, the Go shape)

`gosource._call_fallback` now refuses a bare name that a local binding spans (a test's `assert := func(..)`, a local `atoi`). Re-ingested **contained** (ADR-092) and regraded, 36 s:

```
hobbes edges 2881: confirmed 2832  contradicted 0  abstract 0  silent 49 map[not-loaded:46 unreachable:3]
precision-against-oracle 100.0% (2832/2832)
recall 40.8% (2868/7035 in-repo oracle pairs) at 5 roots
  tier semantic   confirmed 2832  contradicted 0  abstract 0  silent 3
  tier syntactic  confirmed 0  contradicted 0  abstract 0  silent 46
poison check: PASS — 2881 seeded wrong edges: 2832 refused, 49 unjudged, 0 falsely confirmed
```

**Triage ratio (A-8):** 87 contradicted → `oracle-wrong 0 : hobbes-wrong 87 : untriaged 0`. **Direction of fix:** Hobbes; every confirmed edge kept (2,832 = 2,832), recall unchanged, the 87 wrong edges and 5 shadowed silent ones gone (2,973 → 2,881). Misses unchanged: C-58's closure and interface classes.
