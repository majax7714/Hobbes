# Oracle cell — codegraphcontext 0.6.13 on express, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@4` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/expressjs/express, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/expressjs__express` — `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-express/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 9a34acf03cb818ff3f8bc40e44176e277a25cbb9 — **the same commit and the same answer key as the Hobbes cell [express-js-2026-09-19.md](express-js-2026-09-19.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/regrade/h34/express/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9a34acf0`), regenerated by the command that cell names.

Index command: `unshare -rn codegraphcontext --db kuzudb --db-path <db> index <copy>  (0.6.13; CGC_RUNTIME_DB_TYPE=kuzudb)` — exit 0, wall 16 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked. 0.6.13 is the version the other 44 foreign cells ran, and PyPI's latest on 2026-09-26. The tool's summary lists the JavaScript files it parsed: .js 141. It left a `.cgcignore` at the copy's root (its default ignore patterns), nothing else. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. The tool's own log carries 0 error/warning line(s). Conversion: 385 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}; 1813 rows in the tool's separate HEURISTIC_CALLS table, not graded (its own label for name-only guesses). Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-express/edges.json express/oracle.json codegraphcontext-express --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-express` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9a34acf0
hobbes edges 385: confirmed 318  contradicted 8  abstract 0  silent 59 map[line-unresolved:1 no-targets:58]
precision-against-oracle 97.5% (318/326)
precision-strict 97.2% (318/327): the 1 line-unresolved rows counted as contradicted — a lower bound under the lower bound (ADR-124)
recall 20.9% (318/1520 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 494; misses map[func-value→local-binding:25 func-value→parameter:352 func-value→variable:85 static→anonymous-function:31 static→function:709]
recall-collapsed 20.9% (318/1520 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/25)  misses 25 = 2.1% of all misses
  recall[func-value→parameter]   0.0% (0/352)  misses 352 = 29.3% of all misses
  recall[func-value→variable]   0.0% (0/85)  misses 85 = 7.1% of all misses
  recall[static→anonymous-function]  32.6% (15/46)  misses 31 = 2.6% of all misses
  recall[static→closure    ] 100.0% (27/27)  misses 0 = 0.0% of all misses
  recall[static→function   ]  28.0% (276/985)  misses 709 = 59.0% of all misses
  tier codegraphcontext:EXTRACTED confirmed 318  contradicted 8  abstract 0  silent 59
  line-grain tolerance used on 124 edge(s) (several oracle sites on one line)
poison check: PASS — 385 seeded wrong edges: 233 refused, 152 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 385 |
| confirmed | 318 |
| contradicted | 8 |
| abstract | 0 |
| silent | 59 {"line-unresolved": 1, "no-targets": 58} |
| precision-against-oracle (lower bound) | **97.5%** (318/326) |
| precision strict (ADR-124: the 1 line-unresolved rows counted as contradicted) | **97.2%** (318/327) |
| recall | 20.9% (318/1,520) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 318 / contradicted 8 / abstract 0 / silent 59.

## Contradicted (8 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 7; same file as an oracle target, other line 1. By label: `codegraphcontext:EXTRACTED` 8.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `examples/error/index.js:22` → `examples/error/index.js:20` [codegraphcontext:EXTRACTED] — oracle: Console.error @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.dom.d.ts:37885
- `lib/response.js:1029` → `lib/response.js:1025` [codegraphcontext:EXTRACTED] — oracle: JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1171, JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1179
- `lib/response.js:1030` → `lib/response.js:1025` [codegraphcontext:EXTRACTED] — oracle: JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1171, JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1179
- `lib/response.js:481` → `lib/view.js:169` [codegraphcontext:EXTRACTED] — oracle: resolve @ lib/response.js:33
- `lib/response.js:68` → `lib/response.js:1025` [codegraphcontext:EXTRACTED] — oracle: TypeError @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1142, JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1171, JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1179
- `lib/response.js:72` → `lib/response.js:1025` [codegraphcontext:EXTRACTED] — oracle: RangeError @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1109, JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1171, JSON.stringify @ /home/mmarrujo/hobbes_public/bench/oracle/ts/node_modules/typescript/lib/lib.es5.d.ts:1179

**Triage ratio (A-8):** all 8 read by hand in this cell: `oracle-grain 0 : tool-wrong 8 : converter-defect 0` (below). Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** all 8 read against the source: **tool-wrong 8**, oracle-grain 0, converter-defect 0 — `JSON.stringify(…)` drawn to `lib/response.js`'s own `function stringify` (5, lines 68, 72, 757, 1029, 1030), `resolve(…)` (the module's `var resolve = path.resolve`) drawn to `View.prototype.resolve` (2, `lib/response.js:481`, `lib/view.js:114`), and `console.error(…)` drawn to the example's own `function error` (1). The one line-unresolved row is not read; the strict figure counts it against the tool.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 276 / 985 | 709 |
| `func-value→parameter` | 0 / 352 | 352 |
| `func-value→variable` | 0 / 85 | 85 |
| `static→anonymous-function` | 15 / 46 | 31 |
| `func-value→local-binding` | 0 / 25 | 25 |
| `static→closure` | 27 / 27 | 0 |

**Poison check:** PASS — 385 seeded wrong edges: 233 refused, 152 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

