# Oracle cell — repowise 0.49.0 on express, module `.`, 2026-09-26 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **repowise 0.49.0**, run as its README documents on this box (host-run, C-96), converter `repowise-adapter@5` (`bench/oracle/adapters/repowise/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/expressjs/express, run on a copy of Hobbes' clone `~/.hobbes/bench/js-cells/repos/expressjs__express` — `/home/mmarrujo/.hobbes/bench/comparative/repowise-express/src`, made by `cp -a`, the ingest's `.gitignore` line reverted and `.hobbes/` removed, so its tree is the commit's; the tool never ran in Hobbes' clone, whose status was the same before and after, commit 9a34acf03cb818ff3f8bc40e44176e277a25cbb9 — **the same commit and the same answer key as the Hobbes cell [express-js-2026-09-19.md](express-js-2026-09-19.md)**; the key is `/home/mmarrujo/.hobbes/bench/js-cells/regrade/h34/express/oracle.json` (`cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9a34acf0`), regenerated by the command that cell names.

Index command: `unshare -rn repowise init --no-prose -y --no-editor-setup .  (0.49.0, in the copy)` — exit 0, wall 6 s; no network possible (the index ran in its own network namespace with no interface up, `unshare -rn`), no repo build invoked; 0.49.0 is the version the other published repowise cells ran (PyPI's latest on 2026-09-26 is 0.53.0, not used). It wrote `.repowise/` into the copy, nothing else. Graded `--lang ts`, the export's extension set, which holds `.js`, `.jsx`, `.mjs` and `.cjs` — the flag the Hobbes cell's export ran with. **Converted at converter@5** (ADR-101's 2026-09-26 amendment): 57 of the tool's call edges have a `<file>::__module__` caller and are read as sites in that file; at @4 they were dropped as `unknown-caller` and this cell graded 2 edges (`report.at4.*` beside the cell keeps that grade; it is not a published figure). The tool's own log carries 0 error/warning line(s). Conversion: 358 (site, callee) pairs; dropped {"unknown-caller": 0, "unknown-callee": 0, "no-call-lines": 0}. Grain note from the adapter: declaration line = wiki_symbols.start_line advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored); `# define` with spaces is the same directive, and a declaration head split over lines advances to the line holding the name (converter@4; @3 graded both as stored); a `<file>::__module__` caller (a top-level call, no wiki_symbols row) is read as call sites in that file at the tool's call lines (converter@5; @4 dropped every such edge as unknown-caller).

Command: `bench/oracle/grade-foreign.sh repowise-express/edges.json express/oracle.json repowise-express --lang ts`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/repowise-express` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell .  oracle tsc 5.9.3 (harness; no tsconfig — the ingest's generated options) (resolution)  sha 9a34acf0
hobbes edges 358: confirmed 355  contradicted 1  abstract 0  silent 2 map[no-targets:2]
precision-against-oracle 99.7% (355/356)
recall 23.4% (355/1520 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 494; misses map[func-value→local-binding:25 func-value→parameter:352 func-value→variable:85 static→anonymous-function:46 static→closure:3 static→function:654]
recall-collapsed 23.4% (355/1520 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[func-value→local-binding]   0.0% (0/25)  misses 25 = 2.1% of all misses
  recall[func-value→parameter]   0.0% (0/352)  misses 352 = 30.2% of all misses
  recall[func-value→variable]   0.0% (0/85)  misses 85 = 7.3% of all misses
  recall[static→anonymous-function]   0.0% (0/46)  misses 46 = 3.9% of all misses
  recall[static→closure    ]  88.9% (24/27)  misses 3 = 0.3% of all misses
  recall[static→function   ]  33.6% (331/985)  misses 654 = 56.1% of all misses
  tier repowise:global_unique confirmed 0  contradicted 0  abstract 0  silent 2
  tier repowise:import_merged confirmed 4  contradicted 0  abstract 0  silent 0
  tier repowise:import_scoped confirmed 0  contradicted 1  abstract 0  silent 0
  tier repowise:module_alias confirmed 49  contradicted 0  abstract 0  silent 0
  tier repowise:same_file confirmed 302  contradicted 0  abstract 0  silent 0
  line-grain tolerance used on 152 edge(s) (several oracle sites on one line)
poison check: PASS — 358 seeded wrong edges: 214 refused, 144 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 0s
```

| bucket | count |
|---|---|
| graded edges | 358 |
| confirmed | 355 |
| contradicted | 1 |
| abstract | 0 |
| silent | 2 {"no-targets": 2} |
| precision-against-oracle (lower bound) | **99.7%** (355/356) |
| recall | 23.4% (355/1,520) over every resolved site |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `repowise:global_unique` confirmed 0 / contradicted 0 / abstract 0 / silent 2; `repowise:import_merged` confirmed 4 / contradicted 0 / abstract 0 / silent 0; `repowise:import_scoped` confirmed 0 / contradicted 1 / abstract 0 / silent 0; `repowise:module_alias` confirmed 49 / contradicted 0 / abstract 0 / silent 0; `repowise:same_file` confirmed 302 / contradicted 0 / abstract 0 / silent 0.

## Contradicted (1 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 1. By label: `repowise:import_scoped` 1.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `lib/application.js:552` → `lib/view.js:52` [repowise:import_scoped] — oracle: View @ lib/application.js:550

**Triage ratio (A-8):** the one row read by hand: `oracle-grain 0 : tool-wrong 1 : converter-defect 0` (below). Precision-against-oracle stays a lower bound (a contradiction is very strong evidence, not proof; C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-26):** the one row, read against the source: **tool-wrong** — `view = new View(name, {…})` at `lib/application.js:552`, where `View` is the local `var View = this.get('view')` two lines above (the key names that binding), drawn to `lib/view.js`'s `function View`. It is a `__module__`-caller row (converter@5).

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 331 / 985 | 654 |
| `func-value→parameter` | 0 / 352 | 352 |
| `func-value→variable` | 0 / 85 | 85 |
| `static→anonymous-function` | 0 / 46 | 46 |
| `func-value→local-binding` | 0 / 25 | 25 |
| `static→closure` | 24 / 27 | 3 |

**Poison check:** PASS — 358 seeded wrong edges: 214 refused, 144 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix:** first grade of this tool on this cell — nothing to sign. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

