# Oracle cell — codegraphcontext 0.6.13 on sqlite-vector, module `.`, 2026-09-14 (foreign cell, ADR-101)

**A third-party graph graded by the oracle lane.** Tool: **codegraphcontext 0.6.13**, run as its README documents on this box (host-run, C-96), converter `codegraphcontext-adapter@3` (`bench/oracle/adapters/codegraphcontext/adapter.py`, its hand-read fixture in the Go test). Repo: https://github.com/sqliteai/sqlite-vector, clone `/home/mmarrujo/.hobbes/bench/oracle/repos/sqlite-vector`, commit 0c2223ada9dce1fa33248c8835a15f51d9a0f655 — **the same commit and the same answer key as the Hobbes cell [sqlite-vector-c](sqlite-vector-c)**; the key is `/home/mmarrujo/.hobbes/bench/oracle/sqlite-vector-c/oracle.json` (`cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad`), regenerated by the command that cell names.

Index command: `codegraphcontext --db kuzudb --db-path <db> index <clone>  (0.6.13)` — exit 0, wall 136 s; no network observed, no repo build invoked. The tool's own log carries 0 error/warning line(s). Conversion: 23011 (site, callee) pairs; dropped {"outside-repo": 0, "no-line": 0, "dependency-callee": 0}. Grain note from the adapter: declaration line = the tool's node start line, advanced past leading annotation/decorator lines to the identifier's line (converter@2; @1 graded the annotation line); a callee whose declared line begins with #define is kind macro, which the lane excludes (converter@3; @2 graded it as stored).

Command: `bench/oracle/grade-foreign.sh codegraphcontext-sqlite-vector/edges.json sqlite-vector-c/oracle.json codegraphcontext-sqlite-vector --lang c`. Outputs in `/home/mmarrujo/.hobbes/bench/comparative/codegraphcontext-sqlite-vector` (`hobbes.json` is the converted graph, `raw.json` the tool's rows as stored, `edges.json` the minimal shape).

## Numbers (report.txt, head, verbatim)

```
cell   oracle Ubuntu clang version 18.1.3 (1ubuntu1) -ast-dump=json (resolution)  sha 0c2223ad
hobbes edges 22720: confirmed 851  contradicted 12  abstract 0  silent 21857 map[no-targets:343 not-loaded:21363 unreachable:151]
precision-against-oracle 98.6% (851/863)
recall 100.0% (1091/1091 in-repo oracle pairs) over every resolved site in the cell (resolution oracle: no roots); external oracle pairs 1721; misses map[]
recall-collapsed 100.0% (851/851 pairs at site-line × target-file × target-name grain: a symbol's overload signatures fold, and so do repeats of one callee on one line; the per-signature line above is the standing grade)
  recall[static→function   ] 100.0% (1091/1091)  misses 0 = 0.0% of all misses
  tier codegraphcontext:EXTRACTED confirmed 851  contradicted 12  abstract 0  silent 21857
  line-grain tolerance used on 357 edge(s) (several oracle sites on one line)
poison check: PASS — 22720 seeded wrong edges: 863 refused, 21857 unjudged (oracle silent there), 0 falsely confirmed
foreign cell . (edges.json): 1s
```

| bucket | count |
|---|---|
| graded edges | 22,720 |
| confirmed | 851 |
| contradicted | 12 |
| abstract | 0 |
| silent | 21,857 {"no-targets": 343, "not-loaded": 21363, "unreachable": 151} |
| precision-against-oracle (lower bound) | **98.6%** (851/863) |
| recall | 100.0% (1,091/1,091) at 1 roots |

**By the tool's own label** (the edge's tier is the tool's confidence label, C-95): `codegraphcontext:EXTRACTED` confirmed 851 / contradicted 12 / abstract 0 / silent 21857.

## Contradicted (12 rows; all in report.json)

Mechanical shape (no reading): another file than every oracle target 11; same file as an oracle target, other line 1. By label: `codegraphcontext:EXTRACTED` 12.

Sample rows (site → the tool's callee; the oracle's targets at that site):

- `src/sqlite-vector.c:1957` → `libs/sqlite3.c:92844` [codegraphcontext:EXTRACTED] — oracle: context_result_error @ src/sqlite-vector.c:306
- `src/sqlite-vector.c:2180` → `libs/sqlite3.c:185254` [codegraphcontext:EXTRACTED] — oracle: context_result_error @ src/sqlite-vector.c:306
- `src/sqlite-vector.c:2221` → `libs/sqlite3.c:185254` [codegraphcontext:EXTRACTED] — oracle: context_result_error @ src/sqlite-vector.c:306
- `src/sqlite-vector.c:2233` → `libs/sqlite3.c:92400` [codegraphcontext:EXTRACTED] — oracle: vector_context_lookup @ src/sqlite-vector.c:1747
- `src/sqlite-vector.c:2671` → `libs/sqlite3.c:91678` [codegraphcontext:EXTRACTED] — oracle: vector_from_json @ src/sqlite-vector.c:2372
- `src/sqlite-vector.c:293` → `libs/sqlite3.c:185254` [codegraphcontext:EXTRACTED] — oracle: printf @ :0, printf @ :0

**Triage ratio (A-8):** every row read (the paragraph below): `tool-wrong (a name drawn into the vendored amalgamation the build does not compile with the extension) 9 : tool-wrong (a dead-`#if`-arm shim, the C-138 shape) 3 : oracle-grain 0 : converter-defect 0` — the number above is a lower bound (C-62), and a converter defect would count against the tool (C-94).

**Triage (hand-read, 2026-09-14):** Read 2026-09-14, every row, at converter@2 and again at @3 (the tool stored no edge to a `#define` line, so nothing moved): 9 of the 12 contradicted rows draw an `sqlite3_*` API name (`sqlite3_errmsg`, `sqlite3_column_int64`, `sqlite3_user_data`, `sqlite3_value_text`) to its definition in the vendored amalgamation `libs/sqlite3.c`, which the extension's build never compiles with it: in the compiled unit the name is `sqlite3ext.h`'s object-like macro over the API's function-pointer table (the key's 360 dynamic sites, no targets), so the edge is a name match into a file outside the build — tool-wrong at the compiled grain. The other 3 draw `strcasestr` to the shim at `src/sqlite-vector.c:31`, in a `#if` arm the build does not take — the same three rows Hobbes' syntactic tier drew before ADR-111's veto (C-131, C-138). Recall is 1,091/1,091: every static call `src/` spells to a compiled definition, as the Hobbes cell's. The 21,857 silent edges are the amalgamation's own calls, not-loaded by the key exactly as Hobbes' 18,894 are.

## Misses by class

| class | hits / pairs | misses |
|---|---|---|
| `static→function` | 1,091 / 1,091 | 0 |

**Poison check:** PASS — 22,720 seeded wrong edges: 863 refused, 21,857 unjudged (oracle silent there), 0 falsely confirmed.

**Direction of fix (codegraphcontext-adapter@2 → codegraphcontext-adapter@3, 2026-09-14):** nothing moved — the tool stored no edge to a `#define` line; `report.v1.*` beside the cell keeps the @2 grade. **What the record does not say:** anything about the tool beyond this run on this box at this version; the matcher's tolerances were tuned on Hobbes' output (C-95); the conversion is Hobbes' and a misread would be Hobbes' defect (C-94).

