# Oracle lane — the defect record

**What this is.** The oracle grades Hobbes; nothing grades the oracle
except the fixtures and triage. So every defect found in the harness or
an oracle extractor is logged here — what it looked like, what it was,
how it was fixed, and **what it would have cost had it gone unnoticed**
(Max, 2026-08-25: "always honest"). A harness that only records
Hobbes' errors is a harness that flatters itself. Updated in the same
commit as the fix; a defect found but not yet fixed stays here as open.
Reviewed in full on 2026-08-27 — `oracle-defect-review.md` holds the
verdict, the action register (A-1..A-9), the defect-handling method and
the **seen tally** (roots RC-1..RC-7, maintained in the same commit as
any entry that changes it); a new entry here is assigned to a root there.

| # | Date / cell | Looked like | Was | Fix | Cost if unnoticed |
|---|---|---|---|---|---|
| H-1 | 2026-08-25, O1 `minigo` | No test function reachable; the `lib` cell of `twomod` had **no sites at all** | RTA rooted at `main` only; the synthesized test main's table of `Test*` functions is address-taken in its `init`, which `main` never calls | Root at `init` as well as `main` for every main package (as `x/tools`' own `callgraph` does) | Every library module would have graded empty — "no roots" reads as "nothing to grade", and O4's 25 dagger modules are mostly libraries |
| H-2 | 2026-08-25, O1 `twomod` | The `app` cell reported a site in `lib/` | Sites were scoped to the repo, not the cell's module; a replaced sibling module is inside the loaded program | Sites and loaded files limited to the module directory; targets may land anywhere | Cross-cell double counting and spurious `not-loaded` silences on every multi-module repo |
| H-3 | 2026-08-25, O2 `hobbes go/` | 2 contradictions: `sortedKeys(...)` at `knowledge.go:786/819` graded as a call to `sort.Strings` | The synthetic-function unwinder treated a **generic instantiation** as a wrapper and followed into its body, attributing the callee's callees to the caller's site | Instantiations (`Origin() != nil`) are source functions; fold to origin, never unwind | Every call to a generic function would have contradicted Hobbes' correct edge — a false "hobbes-wrong" verdict that scales with how generic the codebase is (dagger's `sdk/go` is) |
| H-4 | 2026-08-25, O2 | Recall 87.5% with 138 of 183 misses at 10 sites, all `defer cancel()`-shaped | Not a bug but an unreported property: a reachability oracle resolves a `func()` value to **every** reachable `func()`, so the recall denominator was an upper bound nobody stated | Recall split by class; `func-value→*` under RTA printed as *inflated*; the static number is the tight one | A recall number quoted without its inflation would have understated Hobbes by up to ~10 points on this cell and been compared across cells (rule 2 violated silently) |
| H-5 | 2026-08-25, O3 kbet | **119 contradictions (precision 81%)**, every one a `useAuthStore()`-shaped call graded against zustand's `react.d.mts:19` | `tsc`'s declaration for a call through a `const` of callable type is the **anonymous call signature** in the type, while Hobbes and scip bind the callee to the variable. The oracle's grain, not Hobbes' error | The binding rule (D-O4): behind an anonymous signature the callee's identity is its binding; the oracle lists the binding and not the signature | 119 false "hobbes-wrong" verdicts — every React hook, store, and callback call in every TS zone; the lane would have reported TS precision at ~80% and been believed |
| H-6 | 2026-08-25, O3 kbet | A miss whose target name was a **file path** (`autoUpdate.test.ts:43`) | A dynamic `import()` resolved to a source file, which the oracle listed as a call target | Source-file declarations are not targets | One spurious miss per dynamic import; small, but a class of pair that is not a call |
| H-7 | 2026-08-25, O3 kbet | 826 misses classed `closure`, 84% of all | `const [x, setX] = useState()` bindings were classed as closures because they are declared inside a function; the binding is a *value*, not a function literal | `local-binding` split from `closure`; TS sites given a mode from the binding's shape so Go and TS classes read alike | The miss record's headline ("closures hurt most") would have been wrong about *what* is missing — state setters, which no reader wants as edges, would have been counted as lost architecture |
| H-8 | 2026-08-25, O3 kbet | Caller names in the triage rows were whole arrow-function bodies | Cosmetic: anonymous callers printed their source text | `<anonymous>` | None to the numbers; triage rows unreadable |

| H-9 | 2026-08-25, O4 dagger root | The root cell **OOM-killed** at 20.5 GB (with tests) and 20.7 GB (`--no-tests`, GOGC=50, GOMEMLIMIT=22GiB) on a 30 GB box; `oracle.err` empty, only the kernel log says why | Whole-program SSA of the root's dependency closure with `InstantiateGenerics`, which RTA requires; not a leak, a size | `--no-tests` and `--packages` so a too-large module is graded by stated subtree; the driver prints the kernel's verdict. **Open in part:** the root *as one program* is not gradeable on this box, and the cell record says so | A silently absent root cell would have read as "dagger graded" on the strength of its 19 small modules; the 7,322 C-33 edges (P9) live only in the root |
| H-10 | 2026-08-25, O4 `e2e/helm` | Recall 72/72 confirmed yet **~250 misses**, all in `e2e/helm/dagger/*` | `export --exclude` dropped the nested module's sites from the Hobbes side, but the oracle still scoped by module *prefix* and kept them — the two sides of one cell disagreed on what the cell was | `go-rta --exclude`, the same list on both sides; `run-cell.sh` passes it through; test on `twomod` | 250 false misses in one small cell; on the root subtrees it would have been every nested module's sites counted against Hobbes twice |
| H-11 | 2026-08-25, O4 | A cell with zero misses crashed the aggregation (`misses: null`) | The report serialises an empty slice as `null` | Aggregation reads it as empty; the report should emit `[]` (cosmetic, left) | None to the numbers |
| H-12 | 2026-08-25, O6 `pipeline` | **186 `observed→function` misses** whose names — `run_pass`, `extract_repo`, `ingest`, `plan_units`, `Unit`, `Extraction` — all live in `__init__.py` files, and 113 fewer Hobbes edges than the graph holds | The export mapped only `module` nodes to files; a Python package is a **`package` node** whose path is its `__init__.py`, so every edge into a package's symbol was silently dropped from the Hobbes side and then charged as a miss on the oracle side | Package nodes are targets; test on a synthetic graph | Recall-against-executed reported at 83.4% instead of 88%, and the miss record's second-largest class would have been a phantom — 186 "missing" calls Hobbes draws |
| H-13 | 2026-08-25, O7 `minirust` | The integration test's `#[test]` attribute line **calling the test function** (`integration_works::{closure#0} → integration_works`), one spurious in-repo miss per test | The test harness's generated closure has a real call terminator whose span is the attribute; the oracle attributed compiler-written code to a source line | Sites in `AstPass`, attribute-macro and derive expansions are dropped and counted (`excluded.generated`) | One false miss per `#[test]`, `#[tokio::test]` and derived impl — on dagger's Rust that is every test in the SDK, read as Hobbes losing the call graph's test edges |
| H-14 | 2026-08-25, O6 `pipeline` | Coverage line `declarations started 2280/2048` — more than 100% | `PY_START` fires for class bodies and lambdas too; the denominator counted only `def`/`class` statements | Started declarations are filtered through the same `ast` index (functions only) | A coverage line nobody can read — and rule 2 says recall is only quotable next to it |
| H-16 | 2026-08-25, O7 dagger `sdk/rust` | **649 `static→closure` misses (90% of all)**, concentrated in `gen.rs` — read as "Hobbes loses every call in the generated SDK" | `.await` desugars to a poll loop whose resolved callee is the async fn's *body* (a coroutine, `connect::{closure#0}`); MIR carries it as a second call at the site of the authored call to `connect(...)`, which Hobbes draws and the oracle confirms | Sites in the `Await` desugaring are compiler-written: dropped and counted with the generated sites | Recall reported at 83.8% instead of 98.1%, and C-58 would have been sized on Rust at ten times its true weight — every async call in every Rust repo doubled into a miss |
| H-15 | 2026-08-25, O7 `rust_proj` | 4 misses at `criterion_group!` / `criterion_main!` lines classed `static→function`, as if the author's direct calls were lost | Calls in the body of a macro the repo does not define were classed like direct calls | Sites in a foreign bang-macro expansion carry mode `macro`; the miss class names it (`macro→function`) | The Rust miss record would have said "static calls missed" where the truth is "calls a dependency's macro makes" — a different constraint (C-58's closure/dispatch hole vs macro expansion), and the wrong one would have been filed |
| H-17 | 2026-08-27, cell loop (ajv `f177fe3`, cheerio `98c7d13`) | The TS oracle **hung** at 100% CPU with no output on both repos (killed at 600 s / 11m47s); `tsc --noEmit` passes on each in seconds and Hobbes ingested ajv cleanly (2,277 call edges) | `siteName()` in `bench/oracle/ts/tsc-oracle.mjs`: `while (isPropertyAccess(e) \|\| isElementAccess(e)) e = isPropertyAccess(e) ? e.name : e;` — on an element-access callee (`obj[key]()`, e.g. `$li[Symbol.iterator]()` at cheerio `traversing.spec.ts:925`, `this.logger[log](...)` at ajv `core.ts:770`) the else branch is `e = e`: no progress, infinite loop. The shape was named as a `tsc` unsoundness in `oracle-grading.md` §4 but never given a site rule (D-O4) and no fixture has one; kbet contained none, so the branch never ran. An **oracle** defect, not a repo defect — found by the loop's TS agent with an instrumented copy; tree untouched | **Fixed 2026-08-27, in the review's order (A-4):** fixture first — the three shapes in `minits/src/lookup.ts`, observed: Hobbes draws no edge for any (C-63); rule second — D-O4's element-access bullet (literal / `Symbol.x` key graded at the key's line; computed key silent as `computed-key`); loop last — `siteName` descends to the key through `descend()`, which throws on no progress (A-5, RR-1); a worker-thread watchdog prints the last position on stall (A-6). ajv regraded in 2 s: 1,543 edges, 99.8%, 1 computed-key site | Coverage, not correctness: no false verdict was issued, but every TS repo with one `obj[key]()` call is ungradeable, so the TS row of §3.8 would rest on kbet alone, silently |
| H-18 | 2026-08-28, triage of the 2026-08-27 loop (mux `db9d1d00`, cheerio `98c7d13`) | 3 (mux) + 44 (cheerio) semantic edges graded **contradicted**: Hobbes' target was a package-level `var` / a module `const` holding a function value (`RegexpCompileFunc = regexp.Compile`; `const parse = getParse(..)`; `_matcher = _getMatcher(..)`), the oracle's the function the value holds (RTA: `regexp.Compile` + the benchmark's closure; tsc: the signature declaration inside `getParse` / `_getMatcher`) | The matcher had one abstract shape — an interface method (`Site.Interface`) — and no rule for a binding that holds a function: two true answers at different grains (RC-3) read as a disagreement. `oracle-misses.md` already listed `func-value→variable` as "drawn when the variable is a graph symbol" — the precision side of the same class was never named | **Fixed 2026-08-28:** D-O4 bullet; the export carries `target_kind`; `grade` buckets a contradicted edge whose target kind is a value binding (`const`/`var`/`variable`/`let`) and whose site has oracle targets as **abstract** (`func-value`). Fixture: `funcvalue_test.go`. mux and cheerio regraded | 47 false contradictions across two languages; cheerio's precision-against-oracle read 97.9% for a cell with no wrong edge |
| H-19 | 2026-08-28, triage (click `36baa15f`) | 47 of click's 85 suspects were the *same* function at a different line: Hobbes' target the first `@t.overload` stub's `def`, the trace's the implementation's (`Context.invoke` 850 vs 857, `termui.prompt` 139 vs 168 …) | The tracer maps a code object to its declaration by `co_firstlineno` → `def` line; an overloaded function has several `def`s under one qualname and the graph keeps the symbol on the first. Grain, not truth (RC-3) | **Fixed 2026-08-28:** D-O4 bullet; `DeclIndex.file` anchors an implementation whose qualname has an `@overload` stub at the first stub's `def` line (`_is_overload`: `overload`, `typing.overload`, `t.overload`). Fixture: `TestOverloadStubsAnchorTheImplementation` (the tracer's index over a snippet). click regraded | 47 suspects that were not suspects; the suspect rate (5.0%) read as a triage queue five times its size |
| H-20 | 2026-08-29, O8 jsoup (`7860d088`) / spring-data-elasticsearch (`cc7bd2b7`) | **237 contradicted on jsoup (precision 98.7%), 634 on spring-data-elasticsearch (96.0%)** — nearly every one a test's `new Element(..)` / `attributes.put(..)` graded against an oracle target with **no position** (`:0`), i.e. "external" | The plugin keyed a declaration by `owner#name(erased params)` built from `TypeMirror.toString()`, which **carries type annotations**: jsoup's jspecify `@Nullable String` in the *source* compilation vs `java.lang.String` from the *class file* the test compilation resolves against. One declaration, two spellings, so the cross-shard key join missed and the declaration read external (RC-8) | **Fixed 2026-08-29:** parameter names are built from the element (`typeName` over `DECLARED`/`ARRAY`/`TYPEVAR`), never from `toString`. Unit test `TestMergeJoinsShardsAndComputesCHA` pins the cross-shard join; both cells regraded | Java would have shipped its first evidence row at 96–99% precision with **871 false "hobbes-wrong" verdicts** — every test→main constructor call in any annotated codebase, i.e. most modern Java |
| H-21 | 2026-08-29, O8 jsoup | After H-20, **102 contradicted** remained (99.5%), all `new T()` on types with no declared constructor; and a `super()` "call" appeared at every constructor's opening brace | Two faces of javac's synthetic code: it inserts `super();` at a constructor body's brace (a site no source line makes) and synthesises a **default constructor at the class line**. The first pass counted the former as a site; the fix then dropped the latter as a declaration too, which cost every `new T()` its target (RC-2, over-corrected) | **Fixed 2026-08-29:** the synthetic `super()` is skipped and counted (`excluded.synthetic`); the generated constructor is **kept** as a declaration, because the class line is exactly where Hobbes draws a call of a class (D-O4). jsoup 102 → 0 contradicted | 102 false verdicts on one cell, and — had only the first half landed — a systematic loss of every implicit-constructor pair from the recall denominator, flattering recall by ~1 point while hiding real misses |

**Pattern so far.** Six of the first eight are the oracle being *right at a
different grain* than Hobbes (H-3, H-5, H-6, H-7) or *silent in a way
that reads as a result* (H-1, H-4); phase 2 added the harness dropping
Hobbes' own edges and reading the hole as misses (H-12), and twice the
oracle attributing code nobody wrote to a source line (H-13, H-15). Neither shape produces a Hobbes
error — both produce a **false verdict against Hobbes**, and the only
thing that caught them was a fixture with hand-computable truth (H-1,
H-2, H-3 via `testdata/generic`) or triage of the contradicted rows
(H-5, H-6). That is the argument for never quoting a cell's number
before its triage is complete (design §8), restated as evidence.

**Open:** H-11 (cosmetic). O8's three defects (H-20, H-21) were all found by **triage of the contradicted rows on the first two real cells** — the pattern the tally already names: a new oracle's first foreign repo is where its identity and provenance rules get tested, and both defects were oracle-side, not Hobbes-side. H-9's larger half — dagger's root module as one program (and every subtree that has roots: integration, dagql, cmd — each 24–25 GB) **needs a bigger box**, ~32 GB+ free; flagged 2026-08-25 (Max) and parked until Hobbes gains compute. P8/P9 wait on it.
