# Harness session `S-20260920T220635Z-de0e`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.66-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — H-36: a comprehension's frame entry is not a call anyone wrote (oracle lane, py-trace) (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T220635Z-de0e/brief.md`, task sha256 `a92466d46e3e`)
- **Parent:** `89b7f58c9d04` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 51 of 80; result `success`; reported cost $2.7610 (the envelope's figure, on the subscription); wall 286.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×1
- **Policy:** 23 exec decision(s) — `allow`×23; records: stream opened→closed
- **Edits:** 18 edit(s) to 6 file(s); first at 1.8 min after launch; files: `bench/oracle/internal/grade/trace_test.go`, `bench/oracle/py/trace_oracle.py`, `bench/oracle/testdata/pygen/pyproject.toml`, `bench/oracle/testdata/pygen/src/pygen/__init__.py`, `bench/oracle/testdata/pygen/src/pygen/core.py`, `bench/oracle/testdata/pygen/tests/test_core.py`
- **Branch:** `hobbes/S-20260920T220635Z-de0e`, 1 commit(s); files: `bench/oracle/internal/grade/trace_test.go`, `bench/oracle/py/trace_oracle.py`, `bench/oracle/testdata/pygen/pyproject.toml`, `bench/oracle/testdata/pygen/src/pygen/__init__.py`, `bench/oracle/testdata/pygen/src/pygen/core.py`, `bench/oracle/testdata/pygen/tests/test_core.py`
- **Gate:** **clear** at `89b7f58c9d04` (gate v2, grounder v4, record `9a9c59cb4b5a`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 31, regressions 0; {'P2P': 25, 'skip': 6}; build {'P2P': 2}

**The doer's closing words:**

> Done.
> 
> **What I changed**
> 
> - `bench/oracle/py/trace_oracle.py` — `on_call` now drops a call whose callee code object is a comprehension's (`<genexpr>`/`<listcomp>`/`<setcomp>`/`<dictcomp>`, all four named so the rule isn't interpreter-dependent; 3.12 inlines the last three) and counts the *call event*. The count is carried in the per-run raw JSON, summed across `--runs`, written as `"excluded": {"generated": n}` (key omitted at 0), and named in the stderr summary. Conventions docstring gained a bullet in the file's voice; the inline comment names H-36 and RC-2.
> - **Site creation:** the check runs *before* the site is made, so a line whose only event was the comprehension gets no site. Reason, stated in the comment: an empty site would count toward the grader's `hobbes sites observed` coverage line *and* would fall into `traceRow`'s default branch — targets seen, none of them Hobbes' — bucketing a Hobbes edge at that line **suspect** instead of leaving it `line-not-called`, i.e. charging Hobbes for a call nobody wrote. Sites with other events are untouched (`hits`, `c_callees`, caller unchanged).
> - `bench/oracle/testdata/pygen/` — the four fixture files, with the hand truth in `core.py`'s docstring.
> - `bench/oracle/internal/grade/trace_test.go` — `TestComprehensionFramesAreExcluded`, run exactly as `TestMiniappTrace` (same skips, same venv interpreter, `Runs: 1`).
> 
> **What I ran**
> 
> - `cd bench/oracle && go test ./...` — all packages ok; `gofmt -l .` clean.
> - Both trace tests **skip** here ("containment unavailable: podman is not installed"), as expected.
> - Exercised the tracer directly with `/work/pipeline/.venv/bin/python` (3.12.13, pytest 9.1.1) on `testdata/pygen`: 5 tests pass, `excluded {"generated": 2}` at `--runs 1` and `4` at `--runs 2` (the sum holds). Targets: `core.py:21 → :16` and `:29 → :16` (`double`, from the genexpr body and the inlined listcomp), `:34 → :33` (the lambda, `kind lambda`, `closure true`), `:33 → :16` (the lambda's own body), `firsts`' line 25 present with only the external `tuple` and no in-repo target — and no comprehension target anywhere. Running `git show HEAD:…/trace_oracle.py` on the same fixture reproduces the defect (`total.<locals>.<genexpr>` and `firsts.<locals>.<genexpr>` keyed on their own lines), so the test fails before the change.
> - One correction to the brief's wording: `tuple` is resolved as an **external class target**, not a C callee, so `firsts`' site survives through that rather than through `c_callees`; `sum` is the C callee at `total`'s line. Comments say the accurate thing.
> - Checked miniapp: no comprehension or lambda anywhere in its `.py` files, so `TestMiniappTrace`'s seven pinned pairs and its coverage header are unaffected and its key carries no `excluded` field.
> 
> Committed as `943d581` on `hobbes/S-20260920T220635Z-de0e`. No docs, grader, export or `pipeline/` files touched.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: H-36's fix at the py-trace extractor, as briefed, 51 turns. The doer chose to
  make **no site** for a line whose only event was the comprehension's frame, and said
  why: an empty site would read as observed and bucket a Hobbes edge there `suspect` —
  charging Hobbes for a call nobody wrote. It corrected the brief (`tuple` is an external
  class target, not a C callee) and checked miniapp holds no comprehension. Nothing
  reworked. On the host, in the image: `TestMiniappTrace` and
  `TestComprehensionFramesAreExcluded` **pass** (not skip). click's key regenerated with
  the same recipe (`click-py-r3`, same sha, same suite exits): 4,595 → 4,561 in-repo
  pairs — exactly the 34 `<genexpr>` rows — 26,090 call events counted `excluded:
  generated`, confirmed 3,356 and suspect 18 unchanged, poison PASS. One egress refusal,
  Claude Code's own `llm` host. Merged `--no-ff`.
