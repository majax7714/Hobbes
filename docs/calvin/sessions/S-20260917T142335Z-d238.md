# Harness session `S-20260917T142335Z-d238`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.38-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make **one ingest of a repo at a time**: a second `hobbes ingest` of a repo whose ingest is already running is refused before it stages or writes anything, and the artifacts are written atomically. Th (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T142335Z-d238/brief.md`, task sha256 `8ac0dfeb577f`)
- **Parent:** `77752dcf2106` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 34 of 80; result `success`; reported cost $1.5738 (the envelope's figure, on the subscription); wall 251.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 15 exec decision(s) — `allow`×15; records: stream opened→closed
- **Edits:** 13 edit(s) to 7 file(s); first at 0.7 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/emit.py`, `pipeline/src/hobbes/extract/ingestlock.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_emit.py`, `pipeline/tests/test_ingestlock.py`
- **Branch:** `hobbes/S-20260917T142335Z-d238`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/emit.py`, `pipeline/src/hobbes/extract/ingestlock.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_emit.py`, `pipeline/tests/test_ingestlock.py`
- **Gate:** **clear** at `77752dcf2106` (gate v2, grounder v4, record `e147f0887fde`); unknown 0; map over 87 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1084, regressions 0; {'skip': 16, 'P2P': 1056, 'new-pass': 11, 'uncollected': 1}

**The doer's closing words:**

> Committed as `4f22143` on `hobbes/S-20260917T142335Z-d238`.
> 
> **What changed**
> 
> - **`pipeline/src/hobbes/extract/ingestlock.py`** (new) — `LOCK_NAME = ".ingest.lock"` in `<resolved root>/<emit.DERIVED_DIR>/`, `IngestBusy(RuntimeError)` as its own type (P10), and `hold(repo_root)`: `mkdir(parents=True, exist_ok=True)`, open `a+` (never truncated before the lock is held), `flock(LOCK_EX | LOCK_NB)`. Contention reads the holder's pid (`unknown` when unreadable) and raises `IngestBusy` naming the root, the pid and ADR-127; any other `OSError` prints the C-159 warning to stderr and yields unlocked; held writes `f"{os.getpid()}\n"`, yields, then unlocks and closes without deleting the file.
> - **`extract/__init__.py`** — `ingest()` takes the lock as its first act, around `ensure_hobbes_ignored`, `extract_repo` and `write_artifacts`; docstring names the refusal and ADR-127.
> - **`emit.py`** — `write_artifacts` writes each file to a sibling `NamedTemporaryFile` in `derived/` and `os.replace`s it over the target, unlinking the temporary if the write raises; bytes unchanged; docstring says it is atomic per file and why.
> - **`cli.py`** — `_cmd_ingest` adds `IngestBusy` to the `(StampError, PlanError, PackRefusal)` catch it prints as `hobbes ingest: <exc>` and exits 1 on. Nothing else moved.
> - **Tests** — `tests/test_ingestlock.py` (8): second hold refused naming root + pid, a symlinked path is the same lock, two repos don't conflict, the lock is released on exit, the file stays holding the holder's pid, `ENOLCK` runs the body with the C-159 warning; and under a subprocess holder, `ingest()` raises with that pid and writes no `graph.json`, and an existing `graph.json` is byte-unchanged. `test_emit.py` (+2): no `.tmp` left behind; a raising `json.dumps` leaves the previous bytes and no temporary. `test_cli.py` (+1): `cli.main(["ingest", …])` under a held lock returns 1 with `another hobbes ingest of` on stderr.
> 
> **What I ran** — `/work/pipeline/.venv/bin/python3 -m pytest tests/test_ingestlock.py tests/test_emit.py tests/test_cli.py -q` → **77 passed** (8 new in `test_ingestlock.py`, +2 in `test_emit.py`, +1 in `test_cli.py`). Full pipeline suite `-q` → **1,709 passed, 11 skipped** (1,720 collected: 1,709 before + 11 new; `HOBBES_SCIP` at its test default).
> 
> **Deviations** — none.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read in full against ADR-127 and merged no-ff as `eef46dc`; no deviations. Two fixes on top by the developer: the temporary is created 0600, so the artifact now keeps the mode a plain write gave (0666 less the umask; the new test is red on the merged code), and an unused `errno` import dropped. Host: full pytest 1,721 passed (the tracker's drift test red until this block), `lane_b` 7 passed, Go `./...` all ok.
