# Harness session `S-20260913T203100Z-e537`

*2026-09-13 · `hobbes dispatch` v1 · Hobbes 0.2.12-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Find C tests by their registrations (C-134). The design is ADR-108's amendment of 2026-09-13 ("C tests are found by their registrations"), at the end of `docs/adr/108-c-at-lane-a.md`. Read it first. I (brief `/home/mmarrujo/.hobbes/sessions/S-20260913T203100Z-e537/brief.md`, task sha256 `2b74134a1a12`)
- **Parent:** `27f98af54816` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 26 of 150; result `success`; reported cost $1.3996 (the envelope's figure, on the subscription); wall 554.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `llm`×1
- **Policy:** 5 exec decision(s) — `allow`×5
- **Edits:** 10 edit(s) to 2 file(s); first at 4.9 min after launch; files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/test_csource.py`
- **Branch:** `hobbes/S-20260913T203100Z-e537`, 1 commit(s); files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/test_csource.py`
- **Gate:** **clear** at `27f98af54816` (gate v2, grounder v3, record `0ed01030d9a7`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 181, regressions 0; {'skip': 4, 'P2P': 165, 'new-pass': 12}

**The doer's closing words:**

> All 79 tests in `test_csource.py` pass, and the full pipeline suite ran with **1482 passed, 3 failed, 9 skipped** (51.56s). I verified the 3 failures (`tests/test_ttt_units.py::TestGitUnits::*` and `test_units_from_git_never_take_a_doers_commit_or_a_session_record`) are pre-existing and unrelated to this change — they fail identically with git-log-based fixtures regardless of my edit (a sandbox git-environment issue, not something `csource.py`/`test_csource.py` touch).
> 
> **What I changed** (only the two files the gate allows):
> - `pipeline/src/hobbes/extract/csource.py`: `CFile` now records registrations (`RUN_TEST`, `cmocka_unit_test*`, `tcase_add_test*`), whether the file defines `main`, and counts of the unread `Test`/`TEST` bodies and `RUN_TEST_CASE` calls. `extract_c` resolves all registrations repo-wide (ranks 1 and 3 of the call fallback, ties abstain) after every file is parsed, assigns each file's tests per decision 3 (registered functions win outright per file; the `c-convention` naming rule only applies where none are registered), and appends the two new `"stage": "c-tests"` degradation records. Updated the module docstring's **Tests.** paragraph and `_is_test_file`'s docstring to match.
> - `pipeline/tests/test_csource.py`: added a `TestRegistrations` class covering Unity (same-file and split runner/test-file layout), CMocka, Check, ties/misses, the convention fallback, both degradation records, and reach — all built in `tmp_path`, per the constraint that `tests/fixtures/minic/` stays untouched.
> 
> Committed as `d284bcc` on `hobbes/S-20260913T203100Z-e537`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged as `4a904f6` (not squashed); released as 0.2.13-beta
- notes: Read the diff (2026-09-13). It follows the amendment's five decisions: registrations resolve by ranks 1 and 3 over a table of functions only, and each file's tests are its registered functions or else its convention tests. **On cJSON (lane A, the branch's code):** 39 tests became 199. cJSON's own `tests/` has all 162 `RUN_TEST` registrations as `unity` tests and neither helper. The vendored Unity tree keeps its 37 convention tests: `example_1` and `example_3` define the same test names, so rank 3 ties and abstains. The ADR predicted that count would move; the prediction was wrong and the rule held. There are five `c-tests` records: two fixture runners (`AllTests.c`, `all_tests.c`) and three directory counts. The body count is a floor: in `unity_fixture_Test.c`, 27 of 32 `TEST(…)` bodies are counted, and tree-sitter's error recovery reshapes the rest. **The doer's full suite showed 3 `test_ttt_units` failures in its sandbox.** They are not D-r. Dispatch sets `GIT_AUTHOR_*`/`GIT_COMMITTER_*` to `hobbes-dispatch`; that environment overrides the fixture's `-c user.name`, and `units_from_git` skips a doer's commits. Reproduced on the host with the same environment (3 failed; 24 pass without it). Registered as D-s in the handoff.
