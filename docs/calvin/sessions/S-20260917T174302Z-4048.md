# Harness session `S-20260917T174302Z-4048`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.40-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-130's **R-arity**: a C++ `calls` fact lane B answered, written with **more arguments than the target symbol can take**, draws no edge and is tailed `arity-mismatch`. The decision is `docs/ad (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T174302Z-4048/brief.md`, task sha256 `59bbfb210909`)
- **Parent:** `3c31b503dc30` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 125 of 140; result `success`; reported cost $12.2605 (the envelope's figure, on the subscription); wall 1066.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×2
- **Policy:** 60 exec decision(s) — `allow`×56, `escalate`×4; escalated: `/bin/sh -c cd /work/pipeline && cat > /tmp/probe.py <<'PYEOF'
from hobbes.extract import cppsource as C
src = open('/tmp/s.cpp','rb').read()
tree = C._PARSER.pa`; `/bin/sh -c cd /work/pipeline && cat > /tmp/probe.py <<'PYEOF'
from hobbes.extract import cppsource as C
src = open('/tmp/s.cpp','rb').read()
tree = C._PARSER.pa → expired`; `/bin/sh -c cat > /tmp/s.cpp <<'EOF'
void b(void);
void c(int a, int b = 2);
void d(int, ...);
template <typename... A> void e(A&&... a);
void g() {
  f({a, b});`; `/bin/sh -c cat > /tmp/s.cpp <<'EOF'
void b(void);
void c(int a, int b = 2);
void d(int, ...);
template <typename... A> void e(A&&... a);
void g() {
  f({a, b}); → expired`; records: stream opened→closed
- **Edits:** 47 edit(s) to 17 file(s); first at 5.6 min after launch; files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/derive/gate.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/src/hobbes/extract/laneacache.py`, `pipeline/src/hobbes/extract/minted.py`, `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/src/hobbes/extract/tail.py`, … and 7 more
- **Branch:** `hobbes/S-20260917T174302Z-4048`, 1 commit(s); files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/derive/gate.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/src/hobbes/extract/laneacache.py`, `pipeline/src/hobbes/extract/minted.py`, `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/src/hobbes/extract/tail.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_gate.py`, `pipeline/tests/test_laneacache.py`, `pipeline/tests/test_minted.py`, `pipeline/tests/test_scipsource.py`, `pipeline/tests/test_tail.py`
- **Gate:** **clear** at `3c31b503dc30` (gate v2, grounder v4, record `a6fcaf18d6f2`); unknown 0; map over 17 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1154, regressions 0; {'P2P': 1100, 'new-pass': 31, 'skip': 19, 'removed': 3, 'uncollected': 1}; build {'P2P': 2}

**The doer's closing words:**

> Committed on `hobbes/S-20260917T174302Z-4048`.
> 
> **What changed**
> 
> - `cppsource.py` — `_argc` (a site's written argument count: `None` for a pack expansion, a braced initialiser, an ERROR/MISSING node, or no list of its own; a comment is not an argument) recorded on every call/construction site and carried onto `ev.Site`; `_max_params` (the declarator's parameter count; `()`/`(void)` = 0; `None` for `...`, a pack, or an ERROR) set wherever `_signature` is read, so a C++ `function`/`method` symbol carries it and a `type` does not.
> - `evidence.py` — `Site.argc` and `Resolved.argc`; the join copies it on the semantic branch only, beside `qualifier`.
> - `minted.py` — `read_max_params`, the same token read `shows_body` makes (literals blanked, `//` dropped, first depth-0 parenthesised list after the terminal name, depth-0 commas), `None` on a macro in the list, an unbalanced or mismatched bracket, a `...`, or no list; set on every minted function/method. `shows_body`'s file read factored into `_lines`.
> - `scipsource.py` — `_SymbolIndex.max_params` (only ids that carry one), `_ARITY_GUARDED` (the C++ suffixes; no `.go`/`.rs`), and the rule beside R-qual's block: a lane B `calls` fact, both counts known, target kind `function`/`method`, `argc > max_params` → `arity_mismatch`, no edge.
> - `tail.py` / `extract/__init__.py` / `gate.py` / `knowledge.go` — the `arity-mismatch` class, its doc paragraph, `CLASSES_AVAILABLE["cpp"]`, `ALL_CLASSES`, the per-file count, the gate's `laneb-miss` reason, the `tailMeanings` gloss.
> - `laneacache.py` — `FORMAT` → `lanea-cpp v2`, so no entry is read back without the two counts.
> 
> **Existing assertions changed** (each minimally, all forced by the class being added):
> - `test_minted.py:108` — the whole-dict mint comparison gains `"max_params": 1`.
> - `test_tail.py` — the cpp class list is now nine; `test_qualifier_mismatch_is_available_to_cpp_alone` → `test_the_two_mismatch_classes_are_available_to_cpp_alone`, `ALL_CLASSES[-2:]` → `[-3:]`; the C-32 "cannot report" string gains `, arity-mismatch`.
> - `knowledge_test.go:867–868` — the same two C-32 strings.
> 
> **Deviations** (each withholds less, never fires on "too few"):
> - A `comment` node inside an argument or parameter list is skipped rather than counted — counting it would inflate `argc` and withhold a right edge.
> - `_list_body` (the mint's read) refuses a list closed by anything but `)`, so `f(int a = b > c)` reads unknown instead of a truncated count.
> - An ALL_CAPS identifier of any length followed by `(` in a parameter list reads unknown (a single-letter `T(` included) — the conservative side.
> - `T t{a, b}` records no site at all today, so the braced-construction test asserts both that and `new T{a, b}` → `argc is None`.
> 
> **Checks run:** `tests/test_cppsource.py test_minted.py test_scipsource.py test_tail.py test_laneacache.py test_gate.py test_evidence.py` → 430 passed, 4 skipped; `cd /work/go && go test ./internal/knowledge/` → ok (plus `go build ./...`); the whole `pytest -q` → **1816 passed, 14 skipped**.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: merged no-ff. On the host: 1,830 pytest, all 9 `lane_b`, `go test ./internal/knowledge/`. §10.14 on the branch's tree against the stored keys: fmt 6,510 confirmed at 0 contradicted, `line-unresolved` 62 → 27 (strict 99.06% → 99.59%); the rule withholds exactly the 40 named rows (35 `copy` rows the mint surfaced, 5 standing: `holds_alternative` ×2, `any_cast` ×2, `scan.h:466`) and none beyond them; against the same tree with the rule disabled by a scratch wrapper, 0 of the 40 were confirmed (35 `line-unresolved`, 5 `no-targets`); args withholds nothing; both C exports identical. The four deviations all withhold less and stand.
