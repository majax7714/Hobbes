# Harness session `S-20260917T134335Z-b0ed`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.37-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Surface what ADR-125's R-qual does not reach: **mark every semantic C++ `calls` edge that starts in a template pattern**, in `who_calls` and in one degradation record, so C-153 moves from unsurfaced t (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T134335Z-b0ed/brief.md`, task sha256 `86d4ec9d4149`)
- **Parent:** `1357e7164bfd` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 61 of 140; result `success`; reported cost $3.2677 (the envelope's figure, on the subscription); wall 418.0 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 11 exec decision(s) — `allow`×11; records: stream opened→closed
- **Edits:** 19 edit(s) to 5 file(s); first at 2.0 min after launch; files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/tests/test_cppsource.py`
- **Branch:** `hobbes/S-20260917T134335Z-b0ed`, 1 commit(s); files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/tests/test_cppsource.py`
- **Gate:** **clear** at `1357e7164bfd` (gate v2, grounder v4, record `2833fa181005`); unknown 0; map over 6 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 902, regressions 0; {'P2P': 879, 'new-pass': 8, 'skip': 14, 'uncollected': 1}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `b874d80` on `hobbes/S-20260917T134335Z-b0ed`.
> 
> ## What changed
> 
> **`pipeline/src/hobbes/extract/cppsource.py`** — `CppFile.template_patterns`, filled during the declaration walk. A `pattern` flag is carried down `_walk_declarations` → `_declaration` → `_function_definition` (never derived at the leaf, since a member is a pattern's because of a header above its enclosing class). `_pattern_header` is the one rule: a `template_declaration` with a **non-empty** parameter list. `template <>` is concrete and does not set it; the flag is never unset once set, so a member at any depth inherits it. The layer dict gains `"template_patterns"`, a frozenset of symbol ids, built exactly as `full_specializations` is.
> 
> **`pipeline/src/hobbes/extract/__init__.py`** — `_cpp_template_pattern_records`, called right after `graph["symbol_edges"]` is final. It writes `graph["cpp_template_patterns"]` (the sorted ids that are the `from` of at least one semantic `calls` edge; written whenever the C++ layer ran, `[]` when none) and returns one `cpp-template-sites` record at `path: "."` with the edge count, or none when the count is 0.
> 
> **`go/internal/knowledge/knowledge.go`** — `graphDoc.CppTemplatePatterns` (optional), kept in `graphIndex.cppPattern` as a set; `(*graphIndex).templateNote` appends the C-153 note after the citation and qualifier on a `calls` line whose tier is `semantic` and whose `From` is in the set. Nothing else reads it.
> 
> ## Deviations
> 
> - **A class nested inside a class body has no symbol in lane A, so its methods go unmarked.** The walk does not descend into `struct N {..}` written in a class body — neither `N` nor `N::nested` is a symbol at all, in any C++ repo, independent of this change. Such a method can therefore be the `from` of no edge, and the region goes unmarked rather than mis-marked. Widening the walk would add symbols to every C++ graph, well outside this partition. The brief's "nested struct's method" case is proved as this limit (`test_a_class_nested_in_a_class_body_is_no_symbol_so_its_members_go_unmarked`); the depth rule itself is proved by a member template inside a class template inside a namespace, which is recorded.
> 
> ## What I ran
> 
> - `/work/pipeline/.venv/bin/python3 -m pytest tests/test_cppsource.py tests/test_scipsource.py tests/test_tail.py tests/test_cli.py -q` → **307 passed, 2 skipped**.
> - `/work/pipeline/.venv/bin/python3 -m pytest -q` (whole pipeline suite) → **1,698 passed, 11 skipped**.
> - `cd /work/go && go test ./internal/knowledge/` → **ok**; `go build ./... && go test ./...` → all packages ok, no failures.
> 
> New tests: 6 in `test_cppsource.py` (3 lane-A unit, 3 ingest-level with lane B hand-fed through `_lane_b_facts`), 2 in `knowledge_test.go`. No existing assertion moved. `test_scipsource.py` was not modified — the hand-fed-lane-B pattern the ingest test needed already lives in `test_cppsource.py`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read in full against ADR-125 §4. The pattern flag is carried down the walk and never cleared, a `template <>` header does not set it, only semantic `calls` edges are marked, and an artifact without the key renders exactly as before. The one deviation (a class nested inside a class body is no lane A symbol, so it goes unmarked) marks less, never more, and is pinned by a test. Host: `lane_b` 7 passed, full pytest 1,708 passed (the tracker's drift test red until this block), Go `./...` all ok.
