# Harness session `S-20260925T011655Z-c141`

*2026-09-25 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E0, part 4: the two rename shadows, G-graph, and two fixes to part 3 (brief `/home/mmarrujo/.hobbes/sessions/S-20260925T011655Z-c141/brief.md`, task sha256 `16279fc9fd07`)
- **Parent:** `296fa0db7884` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 141 of 140; result `error_max_turns` **with an error** (API status None, `max_turns`); reported cost $20.8983 (the envelope's figure, on the subscription); wall 2320.6 s; session exit 1
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 65 exec decision(s) — `allow`×57, `escalate`×8; escalated: `/bin/sh -c cd bench/calvin/lattice && tmp=$(mktemp -d) && F=tests/fixtures/sqlite-vector-kernels && .venv/bin/python -m lattice.cli shadow $F $tmp/desc --graph `; `/bin/sh -c cd bench/calvin/lattice && tmp=$(mktemp -d) && F=tests/fixtures/sqlite-vector-kernels && .venv/bin/python -m lattice.cli shadow $F $tmp/desc --graph  → expired`; `/bin/sh -c cd bench/calvin/lattice && export PYTHONPATH=/work/bench/calvin/lattice/src && tmp=$(mktemp -d) && F=tests/fixtures/sqlite-vector-kernels && .venv/bi`; `/bin/sh -c cd bench/calvin/lattice && export PYTHONPATH=/work/bench/calvin/lattice/src && tmp=$(mktemp -d) && F=tests/fixtures/sqlite-vector-kernels && .venv/bi → expired`; `/bin/sh -c cd bench/calvin/lattice && rm -rf /tmp/sh-desc && PYTHONPATH=src .venv/bin/python -m lattice.cli shadow tests/fixtures/sqlite-vector-kernels /tmp/sh-`; `/bin/sh -c cd bench/calvin/lattice && rm -rf /tmp/sh-desc && PYTHONPATH=src .venv/bin/python -m lattice.cli shadow tests/fixtures/sqlite-vector-kernels /tmp/sh- → expired`; `/bin/sh -c cd bench/calvin/lattice && .venv/bin/python -m pytest -q 2>&1 | tail -6 && git -C /work status --short`; `/bin/sh -c cd bench/calvin/lattice && .venv/bin/python -m pytest -q 2>&1 | tail -6 && git -C /work status --short → expired`; records: stream opened→closed
- **Edits:** 77 edit(s) to 13 file(s); first at 11.3 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/ages.py`, `bench/calvin/lattice/src/lattice/cells.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/facts.py`, `bench/calvin/lattice/src/lattice/graphgrade.py`, `bench/calvin/lattice/src/lattice/shadow.py`, `bench/calvin/lattice/tests/test_ages.py`, `bench/calvin/lattice/tests/test_cells.py`, `bench/calvin/lattice/tests/test_cli.py`, … and 3 more
- **Branch:** `hobbes/S-20260925T011655Z-c141`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/ages.py`, `bench/calvin/lattice/src/lattice/cells.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/facts.py`, `bench/calvin/lattice/src/lattice/graphgrade.py`, `bench/calvin/lattice/src/lattice/shadow.py`, `bench/calvin/lattice/tests/test_ages.py`, `bench/calvin/lattice/tests/test_cells.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_facts.py`, `bench/calvin/lattice/tests/test_graphgrade.py`, `bench/calvin/lattice/tests/test_shadow.py`
- **Gate:** **blocked** at `296fa0db7884` (gate v2, grounder v4, record `394ec6250b09`) — blocking: invented; unknown 0; map over 34 file(s), 3.7% of their lines uncaptured; partition checked
  - `invented` bench/calvin/lattice/tests/test_graphgrade.py:123 `needs_git`
  - `invented` bench/calvin/lattice/tests/test_graphgrade.py:142 `needs_git`
  - `invented` bench/calvin/lattice/tests/test_graphgrade.py:160 `needs_git`
  - `invented` bench/calvin/lattice/tests/test_graphgrade.py:174 `needs_git`
  - `invented` bench/calvin/lattice/tests/test_graphgrade.py:189 `needs_git`
  - `invented` bench/calvin/lattice/tests/test_graphgrade.py:201 `needs_git`
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 189, regressions 0; {'new-pass': 67, 'P2P': 122}

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: false-block
- outcome: merged
- notes: ADR-151's E0, part 4. **The doer ran out of turns** (141 of 140, `error_max_turns`, $20.90, the most any
  unit of this programme cost) and left no closing words. The harness committed the tree at exit. Every item of
  the brief is in the diff, and verify passes. **The block is session 9326's false block again:** all 8 `invented`
  rows are a new `@needs_git`, a decorator naming a module-level value. The brief asked for the condition inline,
  and the doer did not do that; the gate's misreading is still the defect.

  **On the real target** (`~/.hobbes/bench/calvin-lattice/shadows/`):
  - Both shadows first **failed to link** in the target's own `make unittest`: `sqlite3_mutex_alloc` is an in-repo
    `#define` on one `#if` arm of `src/sqlite-vector.c` and SQLite's API under `-DSQLITE_CORE`, the arm the
    ingest did not build. So no external veto names it, and the plan renamed it.
  - **Fixed at the review** (the fix commit on this branch): `declared_outside` keeps any graph symbol that a C file
    outside the renamed set writes in code or `#define`s, with a test. 4 names are kept that way (the three mutex
    calls and one more).
  - Rebuilt, each shadow renames 493 names and keeps 106 (97 `not-a-graph-symbol`, 4 `declared-outside`,
    3 `external`, 2 `entry-point`), and **passes the target's suites: `make unittest` 1,447 of 1,447, and
    `make unittest-simd` on the AVX512 backend**, as the original does.
  - All 93 native golds grade `pass` through each shadow's lattice.
  - **G-graph** over one wave of the 93 golds: Jaccard 1.0 on every cell.
  - A poison body (the `sse2/int8/dot` gold plus a call to `hsum128_ps`) grades numerically `pass`, and G-graph reads
    Jaccard 0.667 with `extra: [hsum128_ps]`. It says what the numbers cannot.
  - Part 3's two defects are fixed: the facts for `avx512/bfloat16/dot` drop the three header-macro expansions,
    each with the name the source wrote, and add `_mm512_cmp_ps_mask` and `_mm512_extractf32x8_ps` as
    `clang-headers:macro`; `lattice ages` reads a bare clone through git.
  - One residue, by the brief's rule: `_mm256_undefined_ps` stays, a real intrinsic function that clang's
    `_mm512_extractf32x8_ps` expands to, which the source never wrote.

  Host pytest 252 passed and 20 skipped with the fix.
