# Harness session `S-20260914T015405Z-47f7`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.14-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Write down the includes lane A cannot place (C-133). The design is ADR-108's amendment of 2026-09-14 ("an include lane A cannot place is written down"), at the end of `docs/adr/108-c-at-lane-a.md`. Re (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T015405Z-47f7/brief.md`, task sha256 `b0b890ce6694`)
- **Parent:** `b154bdfe8d78` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 25 of 100; result `success`; reported cost $0.7906 (the envelope's figure, on the subscription); wall 259.4 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 3 exec decision(s) — `allow`×3; records: stream opened→closed
- **Edits:** 7 edit(s) to 2 file(s); first at 1.8 min after launch; files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/test_csource.py`
- **Branch:** `hobbes/S-20260914T015405Z-47f7`, 1 commit(s); files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/test_csource.py`
- **Gate:** **clear** at `b154bdfe8d78` (gate v2, grounder v3, record `f9d9225c3716`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 187, regressions 0; {'skip': 4, 'P2P': 177, 'new-pass': 6}

**The doer's closing words:**

> Full suite: **1505 passed, 9 skipped** (the 5 `lane_b` cases skip here plus 4 others already skipping before this change). Work is committed as `c9a27d5`.
> 
> **What changed:** `_resolve_include` now returns an `_IncludeResolution(path, ambiguous)` instead of a bare `str | None`, so `_join` can tell an unmatched miss from an ambiguous one. `_join` collects per-directory misses (quoted-unmatched specs, and quoted-or-angle ambiguous specs, deduped and capped at three) and hands them to the new `_include_degradations` helper, which builds one `"c-includes"` record per directory in the amendment's wording (C-133). `extract_c` folds those into `errors` beside the `c-tests` records. Edge behavior is unchanged. Added 6 new tests in `TestIncludeDegradations` covering unmatched, ambiguous (both spellings), non-miss dependencies, the three-spec cap with dedup, the root directory (`"."`), and coexistence with a `c-tests` record on the same file.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged as `a7ab3f6` (not squashed); released as 0.2.15-beta
- notes: Read the diff (2026-09-14). It follows the amendment's four decisions: `_resolve_include` keeps its three steps and reports which miss it was (a small frozen result type), `_join` collects the misses per directory in path order of first sight, and one `c-includes` record per directory lands beside the `c-tests` records in `extract_c`. The edges did not move: **on cJSON** (lane A, the merged code, `fb16e5c`) 305 include edges and 12 external nodes before and after, with **6 records** — the vendored Unity examples' `"ProductionCode.h"`/`"ProductionCode2.h"` are ambiguous in four directories (each example ships its own copy, so the suffix step ties), `example_3/helper`'s `"Types.h"` and six of `test/expectdata`'s mock headers match nothing; **on sqlite-vector** (`0c2223a`) 163 and 57 before and after, with **1 record**, `libs`' six platform and generated headers (`"sqlite_cfg.h"`, `"_mingw.h"`, …). The one egress refusal is the known shape: a plain GET to the host `llm`, a test's fake endpoint reached through the session's proxy variables when the doer ran the full suite (`de81` hit it too). The doer's full suite was 1,505 passed in the sandbox; on the host after the merge, 1,513 passed and the tracker's drift test red until this row was rendered.
