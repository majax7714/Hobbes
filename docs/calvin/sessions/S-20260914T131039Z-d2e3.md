# Harness session `S-20260914T131039Z-d2e3`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.16-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make `oracle import` take `--lang c` honestly: the lane's foreign-graph converter (ADR-101) already reads the export's extension table, which has had C since ADR-110, but every place the language set  (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T131039Z-d2e3/brief.md`, task sha256 `77b6b4217cbb`)
- **Parent:** `7abfdcd4ab7c` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 40 of 60; result `success`; reported cost $1.0359 (the envelope's figure, on the subscription); wall 281.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×1
- **Policy:** 4 exec decision(s) — `allow`×4; records: stream opened→closed
- **Edits:** 12 edit(s) to 7 file(s); first at 3.3 min after launch; files: `bench/oracle/README.md`, `bench/oracle/cmd/oracle/main.go`, `bench/oracle/grade-foreign.sh`, `bench/oracle/internal/foreign/foreign.go`, `bench/oracle/internal/foreign/foreign_test.go`, `bench/oracle/report/foreign_record.py`, `bench/oracle/testdata/foreign/cclang.edges.json`
- **Branch:** `hobbes/S-20260914T131039Z-d2e3`, 1 commit(s); files: `bench/oracle/README.md`, `bench/oracle/cmd/oracle/main.go`, `bench/oracle/grade-foreign.sh`, `bench/oracle/internal/foreign/foreign.go`, `bench/oracle/internal/foreign/foreign_test.go`, `bench/oracle/report/foreign_record.py`, `bench/oracle/testdata/foreign/cclang.edges.json`
- **Gate:** **clear** at `7abfdcd4ab7c` (gate v2, grounder v3, record `a60c9fc6a84b`); unknown 0; map over 7 file(s), 40.3% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 11, regressions 0; {'P2P': 9, 'new-pass': 2}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `be091ff`.
> 
> **What changed:** `oracle import --lang c` now works end-to-end and is spelled consistently everywhere the language set appears:
> - `internal/foreign/foreign.go`: refusal message gains `|c`; the package doc comment now names all six languages and notes C's macro exclusion.
> - `cmd/oracle/main.go`: usage block's `export`/`import` lines and both `--lang` flag helps gain `|c`.
> - `grade-foreign.sh` and `README.md`: usage lines updated; README gains a sentence on C's header-callee grading (`oracle c-clang`).
> - `report/foreign_record.py`: `--lang` help lists C; the command-line-name tuple gains `'c'`.
> - New fixture `testdata/foreign/cclang.edges.json` (hand-read, over the existing `testdata/cclang/` C sources) with 12 rows: 8 gradeable edges including two header crossings, 1 macro-dropped, 1 duplicate, 2 other-language.
> - New tests `TestCclangFixtureConvertsToTheHandTruth` and `TestCclangFixtureGradesAndPoisonRefuses` beside the minigo ones, covering conversion exactness, grading (8 confirmed, tier split 7/1), poison refusal, and a wrong header target grading as contradicted.
> 
> **Checked:** `gofmt -l` clean, `go vet ./cmd/oracle/...` clean, `go test ./internal/foreign/ ./internal/export/ ./internal/grade/` passes, and a full `go test ./...` in `bench/oracle` passes (all packages green, nothing else broke). `git status` confirmed only the seven partitioned files were touched.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged as `109c15b` (not squashed); no version moves (nothing under `bench/` does, ADR-103)
- notes: Read the diff (2026-09-14). It follows the amendment's four decisions and nothing else: `c` spelled in the refusal, the usage block's two lines (the `export` line had read `go|ts`, stale since Python), both `--lang` helps, `grade-foreign.sh`, the README and the foreign record; the hand-read fixture over `testdata/cclang` with the twelve rows as briefed (eight graded, two of them across a header; a `macro` row, a duplicate and two other-language rows dropped and counted); two tests beside the minigo pair, the second grading against a hand-built key (8 confirmed, 7/1 by label, poison refused in full, the header target moved to `lib_sum` contradicted). `Convert` gained no C branch; `export.Exts` untouched. **Host:** `internal/foreign`, `internal/export` and `internal/grade` green on the branch in a worktree (the grade package needs the ts node tree beside it, a worktree lacks it); the full oracle-lane suite on `main` after the merge (the BUILDLOG has the count). **Fixed on the host after the merge:** one check in the conversion test read the truth map against itself (`cclangTruth[k]` for two keys it defines), a tautology; it now asserts the two header pairs were among the converted edges. **The one egress refusal** is `static.rust-lang.org`: the doer's `go test ./...` reached the Rust oracle's driver tests, which look for a nightly toolchain; they skip, and the run was green.
