# Harness session `S-20260920T214123Z-db45`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.65-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-147: a decorator factory's application is a call of the def it returns (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T214123Z-db45/brief.md`, task sha256 `1aab10e7ee5d`)
- **Parent:** `4b0c120bbe4d` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 87 of 140; result `success`; reported cost $8.2294 (the envelope's figure, on the subscription); wall 741.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×7; refused `llm`×1
- **Policy:** 37 exec decision(s) — `allow`×35, `escalate`×2; escalated: `/bin/sh -c find pipeline/tests/fixtures/minideco -type f | head -30; echo ---; for f in $(find pipeline/tests/fixtures/minideco -name '*.py' -o -name '*.toml' -`; `/bin/sh -c find pipeline/tests/fixtures/minideco -type f | head -30; echo ---; for f in $(find pipeline/tests/fixtures/minideco -name '*.py' -o -name '*.toml' - → expired`; records: stream opened→closed
- **Edits:** 29 edit(s) to 9 file(s); first at 3.8 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/decorators.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minideco/src/minideco/app.py`, `pipeline/tests/fixtures/minideco/src/minideco/deco.py`, `pipeline/tests/test_decorator_calls.py`, `pipeline/tests/test_decorator_factories.py`, `pipeline/tests/test_pysource.py`
- **Branch:** `hobbes/S-20260920T214123Z-db45`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/decorators.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minideco/src/minideco/app.py`, `pipeline/tests/fixtures/minideco/src/minideco/deco.py`, `pipeline/tests/test_decorator_calls.py`, `pipeline/tests/test_decorator_factories.py`, `pipeline/tests/test_pysource.py`
- **Gate:** **clear** at `4b0c120bbe4d` (gate v2, grounder v4, record `b71b5181b7c8`); unknown 0; map over 103 file(s), 1.4% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1711, regressions 0; {'P2P': 1646, 'skip': 25, 'removed': 1, 'new-pass': 38, 'uncollected': 1}

**The doer's closing words:**

> Committed as `e01d1e6`.
> 
> ## What I changed
> 
> **`pysource.py`** — three facts, all defaulted so every existing construction stays valid:
> - `Decorator.called` and `Decorator.callee_line` (a bare `@f` and `@f()` digested identically before; `callee_line` is the terminal identifier's line, `None` when `_dotted` can't name the callee).
> - `Symbol.returns_inner` — the strict wording, via `_returns_inner` / `_own_definitions` / `_parameter_names`.
> 
> **`extract/decorators.py`** (new) — `factory_calls(modules, parsed, symbols, symbol_edges) -> (rows, counts)`, the five conditions in the ADR's order, six reason constants in `FACTORY_REASONS`.
> 
> **`extract/__init__.py`** — `timings.step("decorators")` after the fixtures step, `_add_factory_call_edges` mirroring `_add_value_call_edges` (`syntactic` / `tree-sitter`, merged per `(from, to)`, re-sorted by `_edge_order`), `graph["decorators"]` only when non-empty.
> 
> **`cli.py`** — `_print_decorators` beside `_print_fixtures`.
> 
> **Tests** — `test_pysource.py` (`TestCallForm`, `TestReturnsInner`), `test_decorator_factories.py` (new, hand-built graphs + the `lane_b` end-to-end over `minideco`), `minideco` extended with `either` and `wrapped`, `test_decorator_calls.py` updated.
> 
> ## The things the brief asked me to report back
> 
> - **Order in `extract_repo`:** `_build_symbol_layer` is called at `__init__.py:242`, `collect_tests` at `:259`. The new step sits inside `_build_symbol_layer` right after the fixtures block, so it is settled before the test map reads `graph["symbol_edges"]`.
> - **Reusing ADR-145's binding walk:** it fit *partly*. `_rebound` answers the assignment-shaped bindings and I call it with the one candidate name — but it walks `_own_body`, which stops **at** nested `def`/`class` nodes and never yields them, so it cannot see the binding this rule is actually about. I added `_own_definitions`, its companion, for that half.
> - **`returns_inner is None` is one reason** (`no-returned-def`), covering the ADR's separate async / generator / return-shape / binding-shape lines. I added no extra fact to split them: telling them apart needs a second reading of the same body.
> - **`schema.py` / `emit.py` / `laneacache.py`:** none need to know. The edge goes through `tiered_edge` with the existing vocabulary, `emit` dumps whatever dict it is handed, and the lane A cache is C++-only (`lanea-cpp v5`, `CppFile` records) — Python's `Symbol`/`Decorator` never enter a cached payload. Nothing outside the partition changed.
> - **One deliberate divergence from the ADR's wording, stated in the module docstring:** the counts block is written where *some site was asked* (condition 1 named a factory) rather than only where one *reached condition 2*. The single case the two readings differ over is a line with more than one answer — which is a counted refusal, and a refusal the reader cannot see is worse than a block saying one thing was asked and refused. The test for that case asserts its count, which the stricter reading would have thrown away.
> - **`test_decorator_calls.py`:** the `no edge ends in .decorator` assertion now asserts the edge's tier (`syntactic`) and `via` (`decorator-factory`) — that boundary is ADR-146's and ADR-147 moves it. The neighbouring `[to for frm, to in drawn if frm == OUTER] == [...]` assertion moved for the same reason (the brief didn't name it; the new edge also leaves `outer`), and its lane A twin's pinned lines 11–30 still hold — `minideco/app.py` is appended to only, and its docstring rewrite kept the same line count.
> 
> ## What I ran
> 
> - `/work/pipeline/.venv/bin/python3 -m pytest -q` → **2,235 passed, 21 skipped** (59.7 s).
> - The two `lane_b` end-to-end cases **skip here** — no containment in the sandbox, so their first real run is the developer's. I verified the same path by hand instead: lane A over the real `minideco` tree plus the six `semantic` edges the index would put at the decorator lines gives exactly **3 drawn** (`factory.decorator` from the module and from `outer`, `Registry.r …

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-147, as briefed, 87 turns. The doer kept the strict wording, reused
  `_rebound` for the assignment-shaped half of the binding walk and said where it did
  not fit, and stated one divergence from the ADR (the counts block is written where a
  site was *asked*, so a `two-targets` refusal is visible) — accepted, the ADR amended.
  **Reworked after the merge, two things the host's `lane_b` run found** (both skip in
  the sandbox): (1) the edge append kept symbol callers only — **the brief's fault**,
  which said "exactly as `_add_value_call_edges` does", whose callers are never modules
  — so a top-level `@factory("a")` was counted drawn and not in the graph (minideco: 3
  counted, 1 held); the caller may now be a module node, with a lane A test that needs
  no index. (2) two assertions read every edge ending in `either…` / `.decorator`,
  which the index's own direct edges (`either → either.decorator`, `app → either`)
  fail; narrowed to decorator sites. Not classes the gate covers. On the host: pytest
  2,257, the `lane_b` cases through the index. The real cell on `main`: click **368
  drawn, 304 confirmed, 0 contradicted — the probe's figures exactly — 3,356 of 4,595
  (73.0%; 3,052 before), 18 suspect (the same rows), poison PASS, 0 rows lost**. One
  egress refusal, Claude Code's own `llm` host; two escalations, a read-only `find`
  over the fixture tree. Merged `--no-ff`.
