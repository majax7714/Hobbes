# Harness session `S-20260918T160737Z-368a`

*2026-09-18 · `hobbes dispatch` v1 · Hobbes 0.2.45-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-134, route (a): **a minted function or method gets an extent read from the file's own text by brace matching, and the facts written inside it are re-homed to it.** The decision is `docs/adr/ (brief `/home/mmarrujo/.hobbes/sessions/S-20260918T160737Z-368a/brief.md`, task sha256 `28914a329c83`)
- **Parent:** `fe05ca0b6db0` of `hobbes_public`
- **Doer:** 2.1.276 (Claude Code); model claude-opus-5; turns 59 of 120; result `success`; reported cost $5.8783 (the envelope's figure, on the subscription); wall 917.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `llm`×2
- **Policy:** 11 exec decision(s) — `allow`×11; records: stream opened→closed
- **Edits:** 28 edit(s) to 5 file(s); first at 3.6 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/minted.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_minted.py`
- **Branch:** `hobbes/S-20260918T160737Z-368a`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/minted.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_minted.py`
- **Gate:** **clear** at `fe05ca0b6db0` (gate v2, grounder v4, record `94ee91cced10`); unknown 0; map over 5 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1231, regressions 0; {'skip': 18, 'P2P': 1185, 'new-pass': 27, 'uncollected': 1}

**The doer's closing words:**

> Committed.
> 
> **What changed** (only the five partition files):
> 
> - `pipeline/src/hobbes/extract/minted.py` — `EXTENT_REFUSALS`, `_blank_source` (whole-file blanking of `//`, `/* */`, strings, raw strings and char literals, length and newlines kept) with `_blanked_lines` caching beside `sources`, `_extent` (first `{` at paren depth 0 → its `}`, with `no-body` / `runs-off` / `conditional-inside`), `_read_extents` (second pass for `holds-a-definition`, then `end_line` moved), and the pure `rehome`. `mint` now returns `"extents": {"read": n, "refused": {…all four…}}`. Module docstring rewritten to ADR-134 with its measured fmt counts.
> - `extract/__init__.py` — `rehome` called right after the mint, before `project`; `graph["minted"]["extents"]["rehomed"]`; the block comment updated.
> - `cli.py` — `_print_minted`'s first line reworded (definitions read → extents taken → facts re-homed → the rest stay targets), plus a `no extent:` line; an artifact with no `extents` key prints exactly as before.
> - Tests: `TestTheExtent`, `TestTheRehoming`, two new `TestThroughTheIngest` cases over a second lost header (`LOST_CALLER_HEADER`, written beside `LOST_HEADER` so no pinned line number moved), four new `_print_minted` cases.
> 
> **Checks:** `tests/test_minted.py tests/test_cli.py tests/test_cppsource.py tests/test_scipsource.py` → 410 passed, 2 skipped. Whole suite from `/work/pipeline`: **1,923 passed, 14 skipped** (was 1,910 + 14). I also ran `_blank_source`/`_extent` over the `minicpp` fixture as a smoke read: every extent closed at its own function's brace.
> 
> **Assertions I moved** (each minimal):
> - `test_minted.py::TestThroughTheIngest::test_without_the_definitions_row_the_same_fact_is_below_floor` — pinned the whole `minted` block; gained the zeroed `extents` block.
> - `test_minted.py::test_the_refusal_block_names_every_reason_whether_or_not_it_fired` — same, at mint level (no `rehomed`, which is the caller's).
> - No `end_line == line` assertion needed changing: `TestWhatMints`'s minted function has a one-line body, so its extent is its own line.
> 
> **Deviations, one line each:**
> - A mutually-holding pair of extents cannot arise under the rule as written (`line < at <= end` means a later-starting extent never holds an earlier line), so instead of that test I proved the realizable shape: two macro-generated rows whose matches both run into one written-out body are **both** refused, and the written-out one keeps its extent.
> - `rehome` treats `end_line > line` as "has an extent": a one-line extent moves nothing, because it cannot be told from a refusal and `enclosing` already answers for that line.
> - `_extent` running to the end of file without ever finding a `{` is counted `no-body` (`runs-off` is reserved for a brace that opened and never closed).
> - The digit-separator test is tightened beyond the brief — a `'` between two alphanumerics is a separator only when the token it continues starts with a digit — because `L'}'` read as a number would leave its closing quote to blank a brace, the one direction this read must not fail in.
> - The ingest case's re-homed edge is a `uses`, not a `calls`: lane A reads no call site in a file it lost whole, so the reference is lane B's alone; the rule reads both kinds and the assertion is on the edge's `from`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: merged no-ff after the whole suite and `lane_b` on the host, from a worktree of the branch; the developer's fix follows it on `main`. The diff is the brief's — the blanking, the brace match with its four refusals in two passes, the pure `rehome`, the summary lines — with five deviations listed, each toward drawing less, and the tightened digit-separator read is better than the brief's. **The brief's premise was wrong, not the doer's code:** it said a file-scope site carries an empty scope; lane A's C and C++ sites carry the module's id, so the first real ingest re-homed 2 rows of 16 on args. Nothing the gate's classes cover — the code does what it was asked. The fix (a module-id scope is the module; a read extent is marked `extent: "braces"` so a one-line body re-homes and a refusal does not) and an image-dependent end-to-end case are the developer's. Checked after it (§10.19): every graded number ±0; fmt 1,290 `calls` rows re-homed, agreeing 6,392 → 7,610, no right row moved; args lost 15 → 0.
