# Harness session `S-20260920T201508Z-f751`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.64-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-146: a decorator is a call of what it names (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T201508Z-f751/brief.md`, task sha256 `f4861cfa11be`)
- **Parent:** `13c7d13a1681` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 45 of 80; result `success`; reported cost $2.4929 (the envelope's figure, on the subscription); wall 330.9 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 20 exec decision(s) — `allow`×18, `escalate`×2; escalated: `/bin/sh -c find pipeline/tests/fixtures/minifixval -type f | head -20; echo ----; for f in $(find pipeline/tests/fixtures/minifixval -type f); do echo "== $f"; `; `/bin/sh -c find pipeline/tests/fixtures/minifixval -type f | head -20; echo ----; for f in $(find pipeline/tests/fixtures/minifixval -type f); do echo "== $f";  → expired`; records: stream opened→closed
- **Edits:** 7 edit(s) to 7 file(s); first at 2.7 min after launch; files: `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minideco/pyproject.toml`, `pipeline/tests/fixtures/minideco/src/minideco/__init__.py`, `pipeline/tests/fixtures/minideco/src/minideco/app.py`, `pipeline/tests/fixtures/minideco/src/minideco/deco.py`, `pipeline/tests/test_decorator_calls.py`, `pipeline/tests/test_pysource.py`
- **Branch:** `hobbes/S-20260920T201508Z-f751`, 1 commit(s); files: `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minideco/pyproject.toml`, `pipeline/tests/fixtures/minideco/src/minideco/__init__.py`, `pipeline/tests/fixtures/minideco/src/minideco/app.py`, `pipeline/tests/fixtures/minideco/src/minideco/deco.py`, `pipeline/tests/test_decorator_calls.py`, `pipeline/tests/test_pysource.py`
- **Gate:** **clear** at `13c7d13a1681` (gate v2, grounder v4, record `c1260462cf5f`); unknown 0; map over 73 file(s), 2.3% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 530, regressions 0; {'P2P': 502, 'skip': 14, 'new-pass': 12, 'removed': 1, 'uncollected': 1}

**The doer's closing words:**

> Built and committed.
> 
> **Changed**
> 
> - `pipeline/src/hobbes/extract/pysource.py` — `_walk`'s `decorated_definition` branch now walks each decorator expression with the **enclosing** `stack` and no pending decorators, before the definition; where the expression is a bare `identifier`/`attribute` chain it also records one `Call(_scope_qualname(stack), dotted, …)` at `_terminal(expr)`'s line and column. The old "would pollute the call graph" comment is replaced by one naming ADR-146, what is recorded and why the scope is the enclosing one. `Decorator`, `_decorator`, `_call`, `_parametrized` and the pack-facing digest are untouched; the module docstring never claimed decorators were unwalked, so it stands.
> - `pipeline/tests/test_pysource.py` — `test_decorator_expressions_do_not_pollute_calls` replaced by 11 cases in `TestCalls`: `@app.get("/x")` positioned on `get` at module scope; the enclosing-scope rule for a decorated nested def and for a class body; bare `@pass_context` and `@a.b.c`; `@f(g(1))`; `@decos[0]` / `@(lambda f: f)` / `@decos[pick()]` (no bare application, inner calls still walked); a decorated class; stacked decorators in written order; the digest + `parametrized` reading unchanged; an `os.environ.get` in a decorator's arguments.
> - `pipeline/tests/test_decorator_calls.py` + the `minideco` fixture repo (`pyproject.toml`, `src/minideco/{__init__,deco,app}.py`) — a `lane_b` case asserting the four `semantic` edges (`minideco.app →` `deco.plain`, `deco.factory`, `deco.Registry.register`; `minideco.app.outer → deco.factory`), that `outer` has no other callee and no edge ends in `.decorator`; plus a lane-A twin pinning the four sites (and the `Registry()` construction) in the parsed calls. It skips via `containment.unavailable_reason()` as `test_tsjs_shorthand.py` does; fixtures stay out of collection through `norecursedirs = ["fixtures"]` in `pipeline/pyproject.toml`, so no per-repo wiring was needed.
> 
> **Step 2 — what else assumed no call site sits on a decorator line: nothing.** `who_calls` on `hobbes.extract.pysource.Call` gives `_walk` as the only producer; the readers of `parsed.calls` reached through `graph_neighborhood` (`graph`, `testmap`, `fixtures`, `interfaces`, `packs/base`, `packs/http_python`, `scipsource`) all key on a call's *scope*, and every "decorator" reference outside `pysource` reads `symbol.decorators` — the digest — not a call line. The one line-based lookup, `scipsource.py:985`'s `index.enclosing(source_module, fact.line)`, is a fallback for facts with no lane A scope; a decorated def's `Symbol.line` is the `def` line, so a decorator line already resolved to the enclosing class or the module — the same answer the new sites carry. Nothing outside the partition needed changing.
> 
> **Ran:** `cd pipeline && .venv/bin/python3 -m pytest -q` → **2196 passed, 20 skipped** (the lane_b ones skip in the sandbox, including the new `minideco` case, which I could not execute here). Not run: the Go, vitest, tsextract/scip and atlas0 suites — untouched by this change.
> 
> Two things for the developer's follow-up commit, both outside my write set: `CLAUDE.md`'s fixture-repo list in the project map does not yet name `minideco`, and the new `lane_b` case makes 15 lane_b tests where the status block says 14.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-146, lane A's half, as briefed. The doer checked `Call`'s readers through
  `who_calls` / `graph_neighborhood` before grepping and found none that assumed a
  decorator line holds no site. **Reworked after the merge, one defect the review
  found:** the branch read a decorator's expression as `children[-1]`, the idiom
  `_decorator` and `_parametrize_names` already used, and a trailing comment
  (`@pytest.fixture  # shared`) is the node's last child — so the site was lost, and,
  before this unit, so was the digest (a commented fixture was no fixture). Not a
  class the gate covers. Fixed on `main` at all three sites with `_decorator_expr` and
  a test. On the host: pytest 2,217 (15 `lane_b`, the new `minideco` case `semantic`
  through the index). The real cell on `main`: click **3,052 confirmed of 4,595
  (66.4%; 2,136 before), 18 suspect — the same rows — poison PASS**, row-identical to
  the probe's export, 0 rows of 0.2.63-beta's lost. One egress refusal, Claude Code's
  own `llm` host; two escalations, a read-only `find` over a fixture tree. Merged
  `--no-ff`.
