# Harness session `S-20260916T155426Z-8d48`

*2026-09-16 · `hobbes dispatch` v1 · Hobbes 0.2.28-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Fix one defect in the oracle's matcher: a line the key left unresolved still contradicts, because the grader judges a Hobbes edge against the targets of the line's *other* calls (H-30). The decision i (brief `/home/mmarrujo/.hobbes/sessions/S-20260916T155426Z-8d48/brief.md`, task sha256 `2a8efc24a203`)
- **Parent:** `612e5bdd924e` of `hobbes_public`
- **Doer:** 2.1.273 (Claude Code); model claude-opus-5; turns 36 of 100; result `success`; reported cost $1.4898 (the envelope's figure, on the subscription); wall 212.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×5
- **Policy:** 18 exec decision(s) — `allow`×10, `escalate`×8; escalated: `/bin/sh -c node ../../ts/tsc-oracle.mjs --repo ../../../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.oracle.json >/dev/null 2>&1; python3 - <<'E`; `/bin/sh -c node ../../ts/tsc-oracle.mjs --repo ../../../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.oracle.json >/dev/null 2>&1; python3 - <<'E → expired`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.oracle.json && echo ok`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.oracle.json && echo ok → expired`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.oracle.json`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.oracle.json → expired`; `/bin/sh -c for f in src/items.controller.ts src/math.spec.ts src/server.js src/union.ts src/util.js; do echo "== $f"; cat -n "$f"; done`; `/bin/sh -c for f in src/items.controller.ts src/math.spec.ts src/server.js src/union.ts src/util.js; do echo "== $f"; cat -n "$f"; done → expired`; records: stream opened→closed
- **Edits:** 6 edit(s) to 3 file(s); first at 0.4 min after launch; files: `bench/oracle/internal/grade/grade.go`, `bench/oracle/internal/grade/grade_test.go`, `bench/oracle/internal/grade/poison_test.go`
- **Branch:** `hobbes/S-20260916T155426Z-8d48`, 1 commit(s); files: `bench/oracle/internal/grade/grade.go`, `bench/oracle/internal/grade/grade_test.go`, `bench/oracle/internal/grade/poison_test.go`
- **Gate:** **clear** at `612e5bdd924e` (gate v2, grounder v4, record `7ed3fc75491a`); unknown 0; map over 8 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 39, regressions 0; {'P2P': 30, 'new-pass': 4, 'skip': 5}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `981bf87`.
> 
> **What changed**
> 
> - `bench/oracle/internal/grade/grade.go` — `Grade`'s non-trace path tracks `unresolved` (any site on the line with an empty target list) alongside `abstract`, and the switch gains a fifth case between `no-targets` and `contradicted`: `silent` / `line-unresolved`, carrying the line's targets for triage. The four existing cases are untouched and in their original order, so a resolved neighbour still confirms, the two `abstract` cases still fire, and an all-targetless line still reads `no-targets`. `traceRow` is unchanged.
> - `bench/oracle/internal/grade/grade_test.go` — four new tests: the H-30 shape reads `silent`/`line-unresolved` and stays out of the precision denominator (`r.Precision` is nil); a matching edge on that same line still `confirmed`; an all-targetless line still `no-targets` with `line-unresolved` at zero; a fully resolved line still `contradicted` at precision 0. `TestBuckets`' three reasons pass unchanged.
> 
> **The one fixture expectation that moved** — `poison_test.go`, `TestTSFixtureRefusesEveryPoisonedEdge`. Three of minits' five poisoned edges now read `silent`/`line-unresolved`. I checked the key (dumped its sites through a temporary test, since `node` on its own is escalate-gated here) and each line really does carry a site with no targets:
> 
> - `src/server.js:8` — two sites: `res.json(...)`, `static` targets=0 (express untyped without `node_modules`), and `normalize(...)`, targets=1.
> - `tests/util.test.mjs:7` — two sites: `assert.equal(...)`, `static` targets=0 (`node:assert` types absent), and `normalize(...)`, targets=1.
> - `tests/util.test.mjs:11` — the same pair.
> 
> Rather than relax the test to `allowUnjudged`, it now asserts strictly more than it did: `allowUnjudged=true` for the shared helper, then `refused == seeded - 3` and the silenced set is exactly those three named sites. A row that goes quiet for any other reason, or a fourth row going quiet, fails.
> 
> **Deviations** — none.
> 
> **Checked with** `go test ./internal/grade/ -count=1` from `/work/bench/oracle`: ok, 19 pass / 5 skip of 24 (the skips are `TestMiniappTrace` and the four needing cargo or containment, unchanged by this). Also `go test ./... -count=1` across the oracle module: all packages ok, `internal/clang` included and untouched.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Merged no-ff as `f747aef`; the doer's `981bf87` keeps its
  `hobbes-dispatch` authorship. Unit 2 of O10's defect fixes, decided
  before the dispatch in `oracle-grading.md` §7d and D-O4 (`7240ebd`,
  corrected in `21f4f57`).

  **The code is what was specified.** `unresolved` is tracked beside
  `abstract`, and the new case sits exactly between `no-targets` and
  `contradicted`, so the four existing cases keep their meaning: a
  resolved neighbour still confirms, both `abstract` cases still fire,
  and an all-targetless line still reads `no-targets`. `traceRow` is
  untouched. `TestFullyResolvedLineStillContradicts` names H-31's shape
  in its own comment — a call the key holds nowhere leaves the line
  fully resolved, so the rule does not reach it — which is the
  distinction `21f4f57` corrected in the docs.

  **The one moved expectation, and why it is not a laundered
  regression.** `poison_test.go`'s TS fixture flips
  `assertPoisonRefused`'s unjudged flag, which loosens that one
  parameter, but the test now asserts strictly more: exactly three
  poisoned twins go silent, each named, with `refused == seeded - 3`,
  so a fourth row — or a different row — fails. I checked the
  justification myself instead of taking it on trust, and the check is
  exhaustive: minits' own tsc key has **exactly three mixed lines**
  (`src/server.js:8`, `tests/util.test.mjs:7` and `:11`), each carrying
  one targetless site (`res.json`, `assert.equal` — untyped without
  `node_modules`) beside one resolved call. No other line in the fixture
  could have gone quiet.

  **Consequence, named rather than left implicit:** on a line the key
  left unresolved the matcher now declines to judge, so it also declines
  to *refuse* a poisoned edge there — the poison instrument loses
  coverage on exactly those lines. The check still passes with 0 falsely
  confirmed, which is how §10.8's P43 is worded, and the regrade will
  show refused counts falling by the number of such lines per cell.

  **Verified before merging** in `hobbes-session:local`, because this
  host has no clang++ and a fresh worktree has no
  `bench/oracle/ts/node_modules` — a host run would have skipped or
  failed for reasons of my own making. All four new tests plus
  `TestTSFixtureRefusesEveryPoisonedEdge` RUN and PASS; the whole oracle
  module is green (11 ok); `vet` and `gofmt` clean. Deviations: none
  declared, none found.

  With H-28 and H-29 (`eec8141`), three of O10's four defects are fixed.
  H-31 stays open by design. The three-tier regrade is pre-registered in
  §10.8 as P36–P45 and is the developer's next step.
