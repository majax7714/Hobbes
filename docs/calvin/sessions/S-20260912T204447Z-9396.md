# Harness session `S-20260912T204447Z-9396`

*2026-09-12 · `hobbes dispatch` v1 · Hobbes 0.2.4-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build the contained run and CLI of the oracle lane's C oracle, O9 (ADR-110), after fixing four defects review found in unit A: unit B of two. This is bench tooling under `bench/oracle`, with no produc (brief `/home/mmarrujo/.hobbes/sessions/S-20260912T204447Z-9396/brief.md`, task sha256 `8cf8f1bf9865`)
- **Parent:** `1f2baf17404b` of `hobbes_public` — the working tree had uncommitted changes the session did not see
- **Doer:** 2.1.270 (Claude Code); model the default; turns 127 of 150; result `success`; reported cost $6.5341 (the envelope's figure, on the subscription); wall 1338.0 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `static.rust-lang.org:443`×3
- **Policy:** 36 exec decision(s) — `allow`×32, `escalate`×4; escalated: `/bin/sh -c which clang cmake bear make gcc 2>&1; clang --version 2>&1 | head -3`; `/bin/sh -c which clang cmake bear make gcc 2>&1; clang --version 2>&1 | head -3 → expired`; `/bin/sh -c for c in clang cmake bear make gcc; do command -v $c >/dev/null 2>&1 && echo "$c: yes" || echo "$c: no"; done; clang --version | head -1`; `/bin/sh -c for c in clang cmake bear make gcc; do command -v $c >/dev/null 2>&1 && echo "$c: yes" || echo "$c: no"; done; clang --version | head -1 → expired`
- **Branch:** `hobbes/S-20260912T204447Z-9396`, 2 commit(s); files: `bench/oracle/README.md`, `bench/oracle/cmd/oracle/main.go`, `bench/oracle/internal/clang/clang.go`, `bench/oracle/internal/clang/compdb.go`, `bench/oracle/internal/clang/compdb_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/dump_test.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/merge_test.go`, `bench/oracle/internal/clang/probe_test.go`, `bench/oracle/internal/clang/run.go`, `bench/oracle/internal/clang/run_test.go`, `bench/oracle/internal/contain/contain.go`, `bench/oracle/internal/contain/contain_test.go`, `bench/oracle/internal/grade/trace_test.go`, `bench/oracle/run-cell.sh`
- **Gate:** **clear** at `1f2baf17404b` (gate v2, grounder v3, record `8fc00d300e2b`); unknown 0; map over 17 file(s), 12.1% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 49, regressions 0; {'P2P': 29, 'new-pass': 14, 'skip': 6}; build {'P2P': 2}

**The doer's closing words:**

> Both commits are in. Summary:
> 
> **Part 1 — four defects fixed in `internal/clang` (unit A):**
> 1. clang's pseudo-buffers (`<scratch space>`, `<built-in>`, `<command line>`) now tracked via a `Pseudo` field on `rawPos`; a chosen macro position that lands in one falls back to the expansion, mode `macro`.
> 2. `peelCallee` now threads `fn` through and records a nested `CallExpr` (a callee that is itself a call, e.g. `get_fn()(2)`) as its own site instead of silently consuming it; site identity (`siteKey`) now also keys on mode and callee name so the two sites sharing one position stay distinct.
> 3. `resolveCall`'s static branch now falls back to `external` when a static function has no in-repo definition *and* no in-repo declaration (e.g. a header's `static inline`), distinct from `undefined`.
> 4. Verified `grade.go`'s resolution-only `coverage:` line was already correctly gated behind the trace early return; locked it with a regression test.
> 
> New `testdata/cclang-probe`-driven test plus focused unit tests for each rule; `testdata/cclang`'s existing truth is unchanged.
> 
> **Part 2 — contained run and CLI:**
> - `internal/clang/compdb.go`: ADR-109's database-source search (carried/CMake/Make/none) and the argv filter/shell-splitter.
> - `internal/clang/run.go`: `RunUnits` (derives the database, runs clang per unit, writes shards + `roots.txt`) and `Run` (the host-side wrapper that drives it through a new `contain` profile `c-clang`, then merges).
> - New `oracle c-clang` / internal `oracle c-clang-units` subcommands wired into `cmd/oracle/main.go`.
> - `run-cell.sh` gained `--lang c` and now always builds `oracle` with `CGO_ENABLED=0` (needed since the binary is mounted read-only into the container).
> - `README.md` documents the new oracle, its conventions, and the fixtures.
> 
> All hermetic tests plus a from-real-toolchain (`clang`/`cmake`/`bear`/`make`, all present in this session) `RunUnits` suite pass; the full contained end-to-end test correctly skips here (no podman) and I manually verified the whole pipeline once via the `HOBBES_UNCONTAINED=1` escape hatch, reproducing the fixture's exact coverage counts and file list. Ran `go vet ./...`, `gofmt -l .` (clean) and `go test ./...` (all green) from `/work/bench/oracle` before each commit.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear — review found no defect in the diff.
- outcome: merged — fast-forward to `055d3fe`, two commits (`0b0f820`, the fixes to unit A; `055d3fe`, the contained run and CLI), keeping the doer's `hobbes-dispatch` authorship.
- notes:
  - **Checked on the host,** with the image present: `go vet` clean, `gofmt -l` empty, and `go test ./...` passes in every package. `TestOracleCClangEndToEnd` runs the oracle in the image on `testdata/cclang` and passes (0.8 s), so the fixture truth holds on the contained path too.
  - **Part 1.** Fixes 1–3 are as specified and pinned by the probe.
    - For fix 4, the doer found that `Print` returns before the new line for a trace report (`grade.go`, the `r.Kind == "trace"` branch), so trace reports were never changed. It added a test that holds this.
    - The developer's defect 4 in `S-20260912T201059Z-5d5f` was wrong, and that file's note is corrected.
  - **Egress:** three refusals, `static.rust-lang.org:443`, one at each `go test ./...` (20:50:15, 20:54:51, 21:06:33): the Rust fixture test's rustup. Refused as built; nothing needed.
  - **Policy:** four escalations, all expired, from two compound probes (`which clang cmake bear make gcc; clang --version`). `which` is allowed, but `clang --version` matches no rule. They are read-only. **For Max:** the box's toolchain probes could name `clang --version`, `cmake --version` and `bear --version` beside `go version`.
  - **Pace:** first edit at 6.5 minutes (the developer's watcher); 127 of 150 turns in 1,338 s.
  - **A defect found at first use, after the merge,** outside every gate class, fixed by the developer.
    - The first real cell (`minic`) refused with podman's `duplicate mount destination`. `run-cell.sh` builds `oracle` into the cell dir, which the contained step mounts rw, and `Run` also mounted the binary's own directory ro: one path, two mounts.
    - The end-to-end test built its binary in another temp dir, so it never met the layout.
    - **Fixed in `contain.New`**, for every oracle: a ro path a rw mount already exposes is dropped. `TestNewDropsReadOnlyUnderReadWrite` holds it, and the end-to-end test now builds its binary inside the cell dir, as `run-cell.sh` does.
    - After the fix, `minic` graded 5/5 confirmed, 0 contradicted, recall 7/7.
  - Reviewed by Claude (the developer session), 2026-09-12.
