# Harness session `S-20260916T225041Z-d95c`

*2026-09-16 · `hobbes dispatch` v1 · Hobbes 0.2.32-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make lane A's C++ walk record **no call site inside an unevaluated operand**, and stop it recording `noexcept` and `typeid` as callees. The decision is ADR-121 (`docs/adr/121-no-call-in-an-unevaluated (brief `/home/mmarrujo/.hobbes/sessions/S-20260916T225041Z-d95c/brief.md`, task sha256 `ad50b23721a6`)
- **Parent:** `383946075eb4` of `hobbes_public`
- **Doer:** 2.1.273 (Claude Code); model claude-opus-5; turns 32 of 100; result `success`; reported cost $2.0793 (the envelope's figure, on the subscription); wall 291.2 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 10 exec decision(s) — `allow`×10; records: stream opened→closed
- **Edits:** 8 edit(s) to 4 file(s); first at 0.9 min after launch; files: `/tmp/probe.py`, `/tmp/probe2.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/tests/test_cppsource.py`
- **Branch:** `hobbes/S-20260916T225041Z-d95c`, 1 commit(s); files: `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/tests/test_cppsource.py`
- **Gate:** **clear** at `383946075eb4` (gate v2, grounder v4, record `e6fb55e32b02`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 176, regressions 0; {'skip': 5, 'P2P': 166, 'new-pass': 5}

**The doer's closing words:**

> Done, committed as `d87cbea`.
> 
> **What changed** — `pipeline/src/hobbes/extract/cppsource.py` only, ADR-121 §1:
> 
> - Two constants beside `_NAMED_CASTS`: `_UNEVALUATED_OPERANDS` (`sizeof_expression`, `alignof_expression`, `decltype`, `requires_expression`) and `_KEYWORD_CALLEES` (`noexcept`, `typeid`), each with a `#:` comment.
> - `_unevaluated(node)` (`cppsource.py:785`): an unbounded walk of `node.parent` to the root — true on any ancestor in the first set, or a `call_expression` whose `function` child is the identifier `noexcept`. `typeid` is deliberately not in the walk, and the docstring says why ([expr.typeid]/3).
> - `_calls` (`cppsource.py:829`): the loop now selects the three node types up front and drops the whole node once, in one place, when `_unevaluated` holds — so plain calls, `new_expression` and the construction sites all abstain together. A `call_expression` whose terminal is a keyword callee records no site, on the named casts' `continue` (the terminal's text is now read once into `name`).
> - The docstring's **Call sites** paragraph gains the two sentences, citing C-155 and ADR-121 §1.
> 
> **Tests** — five new cases in `TestCallSites`, each on a `tmp_path` repo via `_write`: every unevaluated shape (all nine, one per line) records nothing while the plain `f(2)` and the `f(3)` sharing a line with a `sizeof` both keep their sites; `noexcept`/`typeid` are keywords, with `typeid`'s operand keeping `f`; `static_assert(g(), "")` and a `noexcept` specifier's own condition keep theirs; `decltype(A(1))` and `decltype(new A(1))` record nothing beside a real `A a(2)`; and, through `extract_repo` with a hand-built lane B on a two-file repo, the occurrence at a dropped site lands as one `uses` edge at `semantic` tier and no `calls` edge. No existing test moved.
> 
> The two-file repo needed no Makefile or other scaffolding: the header is claimed by C++ under rule (a) (no `.c` sources) and `#include "lib.h"` resolves same-directory.
> 
> **What I ran** (from `/work/pipeline`, that tree's own interpreter):
> - `.venv/bin/python3 -m pytest tests/test_cppsource.py tests/test_tail.py tests/test_emit.py -q` → **150 passed**.
> - `.venv/bin/python3 -m pytest -q` (whole pipeline suite) → **1643 passed, 10 skipped**.
> 
> **Deviations** — none.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Merged no-ff as `86b826a`; the doer's `d87cbea` keeps its
  `hobbes-dispatch` authorship. C-155's lift, decided in ADR-121 after the
  grammar, both C++ cells and clang's own dump were measured (the dump
  keeps a call under `sizeof`/`noexcept`/`typeid` and not under
  `decltype`, so the oracle carries half the defect: H-32, the next unit).

  **The diff is the ADR, line for line.** One predicate (`_unevaluated`,
  an unbounded walk to the root over four node types and the
  `noexcept(..)` call shape), one drop point where the three site
  branches meet, the keyword callees on the named casts' `continue`, and
  `typeid`'s operand kept with the standard's reason in the docstring.
  Nothing in the join, the tail or the IR moved, as §2 requires.

  **The tests cover every row of the ADR's grammar table** — nine
  unevaluated shapes on nine lines, two evaluated controls (one sharing
  a line with a `sizeof`), the keywords, the constant expressions that
  stay, a construction and a `new` under `decltype`, and the ingest-level
  `uses`-not-`calls` check with a hand-built lane B on a two-file repo,
  which needed no scaffolding (the header is claimed under rule (a)).

  **Verified on the host before merging:** the branch's own run in the
  sandbox was 1,643 passed; on `main` after the merge the whole pipeline
  suite is 1,652 passed and one red — `test_calvin_tracker`'s drift test,
  which is this file's own review block being unfilled, closed by this
  render. The regrade against fmt's and args' standing keys follows
  (§10.10, P52–P56).
