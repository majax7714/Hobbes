# Harness session `S-20260925T161239Z-66c5`

*2026-09-25 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E1's runner, part 2: the run loop, the report, and the Modal batch script (brief `/home/mmarrujo/.hobbes/sessions/S-20260925T161239Z-66c5/brief.md`, task sha256 `0a2c0d3a9530`)
- **Parent:** `0901f68dad7e` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 71 of 140; result `success`; reported cost $8.9373 (the envelope's figure, on the subscription); wall 1135.1 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 25 exec decision(s) — `allow`×23, `escalate`×2; escalated: `/bin/sh -c cd bench/calvin/lattice && .venv/bin/python -m pytest -q 2>&1 | tail -5 && git -C /work status --porcelain`; `/bin/sh -c cd bench/calvin/lattice && .venv/bin/python -m pytest -q 2>&1 | tail -5 && git -C /work status --porcelain → expired`; records: stream opened→closed
- **Edits:** 29 edit(s) to 8 file(s); first at 5.7 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/scripts/modal_e1.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/e1.py`, `bench/calvin/lattice/src/lattice/report.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_e1.py`, `bench/calvin/lattice/tests/test_report.py`
- **Branch:** `hobbes/S-20260925T161239Z-66c5`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/scripts/modal_e1.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/e1.py`, `bench/calvin/lattice/src/lattice/report.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_e1.py`, `bench/calvin/lattice/tests/test_report.py`
- **Gate:** **blocked** at `0901f68dad7e` (gate v2, grounder v4, record `871ad9df0c2b`) — blocking: invented; unknown 0; map over 43 file(s), 4.2% of their lines uncaptured; partition checked
  - `invented` bench/calvin/lattice/tests/test_e1.py:422 `needs_clang`
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 80, regressions 0; {'P2P': 31, 'new-pass': 49}

**The doer's closing words:**

> Done. Committed as `9ebb027` on `hobbes/S-20260925T161239Z-66c5`.
> 
> **What I changed** (the eight files the unit allows, nothing else):
> 
> - **`src/lattice/e1.py`** (new) — `plan` (one chat request per cell×arm×sample, greedy plus *k* drawn, SHA-256 seeds, plus one G-mem probe per cell), `meta`/`write_plan`, the `run` loop with resume off `rows.jsonl`, the iterate rounds for `C-0`/`C-3`, the pre-call ceiling, `default_grade` (one `lattice grade` in the image per round), `replay_generator` and `modal_generator`. Named refusal types: `CeilingReached`, `TargetMoved`, `MissingCompletion`, `GenerateFailed`, `GradeFailed`.
> - **`src/lattice/report.py`** (new) — pass@1, pass@1(sampled), pass@k, per-round cumulative rates; by ISA and by type with a `low-bit` row; class counts, invented names by bucket, G-reg over what compiled, the G-mem split; wrappers a section apart; totals from `calls.jsonl`; a text table.
> - **`scripts/modal_e1.py`** (new) — the `uv run` batch generator: app `hobbes-e1`, vLLM 0.27.1, both 7Bs pinned to A10G/16k, `hobbes-hf-cache`, `llm.chat` and `llm.generate`, a local entrypoint writing completions plus a call record. Written, not run.
> - **`src/lattice/cli.py`** — `lattice e1 plan|run|report`; `--ceiling-usd` required with no default; `--generator modal|replay:<file>`.
> - **`README.md`** — sections for `e1`, `report` and the Modal script, and E1's order of work (plan → E1-g's first unit → price it against the estimate → Max's word → the rest).
> - **`tests/test_e1.py`**, **`tests/test_report.py`** (new) and additions to **`tests/test_cli.py`** — 49 new tests.
> 
> **Two deviations worth your eye:**
> - `generate` returns a mapping (`{"completions", "cost", "seconds"}`), not a bare list — a list has nowhere to carry "the wall seconds inside the function" or the cost the ceiling reads. A bare list is still accepted, and its call is then costed from the estimate with `cost_source: "estimated"`.
> - `run`'s `target` argument would otherwise have been unused, so it is now load-bearing: it is checked against `meta.json`'s `target_sha` and a moved target is `TargetMoved` before anything is sent.
> 
> **What I ran:** `cd bench/calvin/lattice && .venv/bin/python -m pytest -q` → **360 passed**, none skipped (this box has clang, so the end-to-end grade and the `lane`-style tests all ran). No model was called, no Modal run, nothing spent.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: false-block
- outcome: merged
- notes: ADR-151 E1's runner, part 2 (routes E1-d to E1-g): `e1.py` (the plan, the loop, resume, the iterate rounds,
  the ceiling), `report.py`, and `scripts/modal_e1.py`, written and not run. 71 turns of 140. **The block is the known
  decorator false block, the third time** (`@needs_clang`, a module-level `pytest.mark.skipif`; sessions `9326` and
  `c141`). The doer's two departures are accepted: `generate` returns a mapping that carries the call's cost and seconds,
  and `run` checks the target's SHA against the plan (`TargetMoved`).
  **The review found two defects and fixed them (`8e354c9`), with tests.**
  1. `modal_generator` kept the completions in a temporary directory, and rows were written only after grading. So a
     grade that failed in the image threw away a paid round, and a resume bought it again. Now the completions go to
     `completions.jsonl`, and the call's cost to `calls.jsonl`, before anything is graded, and a resume answers from them.
  2. A body the grader did not answer was written with class `None` and retried as a failure. It is `GradeFailed` now.

  The Modal script also priced a call on the function's own seconds, leaving out the container's boot. It is now priced
  on the host's wall around the remote call, an upper bound.
  **The real-target check, run on the host with the grader in the image and no spend:** `lattice e1 plan` over
  E1-g's first unit (Qwen, the 31 `avx2` cells, 5 arms, k = 5, the ledger) made 961 requests, 930 chat and 31 G-mem.
  Round 0 is estimated at $0.73, an upper bound at full `max_tokens` with no prefix caching. A scripted replay was then
  graded for real in 2 min 13 s, over 1,395 rows in rounds 0 to 2. Every class came out as scripted:
  - gold: `pass`;
  - no fence: `no-body` at round 0, then `pass` at round 1;
  - E0's mutants: `invented`, `compile`, `edge`, and C-3's `wrong` → `edge` → `pass` at round 2.

  The report splits wrappers apart and has the `low-bit` row and the round lines. Host suite: 341 passed, 21 skipped
  (no clang on the host).
