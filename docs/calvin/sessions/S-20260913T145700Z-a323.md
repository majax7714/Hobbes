# Harness session `S-20260913T145700Z-a323`

*2026-09-13 · `hobbes dispatch` v1 · Hobbes 0.2.9-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Lift C-139: Go lane A's fallback must not resolve a qualified call through an import when a local binding of the qualifier's name shadows it. The design is ADR-046's amendment of 2026-09-13 ("the qual (brief `/home/mmarrujo/.hobbes/sessions/S-20260913T145700Z-a323/brief.md`, task sha256 `2a5ab07fab53`)
- **Parent:** `e1c9f45e753d` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 28 of 150; result `success`; reported cost $0.7055 (the envelope's figure, on the subscription); wall 182.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 6 exec decision(s) — `allow`×6
- **Edits:** 4 edit(s) to 2 file(s); first at 0.3 min after launch; files: `pipeline/src/hobbes/extract/gosource.py`, `pipeline/tests/test_gosource.py`
- **Branch:** `hobbes/S-20260913T145700Z-a323`, 1 commit(s); files: `pipeline/src/hobbes/extract/gosource.py`, `pipeline/tests/test_gosource.py`
- **Gate:** **clear** at `e1c9f45e753d` (gate v2, grounder v3, record `b325e6d52e8d`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 213, regressions 0; {'skip': 4, 'P2P': 203, 'new-pass': 6}

**The doer's closing words:**

> Committed as `c54e6ee`.
> 
> **Changed:** `pipeline/src/hobbes/extract/gosource.py` — `_call_fallback`'s qualified branch (`elif call["receiver"] in by_alias`) now also requires `not _shadowed(parsed, call["receiver"], call["line"])`; a shadowed qualified call falls to the existing `else: continue`. Updated the docstrings on `_call_fallback` and `_shadowed` to describe the qualifier case and cite ADR-046's amendment/C-139. `tail.py` untouched, per the amendment.
> 
> `pipeline/tests/test_gosource.py` — added `TestQualifierShadow` (sibling to `TestFallbackScopeVeto`) covering: the dagger shape, a parameter shadow, an unshadowed qualified call still resolving, a sibling function's local not reaching across, the declaring statement's own call also being shadowed (named to force a deliberate change on refinement), and the shadowed site classing `attr-call` via `tail.classify`.
> 
> **Checked:** `pipeline/.venv/bin/python3 -m pytest tests/test_gosource.py tests/test_tail.py -q` → 107 passed. Full `pipeline` suite: 1468 passed, 3 failed (`test_ttt_units.py`, unrelated to this change — confirmed failing identically on `HEAD` before the edit, a pre-existing sandbox/git-log environment issue), 9 skipped.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read the diff (2026-09-13). It is the amendment's one condition (`and not _shadowed(parsed, call["receiver"], call["line"])`) and the two docstrings, and the six tests cover the brief, including the stated over-approximation, which is pinned by name. **ADR-046's acceptance gate held:** all 27 Go cells with a stored key were re-ingested contained from a worktree of this branch and graded against their keys (`~/.hobbes/bench/c139-post/`). Confirmed, contradicted and syntactic-edge counts matched the 0.2.8-beta reports in every cell, with 0 falsely confirmed poison. Host pytest on the branch: 1,480 passed. The doer's three `test_ttt_units` failures are D-r, the sandbox's `--shared` clone. The `llm` egress refusal is the suite's own request. On dagger with lane A alone, 95 fallback resolutions into `engine/slog/` are gone and none are new, and dagger's 56 external vetoes read 0: the lift removes those sites at the source. Merged with a merge commit; released as 0.2.10-beta.
