# Harness session `S-20260914T010846Z-de81`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.13-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build the launcher side of ADR-112: a session's records leave the doer's container. The design is `docs/adr/112-session-records-leave-the-doers-container.md`; read it first. It is decided. Follow it;  (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T010846Z-de81/brief.md`, task sha256 `77d68c3dace8`)
- **Parent:** `6ad5567c57bb` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 94 of 150; result `success`; reported cost $6.6949 (the envelope's figure, on the subscription); wall 1230.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×5; refused `llm`×1
- **Policy:** 16 exec decision(s) — `allow`×16
- **Edits:** 47 edit(s) to 7 file(s); first at 5.9 min after launch; files: `go/cmd/hobbes-session/main.go`, `go/cmd/hobbes-session/main_test.go`, `go/internal/sandbox/sandbox.go`, `go/internal/sandbox/sandbox_test.go`, `pipeline/src/hobbes/run/dispatch.py`, `pipeline/tests/test_dispatch.py`, `sandbox/exitcheck.py`
- **Branch:** `hobbes/S-20260914T010846Z-de81`, 1 commit(s); files: `go/cmd/hobbes-session/main.go`, `go/cmd/hobbes-session/main_test.go`, `go/internal/sandbox/sandbox.go`, `go/internal/sandbox/sandbox_test.go`, `pipeline/src/hobbes/run/dispatch.py`, `pipeline/tests/test_dispatch.py`, `sandbox/exitcheck.py`
- **Gate:** **clear** at `6ad5567c57bb` (gate v2, grounder v3, record `1219d5e47d74`); unknown 0; map over 7 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 96, regressions 0; {'P2P': 79, 'skip': 3, 'new-pass': 11, 'removed': 3}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `bbf3a8b` on branch `hobbes/S-20260914T010846Z-de81`.
> 
> ## Summary
> 
> Built unit B of ADR-112 — the launcher, dispatch, and exit-check side of moving a session's records out of the doer's container:
> 
> - **`go/internal/sandbox/sandbox.go`**: `NewPlan` now resolves two worlds from `Config.Network` — the sidecar world (default, `Network == ""`, refuses `--runtime`) puts the session on `hobbes-int-<id>` beside a `hobbes-side-<id>` sidecar (tmpfs HOME, only `in/` mounted read-only, MCP config points `--sink` at the sidecar), and the file world (explicit `--network`) keeps the old rw-mounted plan with `--log-dir`/`--log`. Renamed `EgressProxyName/EgressSetup/EgressTeardown` → `SidecarName/SidecarSetup/SidecarTeardown`; added `InDir`/`InDirHostPath`/`FlightLogHostPath`/`SidecarEnabled`; `PurgeDoerState`'s doc now says it only matters in the file world.
> - **`go/cmd/hobbes-session/main.go`**: creates `in/` before writing the MCP config/settings; `startSidecar` (was `startEgress`) waits on the flight log's `"listening"` line instead of the old egress-only wait; retention purge only runs in the file world; usage/refusal text updated.
> - **`pipeline/src/hobbes/run/dispatch.py`**: `summarize_flight` splits out the sink's own lines into a `stream` bracket; `render_log`'s Policy line reports it; `_cleanup_route` targets `hobbes-side-<id>`; `SCAN_SKIP` drops `go-build` (GOCACHE now dies with the tmpfs).
> - **`sandbox/exitcheck.py`**: driver copied to and run from `in/`.
> 
> Checked: `cd go && go test ./...` (all 15 packages pass; live podman tests skip in this sandbox) and `cd pipeline && python3 -m pytest tests/test_dispatch.py tests/test_harness.py` (19 + 27 passed), plus the full pipeline suite (1499 passed, 9 skipped) for confidence. `gofmt -l` and `go vet` are clean.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-112's unit B, read against the brief and the ADR. The two worlds are as specified: the sidecar world by default (the session's internal network, `hobbes-side-<id>` as the only writer of the records, HOME a tmpfs, `in/` the one read-only host dir, the MCP config and the hook on `--sink`, the launcher waiting for the sink's `listening` line and then the egress `listen` record), and the file world on an explicit `--network`, which keeps the old plan and says so in one line naming C-140; the owned runtime refuses the sidecar world. `dispatch` reports the stream's bracket on its Policy line and cleans up `hobbes-side-<id>`; the exit check's driver moved to `in/`. Host `go test ./...` on the branch, live tests included: every package green but `TestALiveSessionMountsOnlyItsOwnSessionDir`, which still hands the launcher the fake proxy binary — a sidecar made of it never listens — where the other two live tests build the real one; verify could not see that because the live tests skip in the sandbox, and the doer could not run it. Fixed by the developer in the commit after the merge (tests only). The one egress refusal is a plain GET to the host `llm`, a test's fake endpoint reached through the session's proxy variables when the doer ran the Python suite; the refusal worked and nothing else was asked for.
