# Harness session `S-20260917T130613Z-4338`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.35-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Give every `hobbes lanes` site disagreement a **shape** when a registered limit explains it by a checkable rule, and make the command **exit 3** when every disagreement has one. The decision is ADR-12 (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T130613Z-4338/brief.md`, task sha256 `51dd9a4d28de`)
- **Parent:** `4553a5a40190` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 41 of 120; result `success`; reported cost $2.3673 (the envelope's figure, on the subscription); wall 300.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 6 exec decision(s) — `allow`×6; records: stream opened→closed
- **Edits:** 13 edit(s) to 7 file(s); first at 1.5 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_scipsource.py`, `scripts/ci-graph.sh`
- **Branch:** `hobbes/S-20260917T130613Z-4338`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_scipsource.py`, `scripts/ci-graph.sh`
- **Gate:** **clear** at `4553a5a40190` (gate v2, grounder v4, record `800e6a19d0b9`); unknown 0; map over 8 file(s), 1.2% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1096, regressions 0; {'skip': 16, 'P2P': 1069, 'new-pass': 10, 'uncollected': 1}

**The doer's closing words:**

> Done — committed as `2f4eb74` on `hobbes/S-20260917T130613Z-4338`.
> 
> **What changed**
> 
> - `pipeline/src/hobbes/extract/evidence.py` — `disagreement_shapes(syntax, semantic, fallback, disagreements, withhold)`, one entry per row in row order: `same-line-pair` (C-70) when the `(file, line, name)` key holds two or more non-ambiguous call sites and lane A's guess is `match_resolution`'s answer at another of them; else `cpp-withheld` (C-152) when the row's file is in `withhold`; else `None`. Two constants (`SAME_LINE_PAIR`, `CPP_WITHHELD`).
> - `pipeline/src/hobbes/extract/__init__.py` — `_lane_agreement` gains `cpp_site_files` (empty default), puts `"shape"` on each `site_disagreements` row, and adds `"cpp_sites_compared"` / `"cpp_guess_drawn"` via a new `_cpp_site_counts` helper (`agreement`'s predicate restricted to C++ layer files; the drawn count is a C++ file outside `withhold`, no in-repo resolution, a fallback answer). `sites_compared`, row order and the module comparison are untouched; the ingest passes `cpp_site_files` through.
> - `pipeline/src/hobbes/cli.py` — the summary line's split (`— U unexplained, S same-line-pair (C-70), …`, non-zero shapes only), unexplained rows listed first with shaped rows tagged `[shape]` under the same 20-row cap, the C-152 rate line when any `cpp-withheld` row exists, `_lanes_exit` (0/1/3, same for `--json`), the exit-3 sentence, and `_has_unexplained` beside an unchanged `_has_disagreement`.
> - `scripts/ci-graph.sh` — the lanes step catches the status under `set -euo pipefail`: 3 passes and prints a line, 1 and 2 still fail.
> 
> **Tests** — `TestDisagreementShapes` in `test_evidence.py` (the C-70 swap; the honesty case that a same-named line whose guess matches neither sibling stays `None`; withheld vs. the same site outside `withhold`; the rule order); four cases under `TestLanes` in `test_cli.py` (all-shaped → 3 with the sentence and counts, mixed → 1 with the unexplained row first, a row with no `shape` key → 1, `--json` → 3); `TestCppGuessDrawn` in `test_scipsource.py`.
> 
> **Checked** — `/work/pipeline/.venv/bin/python3 -m pytest tests/test_evidence.py tests/test_cli.py tests/test_scipsource.py tests/test_cppsource.py -q` → **278 passed, 2 skipped**; the whole pipeline suite → **1672 passed, 11 skipped** (the `lane_b` ones skip in a dispatch); `bash -n scripts/ci-graph.sh` clean; `hobbes lanes` on this repo's own graph → 10533 sites compared, **0 disagree, exit 0**, as ADR-123's prediction says.
> 
> Deviations: none.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read in full against ADR-123. Rules, order, exit codes and the CI step are as decided; the honesty case (a same-named line whose guess matches no sibling stays unexplained) is tested. Host: `lane_b` 7 passed, full pytest 1,682 passed (the tracker's drift test red until this block, as usual). Two gaps the brief left open, closed by the developer on top: `cpp_guess_drawn` counted sites the external veto (ADR-111) had already dropped, and the C++ rate's numerator counted `cpp-withheld` rows only, so a C++ row shaped `same-line-pair` fell out of it.
