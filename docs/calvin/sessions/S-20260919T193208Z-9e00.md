# Harness session `S-20260919T193208Z-9e00`

*2026-09-19 · `hobbes dispatch` v1 · Hobbes 0.2.53-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Fix oracle defect **H-33**: the TypeScript oracle never labels a call's target `parameter`, so every call through a parameter is graded `static→closure` instead of `func-value→parameter`. The defect's (brief `/home/mmarrujo/.hobbes/sessions/S-20260919T193208Z-9e00/brief.md`, task sha256 `8e1ac13734ca`)
- **Parent:** `22ef2484508c` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 35 of 80; result `success`; reported cost $1.7081 (the envelope's figure, on the subscription); wall 177.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×1
- **Policy:** 18 exec decision(s) — `allow`×10, `escalate`×8; escalated: `/bin/sh -c cd /work/bench/oracle && node ts/tsc-oracle.mjs --repo internal/grade/../../testdata 2>&1 | head -5; ls /work/bench/oracle/testdata | head`; `/bin/sh -c cd /work/bench/oracle && node ts/tsc-oracle.mjs --repo internal/grade/../../testdata 2>&1 | head -5; ls /work/bench/oracle/testdata | head → expired`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.json`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ../../pipeline/tests/fixtures/minits --zone . --out /tmp/minits.json → expired`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go && gofmt -l . && go vet ./... && go test ./internal/grade/ -count=1 -run 'Minijs|NoTsconfig|Minits|TS' -v 2>&1`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go && gofmt -l . && go vet ./... && go test ./internal/grade/ -count=1 -run 'Minijs|NoTsconfig|Minits|TS' -v 2>&1 → expired`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go → expired`; records: stream opened→closed
- **Edits:** 9 edit(s) to 3 file(s); first at 0.5 min after launch; files: `bench/oracle/README.md`, `bench/oracle/internal/grade/minijs_test.go`, `bench/oracle/ts/tsc-oracle.mjs`
- **Branch:** `hobbes/S-20260919T193208Z-9e00`, 1 commit(s); files: `bench/oracle/README.md`, `bench/oracle/internal/grade/minijs_test.go`, `bench/oracle/ts/tsc-oracle.mjs`
- **Gate:** **clear** at `22ef2484508c` (gate v2, grounder v4, record `23fca06540b8`); unknown 0; map over 3 file(s), 44.7% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 29, regressions 0; {'P2P': 23, 'skip': 5, 'new-pass': 1}; build {'P2P': 2}

**The doer's closing words:**

> **What changed** (three files, nothing else):
> 
> - `bench/oracle/ts/tsc-oracle.mjs` — `declKind` asks `ts.isParameter(decl)` first; the now-reachable line moved up from further down (the old occurrence removed, so it is one test in one place), with a three-line comment saying why the ordering matters (H-33). `isClosure`, `isClosureScope`, `target()` and the site walk are untouched, so a destructured parameter's `BindingElement`s still go through the binding branch as before.
> - `bench/oracle/internal/grade/minijs_test.go` — `lib/apply.js:5` now asserts the hand's row (`dynamic lib/apply.js:4`) and its `tsc's reading, not the hand's` comment is gone; the binding check also asserts `Kind == "parameter"`, `Closure == false` and `missClass(s, tg) == "func-value→parameter"`. New `TestMinitsParameterBindingsAreDynamic` runs the oracle on `minits` (`--zone .`, no flag) and asserts every `Via == "binding"`, `Kind == "parameter"` target's site is `dynamic`.
> - `bench/oracle/README.md` — one sentence in the mode/kind paragraph: until H-33's fix (2026-09-19) the kind test asked the closure question first, so earlier TS cells' `static→closure` counts include calls through parameters.
> 
> **Counts.** `go test ./internal/grade/ -count=1 -run 'Minijs|NoTsconfig|Minits|TS' -v`: 6 tests RUN, none skipped, all PASS (`TestMinitsTSAllConfirmed`, `TestMinijsNoTsconfigResolvesTheJavaScriptShapes`, `TestMinijsNoTsconfigSaysSo`, `TestNoTsconfigRefusals` + its 3 subtests, `TestMinitsParameterBindingsAreDynamic`, `TestTSFixtureRefusesEveryPoisonedEdge`). Then `go test ./... -count=1` — 11 packages ok, 4 with no test files, 0 fail; `go vet ./...` and `gofmt -l .` clean. `TestMinitsTSAllConfirmed`'s assertions were not touched and did not move (`Confirmed: 5`, recall 5/8, `static→function` 1, `static→method` 2).
> 
> **The minits parameter-site count: 0.** Measured before asserting — minits holds no binding target of *any* kind (I counted `Via == "binding"` by kind: the map was empty), so it has no call through an anonymous signature at all. The case therefore asserts 0, with the reason in its comment; the JavaScript fixture carries the shape itself.
> 
> **Deviations**
> 
> - Moved the existing `if (ts.isParameter(decl)) return "parameter";` to the top of `declKind` rather than adding a second copy, so the kind is decided in one place.
> - Re-aligned the `want` map's keys in `minijs_test.go`: removing the comment merged two gofmt alignment groups, and `gofmt -l` is in the done-when. (`gofmt -w` is denied by policy here, so the alignment was applied by hand and checked with `gofmt -l`/`-d`.)
> 
> **One thing I did not fix, as instructed to report rather than change:** the destructured-parameter case looks wrong in the same way. A `BindingElement` whose parent chain is a parameter goes to `isClosureScope(decl)` and reads `local-binding` (or `closure`), so `function f({ cb })` calling `cb()` is graded `func-value→local-binding` — the mode is right, the kind names a local rather than the parameter it is part of. It is a smaller misreading than H-33 (the site is already `dynamic`), and it would need a decision about whether a binding element's kind should follow its root declaration; I left it alone.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: H-33's fix. Read whole: the existing `isParameter` line moved to the top of `declKind`, one place; `target()` and the site walk untouched. On the host in a worktree: the six TS tests RUN and PASS, the oracle module green, vet and gofmt clean. The unit measured minits' parameter-binding sites before asserting (0) instead of inventing a fixture. **The regrade after the merge, contained, on the five TS cells' stored exports** (`~/.hobbes/bench/h33-regrade/`; the before pass reproduced every stored grade exactly): totals and every graded row identical on all five; 383 of 1,095 `static→closure` pairs move to `func-value→parameter` (kbet 38, ajv 23, cheerio 65, zod 117, hono 140). The destructured-parameter naming the unit raised (`func-value→local-binding` for `({ cb }) => cb()`) is a kind name, not a mode error; left as it is. Both deviations change less and are kept.
