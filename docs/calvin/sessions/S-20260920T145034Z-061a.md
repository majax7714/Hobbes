# Harness session `S-20260920T145034Z-061a`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.58-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-143: a call whose site name is not its definition's is matched at the site's own column, where both lanes name the same definition (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T145034Z-061a/brief.md`, task sha256 `6102d624555c`)
- **Parent:** `ed697bb12a1d` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 71 of 140; result `success`; reported cost $5.8712 (the envelope's figure, on the subscription); wall 653.4 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 25 exec decision(s) — `allow`×23, `escalate`×2; escalated: `/bin/sh -c find pipeline/tests/fixtures/minicjs -type f | head -50; echo ---; for f in $(find pipeline/tests/fixtures/minicjs -name '*.js' -o -name '*.json'); d`; `/bin/sh -c find pipeline/tests/fixtures/minicjs -type f | head -50; echo ---; for f in $(find pipeline/tests/fixtures/minicjs -name '*.js' -o -name '*.json'); d → expired`; records: stream opened→closed
- **Edits:** 25 edit(s) to 4 file(s); first at 2.7 min after launch; files: `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_tsjs_reexport.py`
- **Branch:** `hobbes/S-20260920T145034Z-061a`, 1 commit(s); files: `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_tsjs_reexport.py`
- **Gate:** **clear** at `ed697bb12a1d` (gate v2, grounder v4, record `740e13f89bd3`); unknown 0; map over 7 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1240, regressions 0; {'skip': 19, 'P2P': 1203, 'new-pass': 17, 'uncollected': 1}

**The doer's closing words:**

> **What I changed** (commit `5aa6e80`, branch `hobbes/S-20260920T145034Z-061a`)
> 
> - `pipeline/src/hobbes/extract/evidence.py` — new documented `match_at_own_column(site, buckets, fallback)`: for a **call** site the by-name match missed, with a column and no ADR-104 abstention, it answers the resolution at the site's own column when lane A's fallback resolved the site and **every** resolution at that column names that same `(def_file, def_line)`. `join` tries it second, so such a site is the hit the by-name match would have been (`calls`, semantic, both lanes, claimed by position, `qualifier`/`argc` riding); nothing else moves — import sites, columnless sites, abstentions, a column one off and a column naming another definition all take today's path. `join`'s docstring gained the rule's paragraph (what, why it draws nothing new, ADR-143).
> - The mirrors read that same function: `external_vetoes`, `withheld_fallbacks`, `agreement`, `disagreement_shapes` (documented as unreachable-but-identical: siblings share a name, so a rule-matched sibling never disagrees), and `_dispositions` — which now takes the fallback its callers already hold, threaded through `coverage` and `unresolved_sites` and passed at both call sites in `extract/__init__.py` (the whole fallback, not `tail_fallback`). The C++ comparison at `extract/__init__.py:1141` counts such a site `compared`, not a drawn guess.
> - `pipeline/tests/test_evidence.py` — `TestARenamedCalleeAtItsOwnColumn`: the rule's case (one semantic `calls`, no `uses`), the `minicjs` `require`-binding shape at the join's level, one column off, a differing definition, two at the column with one disagreeing, no fallback, a columnless site, an ambiguous site, an import site with a differing name, qualifier+argc, the match asked directly, and one case per mirror (not vetoed, not withheld, compared with no disagreement, nothing to shape, `resolved` in the dispositions, the C++ counts). The `call` helper now forwards `ambiguous`/`qualifier`/`argc` by name. `TestDisambiguation::test_a_resolution_with_a_different_name_does_not_match` is untouched and passes (it has no fallback).
> - `pipeline/tests/test_tsjs_reexport.py` — the `lane_b` case now also pins `("test/direct", "lib/app.createApplication")` as `SEMANTIC` with lane-B evidence at line 3 and no `uses` left at that line; `through`/`twohop` assertions unchanged; the no-index twin pins the same edge `SYNTACTIC`.
> 
> **What I ran** (all green): `pipeline` pytest — 2,122 passed, 17 skipped (the `lane_b` cases skip here, including the extended one); `go test ./...` — every package ok; `tsextract` 47; `scip` 87; `web` `npx vitest run --no-cache` 52. No `VERSION`, CHANGELOG, ADR, register or doc edits, and no change to any provider, `scip/index.mjs` or the oracle lane. Not verified here: the `lane_b` end-to-end case and the per-language regrade the ADR requires before merge — those are the developer's on the host.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: 71 turns of 140, $5.87, four files, all inside the partition. The
  diff is the ADR's rule and nothing beside it: one documented function,
  `match_at_own_column`, tried by the join where the by-name match missed;
  every mirror reads that same function (`external_vetoes`,
  `withheld_fallbacks`, `agreement`, `disagreement_shapes`, `_dispositions`,
  and the C++ comparison in `extract/__init__.py`), and `_dispositions` is
  handed the fallback its two callers already held rather than re-deriving
  it, as the brief asked. `match_resolution`'s own contract is untouched.
  **On the host:** all twelve `lane_b` tests pass in a worktree of the
  branch, the extended `minicjs` case among them (`test/direct` drawn
  semantic, no `uses` left at the line). **The real cells before the merge:**
  xmpp.js, hono, ajv and cheerio regraded with the branch's code are
  **row-identical to the simulation** (38, 76, 5 and 8 tiers raised, no row
  added or lost, 0 contradicted, poison PASS); cJSON, click, jsoup, memchr,
  fzf, fmt and args, each ingested by `main` and then by the branch, are
  row-identical before and after with no tier moved. Merged `599886f`,
  `--no-ff`.
