# Harness session `S-20260924T222939Z-2fd4`

*2026-09-24 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E0, part 1: the lattice map, the holes and the task record (text only, nothing executes) (brief `/home/mmarrujo/.hobbes/sessions/S-20260924T222939Z-2fd4/brief.md`, task sha256 `c2045b71ead8`)
- **Parent:** `4b8dea2b22be` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 45 of 80; result `success`; reported cost $4.7750 (the envelope's figure, on the subscription); wall 670.4 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 20 exec decision(s) — `allow`×12, `escalate`×8; escalated: `/bin/sh -c cd bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src && for f in distance-*.c; do echo "=== $f"; grep -n '^[A-Za-z_].*(' $f | grep -v ';$`; `/bin/sh -c cd bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src && for f in distance-*.c; do echo "=== $f"; grep -n '^[A-Za-z_].*(' $f | grep -v ';$ → expired`; `/bin/sh -c cd bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src && for f in distance-*.c; do echo "=== $f"; grep -n 'distance_\|^#define\|_imp\b' $f`; `/bin/sh -c cd bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src && for f in distance-*.c; do echo "=== $f"; grep -n 'distance_\|^#define\|_imp\b' $f → expired`; `/bin/sh -c cd bench/calvin/lattice && ls .venv/bin | head -20 && (.venv/bin/lattice map tests/fixtures/sqlite-vector-kernels | head -4 || echo "no console scrip`; `/bin/sh -c cd bench/calvin/lattice && ls .venv/bin | head -20 && (.venv/bin/lattice map tests/fixtures/sqlite-vector-kernels | head -4 || echo "no console scrip → expired`; `/bin/sh -c cd /work/bench/calvin/lattice && .venv/bin/lattice map tests/fixtures/sqlite-vector-kernels | head -4`; `/bin/sh -c cd /work/bench/calvin/lattice && .venv/bin/lattice map tests/fixtures/sqlite-vector-kernels | head -4 → expired`; records: stream opened→closed
- **Edits:** 16 edit(s) to 11 file(s); first at 6.4 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/cells.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/holes.py`, `bench/calvin/lattice/src/lattice/scan.py`, `bench/calvin/lattice/src/lattice/task.py`, `bench/calvin/lattice/tests/test_cells.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_holes.py`, `bench/calvin/lattice/tests/test_scan.py`, … and 1 more
- **Branch:** `hobbes/S-20260924T222939Z-2fd4`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/cells.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/holes.py`, `bench/calvin/lattice/src/lattice/scan.py`, `bench/calvin/lattice/src/lattice/task.py`, `bench/calvin/lattice/tests/test_cells.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_holes.py`, `bench/calvin/lattice/tests/test_scan.py`, `bench/calvin/lattice/tests/test_task.py`
- **Gate:** **clear** at `4b8dea2b22be` (gate v2, grounder v4, record `ba412412b2ab`); unknown 0; map over 3 file(s), 37.9% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 37, regressions 0; {'new-pass': 37}

**The doer's closing words:**

> Done. Committed as `5cae076` on `hobbes/S-20260924T222939Z-2fd4`.
> 
> **What I changed** — eleven files, exactly the list the gate checks:
> 
> - `src/lattice/scan.py` — a hand scanner for C text. It masks what cannot hold code (comments, string and char literals, preprocessor lines, a `\`-continued `#define` as one logical line) and walks the top level for a `)` then a `{`, returning name, signature, signature/body spans, `static`/`inline`, and the file's `#define` names. Its docstring names what it reads wrongly rather than refuses (K&R, a macro-made definition, a function-pointer return), and that **every arm of an `#if`/`#else` is a definition here** — no preprocessor runs, so picking an arm would be a guess (`distance-cpu.c` really defines `cpu_supports_neon` three times).
> - `src/lattice/cells.py` — the grid over `src/distance-*.c`: 13 cells per ISA file, `kind` read from the body (a wrapper is a one-statement call to this type's `_impl`, never its name), slots from the init function's assignment lines, `graded_via` = own slots or, for an `_impl`, the wrappers' that reach it. `sse2`/`avx2`/`avx512` native; `cpu` the reference; `neon`/`rvv` mapped, not native. NEON's `int8_distance_l2_neon_imp` maps to `(int8, l2_impl, neon)` with its real name kept; AVX-512's `hamming` kernel is recorded `static`. Unplaceable kernel-shaped names go to `Lattice.unmatched` with a reason.
> - `src/lattice/holes.py` — `punch`/`fill`/`gold_body`, round trip held; an unbalanced body raises `UnbalancedBody` and is not written.
> - `src/lattice/task.py` — §5.2's record, `lattice-task/1`, with `callees`/`contract`/`edge_cases`/`like` null and no absolute path.
> - `src/lattice/cli.py` — `lattice map|task|punch`; `README.md` gained a section per module.
> - Five test files on the fixture.
> 
> **Two things worth your eye**
> 
> - **The `cpu` reference has 0 slots, by a stated rule.** `distance-cpu.c` fills the table through a local `cpu_table` designated initialiser plus `memcpy`, not through `dispatch_distance_table[…][…] =` lines. I read only the assignment shape the unit specifies, so cpu's cells carry no slots (and its `_impl`s no `graded_via`). It is documented in `cells.py` and pinned by `test_the_cpu_reference_fills_its_table_in_a_shape_the_map_does_not_read`. Cheap to add the initialiser shape later if the graders want cpu slots.
> - **Spans are character offsets, not byte offsets.** The unit says "byte span"; the target's comments are not all ASCII (`8×bf16` in `distance-avx2.c`), so the two differ. Everything here slices the same `str`, the round trip is exact, and the docstring says so plainly.
> 
> **What I ran**
> 
> - `cd bench/calvin/lattice && .venv/bin/python3 -m pytest -q` → **38 passed** (includes the pre-existing `test_fixture.py`). The round trip covers all 78 cells of all six files.
> - `lattice map` on the fixture: 13 cells per ISA; 2 impl / 4 wrapper / 7 body and 11 slots for each of `sse2`, `avx2`, `avx512`; `unmatched: none`.
> - Nothing compiled or ran C; no other tree's suite was touched.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-151's E0, part 1, built as briefed: text only, nothing executes. **The real target, read on the host
  (the session cannot see it):** `lattice map` over sqlite-vector at `0c2223a` gives 31 cells per ISA file, and per
  native ISA 5 `impl`, 10 `wrapper`, 16 `body`, 26 slots, with nothing unmatched. `fill(punch(t, c), gold(t, c)) == t`
  holds for all 186 cells. `avx512/bit1/hamming` is `static`, and an `_impl`'s `graded_via` is its two wrappers'
  slots. Fixture pytest 38 passed. **One defect, the brief's:** `prelude` is "the file's text above the signature",
  as briefed, and on the real file that carries sibling cells' bodies above the hole (the float16 `dot` body above
  `avx512/float16/cosine`). The C-0 arm would then read pattern it is meant to withhold. It is not fixed here: part 2
  adds a bare prelude (other cells' bodies reduced to their prototypes) and E1's arms are built from that. The ISA
  neighbours include `cpu` (the scalar reference) and every other ISA, which is what the shot selector picks from.
