# Harness session `S-20260919T151056Z-5393`

*2026-09-19 · `hobbes dispatch` v1 · Hobbes 0.2.48-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-137, route (a), the pipeline's half: **a pytest fixture injection is a syntactic `uses` edge, and a test's reach follows it.** The decision is `docs/adr/137-a-pytest-fixture-injection-is-an- (brief `/home/mmarrujo/.hobbes/sessions/S-20260919T151056Z-5393/brief.md`, task sha256 `dcee4cc1a802`)
- **Parent:** `c3c2ba540835` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 89 of 100; result `success`; reported cost $7.6988 (the envelope's figure, on the subscription); wall 828.4 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 24 exec decision(s) — `allow`×20, `escalate`×4; escalated: `/bin/sh -c cd /work/pipeline && HOBBES_SCIP=0 .venv/bin/python3 - <<'PY'
import re, collections
from pathlib import Path
from hobbes.extract import extract_repo`; `/bin/sh -c cd /work/pipeline && HOBBES_SCIP=0 .venv/bin/python3 - <<'PY'
import re, collections
from pathlib import Path
from hobbes.extract import extract_repo → expired`; `/bin/sh -c cd /work && git commit -q -F - <<'MSG'
feat(extract): a pytest fixture injection is a uses edge, and test reach follows it (ADR-137)

C-4: a test tha`; `/bin/sh -c cd /work && git commit -q -F - <<'MSG'
feat(extract): a pytest fixture injection is a uses edge, and test reach follows it (ADR-137)

C-4: a test tha → expired`; records: stream opened→closed
- **Edits:** 36 edit(s) to 10 file(s); first at 4.0 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/graph.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/src/hobbes/extract/testmap.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_fixtures.py`, `pipeline/tests/test_pysource.py`, `pipeline/tests/test_testmap.py`
- **Branch:** `hobbes/S-20260919T151056Z-5393`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/graph.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/src/hobbes/extract/testmap.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_fixtures.py`, `pipeline/tests/test_pysource.py`, `pipeline/tests/test_testmap.py`
- **Gate:** **clear** at `c3c2ba540835` (gate v2, grounder v4, record `584e19d3eee5`); unknown 0; map over 94 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1405, regressions 0; {'P2P': 1337, 'skip': 19, 'new-pass': 48, 'not-run': 1}

**The doer's closing words:**

> Committed as `fb7e8b3`.
> 
> ## What I changed
> 
> - **`extract/pysource.py`** — `Symbol` gains `params` (each undefaulted, non-splat parameter with its 1-based line; positional-only, plain, keyword-only and typed alike; `self`/`cls` recorded like any other) and `parametrized` (the names every `parametrize` decorator binds, read from the node because `Decorator` keeps string literals only; `None` when one spells its first argument as something unreadable). Both defaulted; `Decorator` unchanged.
> - **`extract/graph.py`** — one new public function, `symbol_imports`, exposing `_NameEnv`'s `("symbol", …)` bindings per module.
> - **`extract/fixtures.py`** (new) — `injections(modules, parsed) -> (list, counts)`: pytest's order (class chain innermost outward → file, imports included → conftest chain upward), the `name=` alias, the `def repo(repo)` override skip, and the three counted abstentions (`not-in-repo`, `two-definitions`, `parametrize-unread`) plus `usefixtures` counted and not followed. Returns `{}` for counts where no fixture is defined and no parameter was looked up.
> - **`extract/__init__.py`** — after `graph["symbol_edges"] = projected["symbol_edges"]`, the injections are drawn through `tiered_edge` as `uses`/`syntactic`/`tree-sitter` (one edge per `(from, to)`, evidence sorted and unique) and the list re-sorted by the projection's `(from, to, type, tier, lane)`; `graph["fixtures"]` written only when there is something to say.
> - **`extract/testmap.py`** — `collect_tests(..., injections=())`; reach is the closure over `calls` **and** injections; every pytest record gains `through_fixtures`. `_is_test_symbol` is now public `is_test_symbol` (old name kept as an alias).
> - **`cli.py`** — `_print_fixtures`, called after `_print_constructions`.
> 
> **Return shape (the choice the brief asked me to name):** `_build_symbol_layer` keeps returning the degradation list and takes an out-parameter `injections: list[dict] | None = None`, the same idiom as `counts=operator_counts` into `ev.join`. That is the smaller diff — a second return value would have broken `_build_symbol_layer(...) == []` in `test_testmap.py`'s `tests_doc` fixture and every other call site.
> 
> **Deviations:** one — a fixture imported from a module that is neither a test file nor a `conftest.py` resolves to nothing (`not-in-repo`), because `_fixture_decorator` only reads definitions in those two file kinds; it draws less than guessing.
> 
> **Existing assertions:** none moved. No test pinned a pytest record's key set or unpacked the changed return.
> 
> ## What I ran
> 
> - `pytest tests/test_pysource.py tests/test_fixtures.py tests/test_testmap.py tests/test_cli.py tests/test_graph.py` — **182 passed**.
> - The whole pipeline suite — **2,013 passed, 15 skipped** (was 1,980 + 10 `lane_b`; +33 new).
> - `extract_repo` over `/work` with lane B off: `{'drawn': 1008, 'requesters': 868, 'fixtures': 82, 'abstained': {'not-in-repo': 1030, 'two-definitions': 0, 'parametrize-unread': 0}, 'usefixtures': 0}`; 1,008 syntactic `uses` edges and no others; 373 pytest records gain a non-empty `through_fixtures`, 235 of which reached nothing before.
> - **Against the key:** `pytest --fixtures-per-test` over `pipeline/tests`, compared per test on the repo-defined fixtures each ends up with — 1,945 tests compared, **998 pairs, 998 right, 0 wrong, 0 missed**. ADR-137's pre-registration was 1,004 ± 5 right and 0 wrong; 0 wrong and 0 missed hold, and 998 sits one below the band's edge — my compare matched tests by name rather than by line (0 unmatched, against the ADR harness's 1,906 tests and 6 unmatched), so the two counts are over slightly different denominators.
> - The held-out second Python repo the ADR's consequences ask for before merge needs collection in the image; I did not run it.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: merged no-ff after the whole suite (2,028) and `lane_b` (10 of 10) on the host, from a worktree of the branch, and the pre-registered check on the branch's own ingest: 1,004 test–fixture pairs against `pytest --fixtures-per-test`, 1,004 right, 0 wrong, 0 missed — ADR-137's figure exactly (the doer's 998 was its own name-matched compare). Then two held-out repos, collected in the image offline: flask (`d73fa1c`) 504 right, 0 wrong, 370 missed, every one an `autouse` fixture; attrs (`8f76777`) 104 right, 0 wrong, 8 missed, every one a `usefixtures` mark — and the ingest's own `usefixtures` count there is 8. The first compare read 3 + 5 wrong pairs on them; both were the key's display, not an edge (pytest prints a fixture at its first decorator line, and one row per fixture *name*, so the outer `app` a `def app(self, app)` override requests is run and not printed) — `compare.py` now says so, `compare-v1.py` keeps the first form. The diff is the brief's: an out-parameter for the injections rather than a second return (the smaller diff, as asked), one deviation that draws less (a fixture imported from a file that is neither a test file nor a conftest resolves to nothing). The escalations were heredocs (a scratch `extract_repo` run and the commit message); they expired and the doer went another way. Not in the unit and the developer's: a class's *inherited* fixture (no bases are read), `tests_guarding`'s and `hobbes review`'s label, the register and the version.
