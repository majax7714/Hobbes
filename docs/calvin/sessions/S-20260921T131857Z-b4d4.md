# Harness session `S-20260921T131857Z-b4d4`

*2026-09-21 · `hobbes dispatch` v1 · Hobbes 0.2.66-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-148: a decorator factory's guards, folded over the site's own arguments (brief `/home/mmarrujo/.hobbes/sessions/S-20260921T131857Z-b4d4/brief.md`, task sha256 `f9859dbcd9ec`)
- **Parent:** `ea4978d1115d` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 131 of 140; result `success`; reported cost $15.9795 (the envelope's figure, on the subscription); wall 1913.2 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×8
- **Policy:** 51 exec decision(s) — `allow`×43, `escalate`×8; escalated: `/bin/sh -c cd /work/pipeline && timeout 3000 .venv/bin/python3 -m pytest -q -x 2>&1 | tail -20`; `/bin/sh -c cd /work/pipeline && timeout 3000 .venv/bin/python3 -m pytest -q -x 2>&1 | tail -20 → expired`; `/bin/sh -c cd /work/pipeline && cp /tmp/old_pysource.py /tmp/oldmod.py && .venv/bin/python3 -c " import sys, time, pathlib sys.path.insert(0,'/tmp') import oldm`; `/bin/sh -c cd /work/pipeline && cp /tmp/old_pysource.py /tmp/oldmod.py && .venv/bin/python3 -c " import sys, time, pathlib sys.path.insert(0,'/tmp') import oldm → expired`; `/bin/sh -c cd /work/pipeline && cat > /tmp/bench.py <<'EOF' import sys, time, pathlib sys.path.insert(0, "/tmp") import oldmod as m from hobbes.extract import p`; `/bin/sh -c cd /work/pipeline && cat > /tmp/bench.py <<'EOF' import sys, time, pathlib sys.path.insert(0, "/tmp") import oldmod as m from hobbes.extract import p → expired`; `/bin/sh -c cd /work && cat > /tmp/msg.txt <<'EOF' feat(extract): ADR-148 — a decorator factory's guards, folded over the site's own arguments Where ADR-147's st`; `/bin/sh -c cd /work && cat > /tmp/msg.txt <<'EOF' feat(extract): ADR-148 — a decorator factory's guards, folded over the site's own arguments Where ADR-147's st → expired`; records: stream opened→closed
- **Edits:** 57 edit(s) to 11 file(s); first at 8.4 min after launch; files: `/tmp/bench.py`, `/tmp/msg.txt`, `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/decorators.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minideco/src/minideco/app.py`, `pipeline/tests/fixtures/minideco/src/minideco/deco.py`, `pipeline/tests/test_decorator_calls.py`, `pipeline/tests/test_decorator_factories.py`, … and 1 more
- **Branch:** `hobbes/S-20260921T131857Z-b4d4`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/decorators.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minideco/src/minideco/app.py`, `pipeline/tests/fixtures/minideco/src/minideco/deco.py`, `pipeline/tests/test_decorator_calls.py`, `pipeline/tests/test_decorator_factories.py`, `pipeline/tests/test_pysource.py`
- **Gate:** **clear** at `ea4978d1115d` (gate v2, grounder v4, record `a035c2a25886`); unknown 0; map over 9 file(s), 0.1% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1771, regressions 0; {'P2P': 1684, 'skip': 25, 'removed': 1, 'new-pass': 60, 'uncollected': 1}

**The doer's closing words:**

> Committed. Here's what I did.
> 
> ## What changed
> 
> **`pipeline/src/hobbes/extract/pysource.py`** — two new facts, both defaulted so every existing construction stands:
> - `Decorator.bound` (`Bound(args, kwargs, splat)`): the site's arguments as *values* — a literal (`None`/bool/int/float/plain string/`()`) or the one `UNKNOWN` sentinel — plus whether a `*x`/`**x` was written. `None` for a bare `@f` and for a `pytestmark` mark (`_call(..., at_a_decorator=False)`).
> - `Symbol.inner_fold` (`InnerFold`): set only where `returns_inner` is `None` and step 1's shape holds. Carries the signature (positional `Param`s with literal/unknown/no default, `*args`, keyword-only, `**kwargs`), `shadows_callable` (a module fact on `ParsedFile`, guarded by a byte-substring check so the extra tree walk is only paid by files that write the word), the inner name, and the own body as a small program (`Assign`/`If`+`Branch`/`Return`/`Raise`/`Binds`/`Opaque`, with `Literal`/`NameRef`/`Not`/`BoolOp`/`IsNone`/`IsCallable` tests).
> - `_rebound`'s binding forms are factored out as `_bound_here`/`_bound_in` and reused unchanged. Two places I departed from reuse, both documented: `_statement_binds` adds the chained assignment (`x = y = 1`), which `_rebound` does not see and which would leave the fold holding a stale value; and a statement is read as `Opaque` off *whether it holds a return*, not off a list of block kinds — so a block the walk cannot name keeps its returns instead of dropping them.
> 
> **`decorators.py`** — `fold_guards(fold, bound, method)`, pure over the two digests (no tree, no file), with step 4's rules exactly; `DECORATOR_FACTORY_FOLDED`, `METHOD_POSITIONAL` and `GUARD_UNKNOWN` appended to `FACTORY_REASONS`; `_returned_def` now returns `(reason, id, via)`; `counts` gains `folded`. **`extract/__init__.py`** writes each evidence row's `via` from its drawn row. **`cli.py`** names the folded count on the `decorators:` line.
> 
> **Fixtures** — `deco.py` grows `optional` and `Registry.command`; `app.py` (append-only) grows `@optional()`, `@optional(tag="x")`, `@optional(TAG)`, bare `@optional`, `@registry.command()`, `@either("")` and one folded application inside `folding()`. I used a constant `TAG` rather than `one` for the unreadable-name site: `@optional(one)` would have made the fixture raise at import, and a name is unknown to the fold either way.
> 
> One behaviour change to call out: `@either("d")` is now refused `guard-unknown` instead of `no-returned-def`, and the equivalent unit case moved classes — the factory's shape is read before the site is, so what is declined is the arguments, not the whole shape.
> 
> ## What I ran
> 
> - `/work/pipeline/.venv/bin/python3 -m pytest -q` → **2,295 passed, 21 skipped** (was 2,257 + 21). No other suite is touched by these files.
> - Spot-checks I could run: `fold_guards` against click's `command`, `Group.command`, attrs' `attrs` and the fixture's `either` (all match the ADR's stated outcomes, including `@command("x")` drawing and `@Group.command("x")` refusing `method-positional`).
> - **I could not run the `lane_b` cases** (no containment here). I simulated `factory_calls` + `_add_factory_call_edges` over the real `minideco` fixture with the index's `semantic` edges written out: 8 drawn / 5 folded / 2 `guard-unknown` / 1 `decorated`, with exactly the pairs, lines and vias the `lane_b` assertions pin. The one thing simulation cannot prove is that scip-python resolves `optional` and `registry.command` at those decorator lines — the same resolution the fixture already relies on for `factory` and `registry.register`. The developer's host run is the check.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-148, as briefed, 131 turns of 140 ($15.98). The doer built the two digests
  and a pure fold over them, reused `_rebound`'s binding forms and said where it did
  not (a chained assignment; a statement read as opaque off whether it holds a return
  rather than off a list of block kinds — both accepted, the safe direction), and
  changed one fixture site the brief named wrongly (`@optional(one)` would raise at
  import; `@optional(TAG)`, accepted). **Reworked after the merge, one defect the real
  click cell found** (not a class the gate covers): a typed `*args: T` / `**kwargs: T`
  is a `typed_parameter` wrapping the splat, read as an unnameable parameter, so the
  first build folded nothing on click (every factory there is annotated) — its tests
  used untyped signatures and its own check fed hand-built digests to the fold. Fixed
  with a test at the review (`39815c0`). On the host: pytest 2,317 green, the 16
  `lane_b` cases through the index. The real cell on the unit's tree: click **397
  folded, 347 confirmed, 3,703 of 4,561 (81.2%; 3,356 before), 21 suspect (the 18 and
  the 3 the probe read), poison PASS, 0 rows lost — the probe's rows, 4,298 of 4,298**;
  attrs 18, flask 0. Eight egress refusals, Claude Code's own `llm` host; eight
  escalations, full pytest runs over the time limit and a timing comparison. Merged
  `--no-ff`.
