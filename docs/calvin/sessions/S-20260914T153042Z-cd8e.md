# Harness session `S-20260914T153042Z-cd8e`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.16-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make `hobbes dispatch` read the doer's model from `$HOBBES_DISPATCH_MODEL` when `--model` is not given. The design is ADR-107's amendment of 2026-09-14, the last section of `docs/adr/107-calvin-as-a-h (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T153042Z-cd8e/brief.md`, task sha256 `cb5506e298d5`)
- **Parent:** `acf093c998e3` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model claude-opus-5; turns 22 of 60; result `success`; reported cost $1.1112 (the envelope's figure, on the subscription); wall 75.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 5 exec decision(s) — `allow`×5; records: stream opened→closed
- **Edits:** 5 edit(s) to 3 file(s); first at 0.4 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/run/dispatch.py`, `pipeline/tests/test_dispatch.py`
- **Branch:** `hobbes/S-20260914T153042Z-cd8e`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/run/dispatch.py`, `pipeline/tests/test_dispatch.py`
- **Gate:** **clear** at `acf093c998e3` (gate v2, grounder v3, record `5e098e04270c`); unknown 0; map over 3 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 388, regressions 0; {'P2P': 383, 'skip': 2, 'new-pass': 3}

**The doer's closing words:**

> Done — committed as `01c6373` on `hobbes/S-20260914T153042Z-cd8e`.
> 
> **What changed** (only the three partition files):
> 
> - `pipeline/src/hobbes/run/dispatch.py` — `MODEL_ENV = "HOBBES_DISPATCH_MODEL"` beside `DEFAULT_EGRESS`, with a one-line comment saying it is the box's setting, not the repo's; `default_model() -> str | None` above `_git`, returning `os.environ.get(MODEL_ENV) or None`, its docstring saying `--model` beats it and that unset or empty leaves the choice to Claude Code. Nothing else in the module.
> - `pipeline/src/hobbes/cli.py` — `_cmd_dispatch` passes `model=args.model or dp.default_model()`; the `--model` help now reads `the doer's model (default: $HOBBES_DISPATCH_MODEL, else Claude Code's own)`.
> - `pipeline/tests/test_dispatch.py` — three tests, each reading `argv.json` back through a small `_argv_of` helper beside `_run`: the variable set and no flag puts `--model claude-opus-5` adjacent in the session argv and `claude-opus-5` in `rec["doer"]["model"]`; `--model other` beside the variable carries `other` and no `claude-opus-5` anywhere in the argv; the variable deleted (`raising=False`) and no flag leaves `--model` out of the argv and `rec["doer"]["model"]` `None`.
> 
> **What I ran** (through `mcp__hobbes__exec`, from `/work/pipeline`, the tree's own python):
> 
> - `python -m pytest tests/test_dispatch.py tests/test_version.py -q` → **23 passed** (20 before the change, 3 new).
> - `python -m pytest tests/test_cli.py -q` → **41 passed**, since the help string moved.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: The diff is the amendment's spec line for line: `MODEL_ENV` and `default_model()` in `dispatch.py`, `args.model or dp.default_model()` and the help text in `cli.py`, three tests through the fake session's `argv.json`. The first session whose Doer line names its model — run with `--model claude-opus-5` said explicitly, since the variable it builds reaches only a shell started after the setting. No egress refusal; five exec decisions, all allowed. Merged as `hobbes/S-20260914T153042Z-cd8e`, not squashed. The layer moves to 0.2.17-beta (patch) after the merge.
