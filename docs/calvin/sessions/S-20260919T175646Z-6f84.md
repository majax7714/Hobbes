# Harness session `S-20260919T175646Z-6f84`

*2026-09-19 · `hobbes dispatch` v1 · Hobbes 0.2.51-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-139, route (a), the pipeline's half: **a `usefixtures` string and an `autouse=True` fixture's name are looked up exactly as ADR-137 looks a parameter up, drawn as the same syntactic `uses` e (brief `/home/mmarrujo/.hobbes/sessions/S-20260919T175646Z-6f84/brief.md`, task sha256 `f5be51b23b49`)
- **Parent:** `59e5c473ac9d` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 68 of 80; result `success`; reported cost $5.6990 (the envelope's figure, on the subscription); wall 720.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×2
- **Policy:** 15 exec decision(s) — `allow`×13, `escalate`×2; escalated: `/bin/sh -c git -C /work status --short && git -C /work diff --stat`; `/bin/sh -c git -C /work status --short && git -C /work diff --stat → expired`; records: stream opened→closed
- **Edits:** 26 edit(s) to 9 file(s); first at 2.6 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/src/hobbes/extract/testmap.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_fixtures.py`, `pipeline/tests/test_pysource.py`, `pipeline/tests/test_testmap.py`
- **Branch:** `hobbes/S-20260919T175646Z-6f84`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/src/hobbes/extract/testmap.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_fixtures.py`, `pipeline/tests/test_pysource.py`, `pipeline/tests/test_testmap.py`
- **Gate:** **clear** at `59e5c473ac9d` (gate v2, grounder v4, record `e55c3707e44e`); unknown 0; map over 9 file(s), 0.6% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1468, regressions 0; {'P2P': 1408, 'skip': 19, 'new-pass': 39, 'removed': 1, 'not-run': 1}

**The doer's closing words:**

> Committed as `0ba14b3`. 
> 
> **What I changed** (the nine files in the partition, nothing else):
> 
> - `extract/pysource.py` — `Decorator` gains `true_kwargs` / `unread_kwargs` (defaulted, at the end); `ParsedFile` gains `pytestmark_usefixtures`, counted by a new `_pytestmark_usefixtures(root)` over module-level assignments. The walk itself is unmoved.
> - `extract/fixtures.py` — one `request(name, line, via)` path walks pytest's order for all three vias with ADR-137's abstentions unchanged; `usefixtures` strings (test's own mark + each class of the chain) and autouse names (`_autouse_names` over the chain, `_Scopes.autouse`) are looked up for a **test only**; one pair keeps the first of parameter → usefixtures → autouse. Counts gain `via`, `autouse_fixtures`, `unread{autouse-value, pytestmark}`; every old key kept.
> - `extract/__init__.py` — evidence rows are `{path, line, via}`, sorted and unique as before.
> - `extract/testmap.py` — three adjacencies; `through_fixtures` computed without autouse (meaning unchanged), new `through_autouse` (module → sorted fixture ids, `{}` when none), each fixture's own reach computed once and shared across the tests that request it.
> - `cli.py` — the via clause after the drawn count when non-zero; "usefixtures mark(s) not followed" removed; `pytestmark` and non-literal-`autouse` clauses added when non-zero.
> 
> **Ran:** from `/work/pipeline`, `.venv/bin/python3 -m pytest tests/test_pysource.py tests/test_fixtures.py tests/test_testmap.py tests/test_cli.py -q` → **210 passed**; then the whole suite → **2,075 passed, 15 skipped** (no failures). Smoke check on this tree (`extract_repo('/work/pipeline')`, lane B off): 5,017 injections drawn, 4,056 of them autouse, 2,028 of 2,044 records carrying a `through_autouse` — e.g. `hobbes.extract.staging` under `tests.conftest._lane_a_only`, matching ADR-139's measurement.
> 
> **Assertions moved** (each with what it pinned):
> - `test_fixtures.py::TestThroughTheIngest::test_the_injection_is_a_uses_edge_and_no_call` — pinned an evidence row's exact keys; now carries `"via": "parameter"`.
> - `test_fixtures.py::TestWhatIsNotARequester::test_usefixtures_is_counted_and_not_followed` — pinned that a mark on a test or a test class draws nothing, which ADR-139 reverses; replaced by `test_a_usefixtures_mark_on_a_fixture_draws_nothing`, keeping the half still true (a mark on a *fixture*) and the count.
> - `test_cli.py::TestFixtureLine::test_each_optional_clause_is_said_when_there_is_some` — pinned "7 usefixtures mark(s) not followed"; the clause is removed, and the test now pins the two clauses that replaced it.
> 
> **Deviations** (each the nearest thing that draws or says less):
> - `pytestmark` is counted only on a plain module-level assignment statement; `pytestmark += [...]`, or one nested under `if`/`try`, counts 0.
> - A list keyword value the walk cannot read whole (empty, or with a non-string item) counts in `unread_kwargs` — it was read as nothing, exactly like a name.
> - The CLI spells the new clause "N usefixtures mark(s) in a module-level pytestmark not followed"; the brief left the wording open and "not followed" is gone from the decorator clause only.
> - `through_autouse` lists the fixture's own module (usually `conftest`) when the test reaches it only that way — it follows from §4 as written, and keeps `through_fixtures` free of it.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-139's unit (route a), the pipeline's half. Read whole: one `request` path for the three vias with ADR-137's abstentions untouched, parameters first so a pair keeps `via: parameter`; reach in three adjacencies, `through_fixtures` unchanged in meaning. On the host: 2,090 pytest and the 10 `lane_b` cases pass on a worktree of the branch. The real cell before merging: the built lookup against the three `-v` keys — this repo 4,971 right, flask 1,238, attrs 118, 0 missed, 0 wrong — and its edges identical to the probe's on all three. The four deviations each draw or say less and are kept. The two escalations were a `git status` the policy queues; one expired unanswered, harmlessly.
