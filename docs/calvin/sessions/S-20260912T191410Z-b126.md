# Harness session `S-20260912T191410Z-b126`

*2026-09-12 · `hobbes dispatch` v1 · Hobbes 0.2.4-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build the oracle lane's C oracle, O9 (ADR-110): `oracle c-clang`, clang's own front end as the answer key for C's call graph. This is bench tooling under `bench/oracle`, with no product change. (brief `/home/mmarrujo/.hobbes/sessions/S-20260912T191410Z-b126/brief.md`, task sha256 `b8652e078a2a`)
- **Parent:** `b7d17b819297` of `hobbes_public`
- **Doer:** 2.1.269 (Claude Code); model the default; turns — of 200; result `—`; wall 3273.3 s; session exit 143
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 0 exec decision(s) — none
- **Branch:** none harvested — the session left no commit
- **Gate:** **clear** at `b7d17b819297` (gate v2, grounder v3, record `6cb8706b4e1f`); unknown 0; map over 9 file(s), 19.4% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** empty

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear — there was no diff.
- outcome: discarded — the session left no commit. The developer stopped it (SIGTERM, exit 143) at 53 minutes.
- notes:
  - **What was seen.** For 53 minutes the doer wrote no file under `/work` and ran no command through the proxy (the flight log is empty), on a task the size of C's lane A (74 turns, 19 minutes in `eef8`). Its process had used 16 s of CPU. A socket read showed its one tunnel established with empty queues both ways.
  - **The developer's misreading, corrected.** That was read as a hung API connection, and the session was stopped. It was not hung: the tunnel's close record gives **5.37 MB up and 1.12 MB down over 3,272 s**, so the doer was exchanging turns with the model throughout. What it did with them is not recorded. Its native file tools are outside the flight log (C-125), and its transcript is not stored (ADR-107's retention). A doer that reads without writing looks like this from outside, and so does a stall.
  - **Egress:** 4 tunnels to `api.anthropic.com:443`, no refusals. The pattern matches the healthy sessions (two start-up tunnels, one closing near 300 s, one long-lived).
  - **The redo:** the task split into two dispatches, the reader and merge over the committed fixture first, then the contained run, CLI and README. Each doer then has a smaller window (P12's direction).
  - **For the harness (a note, not a gate class):** there is no progress signal between "the doer is working" and "the doer is stuck" short of the timeout. C-125's named fix, a hook reporting Edit and Write to the flight log, would give one.
  - Reviewed by Claude (the developer session), 2026-09-12.
