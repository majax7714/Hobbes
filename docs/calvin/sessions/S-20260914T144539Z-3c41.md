# Harness session `S-20260914T144539Z-3c41`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.16-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make the two foreign-graph converters read a callee whose declared line begins with `#define` as kind `macro` (converter@3), so the lane's existing macro exclusion applies to a tool's edge to a C func (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T144539Z-3c41/brief.md`, task sha256 `8ace7a670515`)
- **Parent:** `d16c978e0db1` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 45 of 80; result `success`; reported cost $1.0973 (the envelope's figure, on the subscription); wall 194.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 7 exec decision(s) — `allow`×7; records: stream opened→closed
- **Edits:** 18 edit(s) to 7 file(s); first at 0.9 min after launch; files: `bench/oracle/adapters/codegraphcontext/adapter.py`, `bench/oracle/adapters/codegraphcontext/adapter_test.go`, `bench/oracle/adapters/codegraphcontext/testdata/cclang.raw.json`, `bench/oracle/adapters/repowise/adapter.py`, `bench/oracle/adapters/repowise/adapter_test.go`, `bench/oracle/adapters/repowise/testdata/cclang.raw.json`, `bench/oracle/report/foreign_record.py`
- **Branch:** `hobbes/S-20260914T144539Z-3c41`, 1 commit(s); files: `bench/oracle/adapters/codegraphcontext/adapter.py`, `bench/oracle/adapters/codegraphcontext/adapter_test.go`, `bench/oracle/adapters/codegraphcontext/testdata/cclang.raw.json`, `bench/oracle/adapters/repowise/adapter.py`, `bench/oracle/adapters/repowise/adapter_test.go`, `bench/oracle/adapters/repowise/testdata/cclang.raw.json`, `bench/oracle/report/foreign_record.py`
- **Gate:** **clear** at `d16c978e0db1` (gate v2, grounder v3, record `86ab4793d751`); unknown 0; map over 7 file(s), 47.9% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 6, regressions 0; {'new-pass': 2, 'P2P': 4}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `b8f0559` on branch `hobbes/S-20260914T144539Z-3c41`.
> 
> **Changes:**
> - `bench/oracle/adapters/{codegraphcontext,repowise}/adapter.py`: added `declared_kind(repo, path, line, stored)`, wired into `convert`, bumped `VERSION` to `@3`, extended `notes.grain` and the module docstrings.
> - `bench/oracle/adapters/{codegraphcontext,repowise}/testdata/cclang.raw.json`: new hand-made raw fixtures (macro call at `main.c:18`→`CALL_SUM`, function call at `main.c:19`→`lib_sum`).
> - `bench/oracle/adapters/{codegraphcontext,repowise}/adapter_test.go`: added `TestCclangMacroRowIsExcluded`.
> - `bench/oracle/report/foreign_record.py`: the signed direction line now reads the converter pair from `edges.json`/`edges.v1.json` and signs the `@3` macro cause (or the old `@2` annotation cause), falling back to the original `converter@1 → converter@2` text when `edges.v1.json` is absent.
> 
> **Checked:** `gofmt -l adapters/ internal/foreign/` clean; `go test ./adapters/... ./internal/foreign/...` — 8 tests pass across 3 packages (`codegraphcontext`: 3, `repowise`: 3, `internal/foreign`: 7 counted individually — full run `ok` in all three packages, 0 failures); `python3 -m py_compile` clean on all three touched `.py` files.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged as `d575428` (not squashed); no version moves (nothing under `bench/` does, ADR-103)
- notes: Read the diff (2026-09-14). It follows the amendment: one `declared_kind` beside `declaration_line` in each adapter, reading the source at the already-advanced declaration line and returning `macro` on a leading `#define`, the stored kind otherwise, the stored kind on unreadable source; wired after each converter's own kind choice and nothing else re-read; `VERSION` at `@3`; the grain strings and module docs say so. Two hand-made C raw fixtures over `testdata/cclang` (repowise's stores both symbols as `function`, the finding itself) and `TestCclangMacroRowIsExcluded` in each package, reading the converted file's kinds and the lane's one graded edge with `Excluded[macro] == 1`. The record tooling reads the converter pair from `edges.v1.json`/`edges.json` and signs the `@3` cause, the `@2` cause otherwise, the old fixed text with no v1 file. **Host, before the merge:** both adapter packages and `internal/foreign` green in a worktree; `py_compile` clean. **After the merge:** the four C cells regraded from their stored dumps by `~/.hobbes/bench/comparative/regrade-c-cell.sh`, the `@2` grade kept beside each as `*.v1.*` (the BUILDLOG has the numbers). No egress refusal this time.
