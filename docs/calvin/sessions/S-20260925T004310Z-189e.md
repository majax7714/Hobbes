# Harness session `S-20260925T004310Z-189e`

*2026-09-25 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E0, part 3: the facts arm (callees from the graph and the clang key), cell ages, the G-mem probe (brief `/home/mmarrujo/.hobbes/sessions/S-20260925T004310Z-189e/brief.md`, task sha256 `6da9eb9aa3c5`)
- **Parent:** `1186bf2ea627` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 85 of 140; result `success`; reported cost $8.5802 (the envelope's figure, on the subscription); wall 1156.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 28 exec decision(s) — `allow`×28; records: stream opened→closed
- **Edits:** 47 edit(s) to 13 file(s); first at 7.2 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/ages.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/facts.py`, `bench/calvin/lattice/src/lattice/gmem.py`, `bench/calvin/lattice/src/lattice/intrinsics.py`, `bench/calvin/lattice/src/lattice/task.py`, `bench/calvin/lattice/tests/test_ages.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_facts.py`, … and 3 more
- **Branch:** `hobbes/S-20260925T004310Z-189e`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/ages.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/facts.py`, `bench/calvin/lattice/src/lattice/gmem.py`, `bench/calvin/lattice/src/lattice/intrinsics.py`, `bench/calvin/lattice/src/lattice/task.py`, `bench/calvin/lattice/tests/test_ages.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_facts.py`, `bench/calvin/lattice/tests/test_gmem.py`, `bench/calvin/lattice/tests/test_intrinsics.py`, `bench/calvin/lattice/tests/test_task.py`
- **Gate:** **clear** at `1186bf2ea627` (gate v2, grounder v4, record `7be7f094c4eb`); unknown 0; map over 26 file(s), 3.5% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 74, regressions 0; {'new-pass': 53, 'P2P': 21}

**The doer's closing words:**

> **What I changed** — all under `bench/calvin/lattice/`, the 13 files the brief names:
> 
> - **`facts.py` (new)** — C-1. `callees(lattice, cell, graph, key, intrinsics=None)` asks Hobbes' graph first (in-repo callees, kind + signature from the file, `provenance` `hobbes:<tier>`), then fills from the clang key what the graph cannot see (`provenance` `clang-key:<mode>`, signature from the index or null). First-use order: the graph's rows by their first evidence line, then the key's by their first site, **macro-mode sites after the direct ones** (the name isn't written in the body — the macro that is already stands in the body's order). A site is the cell's only when `pos.path` **and** a line inside the body span agree; the bare `caller` name is never enough. A callee both name keeps the graph's row plus `"also": "clang-key"`. `Facts`/`load` are the ledger, `Facts.source()` its provenance, and `Callees.missing` names any instrument absent — no gap is filled by inference.
> - **`intrinsics.py` (new)** — clang's two declaration forms and the `#define` form, signatures normalised to one line with the storage keywords and attribute macros dropped. Checked against this box's real clang 18.1.3 headers: **6,435 names (1,916 macros) over 197 headers, 0 signatures carrying an attribute macro**, every spot-check exact.
> - **`ages.py` (new)** — `name_since` / `body_since`, walked contiguously back from a ref with `git log`/`git show`, each version found with the package's own scanner, decoded `errors="replace"`; `identical_at` is the quarter count the E0 card compares against.
> - **`gmem.py` (new)** — the probe (`prelude_bare` + signature + first `K = max(2, ⌊lines/4⌋)` lines), the token-prefix `score`, the 0.5/0.15 labels credited to ADR-099's navigation probe, and `run(probes, complete)` with the completer injected. A row with nothing to continue is kept and marked `evidence: False`.
> - **`task.py`** — schema `lattice-task/3`; `callees` filled from a ledger handed in, `callees_source` (graph sha + Hobbes version, key oracle, `missing`) beside it; both null when no ledger was given.
> - **`cli.py`** — `facts`, `intrinsics`, `ages`, `mem-probes`; `task` gained the same three ledger flags. No verb calls a model.
> - **`README.md`** — a section per new module and the provenance rule; the `task` section updated.
> 
> **What I ran:** `cd bench/calvin/lattice && .venv/bin/python3 -m pytest -q` → **203 passed** (was 155; the new files add 48, including the grade/selftest tests that run because clang is present here). Plus two by-hand checks through `cli.main`: `lattice facts` on the fixture's `derived/` for `avx512/int8/l2_impl`, and `intrinsics.load` over `clang -print-file-name=include`. Committed as `dd2469c` on `hobbes/S-20260925T004310Z-189e`; nothing pushed.
> 
> One thing for review: the brief's expected order for `avx2/float32/dot` puts `_mm256_fmadd_ps` last although its site is at line 161 col 16, ahead of `_mm256_loadu_ps` on the same line — I read that as macro-reached callees ordering after the direct ones and implemented it that way, documented in the module docstring and the README.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-151's E0, part 3, built as briefed. **On the real target** (host, with the image for the headers;
  `~/.hobbes/bench/calvin-lattice/facts/`):
  - `lattice intrinsics` over clang 18's headers indexes 6,435 names (4,519 functions, 1,916 macros, 66 headers),
    both declaration forms read right.
  - The target was re-ingested at 0.2.70-beta. `facts.callees` is non-empty for all 93 native cells: 680 rows,
    210 `hobbes:semantic` and 470 from the clang key. The only rows without a signature are libm and compiler
    builtins (`sqrtf`, `fma`, `__builtin_inff`), which the index does not cover, by its `_mm`/`_cvt` rule.
  - `lattice ages` on a full clone matches the developer's hand count to the cell: 18, 42, 54, 59 and 59 of 93
    at the five quarter ends.
  - `mem-probes` writes 93 probes.
  - Host pytest 185 passed, 18 skipped.

  **Two defects, both carried to part 4.** (1) **The brief's:** a key site in mode `macro` names the macro's
  *expansion*. For an in-repo macro that is the real intrinsic (`MM256_FMA_PS` → `_mm256_fmadd_ps`, which the brief
  cited). For a *header* macro it is compiler internals. The source of `avx512/bfloat16/dot` writes
  `_mm512_cmp_ps_mask`, `_mm512_extractf32x8_ps` and `INFINITY`, and the facts list `__builtin_ia32_cmpps512_mask`,
  `__builtin_ia32_extractf32x8_mask`, `_mm256_undefined_ps` and `__builtin_inff` instead: facts a model should
  never be handed, and names it did need, missing. (2) `lattice ages` over a bare clone reads no working tree and
  reports 0 cells rather than refusing.
