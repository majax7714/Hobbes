# Harness session `S-20260926T135153Z-157a`

*2026-09-26 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E2's runner: `e1` over a rename shadow, the leak gate, and the run cap's two holes (brief `/home/mmarrujo/.hobbes/sessions/S-20260926T135153Z-157a/brief.md`, task sha256 `ab32e6c0cc2f`)
- **Parent:** `ff83060d993e` of `hobbes_public`
- **Doer:** 2.1.283 (Claude Code); model claude-opus-5; turns 104 of 160; result `success`; reported cost $15.1456 (the envelope's figure, on the subscription); wall 1425.2 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 28 exec decision(s) — `allow`×22, `escalate`×6; escalated: `/bin/sh -c cd bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels && du -sh . && du -sh * && ls libs`; `/bin/sh -c cd bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels && du -sh . && du -sh * && ls libs → expired`; `/bin/sh -c du -sh bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels`; `/bin/sh -c du -sh bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels → expired`; `/bin/sh -c cd bench/calvin/lattice && .venv/bin/python -m pytest -q 2>&1 | tail -5 && git -C /work status --short`; `/bin/sh -c cd bench/calvin/lattice && .venv/bin/python -m pytest -q 2>&1 | tail -5 && git -C /work status --short → expired`; records: stream opened→closed
- **Edits:** 63 edit(s) to 12 file(s); first at 3.1 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/scripts/modal_e1.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/compare.py`, `bench/calvin/lattice/src/lattice/e1.py`, `bench/calvin/lattice/src/lattice/facts.py`, `bench/calvin/lattice/src/lattice/shadow.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_compare.py`, `bench/calvin/lattice/tests/test_e1.py`, … and 2 more
- **Branch:** `hobbes/S-20260926T135153Z-157a`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/scripts/modal_e1.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/compare.py`, `bench/calvin/lattice/src/lattice/e1.py`, `bench/calvin/lattice/src/lattice/facts.py`, `bench/calvin/lattice/src/lattice/shadow.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_compare.py`, `bench/calvin/lattice/tests/test_e1.py`, `bench/calvin/lattice/tests/test_facts.py`, `bench/calvin/lattice/tests/test_shadow.py`
- **Gate:** **clear** at `ff83060d993e` (gate v2, grounder v4, record `4d57ef7ad7a3`); unknown 0; map over 47 file(s), 4.6% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 234, regressions 0; {'new-pass': 43, 'P2P': 191}

**The doer's closing words:**

> Done — all twelve files are inside the unit's allowed set (`report.py` needed no change; `compare` is its own module).
> 
> **What I changed**
> 
> - **`shadow.py`** — `tree_digest(root)` (one SHA-256 over `RENAMED`'s files, path then bytes), `map_digest(path)` (the map file's own bytes) and `read_map`, so `e1 plan` and `e1 run` identify a shadow the same way.
> - **`facts.py`** — `Translated`: a facts arm over a shadow asks the **original** lattice's cell of the same grid id and writes each row's code-naming fields (`name`, `signature`; `name`, `wrote` on a dropped row) forward with `shadow.apply`. `missing` and `dropped` are carried; `source()` adds `translated_through`.
> - **`e1.py`** — `meta(..., shadow=…)` and `shadow_meta` (style, both digests, `from_sha`, and the map's `kept` rows, nested in the block); `ShadowLeak` + `check_leaks` (identifier-grained, messages and a G-mem `prompt`); `_same_target` holds a shadow run to `tree_sha256`; `default_grade(…, rename_in_target=…)`; `GenerateFailed(call=…)`; a failed call's `calls.jsonl` row written before the re-raise, at the record's cost or the estimate with `FAILED_COST`; `modal_generator(…, run_dir=…, ceiling_usd=…)` passing `--max-usd` = ceiling − spent.
> - **`modal_e1.py`** — `timeout_for(max_usd, gpu)` (`min(4 h, budget/$·s⁻¹ − 120 s)`, refused under 60 s, exit 2 with nothing called), `with_options(gpu=…, timeout=…)`, the record stating `max_usd` and `timeout`, and written in a `finally` so a timeout leaves `call.json` with `answered: 0`, the error and the host-wall cost before exiting non-zero.
> - **`compare.py` + `lattice e2 compare`** — per-arm pass@1 / pass@1(s) / pass@k in both runs with the shadow's deltas, per-cell greedy flips each way, each shadow named by its `shadow.style`; `NotComparable` (exit 2) on a model, `k`, params or cells mismatch.
> - **`cli.py`** — `e1 plan --rename/--original` (facts arm without `--original` refused, exit 2; leak gate before anything is written), `e1 run` reading the shadow from `meta.json`, the Modal generator given the run dir and the ceiling, and the `e2` verb.
> - **README** — E2 over a shadow (the two flags, the translation, the leak gate, what `kept` means), the cap's two fixes, `e2 compare`, and why the timeout arithmetic stays in the script (one place beside the price table; the test loads the script's source with `modal` stubbed, since the package may not import it).
> 
> **What I ran:** `cd bench/calvin/lattice && .venv/bin/python -m pytest -q` — **428 passed** (385 before). No model call, no Modal run, no shadow written outside a test's `tmp_path`.
> 
> One judgement call worth flagging: the spec listed `shadow` as `{style, map_sha256, tree_sha256, from_sha}` and separately said `meta.json` also records `kept`. I nested `kept` inside the `shadow` block rather than at the top level — it is a fact about the shadow, not about the run — and said so in the README and the docstring.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-151's E2 runner, routes E2-a to E2-g. Every item of the brief is in the diff; no defect found at the
  review. The doer nested `kept` inside the `shadow` block rather than beside it (said in its closing words) —
  right, it is a fact about the shadow.

  **On the host:** the package's suite from the branch, 407 passed and 21 skipped (all 21 need clang). **In the
  image** (pytest from a deps dir): 427 passed and 1 failed. The failure is `target_sha` on the fixture, an E1-era
  test, and it failed only because the worktree's `.git` file points outside the mount. It passes on the host.

  **On the real target** (`~/.hobbes/bench/calvin-lattice/e2/`):
  - The opaque shadow was written for E2-e and accepted in the image as E0 accepted the descriptive one: 493
    renamed, 106 kept, 0 prefixed; `make unittest` 1,447 of 1,447; `make unittest-simd` on the AVX512 backend,
    1,447 of 1,447; all 93 golds `pass` through its lattice.
  - Both shadows were planned for E2-a (Qwen, C-2 and C-3, k = 5, with the ledger through `--original`). The leak
    gate passed. Each plan is 1,209 requests with **E1's own ids and params**. **Every prompt equals E1's
    round-0 prompt with `shadow.apply` of that shadow's map, 1,209 of 1,209 on each**, so the names are the only
    variable.
  - The run path end to end with no spend: a C-2, k = 0 plan on each shadow, the shadow's golds replayed as
    completions, `e1 run --rounds 0` graded in the image through `--rename /target/shadow-map.json`: **93 of 93
    `pass` on each**, $0.00.
  - Before any of this, E1's own C-2/C-3 plan re-made on the target with the same ledger paths reproduced E1's
    requests 1,116 of 1,116, so E1's Qwen rows are E2's original.
