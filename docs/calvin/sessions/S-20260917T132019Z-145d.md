# Harness session `S-20260917T132019Z-145d`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.36-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build **R-qual**: a C++ semantic `calls` edge is **not drawn** when the callee is written through an explicit specialisation's name with template arguments (`test_format<20>::format(..)`) and lane B r (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T132019Z-145d/brief.md`, task sha256 `0f114cddf698`)
- **Parent:** `62ab0d996d55` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 94 of 140; result `success`; reported cost $7.1945 (the envelope's figure, on the subscription); wall 677.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×2
- **Policy:** 44 exec decision(s) — `allow`×42, `escalate`×2; escalated: `/bin/sh -c cd /tmp && rm -rf qm && mkdir -p qm && cat > qm/a.cpp <<'EOF'
namespace ns {
template <int N> struct test_format { static void format() {} };
templat`; `/bin/sh -c cd /tmp && rm -rf qm && mkdir -p qm && cat > qm/a.cpp <<'EOF'
namespace ns {
template <int N> struct test_format { static void format() {} };
templat → expired`; records: stream opened→closed
- **Edits:** 32 edit(s) to 13 file(s); first at 2.4 min after launch; files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/derive/gate.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/src/hobbes/extract/tail.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`, … and 3 more
- **Branch:** `hobbes/S-20260917T132019Z-145d`, 1 commit(s); files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/derive/gate.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/src/hobbes/extract/tail.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_gate.py`, `pipeline/tests/test_scipsource.py`, `pipeline/tests/test_tail.py`
- **Gate:** **clear** at `62ab0d996d55` (gate v2, grounder v4, record `5239d5c97155`); unknown 0; map over 13 file(s), 3.9% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1040, regressions 0; {'P2P': 1004, 'skip': 16, 'new-pass': 18, 'removed': 1, 'uncollected': 1}; build {'P2P': 2}

**The doer's closing words:**

> R-qual is built and committed (`9f87cdf`), one commit on `hobbes/S-20260917T132019Z-145d`.
> 
> **What changed**
> 
> - `evidence.py` — `Site.qualifier` (syntax provider only; C++'s immediate qualifier, set only when it carries template arguments) and `Resolved.qualifier`, which `join` copies from the site on the semantic-hit branch alone.
> - `cppsource.py` — `_written_qualifier` fills the site's qualifier from `qualifiers[-1]` for a `qualified` shape containing `<`; `_record_full_specialisation` notes a `template <>` (empty parameter list) on a `struct`/`class` whose name is a `template_type`, and the layer dict gains `full_specializations` as symbol ids. `graph.json`'s symbols are untouched.
> - `scipsource.project` — new `full_specializations` keyword. For a lane-B `calls` fact with a qualifier, the callee's owner is taken (`id` without the last `::member`); if it is a full specialisation and `_qualifier_contradicts` reads the same base name with different argument lists (whitespace removed, bracket-depth parsing, never a regex), nothing is drawn and the site joins a new `qualifier_mismatch` list beside `below_floor`.
> - `extract/__init__.py` — passes the C++ layer's set (empty without C++) and counts the sites per file as `qualifier-mismatch`, exactly as `below_floor` becomes `below-floor`.
> - `tail.py`, `gate.py`, `knowledge.go` — the class, documented in the docstring list, in `CLASSES_AVAILABLE["cpp"]` only, second-to-last in `ALL_CLASSES`; the gate's `laneb-miss` row; the proxy's meaning, not in `notModelled`.
> 
> **Checked**
> 
> - `/work/pipeline/.venv/bin/python3 -m pytest tests/test_cppsource.py tests/test_evidence.py tests/test_scipsource.py tests/test_tail.py tests/test_gate.py -q` → **332 passed, 3 skipped**; the whole pipeline suite → **1691 passed, 11 skipped**.
> - `cd go && go test ./internal/knowledge/` → ok; `go build ./... && go test ./...` → all packages ok.
> - An end-to-end read of fmt's own shape (a temp repo, lane B's resolution hand-built): `ns::test_format<20>::format()` resolved to `test_format<0>::format` draws `a.g → a.ns::test_format<0>::format` without the set and nothing with it, with the site in `qualifier_mismatch`.
> 
> **Counts.** New tests: 3 in `test_cppsource.py` (the qualifier as written; the two shapes that record none; the specialisation set holding only the `template <>` class), 11 in `test_scipsource.py` (3 fire — namespaced, file-scope, the derived-specialisation base call; 8 draw, including the partial owner, the same owner declared full, the matching specialisation, whitespace, a different base name, no qualifier, syntactic tier, no C++ layer), 1 in `test_evidence.py`, 2 in `test_tail.py`, 1 in `test_gate.py`. Three existing assertions were widened because a drift test forced it: cpp's class list (seven → eight), and the two "classes this lane cannot report" strings (one in `test_tail.py`, one in `knowledge_test.go`) that now end in `qualifier-mismatch`. No other assertion moved.
> 
> **Deviations**
> 
> - `_owner_component` splits the owner id's last `::` component at bracket depth 0, not by a plain `rpartition`, and strips a module prefix at the last `.` before the first `<`. Without both, an owner like `ns::P<std::vector<U>>` or a file-scope `src/fmt.T<0>` reads as the wrong class — in the safe direction (the rule silently stops), but the file-scope case is one fmt has, so it is parsed properly rather than left to fail quietly.
> - The rule reads the callee's owner from the symbol id, so a specialisation whose member the graph keeps no symbol for never reaches it (the fact is `below_floor` first). Fires less, not more.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read in full against ADR-125 as amended. The four conditions hold in the code: the qualifier is the immediate one and only on a lane B hit; the owner must be in the `template <>` set; the argument lists are parsed by bracket depth, and an unreadable list does not fire. Both deviations fire less, not more. Tests cover the partial and primary owners, whitespace, a different base name, and syntactic tier. Host: `lane_b` 7 passed, full pytest 1,701 passed (the tracker's drift test was red until this block), Go knowledge ok. One residual beyond the ADR's alias case, registered in C-153 by the developer: a written default argument (`formatter<int>` for an owner spelled `formatter<int, char>`) also reads as a difference. A duplicate launch of this same brief (`S-20260917T132013Z-e1c6`, the developer's mistake: a chained command read as failed) was stopped before its gate; its branch holds commit `b4d8b38`, is not merged and has no session log.
