# Harness session `S-20260917T180521Z-b597`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.40-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Surface ADR-129's minted symbols where a user meets them: **the ingest summary says how many definitions were read from the index and how many were refused, by reason; `who_calls` marks a symbol lane  (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T180521Z-b597/brief.md`, task sha256 `705b3335f6e6`)
- **Parent:** `37a157a5a65c` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 40 of 100; result `success`; reported cost $1.9013 (the envelope's figure, on the subscription); wall 241.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 10 exec decision(s) — `allow`×8, `escalate`×2; escalated: `/bin/sh -c git add pipeline/src/hobbes/cli.py pipeline/tests/test_cli.py go/internal/knowledge/knowledge.go go/internal/knowledge/knowledge_test.go && git commi`; `/bin/sh -c git add pipeline/src/hobbes/cli.py pipeline/tests/test_cli.py go/internal/knowledge/knowledge.go go/internal/knowledge/knowledge_test.go && git commi → expired`; records: stream opened→closed
- **Edits:** 9 edit(s) to 4 file(s); first at 1.2 min after launch; files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/cli.py`, `pipeline/tests/test_cli.py`
- **Branch:** `hobbes/S-20260917T180521Z-b597`, 1 commit(s); files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/src/hobbes/cli.py`, `pipeline/tests/test_cli.py`
- **Gate:** **clear** at `37a157a5a65c` (gate v2, grounder v4, record `1c098bcb9f07`); unknown 0; map over 6 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 496, regressions 0; {'P2P': 485, 'new-pass': 9, 'skip': 2}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `3758b57`.
> 
> **What changed**
> 
> - `pipeline/src/hobbes/cli.py` — `_print_minted(graph.get("minted"))` after `_print_implements`, in its style: silent when the key is absent or nothing was read; otherwise `    read from the index: <n> definition(s) in <m> file(s) lane A parsed with errors (C-145; targets only — calls written inside them keep their caller)`, and a second `    not minted: …` line when any refusal is non-zero.
> - `go/internal/knowledge/knowledge.go` — `symbol` gains optional `declared_by`; `graphIndex` gains `laneBDeclared` and `mintedNote`; `WhoCalls` writes the note right after the header, before any caller. No other tool touched.
> - `pipeline/src/hobbes/graphdiff.py` — **unchanged, deliberately.** It reads `nodes`, `module_edges` and `symbol_edges` only; it never reads `symbols`, a symbol's `line` or `end_line`, so a minted symbol's `end_line == line` breaks no assumption there. `test_graphdiff.py` untouched for the same reason.
> 
> **Checks**
> 
> - `cd /work/pipeline && .venv/bin/python3 -m pytest tests/test_cli.py -q` → **59 passed**; the full pipeline suite → **1823 passed, 14 skipped**.
> - `cd /work/go && go test ./internal/knowledge/` → **ok**, 50 test functions (`go vet` clean; `go build ./...` and the proxy/web suites green, since they decode the same graph).
> 
> **Deviations**
> 
> - The task's example second line read `not minted: 308 declaration, 74 kind`, but its rule said "the fixed order `minted.py` declares them" — where `kind` precedes `declaration`. I followed the rule (declared order, not count order); a test pins it, with the sample data from the example.
> - One addition the task didn't ask for, on the "says more about the limit, never less" rule: a refusal reason not in this Hobbes's `REFUSALS` (an artifact from another version) is printed after the known ones rather than dropped — a silently omitted refusal would read as a row that was minted.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: merged no-ff. The summary's two lines and `who_calls`' note read on fmt after the merge; `graphdiff` left alone for the reason the doer gives (it never reads `symbols`). Both deviations say more about the limit and stand.
