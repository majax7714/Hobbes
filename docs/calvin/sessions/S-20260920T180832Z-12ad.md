# Harness session `S-20260920T180832Z-12ad`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.61-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-144: a reference to a shorthand property is a reference to what the shorthand names (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T180832Z-12ad/brief.md`, task sha256 `2edd47f08a44`)
- **Parent:** `e7088a777d42` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 53 of 140; result `success`; reported cost $3.5270 (the envelope's figure, on the subscription); wall 482.7 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 28 exec decision(s) — `allow`×18, `escalate`×10; escalated: `/bin/sh -c cd pipeline/tests/fixtures/minicjs && for f in package.json index.js index2.js lib/app.js test/direct.js test/through.js test/twohop.js; do echo "===`; `/bin/sh -c cd pipeline/tests/fixtures/minicjs && for f in package.json index.js index2.js lib/app.js test/direct.js test/through.js test/twohop.js; do echo "=== → expired`; `/bin/sh -c cd /work && git --no-pager diff --stat && git status --short && git --no-pager diff scip/index.mjs`; `/bin/sh -c cd /work && git --no-pager diff --stat && git status --short && git --no-pager diff scip/index.mjs → expired`; `/bin/sh -c cd /work && git --no-pager diff -- scip/index.mjs`; `/bin/sh -c cd /work && git --no-pager diff -- scip/index.mjs → expired`; `/bin/sh -c cd /work && git add scip/index.mjs scip/test/index.test.mjs pipeline/tests/fixtures/minicjs/lib/tools.js pipeline/tests/fixtures/minicjs/test/namespa`; `/bin/sh -c cd /work && git add scip/index.mjs scip/test/index.test.mjs pipeline/tests/fixtures/minicjs/lib/tools.js pipeline/tests/fixtures/minicjs/test/namespa → expired`; `/bin/sh -c cd /work && git commit -q -F - <<'MSG'
feat(extract): a reference to a shorthand property is a reference to what the shorthand names (ADR-144)

In th`; `/bin/sh -c cd /work && git commit -q -F - <<'MSG'
feat(extract): a reference to a shorthand property is a reference to what the shorthand names (ADR-144)

In th → expired`; records: stream opened→closed
- **Edits:** 16 edit(s) to 5 file(s); first at 3.0 min after launch; files: `pipeline/tests/fixtures/minicjs/lib/tools.js`, `pipeline/tests/fixtures/minicjs/test/namespace.js`, `pipeline/tests/test_tsjs_shorthand.py`, `scip/index.mjs`, `scip/test/index.test.mjs`
- **Branch:** `hobbes/S-20260920T180832Z-12ad`, 1 commit(s); files: `pipeline/tests/fixtures/minicjs/lib/tools.js`, `pipeline/tests/fixtures/minicjs/test/namespace.js`, `pipeline/tests/test_tsjs_shorthand.py`, `scip/index.mjs`, `scip/test/index.test.mjs`
- **Gate:** **clear** at `e7088a777d42` (gate v2, grounder v4, record `e08f41a4c780`); unknown 0; map over 76 file(s), 2.2% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 91, regressions 0; {'new-pass': 2, 'skip': 1, 'P2P': 87, 'not-run': 1}

**The doer's closing words:**

> Committed clean. Done.
> 
> **What I changed**
> 
> - `scip/index.mjs`, inside `decode` only:
>   - a `TS_SCHEME` guard and a `rangeKey(file, range)` helper (SCIP's 3-element single-line form spelled out, so two occurrences at one token key the same);
>   - the first pass now also collects, for `scip-typescript` symbols, `propertyDefs` (a `meta` property → a key per definition occurrence) and `refsAtRange` (an exact `(file, range)` → the symbols referenced there), with the doc comment the brief asked for (the two shapes, `alpha0:`/`alpha1:`, why a value property and a literal's method are not taken, ADR-144);
>   - once `definitions` is settled, `aliasOf`: a property with exactly one definition occurrence where exactly one symbol *that has a definition* is referenced at that same range;
>   - in the reference loop, an occurrence with no `definitions` entry that is such an alias is filed under what the shorthand names — `nameOf(F)`, F's `def_file`/`def_line`, its own `file`/`line`/`col` — instead of an `external` row. `ownFile`, the ambiguous/multi-defined sets and the C++ rules are untouched.
> - `scip/test/index.test.mjs` — one `describe` with seven cases: the shorthand (reference onto the function, no external row), the value property, two defined symbols at the range, a property defined twice, a shorthand naming an undefined (package) symbol, a scip-python `:` descriptor untouched, and an ordinary reference plus an ordinary external unchanged.
> - `pipeline/tests/fixtures/minicjs/lib/tools.js` and `test/namespace.js` (new files only; no existing fixture file touched), and `pipeline/tests/test_tsjs_shorthand.py` — the `lane_b` case for `alpha`/`beta` (`calls`, `semantic`, lane B evidence at the call lines, and nothing else leaving `run`) and its lane-A twin.
> 
> **The count.** `decode` returns `shorthand_refs` beside `tu_split` and `overload_sites`, and the new tests assert it. It does **not** reach the facts header: `indexStage` forwards an enumerated list of fields, so a counter of that kind rides out only when it is named there (`implements_outside` and its two siblings do; `tu_split` and `overload_sites` do not — they are read by `degradations`). Forwarding it would be a change outside `decode`, which the brief excludes; the facts trailer would take an extra root-level integer without a schema change if you later want it.
> 
> **What I ran:** `cd scip && npm test` (94 pass, 0 fail — 87 existing plus the 7 new), `cd pipeline && .venv/bin/python3 -m pytest -q` (2128 passed, 18 skipped; `test_tsjs_reexport.py` unchanged and still passing, its `lane_b` half skipping as before), `cd tsextract && npm test` (47 pass). The two `lane_b` cases skip here — containment is unavailable in the sandbox — so the rule's end-to-end run through a real index is the developer's first run. I also checked the lane-A twin's claim directly against the tree before asserting it: with `HOBBES_SCIP=0` the fixture draws no edge at all out of `test/namespace.run`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: 53 turns of 140, $3.53, five files, all the partition's (the two new
  fixture files spelled out at file grain). The rule was measured, read raw in
  the image and simulated from a scratch worktree before the brief; the doer's
  `decode` is that rule with one addition of its own and a right one — a
  `rangeKey` that spells SCIP's three-element range out. It reported that
  `shorthand_refs` cannot reach the facts header from inside `decode` and left
  it, as the brief said to. It checked the lane-A twin's claim in the tree
  before asserting it. On the host, in a worktree of the branch: node 94,
  pytest 2,146, thirteen `lane_b` green — the new case against a real index, its
  first run; nine TS/JS cells regraded, **each row-identical to its simulation,
  tiers included** (cue +124, xmpp.js +29, 0 contradicted); seven
  other-language cells row-identical pre/post with the index re-run (cache 0
  hit). Merged `--no-ff` (`9f8dff9`).
