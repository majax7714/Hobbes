# Harness session `S-20260912T174351Z-404f`

*2026-09-12 · `hobbes dispatch` v1 · Hobbes 0.2.1-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Hold the knowledge proxy's language tables to the pipeline's tail table, so C (and `.mts`/`.cts`) show up in `list_blind_spots`. (brief `/home/mmarrujo/.hobbes/sessions/S-20260912T174351Z-404f/brief.md`, task sha256 `90088326a698`)
- **Parent:** `fffded565d7e` of `hobbes_public`
- **Doer:** 2.1.269 (Claude Code); model the default; turns 25 of 60; result `success`; reported cost $0.8995 (the envelope's figure, on the subscription); wall 203.2 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 16 exec decision(s) — `allow`×8, `escalate`×8; escalated: `/bin/sh -c gofmt -l internal/knowledge`; `/bin/sh -c gofmt -l internal/knowledge → expired`; `/bin/sh -c gofmt -l internal/knowledge`; `/bin/sh -c gofmt -l internal/knowledge → expired`; `/bin/sh -c go fmt ./internal/knowledge/...`; `/bin/sh -c go fmt ./internal/knowledge/... → expired`; `/bin/sh -c cd /work/go && gofmt -l internal/knowledge/knowledge.go internal/knowledge/knowledge_test.go`; `/bin/sh -c cd /work/go && gofmt -l internal/knowledge/knowledge.go internal/knowledge/knowledge_test.go → expired`
- **Branch:** `hobbes/S-20260912T174351Z-404f`, 1 commit(s); files: `go/internal/knowledge/knowledge.go`, `go/internal/knowledge/knowledge_test.go`, `pipeline/tests/test_tail.py`
- **Gate:** **clear** at `fffded565d7e` (gate v2, grounder v3, record `7dc7ac02d7b5`); unknown 0; map over 3 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 127, regressions 0; {'P2P': 123, 'new-pass': 4}; build {'P2P': 2}

**The doer's closing words:**

> **What changed:**
> - `go/internal/knowledge/knowledge.go`: `langByExt` gained `.c`/`.h` → `c` and `.mts`/`.cts` → `ts/js`; `artifactLangBucket` gained `"c": "c"`; updated the comment above it to point at the two tables mirroring `tail._LANG_BY_EXT` and being held to it by a test.
> - `go/internal/knowledge/knowledge_test.go`: added `TestBlindSpotsNameCAsUnverified` (a C-scoped `ListBlindSpots` now prints `c: not verified on any repo` under `verification base` and a `capture [c]` line) and `TestBlindSpotsMtsBucketsAsTsJs` (a `.mts` row buckets as `ts/js`).
> - `pipeline/tests/test_tail.py`: added a drift-check class that regex-parses the `langByExt` and `artifactLangBucket` Go map literals out of `knowledge.go` and asserts `langByExt == tail._LANG_BY_EXT`, and that every `tail.CLASSES_AVAILABLE` language appears as a value in `artifactLangBucket`.
> 
> **What I ran:**
> - `go vet ./internal/knowledge/` — clean.
> - `gofmt -l internal/knowledge` — the sandbox policy escalated every invocation (plain, via `go fmt`, and scoped to the two files) and each escalation expired with no approver, so it never ran; per the session's own rule this is the policy's answer, not something to route around. I kept the new code in the same tab/brace style as the surrounding file, which is what `gofmt` would produce.
> - `go test ./internal/knowledge/... ./internal/proxy/...` — both packages pass.
> - `python -m pytest -q tests/test_tail.py` from `pipeline` — note the shell's `PATH` puts `bench/atlas0/.venv` ahead of `pipeline/.venv`, which lacks `tree_sitter_c` and breaks collection; running the pipeline's own venv explicitly (`/work/pipeline/.venv/bin/python -m pytest -q tests/test_tail.py`) gives 49 passed, including the two new drift tests.
> 
> Committed to `main`... actually to the session branch `hobbes/S-20260912T174351Z-404f` as `7123217`, touching only the three files the gate checks.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear — the diff held nothing a blocking class covers, and review found no error.
- outcome: merged — fast-forward to `7123217`, keeping the doer's `hobbes-dispatch` authorship.
- notes:
  - **Every expired escalation was read: four, all `gofmt`/`go fmt`.**
    - A dispatch runs under `calvin.box.policy` (`default: escalate`,
      a 5 s timeout). It allows `go test`, `go build`, `go vet`,
      `go generate`, `go list`, `go env` and `go version`, but not
      `gofmt`.
    - The brief asked for `gofmt -l`, so the doer needed it: the policy
      and the brief disagree.
    - The doer did not route around the refusal, as the brief says.
    - Checked on the host: both Go files are `gofmt`-clean.
    - Proposed, for Max: allow `gofmt -l*` (read-only) in the box.
  - **The session's `PATH` puts the wrong venv first.**
    - `dispatch.py` builds `--path` from the venv bins *sorted*, so
      `/work/bench/atlas0/.venv/bin` comes before
      `/work/pipeline/.venv/bin`.
    - A bare `python` is Atlas-0's, which lacks `tree_sitter_c`.
    - The brief's "the venv's python is first on PATH" is wrong once two
      venvs are mounted. The doer found it and ran
      `/work/pipeline/.venv/bin/python`.
    - It may be behind the three sandbox-only `test_ttt_units` failures
      of `eef8` and `e6db`; that is not verified.
    - Proposed: the pipeline venv first, or the interpreter named per
      tree in the brief.
  - **Egress:** none refused. `test_bench.py`'s `llm` GET did not run,
    since only `test_tail.py` was run in Python.
  - **Host, after the merge:** `go vet`, `gofmt`, `go test ./...` (330)
    and pytest 1,445, all green.
  - Reviewed by Claude (the developer session), 2026-09-12. Max's review of the merge is pending.
  - **2026-09-12, later: both proposals were made (Max), 0.2.3-beta.**
    `gofmt -l` and `-d` are allowed and `-w` escalates. The session's
    `PATH` puts the outermost Python tree first, and the brief names
    each tree's interpreter.
