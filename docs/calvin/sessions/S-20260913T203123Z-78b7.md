# Harness session `S-20260913T203123Z-78b7`

*2026-09-13 · `hobbes dispatch` v1 · Hobbes 0.2.12-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Render a tracker of the harness sessions from their logs into `docs/calvin/sessions/README.md`, with a drift test. The design is decided (Max, 2026-09-13). Follow it; do not re-decide it. (brief `/home/mmarrujo/.hobbes/sessions/S-20260913T203123Z-78b7/brief.md`, task sha256 `4ef35760a64d`)
- **Parent:** `27f98af54816` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 29 of 100; result `success`; reported cost $1.3611 (the envelope's figure, on the subscription); wall 518.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 4 exec decision(s) — `allow`×4
- **Edits:** 3 edit(s) to 3 file(s); first at 7.7 min after launch; files: `docs/calvin/sessions/README.md`, `pipeline/scripts/calvin_tracker.py`, `pipeline/tests/test_calvin_tracker.py`
- **Branch:** `hobbes/S-20260913T203123Z-78b7`, 1 commit(s); files: `docs/calvin/sessions/README.md`, `pipeline/scripts/calvin_tracker.py`, `pipeline/tests/test_calvin_tracker.py`
- **Gate:** **clear** at `27f98af54816` (gate v2, grounder v3, record `8a489ca31369`); unknown 0; map over 78 file(s), 0.3% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 9, regressions 0; {'new-pass': 9}

**The doer's closing words:**

> Committed as `18001d5` on the current branch, touching only the three files the gate checks.
> 
> **What changed:**
> - `pipeline/scripts/calvin_tracker.py` — parses the seven harness record lines (`Task`, `Doer`, `Egress`, `Policy`, `Branch`, `Gate`, `Verify`) and the review block's `gate`/`outcome` lines out of every `docs/calvin/sessions/S-*.md`, raising `ValueError` naming the file and line on anything unrecognized. `render` rewrites the README's `<!-- tracker:begin/end -->` block; `check` exits 1 (naming `render`) if it's stale.
> - `pipeline/tests/test_calvin_tracker.py` — the drift test, pinned-field checks against the real `9cad`/`96df`/`b126`/`3c45`/`a323`/`5d5f` logs, a totals test over three synthetic logs in `tmp_path`, and an unparseable-`Doer`-line test.
> - `docs/calvin/sessions/README.md` — new `## Tracker` section with the rendered table for all fourteen sessions and the totals lines (14 of 40, 4 areas, 0 false-blocks/missed, 14 egress refusals, 18 policy escalations, $32.16 reported cost).
> 
> **Ran:** `/work/pipeline/.venv/bin/python3 -m pytest tests/test_calvin_tracker.py -q` (9 passed) and `/work/pipeline/.venv/bin/python3 scripts/calvin_tracker.py check` (exit 0) from `pipeline/`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged as `578ff1b` (not squashed), with a developer follow-up in the release commit; released as 0.2.13-beta
- notes: Read the diff (2026-09-13). The parser is strict by design: a line that fits no known shape raises, naming the file and the line. The drift check and the 9 tests pass on the host from a worktree of the branch. **Two follow-ups by the developer, in the release commit:** `417f` (the `path` alias) read `harness and sandbox`, because the brief's prefix table put all of `go/internal/proxy/` under the harness, while the knowledge tools' MCP schemas live in `go/internal/proxy/knowledge.go`. That was the brief's error, not the doer's; the file now maps to `knowledge tools`, pinned by a test. And `main()` gained its docstring. The table was re-rendered with this session and `e537`.
