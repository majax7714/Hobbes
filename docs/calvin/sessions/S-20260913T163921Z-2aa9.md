# Harness session `S-20260913T163921Z-2aa9`

*2026-09-13 · `hobbes dispatch` v1 · Hobbes 0.2.10-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Contain a session to its own dir, and make `find`'s deleting and executing forms and `xargs` a question in both boxes. The design is ADR-107's amendment of 2026-09-13 ("a session mounts only its own d (brief `/home/mmarrujo/.hobbes/sessions/S-20260913T163921Z-2aa9/brief.md`, task sha256 `f9c5ee66f974`)
- **Parent:** `c82e6868dbb1` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 57 of 150; result `success`; reported cost $1.5814 (the envelope's figure, on the subscription); wall 346.6 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 5 exec decision(s) — `allow`×5
- **Edits:** 16 edit(s) to 7 file(s); first at 1.2 min after launch; files: `go/cmd/hobbes-policy/main_test.go`, `go/cmd/hobbes-session/main_test.go`, `go/internal/sandbox/sandbox.go`, `go/internal/sandbox/sandbox_test.go`, `pipeline/src/hobbes/bench/bench.box.policy`, `pipeline/src/hobbes/derive/calvin.box.policy`, `sandbox/exitcheck.py`
- **Branch:** `hobbes/S-20260913T163921Z-2aa9`, 1 commit(s); files: `go/cmd/hobbes-policy/main_test.go`, `go/cmd/hobbes-session/main_test.go`, `go/internal/sandbox/sandbox.go`, `go/internal/sandbox/sandbox_test.go`, `pipeline/src/hobbes/bench/bench.box.policy`, `pipeline/src/hobbes/derive/calvin.box.policy`, `sandbox/exitcheck.py`
- **Gate:** **clear** at `c82e6868dbb1` (gate v2, grounder v3, record `0b368cb17068`); unknown 0; map over 7 file(s), 14.5% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 77, regressions 0; {'P2P': 73, 'new-pass': 2, 'skip': 2}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `4b7e5eb` on branch `hobbes/S-20260913T163921Z-2aa9`.
> 
> **What changed:**
> 1. `go/internal/sandbox/sandbox.go`: `mounts()` now mounts `<HostSessions>/<SessionID>` at `/sessions/<SessionID>` (via `p.sessionHome()`) instead of the whole sessions root — no other session's clone or records are reachable. Updated the package comment and `SessionsRoot`'s comment accordingly.
> 2. `go/internal/sandbox/sandbox_test.go`: updated the mounts assertion to the new per-session path, added `TestSessionsMountIsOnlyTheSessionsOwnDir` (no mount's host side is the sessions root), and fixed the reviewer-worktree test's stale `SessionsRoot+":rw,z"` assertion.
> 3. `go/cmd/hobbes-session/main_test.go`: added `TestALiveSessionMountsOnlyItsOwnSessionDir`, the P10 guarantee test — a real session lists `/sessions` and can't read a sibling session's file; skips without podman/image like the egress live test.
> 4. `go/cmd/hobbes-policy/main_test.go`: added `TestFindsExecutingFormsAndXargsEscalateInBothBoxes`, a table test resolved against both real box policies covering all the cases from the task spec.
> 5. `pipeline/src/hobbes/derive/calvin.box.policy` and `.../bench/bench.box.policy`: added escalate rules for `find *-delete*`, `find *-exec*`, `find *-ok*` after the `find*` allow, changed `xargs*` to escalate, and rewrote the headers to describe the new mount boundary and that these are still questions, not boundaries (C-140 for the calvin box).
> 6. `sandbox/exitcheck.py`: the scripted driver now drops into `<sessions>/<session>/driver.py` and runs from there, since the root is no longer mounted.
> 
> **Checks run:**
> - `cd go && go build ./...` — clean
> - `cd go && go test ./internal/sandbox/ ./cmd/hobbes-policy/ ./cmd/hobbes-session/` — all pass
> - `cd pipeline && .venv/bin/python3 -m pytest tests/test_dispatch.py tests/test_bench.py tests/test_harness.py tests/test_policy.py -q` — 104 passed, 1 skipped
> 
> I confirmed `setupWithStart` in `main.go` uses `os.MkdirAll(sessionDir, 0o700)`, which doesn't refuse an already-existing session dir, so `exitcheck.py`'s `os.makedirs(exist_ok=True)` before the launch is safe and I left the launcher untouched.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes:
  - The diff does what ADR-107's 2026-09-13 amendment decided.
    - The mount is the session's own dir at `/sessions/<id>`, with every
      in-container path unchanged.
    - Both boxes escalate `find -delete/-exec/-ok` and `xargs`.
    - The Calvin box's header names C-140.
    - The exit check's driver moved into the session dir.
    - The doer also fixed a stale reviewer-worktree assertion.
  - **The live test failed on the host, its first real run.** It skips
    in the sandbox, and verify's two skips are the live tests.
    - The guarantee held: `ls /sessions` listed only `S-live-mount`,
      and the sibling's file read "No such file or directory".
    - The assertion searched all of stdout for `S-sibling`, and the
      `cat` error echoes that path.
    - This is an error outside every class (a test that matches its own
      command's echo), not a gate defect. The developer fixed it in the
      following commit.
    - A live test's first run is always the developer's host run.
  - **Egress refused a plain GET to `llm` once**, at 16:44:32, inside the
    doer's `go test` window (16:44:25–16:44:35). It was a test's own
    request, not the doer reaching out, and the refusal worked. `a323`'s
    refusal of the same host came during its pytest run.
  - **Policy:** 5 exec decisions, all allowed. No escalation and no deny.
