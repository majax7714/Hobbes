# Harness session `S-20260912T164904Z-eef8`

*2026-09-12 · `hobbes dispatch` v1 · Hobbes 0.2.0-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Add C as a language to Hobbes: lane A, the syntax provider (architecture §3.7 step 2). This is the first unit of the C work. (brief `/home/mmarrujo/.hobbes/sessions/S-20260912T164904Z-eef8/brief.md`, task sha256 `012ac120436f`)
- **Parent:** `fb24216b942e` of `hobbes_public`
- **Doer:** 2.1.269 (Claude Code); model the default; turns 74 of 200; result `success`; reported cost $4.6839 (the envelope's figure, on the subscription); wall 1141.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×5; refused `llm`×1
- **Policy:** 20 exec decision(s) — `allow`×20
- **Branch:** `hobbes/S-20260912T164904Z-eef8`, 1 commit(s); files: `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/csource.py`, `pipeline/src/hobbes/extract/tail.py`, `pipeline/tests/fixtures/minic/Makefile`, `pipeline/tests/fixtures/minic/include/minic/config.h`, `pipeline/tests/fixtures/minic/src/main.c`, `pipeline/tests/fixtures/minic/src/shapes.c`, `pipeline/tests/fixtures/minic/src/util.c`, `pipeline/tests/fixtures/minic/src/util.h`, `pipeline/tests/fixtures/minic/tests/test_util.c`, `pipeline/tests/test_csource.py`, `pipeline/tests/test_discover.py`, `pipeline/tests/test_tail.py`
- **Gate:** **clear** at `fb24216b942e` (gate v2, grounder v3, record `dea63a772a14`); unknown 0; map over 76 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 662, regressions 0; {'P2P': 588, 'skip': 12, 'F2F': 1, 'new-pass': 60, 'uncollected': 1}

**The doer's closing words:**

> Committed on `hobbes/S-20260912T164904Z-eef8` as `984daab`.
> 
> **What I built:** `pipeline/src/hobbes/extract/csource.py` — a tree-sitter-c lane A walk on the `rustsource.py`/`javasource.py` contract, with no lane B (no C indexer exists yet). Wired into `extract_repo` (a `c` layer, no `PackContext` field, no `_lane_b_facts` entry) and `tail.py` (`.c`/`.h` → `"c"`, a pinned C11 builtin list + `__builtin_*` prefix rule, `CLASSES_AVAILABLE["c"]` restricted to the five classes this walk can produce). Fixture at `tests/fixtures/minic/` hand-built to exercise every decision in the spec; tests in `test_csource.py`, plus C cases added to `test_discover.py` and `test_tail.py`.
> 
> **Where the spec pinched in practice:**
> - Header guards (`#ifndef`/`#define`/`#endif`) wrap their contents in a `preproc_ifdef` node one level deeper than `translation_unit`'s direct children — a naive top-level walk would have seen nothing inside any of the fixture's headers. Fixed by recursing transparently through `preproc_ifdef`/`preproc_if`/`preproc_elif`/`preproc_else`.
> - A local variable declared *with* an initializer (`int (*fp)(int,int) = adder.add;`) wraps its declarator in `init_declarator` before the `function_declarator` — a parameter never does this. The function-pointer-binding detector has to check for and unwrap that case separately.
> - Decision 8 explicitly excludes `below-floor` from `CLASSES_AVAILABLE["c"]`, unlike every other language's row — correct, since that class is only ever populated from a lane-B projection, which never runs for C.
> - The parenthesized-dereference call's terminal identifier (`fp` in `(*fp)(x)`) turned out to double naturally as this language's "local-binding" tail evidence once function-pointer bindings were tracked for the fallback-suppression rule — no separate mechanism needed.
> 
> **What I ran:** `python -m pytest -q tests/test_csource.py tests/test_discover.py tests/test_tail.py tests/test_emit.py tests/test_verification.py` (141 passed), then the whole suite (`python -m pytest -q`): 1421 passed, 3 failed, 8 skipped. The 3 failures are in `test_ttt_units.py` (git-based unit extraction) and are pre-existing — I confirmed via `git stash` that they fail identically on the unmodified tree, unrelated to this change.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear — the diff held nothing a blocking class covers. The four defects below are outside every class (recall, a tie rule, id consistency, a path rule), so they are notes, not a `missed`.
- outcome: merged — fast-forward to `984daab`, keeping the doer's `hobbes-dispatch` authorship. A rework session was dispatched for the four defects.
- notes:
  - **Egress.** The one refusal (`GET llm`) came from the suite, not the doer. `test_bench.py` builds runtimes on `http://llm/v1`, and inside the session the proxy answers a plain GET with a refusal. The refusal worked; nothing was needed.
  - **The doer's three `test_ttt_units.py` failures** are the sandbox's alone. On the host that file passes 24/24, and the full suite on `main` after the merge passed 1,432.
  - **Checked on a real C repo** (DaveGamble/cJSON `fb16e5c`, host, no lane B):
    - 0.3 s; 99 C modules; 1,298 symbols;
    - 1,189 call edges, all `syntactic`;
    - 39 tests;
    - of 4,292 C sites: 2,125 fallback-resolved, 344 builtin-name, 1,796 unclassified (mostly Unity's `TEST_ASSERT_*` macros, which see defect 1).
    - Four sampled edges were hand-checked against their cited lines, and all four were right.
  - **Defects found at review**, all in the rework:
    1. `extern "C"` bodies are never walked. tree-sitter-c parses the guard as a `linkage_specification`, so `unity.h` yields 0 of its 341 function-like macros and `cJSON.h` 0 of 9.
    2. Rank 1 of the fallback picks the last same-file definition on a tie, where decision 6 says abstain. `#ifdef` arms defining one name twice are common C.
    3. Duplicate symbol ids leave the layer (73 in cJSON's Unity tree). `_merge_symbols` keeps the first, so a fallback target can name the dropped one.
    4. `_normalize` clamps a `..` above the repo root, so `#include "../x.h"` from a root file resolves to the root's `x.h`.
  - **The brief's decisions that pinched,** per the doer: header guards needed a transparent walk, and an initialized local needed its `init_declarator` unwrapped. Both are handled.
  - Reviewed by Claude (the developer session), 2026-09-12. Max's review of the merge is pending.
