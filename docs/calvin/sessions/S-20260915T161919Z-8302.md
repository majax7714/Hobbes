# Harness session `S-20260915T161919Z-8302`

*2026-09-15 · `hobbes dispatch` v1 · Hobbes 0.2.21-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make C++'s lane B answer only where it is sure, in two rules: the helper's decode abstains where one call site's references name more than one overload, and the join withholds lane A's fallback in a C (brief `/home/mmarrujo/.hobbes/sessions/S-20260915T161919Z-8302/brief.md`, task sha256 `9372bb890731`)
- **Parent:** `d35e2bf0ff0e` of `hobbes_public`
- **Doer:** 2.1.272 (Claude Code); model claude-opus-5; turns 77 of 120; result `success`; reported cost $6.4806 (the envelope's figure, on the subscription); wall 523.9 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `llm`×1
- **Policy:** 26 exec decision(s) — `allow`×26; records: stream opened→closed
- **Edits:** 27 edit(s) to 7 file(s); first at 2.9 min after launch; files: `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_scipsource_c.py`, `scip/index.mjs`, `scip/test/index.test.mjs`
- **Branch:** `hobbes/S-20260915T161919Z-8302`, 3 commit(s); files: `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_scipsource_c.py`, `scip/index.mjs`, `scip/test/index.test.mjs`
- **Gate:** **clear** at `d35e2bf0ff0e` (gate v2, grounder v3, record `965b8802585d`); unknown 0; map over 7 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 862, regressions 0; {'skip': 13, 'P2P': 833, 'new-pass': 14, 'uncollected': 1, 'not-run': 1}

**The doer's closing words:**

> ADR-113 §2's third amendment is implemented in two rules, three commits.
> 
> **Rule 1 — the helper's decode (`scip/index.mjs`).** `decodeOptions` gives a `cpp` root `abstainOverloadSites: true`. In `decode`'s one-target-per-site block, after the tu-split check, a site whose references name more than one moniker is dropped and counted; `monikerOf` is now filled for either C++ rule that needs it. `decode` returns `overload_sites` and up to three `overload_examples` (`{name, file, line}`), sorted by file/line/name so the sample does not depend on scip-clang's listing order. `degradations` gains one `scip-decode` record in the file's voice, shaped like the tu-split one. C decodes exactly as before, and so does a `cpp` site whose references name one moniker.
> 
> **Rule 2 — the join (`pipeline/src/hobbes/extract/evidence.py`).** `join` takes `withhold: frozenset[str]`; a call site in a withheld file with no semantic hit and no external veto draws no fallback edge. Import sites are untouched, and the veto wins where both apply. `withheld_fallbacks(...)` returns the same `(site, guess)` pairs by the same predicate, as `external_vetoes` does.
> 
> **The wiring (`extract/__init__.py`).** The withheld set is the C++ provider's call-site files carrying any definition, reference or external reference in lane B's facts. `_lane_agreement` reports `cpp_withheld` beside `external_vetoes` in one shared shape (`_withheld_report`), up to ten examples; `_cpp_fallback_records` reads that count off and emits the one `cpp-fallback` record at `.`. The tail is given the fallback without the withheld files' keys, so those sites class as ones lane A had no guess for; `ev.agreement` still gets the whole fallback, so `sites_compared` is unmoved. A C++ file lane B did not index, and every C file, are unchanged.
> 
> **Checks run here**
> 
> - `scip`: `node --test test/index.test.mjs` — **71 pass, 0 fail** (68 before; 3 new).
> - `pipeline`: `/work/pipeline/.venv/bin/python3 -m pytest tests/test_evidence.py tests/test_cppsource.py -q` — **112 passed** (11 new: 5 in `test_evidence.py`, 6 in `test_cppsource.py`).
> - Whole pipeline suite: **1,603 passed, 10 skipped** (the `lane_b` and containment tests skip here).
> - `pipeline/tests/test_scipsource_c.py`'s `test_minicpp_gets_semantic_cpp_edges_through_bear_over_its_makefile` gained the two assertions (`cpp_withheld["sites"]` is an int; no `syntactic` `calls` edge has evidence in a file the fixture's Makefile builds). It skips here — no podman — so the developer runs it.
> 
> **Deviations**
> 
> - The withheld set is not read from a facts document's `language`: `scipsource.extract_scip_c` merges every C and C++ build root into one facts document that carries no `language` key, and `scipsource.py` is outside the write partition. I intersect the C++ provider's own call-site files with the files carrying occurrences in lane B's facts instead, which names the same set — a root holding any C++ file is a `cpp` root by `extract_scip_c`'s own rule, and no other indexer produces an occurrence in a C++ file. The one case where the two could differ is a C++-claimed header that a `c` root's compile database happens to pull in: it would be withheld though its root is C's, which drops a guess rather than drawing a wrong edge.
> - The task asked that `join` withhold "a call site … that no semantic resolution matched and no external veto dropped"; I placed the check after the fallback lookup, so a site with no lane A guess is not counted. It draws no edge either way, and it keeps `withheld_fallbacks` and the join on one predicate.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged (`0f1b4d6`), not squashed
- notes:
  - Both rules are as ADR-113 §2's third amendment wrote them.
  - Two deviations, accepted:
    - The withheld set is the C++ layer's call-site files that carry a
      lane B occurrence. The C and C++ roots merge into one facts
      document with no `language`, so it cannot be read from the
      facts.
    - The count sits after the fallback lookup.
  - Host: node 71/71 and pytest 1,612 pass. The `minicpp` `lane_b` test
    was red on the doer's new assertion, which it could not run. That
    assertion counted `tests/test_shapes.cpp` as built, but the
    Makefile's default target does not build it and lane B has no
    occurrence there. So its fallback edge stands by rule 2's other
    half. Fixed on the host after the merge, with that half asserted.
  - One egress refusal: a `GET` to `llm` from the doer's whole-suite
    pytest run, refused by the sidecar's CONNECT-only rule.
  - What verify could not see was, again, a live test on the host
    (§2's rule held).
