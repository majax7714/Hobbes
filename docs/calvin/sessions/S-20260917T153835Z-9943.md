# Harness session `S-20260917T153835Z-9943`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.39-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** **Contain the trusted stores**: every contained lane B step gets the Hobbes cache root read-write, including the steps that execute repo code, so repo code can write into the index cache's store (ADR- (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T153835Z-9943/brief.md`, task sha256 `0b379d2b978b`)
- **Parent:** `25f66baf16b4` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 34 of 80; result `success`; reported cost $2.3118 (the envelope's figure, on the subscription); wall 330.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `llm`×1
- **Policy:** 11 exec decision(s) — `allow`×9, `escalate`×2; escalated: `/bin/sh -c git add pipeline/src/hobbes/extract/containment.py pipeline/src/hobbes/cli.py pipeline/tests/test_containment.py pipeline/tests/test_cli.py && git -c`; `/bin/sh -c git add pipeline/src/hobbes/extract/containment.py pipeline/src/hobbes/cli.py pipeline/tests/test_containment.py pipeline/tests/test_cli.py && git -c → expired`; records: stream opened→closed
- **Edits:** 8 edit(s) to 4 file(s); first at 1.4 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/containment.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_containment.py`
- **Branch:** `hobbes/S-20260917T153835Z-9943`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/containment.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_containment.py`
- **Gate:** **clear** at `25f66baf16b4` (gate v2, grounder v4, record `4dd523f06d99`); unknown 0; map over 4 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** fail; tests 682, regressions 2; {'P2P': 661, 'skip': 13, 'new-pass': 5, 'P2F': 2, 'uncollected': 1}

**The doer's closing words:**

> Committed as `b503774`.
> 
> ## What I changed
> 
> **`pipeline/src/hobbes/extract/containment.py`**
> - `TRUSTED_STORES = ("index", "lanea")` with the comment from ADR-128 §1 (the stores only the host writes).
> - `plan(...)` creates `<cache>/index` and `<cache>/lanea` (`mkdir(parents=True, exist_ok=True)`) and folds them into the nested read-only set **in addition to** the caller's `ro_cache`, for every profile — same sorted-set ordering and dedupe, same `is_dir()`/under-the-cache filter. Mount layout otherwise unchanged. `indexcache.store_root()` is `cache_root()/"index"`, so no deviation there.
> - One clause added to the module docstring's cache-root bullet.
> 
> **`pipeline/src/hobbes/cli.py`** — `_print_containment` restructured so the early return no longer skips the new note: the C-64 host-run WARNING is unchanged, then when any recorded step whose profile `executes_repo_code` ran contained, one `NOTE:` to stderr naming the count, the sorted distinct step names, the writable tool caches and stage, C-161, and that the two stores rode read-only (ADR-128). `<n>` is the number of recorded step runs; the names are the distinct set (as specified). An unknown step name counts as not executing repo code.
> 
> **Tests** — `test_containment.py`: new `TestPlan.test_every_profile_lays_the_trusted_stores_read_only_over_the_rw_mount` (loops `PROFILES`, asserts both `:ro` mounts after the `:rw` one and that both directories now exist); new `@pytest.mark.lane_b TestTrustedStoresLive` following `TestCanary`'s skip (a contained `index-python` `sh -c` whose write into `<cache>/index/` fails — read-only file system, file absent on the host — while `<cache>/stage-probe/ran` succeeds as the control). `test_cli.py`: new `TestContainmentNote` (4 cases: contained `index-rust` names itself and C-161; `index-python` prints nothing; empty record and `None` print nothing; a host-run `index-rust` still warns with C-64 and earns no NOTE).
> 
> **Existing assertions changed** — only the two named ones, in `test_a_path_under_the_cache_can_ride_read_only_over_the_rw_mount`: `plan.ro_cache` is now `(go/mod, index, lanea)` and the no-arg case `(index, lanea)`.
> 
> ## What I ran
> 
> - `/work/pipeline/.venv/bin/python3 -m pytest tests/test_containment.py tests/test_cli.py -q` → **86 passed, 3 skipped**. The 3 skips are the `lane_b` tests, including the new live one: "containment unavailable here: podman is not installed" — no podman inside the session, so the developer runs it on the host.
> - Full tree: `python3 -m pytest -q` → **1,713 passed, 2 failed, 12 skipped**.
> 
> ## Deviations
> 
> - None in the implementation; the store root matched `cache_root()/"index"`.
> 
> ## Two failures outside the partition (the developer's, I cannot write them)
> 
> Both are pre-ADR-128 expectations that the decided change invalidates, each a one-line update:
> 
> - `pipeline/tests/test_harness.py:605` — `assert all(p.ro_cache == (f"{cache}/go/mod",) for p in fake.plans)`; now `(f"{cache}/go/mod", f"{cache}/index", f"{cache}/lanea")`.
> - `pipeline/tests/test_indexcache.py:244` — `assert not (cache / "index").exists()`; the store directory now exists (empty) because `plan()` creates it, so the "never cached" claim wants `assert not any((cache / "index").iterdir())`. Nothing is stored — `hits() == [False, False]` on the next line still passes.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read in full against ADR-128 §1–2 and merged no-ff; no deviations. Verify's two regressions were mine, not the doer's: the partition left out `test_indexcache.py` and `test_harness.py`, which asserted the old nested-mount set and an absent `index/` directory; the doer named both and the one-line fix for each, applied on top. Host: full pytest passes once this block is filled, `lane_b` 8 passed — the new live test shows a write into `<cache>/index` failing in the image while the stage write succeeds.
