# Harness session `S-20260912T221854Z-42d1`

*2026-09-12 · `hobbes dispatch` v1 · Hobbes 0.2.6-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-111: the join vetoes lane A's fallback at a site where lane B resolved the reference outside the repo (C-138). Read `docs/adr/111-external-veto.md` first. It is decided; follow it and do not (brief `/home/mmarrujo/.hobbes/sessions/S-20260912T221854Z-42d1/brief.md`, task sha256 `a2218ff26bef`)
- **Parent:** `e2ab96d959ba` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 99 of 200; result `success`; reported cost $3.3337 (the envelope's figure, on the subscription); wall 817.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×4
- **Policy:** 21 exec decision(s) — `allow`×21
- **Edits:** 21 edit(s) to 12 file(s); first at 0.6 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/tests/fixtures/minic/src/util.c`, `pipeline/tests/fixtures/minic/src/util.h`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_scipsource.py`, `pipeline/tests/test_scipsource_c.py`, … and 2 more
- **Branch:** `hobbes/S-20260912T221854Z-42d1`, 5 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/tests/fixtures/minic/src/util.c`, `pipeline/tests/fixtures/minic/src/util.h`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_evidence.py`, `pipeline/tests/test_scipsource.py`, `pipeline/tests/test_scipsource_c.py`, `scip/index.mjs`, `scip/test/index.test.mjs`
- **Gate:** **clear** at `e2ab96d959ba` (gate v2, grounder v3, record `c4bbd8318edf`); unknown 0; map over 13 file(s), 0.3% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 951, regressions 0; {'unsupported': 2, 'skip': 14, 'P2P': 921, 'F2F': 1, 'new-pass': 11, 'uncollected': 1, 'not-run': 1}

**The doer's closing words:**

> That was just a directory-path invocation issue, not an actual test failure — the file-targeted run passes all 43. Everything is green and all changes are committed within the allowed file set.
> 
> ## Summary
> 
> Implemented ADR-111 (the external veto) in six committed steps:
> 
> 1. **`scip/index.mjs`** — the decode collects every in-repo definition occurrence (any kind, before the `GRAPH_KINDS` filter) and marks an `external.push` entry `in_repo: true` when its moniker is among them. Node test added.
> 2. **`scipsource.join_cross_unit`** — a reference left external because its moniker is in the cross-unit `ambiguous` set is now marked `in_repo: True`. Existing cross-unit tests extended.
> 3. **`evidence.join`** — gained an `external` parameter; a fallback site whose `(file, line, name)` matches a genuinely-external reference (not `in_repo`) draws no edge. Added public `external_vetoes()` sharing the same veto rule so the count and the join can't drift. `_build_symbol_layer` now passes `external` through. Per-language veto tests added (Python/TS/Go/Rust/Java/C) plus tests for `external_vetoes` itself.
> 4. **`_lane_agreement`** gained `external_vetoes: {sites, examples}`; `hobbes lanes` prints the one-line summary with examples when `n > 0`, without changing exit status. CLI test added.
> 5. (folded into 3) per-language join cases in `test_evidence.py`.
> 6. **`minic` fixture** — `util.c` grew a dead `#if defined(_WIN32)` `strcasestr` shim plus a live call to the real one (`mentions_add`), mirroring sqlite-vector's C-138 shape exactly (rank-1 same-file fallback names the shim). Extended the existing `lane_b`-marked test in `test_scipsource_c.py` to assert no `calls` edge to the shim, the site's `external` coverage fate, and `external_vetoes.sites == 1`; it skips here (no podman) and needs a host run to actually execute.
> 
> Checked with: `node --test scip/test/index.test.mjs` (43/43), and `pipeline/.venv/bin/python3 -m pytest -q` (1462 passed, 9 skipped, 3 pre-existing unrelated failures in `test_ttt_units.py` confirmed present before my changes too via `git stash`). All edits are confined to the allowed file list; commits are on `hobbes/S-20260912T221854Z-42d1`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear. Review found no defect in the diff.
- outcome: merged (0.2.8-beta, Max's call after the acceptance regrade). A merge commit keeps the five `hobbes-dispatch` commits (`9a2953b`, `3b27bb2`, `fb03b49`, `55142ab`, `4e3a983`); `main` had moved past the parent with the box policy.
- notes:
  - **The first session with the progress hook live.**
    - Dispatch printed "first edit at 0.8 min — scip/index.mjs".
    - The flight log holds 21 edit lines by time, tool and relative path, with every other field empty and no edit text.
  - **Checked on the host,** in a worktree of the branch (`~/.hobbes/bench/adr111-wt`, its own venv):
    - node 43/43;
    - the `lane_b` minic shape passes in the image: no edge to the dead-arm shim, fate `external`, `external_vetoes` 1;
    - hermetic pytest green.
    - The four failures a first run showed came from symlinked `node_modules`, which the lane B container cannot follow. Real copies fixed them; they were not the tree.
  - **The acceptance regrade** covers all 44 cells with a stored key.
    - The pre-veto pass (`adr111-pre/`) reproduced every stored number to the digit.
    - The post-veto pass (`adr111-post/`) moved no confirmed count anywhere.
    - sqlite-vector went 851/854 → 851/851, with 4 fewer syntactic edges where the gate said exactly 3. The fourth is a prototype read as a call (`libs/sqlite3.h:6943`), silent. Max accepted it.
  - **Dagger: 56 vetoes, all in the root module, which has no key.** The ten examples were method calls on a local `slog := slog.SpanLogger(...)`, which lane A had drawn to `engine/slog.Info`. So wrong edges were removed, and the shape is registered as C-139.
  - **Egress:** four refusals of `llm`, the suite's own `http://llm` GET. **Policy:** 21 exec decisions, all allowed.
  - **Pace:** 99 of 200 turns in 817 s.
  - Reviewed by Claude (the developer session), 2026-09-12.
