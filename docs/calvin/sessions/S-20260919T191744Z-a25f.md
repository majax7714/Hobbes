# Harness session `S-20260919T191744Z-a25f`

*2026-09-19 · `hobbes dispatch` v1 · Hobbes 0.2.53-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-140's step 3: **the TypeScript oracle grades a zone that has no `tsconfig.json`, under the same compiler options the ingest generates for such files, and a small plain-JavaScript fixture (`m (brief `/home/mmarrujo/.hobbes/sessions/S-20260919T191744Z-a25f/brief.md`, task sha256 `90ae15ed8a2a`)
- **Parent:** `83551d145b2e` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 56 of 80; result `success`; reported cost $3.1470 (the envelope's figure, on the subscription); wall 406.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×1
- **Policy:** 24 exec decision(s) — `allow`×16, `escalate`×8; escalated: `/bin/sh -c node --version; ls bench/oracle/ts/; ls bench/oracle/ts/node_modules/typescript/package.json && node -e "console.log(require('/work/bench/oracle/ts/n`; `/bin/sh -c node --version; ls bench/oracle/ts/; ls bench/oracle/ts/node_modules/typescript/package.json && node -e "console.log(require('/work/bench/oracle/ts/n → expired`; `/bin/sh -c node -e "console.log(require('/work/bench/oracle/ts/node_modules/typescript').version)"`; `/bin/sh -c node -e "console.log(require('/work/bench/oracle/ts/node_modules/typescript').version)" → expired`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go; go test ./internal/grade/ -count=1 -run 'Minijs|NoTsconfig|Minits' -v 2>&1 | tail -30`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go; go test ./internal/grade/ -count=1 -run 'Minijs|NoTsconfig|Minits' -v 2>&1 | tail -30 → expired`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go`; `/bin/sh -c gofmt -w internal/grade/minijs_test.go → expired`; records: stream opened→closed
- **Edits:** 18 edit(s) to 11 file(s); first at 2.8 min after launch; files: `bench/oracle/README.md`, `bench/oracle/internal/grade/minijs_test.go`, `bench/oracle/ts/tsc-oracle.mjs`, `pipeline/tests/fixtures/minijs/esm/greet.mjs`, `pipeline/tests/fixtures/minijs/esm/main.mjs`, `pipeline/tests/fixtures/minijs/index.js`, `pipeline/tests/fixtures/minijs/lib/apply.js`, `pipeline/tests/fixtures/minijs/lib/counter.js`, `pipeline/tests/fixtures/minijs/lib/math.js`, `pipeline/tests/fixtures/minijs/package.json`, … and 1 more
- **Branch:** `hobbes/S-20260919T191744Z-a25f`, 1 commit(s); files: `bench/oracle/README.md`, `bench/oracle/internal/grade/minijs_test.go`, `bench/oracle/ts/tsc-oracle.mjs`, `pipeline/tests/fixtures/minijs/esm/greet.mjs`, `pipeline/tests/fixtures/minijs/esm/main.mjs`, `pipeline/tests/fixtures/minijs/index.js`, `pipeline/tests/fixtures/minijs/lib/apply.js`, `pipeline/tests/fixtures/minijs/lib/counter.js`, `pipeline/tests/fixtures/minijs/lib/math.js`, `pipeline/tests/fixtures/minijs/package.json`, `pipeline/tests/fixtures/minijs/ui/button.jsx`
- **Gate:** **clear** at `83551d145b2e` (gate v2, grounder v4, record `eeffc78c0304`); unknown 0; map over 10 file(s), 19.4% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 28, regressions 0; {'new-pass': 23, 'skip': 5}; build {'P2P': 2}

**The doer's closing words:**

> Done and committed (`97624d9`).
> 
> **What changed**
> 
> - `bench/oracle/ts/tsc-oracle.mjs` — a bare-flag argv reader (`--x` followed by another `--` token or by nothing is `true`; `--x value` unchanged), and `--no-tsconfig`: the program is built from the zone's own walked source set (tsextract's eight extensions; dot-dirs and the six `SKIPPED_DIRS` pruned, copied with a comment naming the source) under the eight options `scipsource._generated_tsconfig` writes. Refuses exit 1 at a `tsconfig.json` under the zone (naming the file), exit 1 without the flag where there is none (the old message plus `(a zone with no tsconfig: --no-tsconfig)`), exit 2 with `--config`. `jsconfig.json` is listed on stderr and in the output, never read. Flag mode only adds `generated_config` and the `; no tsconfig — the ingest's generated options` suffix to the `oracle` string; without the flag the export's bytes are unchanged. Nothing after the program is built was touched. `edges.OracleExport` needed no field — nothing in `bench/oracle` uses `DisallowUnknownFields`.
> - `pipeline/tests/fixtures/minijs/` — 7 source files (CommonJS `exports.x`/`module.exports`/prototype/destructured `require`, ESM `.mjs`, a JSDoc-typed higher-order function, a `.jsx`), `package.json`, no config of any kind.
> - `bench/oracle/internal/grade/minijs_test.go` — the hand-keyed table (written from the fixture before the first run) as the header comment, then 16 keyed lines / 17 sites, the `generated_config` assertions, and the three refusals.
> - `bench/oracle/README.md` — one paragraph in the TS section.
> 
> **tsc's reading, not the hand's** (asserted as the oracle says, hand reading kept beside each):
> - `lib/apply.js:5` `f("a")` — reaches the parameter's binding (`via: binding`) as the hand read, but `static`, kind `closure`: `declKind` walks up from a parameter into its own function and calls it a closure, and closure isn't one of the kinds that make a site dynamic. Hand read `dynamic` / kind `parameter`. Resolution code, so not mine to change.
> - `esm/main.mjs:3` `new Greeter()` — silent: `Greeter` has no constructor, so the resolved construct signature is synthesized with no declaration. Hand read `→ esm/greet.mjs:5`. The `.hello()` on the same line resolving to `greet.mjs:6` shows the class itself resolved.
> 
> **Deviations** (each the option that grades less when unsure):
> - The walk does not descend directory symlinks (tsextract's C-73 rule not copied): an unwalked tree contributes no sites rather than a second copy of one.
> - The refusal/listing block uses its own `repoRel` helper rather than the shared `rel`, which is declared after the program and would have meant editing the shared region.
> 
> **Checks run** (from `/work/bench/oracle`): `go test ./internal/grade/ -count=1 -run 'Minijs|NoTsconfig|Minits' -v` — 4 tests + 3 subtests, all RUN and PASS, none skipped (node v22.14.0, typescript 5.9.3 in `ts/node_modules`), `TestMinitsTSAllConfirmed` untouched and passing; `go test ./... -count=1` — 11 packages ok, 4 without test files; `go vet ./...` and `gofmt -l .` clean.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-140's step 3, all under `bench/` plus the fixture tree. Read whole: the flag only replaces how the program is built; the site walk, targets, modes and watchdog are untouched. Without the flag the minits export is byte-identical to the parent's once the typescript install path in external targets is normalised (checked on the host). On the host, in a worktree with the oracle's `node_modules` copied: the four tests and three subtests RUN and PASS, the oracle module green, `vet` and `gofmt` clean. The hand-keyed table was written before the run and tsc agrees on every CommonJS shape (`exports.double`, `Counter.prototype.inc`, the destructured `require`). Of its two "tsc's reading" rows, `new Greeter()` silent is the compiler's (no declared constructor); `f("a")` as `static`/`closure` is **an oracle defect the unit surfaced, not caused**: `declKind` tests `isClosure` before `isParameter`, and a parameter is always inside a function, so no target is ever `parameter` and every call through a parameter reads `static→closure` on every TS cell. Precision is unaffected; the miss-class attribution is not. Logged as H-33; not fixed here. The two deviations (no symlinked-directory descent; a local `repoRel`) each grade less and are kept. The escalations were read-only probes the policy queues; each expired unanswered, harmlessly.
