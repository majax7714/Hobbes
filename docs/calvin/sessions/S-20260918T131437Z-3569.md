# Harness session `S-20260918T131437Z-3569`

*2026-09-18 · `hobbes dispatch` v1 · Hobbes 0.2.44-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-133: **`evidence.join` claims a lane B resolution by position — `(file, line, name, col)` — not by `(file, line, name)`.** The decision is `docs/adr/133-the-joins-claim-is-by-position.md`. R (brief `/home/mmarrujo/.hobbes/sessions/S-20260918T131437Z-3569/brief.md`, task sha256 `41ec0afe7081`)
- **Parent:** `8738fc22cefe` of `hobbes_public`
- **Doer:** 2.1.276 (Claude Code); model claude-opus-5; turns 26 of 80; result `success`; reported cost $1.7835 (the envelope's figure, on the subscription); wall 257.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 4 exec decision(s) — `allow`×4; records: stream opened→closed
- **Edits:** 8 edit(s) to 2 file(s); first at 1.7 min after launch; files: `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_evidence.py`
- **Branch:** `hobbes/S-20260918T131437Z-3569`, 1 commit(s); files: `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_evidence.py`
- **Gate:** **clear** at `8738fc22cefe` (gate v2, grounder v4, record `06a8e9c9aea2`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 561, regressions 0; {'skip': 9, 'P2P': 541, 'new-pass': 10, 'uncollected': 1}

**The doer's closing words:**

> **What I changed**
> 
> - `pipeline/src/hobbes/extract/evidence.py` — `join`'s claim is now by position. `claimed` holds `(file, line, name, col)` and is added to at both claim points (the ambiguous branch, ADR-104, and the resolved branch); the unclaimed loop tests the same four-part key. A second set, `claimed_by_name`, is fed only by sites with `site.col < 0` and the loop skips a hit found in either — a columnless site keeps hiding its whole name on the line, since `match_resolution` took `named[0]` there. A resolution with `hit.col < 0` needs nothing: its key carries `-1`. Docstring and both claim comments say what the key is, why the column is in it (same-column alternates), and why a columnless site is the exception. Nothing else moved — `match_resolution`, `_veto_set`, the fallback, `withhold`, `agreement`, `coverage`, `_dispositions`, `unresolved_sites`, the signature and the return type are untouched.
> - `pipeline/tests/test_evidence.py` — new class `TestTheClaimIsByPosition` (6 cases: same name at another column onto another definition → `calls` + one semantic lane-SCIP unscoped `uses`; at the matched column → no `uses`; onto the same definition at another column → `uses` anyway; the ambiguous site hiding its own column and no other; the columnless site keeping today's behaviour; a columnless resolution a site claimed drawing no `uses`). Two cases added to `TestAConstruction` and two to `TestAnOperatorAppliedBySymbol`: a released same-name resolution at a recorded token draws the construction/operator `calls` and is counted `drawn` outside a template; inside one it is the `uses` counted `in_template` (construction) and withheld counted `in_template` (operator).
> 
> No existing assertion moved, and no deviations from the rule as written.
> 
> **What I ran** (from `/work/pipeline`, with `.venv/bin/python3 -m pytest`)
> 
> - `tests/test_evidence.py tests/test_cppsource.py tests/test_scipsource.py` → 339 passed, 2 skipped.
> - The whole suite → **1896 passed, 14 skipped** in 54.7s (1886 + 14 before; the 10 new cases are the difference). Committed as `5bc45cd` on `hobbes/S-20260918T131437Z-3569`; not pushed.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: merged no-ff (`83cde1b`) after the whole suite (1,910) and `lane_b` 9 of 9 on the host, from a worktree of the branch. The diff is the brief's: the four-part key at both claim points and in the unclaimed loop, a by-name set fed only by columnless sites, the docstring and comments; no existing assertion moved, no deviation listed. Regraded after the merge, before and after on the same clones (§10.18): 44 stored-key cells, fmt 6,993 → 7,012 at 0 contradicted — ADR-132's P102 number — and every other cell ±0; 1,785 `uses` and 11 `calls` symbol edges added, none removed; two module edges, both read true. P108–P113 met.
