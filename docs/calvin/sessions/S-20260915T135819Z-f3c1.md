# Harness session `S-20260915T135819Z-f3c1`

*2026-09-15 · `hobbes dispatch` v1 · Hobbes 0.2.20-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Index C and C++ one translation unit per scip-clang run, and decode the units as one. The design is ADR-109 decision 1's new bullet "One translation unit per run" (`docs/adr/109-c-lane-b-scip-clang.md (brief `/home/mmarrujo/.hobbes/sessions/S-20260915T135819Z-f3c1/brief.md`, task sha256 `eecc7720671b`)
- **Parent:** `c7782f069ed2` of `hobbes_public`
- **Doer:** 2.1.272 (Claude Code); model claude-opus-5; turns 59 of 120; result `success`; reported cost $4.9228 (the envelope's figure, on the subscription); wall 542.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 19 exec decision(s) — `allow`×11, `escalate`×8; escalated: `/bin/sh -c cd /tmp && node -e 'const a=new Array(250000).fill(1); const b=[]; try{ b.push(...a); console.log("spread ok", b.length) } catch(e){ console.log("spr`; `/bin/sh -c cd /tmp && node -e 'const a=new Array(250000).fill(1); const b=[]; try{ b.push(...a); console.log("spread ok", b.length) } catch(e){ console.log("spr → expired`; `/bin/sh -c node -e 'const a=new Array(250000).fill(1); const b=[]; try{ b.push(...a); console.log("spread ok", b.length) } catch(e){ console.log("spread threw:"`; `/bin/sh -c node -e 'const a=new Array(250000).fill(1); const b=[]; try{ b.push(...a); console.log("spread ok", b.length) } catch(e){ console.log("spread threw:" → expired`; `/bin/sh -c d=$(mktemp -d) && mkdir -p "$d/bin" "$d/units" && printf '#!/bin/sh\nfor a in "$@"; do case "$a" in --index-output-path=*) out=${a#--index-output-pat`; `/bin/sh -c d=$(mktemp -d) && mkdir -p "$d/bin" "$d/units" && printf '#!/bin/sh\nfor a in "$@"; do case "$a" in --index-output-path=*) out=${a#--index-output-pat → expired`; `/bin/sh -c d=$(mktemp -d) && for i in 0000 0001 0002; do echo "[{\"file\":\"$i.c\"}]" > "$d/$i.json"; done && ls "$d"`; `/bin/sh -c d=$(mktemp -d) && for i in 0000 0001 0002; do echo "[{\"file\":\"$i.c\"}]" > "$d/$i.json"; done && ls "$d" → expired`; records: stream opened→closed
- **Edits:** 19 edit(s) to 2 file(s); first at 2.9 min after launch; files: `scip/index.mjs`, `scip/test/index.test.mjs`
- **Branch:** `hobbes/S-20260915T135819Z-f3c1`, 1 commit(s); files: `scip/index.mjs`, `scip/test/index.test.mjs`
- **Gate:** **blocked** at `c7782f069ed2` (gate v2, grounder v3, record `604f27a8c4d4`) — blocking: near-miss; unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
  - `near-miss` scip/index.mjs:264 `check`
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 67, regressions 0; {'P2P': 57, 'removed': 1, 'new-pass': 8, 'not-run': 1}

**The doer's closing words:**

> **What changed** (two files, `scip/index.mjs` and `scip/test/index.test.mjs`; commit `b389280`):
> 
> - **`cPlan` indexes per unit.** For all three compile-database sources (`repo`, `cmake`, `make`) the earlier steps are unchanged, and C-135's check still judges the whole database before any indexing. The index step's `check` now runs that check and then writes the database out as one-entry databases (`<output>.units/NNNN.json`, exported `splitCompdb(entries)`, each entry verbatim). The step itself is a `sh -c` driving `xargs -P <availableParallelism()> -I@ scip-clang -j 1 --compdb-path=…@.json --index-output-path=…@.scip; exit 0` — one unit per run, a failed unit stopping none of the others. `cPlan` returns `unitsDir` beside its steps; `runIndexer` carries it out.
> - **`indexStage` decodes the units as one.** `readUnitIndexes` deserializes every `NNNN.scip` in name order (database order) and counts the missing/unreadable ones; exported `mergeUnitIndexes(indexes)` concatenates their `documents` into `{ documents }`, decoded with `decodeOptions(config)` exactly as before. `units`/`units_failed` ride on the facts and into `degradations`, which gains the `scip-decode` record when any failed. The units dir and `config.output` are removed in a `finally`. Every unit failing throws with `indexerExit`, so the helper exits `INDEXER_EXIT` as today.
> - **The stack fix:** both `references.push(...kept)` are loops now.
> - Doc comments on `INDEXERS.c`, `cPlan` and `indexStage` say what the plan does and why (C-149); no other language's plan or decode moved.
> 
> **Tests** (7 new, 1 updated): the plan test now asserts the per-unit index step for `repo`/`cmake`/`make` (shell, `-j 1`, units dir beside the output, the check still on the step that first reads the database); `splitCompdb` numbering and verbatim entries, plus the check writing them; the step's shell run for real against a stub `scip-clang` (one unit fails, the other two index, step exits 0); merged-unit decode — a site two units answer into different files is dropped as tu-split in either order, a header both answer alike is one reference row and one definition, a C++ moniker two units define at two lines of one file abstains; 250,000 references decode without overflowing (verified failing before the fix: `RangeError: Maximum call stack size exceeded`); the failed-unit record's exact words, and silence when none failed.
> 
> **What I ran:** `cd /work/scip && node --test test/index.test.mjs` → 65 tests, 65 pass, 0 fail. Also `pipeline/.venv/bin/python3 -m pytest tests/test_scipsource_c.py tests/test_helper_contracts.py -q` → 22 passed, 2 skipped (`lane_b`). No podman/cmake/scip-clang here, so the real indexer and the host measurements are the developer's.
> 
> **Deviations**
> 
> - A unit whose `.scip` exists but does not deserialize is counted as failed, like a missing one; the amendment names only missing outputs.
> - The index step's `install` line names a POSIX shell (it is an `sh` step); scip-clang's install line is named in the every-unit-failed error instead, where it is the actual answer.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: false-block
- outcome: merged
- notes: Per-unit indexing, as ADR-109's amendment wrote it:
  - the check judges the whole database, then splits it;
  - each unit runs `scip-clang -j 1` under `xargs -P`, and a failing unit stops none of the others;
  - the units' indexes are decoded as one;
  - failed units are counted in their own record, and if every unit fails the helper exits as a failed indexer does;
  - the decode's two spread-pushes are loops.

  Two deviations, both reasonable.

  **The gate's block was false**, the harness's first. The one near-miss is `check(previous)` inside `before = (check) => (previous) => …`, a call through an arrow-function parameter. For TS/JS the grounder reads parameters from the text (`_TS_PARAMS`), and that pattern needs `function` or a word character before the `(`, so an arrow's parameters after `=` are never read. C-91 is amended; the fix is a small unit.

  On the host:
  - the scip node suite is 65/65, and both `lane_b` tests pass;
  - three cJSON ingests are identical, edges and tail;
  - three fmt ingests are identical at the edge level (two external type sites flip in the tail).

  ScummVM (5,958 units, built in the image with the null backend, a cost measurement only):
  - per-unit indexing took 207 s against 143 s for one whole-database run;
  - but the merge holds every unit's index at once: 9.36 GB of unit indexes, 5.75 GB of memory already at 400 units, about 84 GB projected for all;
  - `main`'s helper crashed outright on the whole index (the spread-push this unit fixed).

  So Max's size guard followed on the host (`4832883`): over 400 units, one whole-database run, with a record naming C-149. Merged `e2f7704`, not squashed.
