# Harness session `S-20260917T202723Z-4033`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.42-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-131's amendment of 2026-09-17: **a lane B reference named `operator…` at exactly a lane A operator token inside a template draws nothing — no `calls` fact (as today) and no `uses` fact (new) (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T202723Z-4033/brief.md`, task sha256 `e5cbf1ce6d96`)
- **Parent:** `17ddef0cbc5c` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 36 of 80; result `success`; reported cost $1.7766 (the envelope's figure, on the subscription); wall 246.0 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 7 exec decision(s) — `allow`×7; records: stream opened→closed
- **Edits:** 11 edit(s) to 5 file(s); first at 0.7 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`
- **Branch:** `hobbes/S-20260917T202723Z-4033`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/evidence.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_evidence.py`
- **Gate:** **clear** at `17ddef0cbc5c` (gate v2, grounder v4, record `8a1ba8015a39`); unknown 0; map over 6 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 843, regressions 0; {'skip': 11, 'P2P': 823, 'removed': 3, 'new-pass': 5, 'uncollected': 1}

**The doer's closing words:**

> Committed as `3c6cab6`.
> 
> **What changed**
> 
> - `extract/evidence.py` — `join`'s unclaimed-resolution loop: `in_template is True` now increments `counts["in_template"]` (when `counts` is given) and `continue`s, emitting no fact. `False` and `None` untouched; per hit, not per line or bucket. `_operator_call`'s docstring now spells the three fates (`False` → call, `True` → nothing, `None` → `uses`); `join`'s *operators* and *counts* paragraphs say withheld and why.
> - `cli.py` — `_print_operators` prints `… M inside a template withheld, neither a call nor a uses (ADR-131, C-153)`; docstring rewritten to match.
> - `cppsource.py` — **not written.** Neither the module docstring nor `pack_operator`'s says an in-template reference "stays `uses`" (the only `uses` sentence is `operator_token`'s, about `None`, still accurate), so item 3's conditional didn't fire and I changed nothing else there.
> 
> **Assertions changed** (each one named in the task, plus one addition):
> 
> - `test_evidence.py::test_inside_a_template_it_stays_a_use_and_is_counted` → renamed `…it_is_withheld_entirely_and_counted`; `[fact.kind …] == ["uses"]` → `out == []`. `counts` assertion unchanged.
> - `test_evidence.py` — new `test_only_the_hit_at_the_token_is_withheld_not_its_line` (two unclaimed resolutions on line 10 onto `fmt.h:5`, one at an in-template token, one at col 20 named `plain` → exactly one `uses`) and new `test_it_is_withheld_whether_or_not_the_caller_counts` (`counts=None`, nothing emitted). Class docstring extended. The outside-template, one-column-off, other-spelling, `col=-1`, by-name and no-`operators` cases are untouched.
> - `test_cppsource.py::test_the_plain_function_calls_the_operator_and_the_template_uses_it` → renamed `…_and_the_template_draws_nothing`; the `uses`-evidence assertion `[…] == [6]` replaced by: only a `calls` edge among the file's edges, and no edge in `symbol_edges + module_edges` whose evidence names line 6. The `plain` `calls` assertions, `("src/use.tpl", "calls") not in edges` and `graph["operators"] == {"drawn": 1, "in_template": 1}` stand.
> - `test_cli.py::test_the_line_says_what_was_drawn_and_what_was_left` → renamed `…_what_was_withheld`; `"175 inside a template left as uses"` → the `withheld, neither a call nor a uses` phrasing plus `"left as uses" not in line`; `(ADR-131, C-146)` → `(ADR-131, C-153)`.
> 
> **Deviations** — one, and it draws no more:
> 
> - The cppsource test's module edge `src/use → src/lib.h` is *not* sole-supported by the template's reference (the `#include` and the line-3 call hold it), so instead of asserting absence I asserted no module edge stands on line 6, and that `imports` is the only module edge left. The graph's own output confirmed the edge survives on the include.
> 
> **Checked:** `/work/pipeline/.venv/bin/python3 -m pytest tests/test_evidence.py tests/test_cppsource.py tests/test_cli.py tests/test_scipsource.py -q` → 369 passed, 2 skipped. Then the whole suite: **1,853 passed, 14 skipped** (1,867 collected — the 1,865 in CLAUDE.md plus the two new evidence cases).

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: The diff is the rule as briefed — `in_template is True` counts and `continue`s, per hit — with the three docstrings and the ingest line restated; `cppsource.py` left alone because no sentence there said "stays `uses`" (checked: true). One deviation, drawing no more: the ingest-level test's module edge also stands on the `#include`, so it asserts that no edge stands on the template's line rather than that the module edge is gone. On the host: 1,867 pytest and the 9 `lane_b` pass. Regraded from the branch before the merge (§10.16): P98–P101 met, the built graphs equal the probe's edge for edge on fmt and args.
