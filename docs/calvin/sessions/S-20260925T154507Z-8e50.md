# Harness session `S-20260925T154507Z-8e50`

*2026-09-25 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E1's runner, part 1: the five context arms, extracting a body, and feedback's reference (brief `/home/mmarrujo/.hobbes/sessions/S-20260925T154507Z-8e50/brief.md`, task sha256 `4e26e00d7ab8`)
- **Parent:** `02b8aa7558e0` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 70 of 140; result `success`; reported cost $8.4861 (the envelope's figure, on the subscription); wall 1414.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 21 exec decision(s) — `allow`×21; records: stream opened→closed
- **Edits:** 32 edit(s) to 9 file(s); first at 9.3 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/extract.py`, `bench/calvin/lattice/src/lattice/feedback.py`, `bench/calvin/lattice/src/lattice/prompts.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_extract.py`, `bench/calvin/lattice/tests/test_feedback.py`, `bench/calvin/lattice/tests/test_prompts.py`
- **Branch:** `hobbes/S-20260925T154507Z-8e50`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/extract.py`, `bench/calvin/lattice/src/lattice/feedback.py`, `bench/calvin/lattice/src/lattice/prompts.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_extract.py`, `bench/calvin/lattice/tests/test_feedback.py`, `bench/calvin/lattice/tests/test_prompts.py`
- **Gate:** **clear** at `02b8aa7558e0` (gate v2, grounder v4, record `6ebd3554e3cf`); unknown 0; map over 38 file(s), 4.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 92, regressions 0; {'new-pass': 39, 'P2P': 53}

**The doer's closing words:**

> Committed on `hobbes/S-20260925T154507Z-8e50`, exactly the nine files the gate allows.
> 
> **What I changed**
> 
> - **`src/lattice/prompts.py`** (new) — the five arms of §5.3 as data and as chat turns.
>   - `shots` is E1-a: at most one per axis in the order ISA, type, metric; the pairing first (`avx2↔sse2`/`avx512→avx2`, `float32↔float16`/`bfloat16→float16`/`uint8↔int8`, `l2_impl↔l1`/`dot↔cosine`, and `l2↔l2_squared` for a wrapper hole), then the first qualifying neighbour in the record's order on the type and metric axes only. Never `cpu`/`neon`/`rvv`, never the hole, real bodies unless the hole is a wrapper; each shot records `pairing` or `fallback`.
>   - `control` is E1-b: whole real bodies from the hole's own file differing on **both** type and metric, from a SHA-256-derived start (never `hash()`), the last cut at a line boundary; `lines` holds the match and what was carried, so a shortfall is read off the record — two fixture cells fall short.
>   - `context`/`messages`: `prelude_bare` in **every** arm, the facts arms carrying the ledger's `callees` plus the callers the lattice knows (an `_impl`'s wrappers with their slots, a body's own slot), `NoLedger` — its own type — for C-1/C-3 with no ledger, and a heading dropped with its section. `lines["shots"]` on C-4 is the figure it was matched to; that is documented, not implied.
>   - One departure from the brief's shorthand: `shots`/`control` return `Shot`/`Control` namedtuples (they carry the rule and the `cut` flag the record needs) and `context` carries a `callers` key beside `facts`, since the callers are part of what C-1 claims.
> - **`src/lattice/extract.py`** (new) — the first fenced block whatever its tag, unclosed running to the end; the definition found by `scan`; four reasons, one per case. On every shape I could construct, `scan` refuses a truncated body first, so the `holes` guard is tested through the collaborator with a note saying why.
> - **`src/lattice/feedback.py`** — a failing case names its reference: `scalar` keeps today's wording, `gold` reads "disagrees with the kernel it replaces (the target's own gold; a non-finite case)", an absent field keeps the old wording.
> - **`src/lattice/cli.py`** — `lattice prompts`, one row per (cell, arm), default every native cell and arm; a facts arm with no ledger is skipped and named on stderr, once per arm, never filled empty. Reads files only.
> - **`README.md`** — sections for `prompts` and `extract` (what C-4 controls for, why the prelude is always the bare one), the verb in the CLI list and the examples, and feedback's reference rule.
> 
> **What I ran:** `cd bench/calvin/lattice && .venv/bin/python -m pytest -q` → **311 passed** (272 before; 39 new across `test_prompts.py`, `test_extract.py`, `test_feedback.py`, `test_cli.py`). No model, no Modal, no target checkout touched.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-151 E1's runner, part 1 (routes E1-a to E1-c): the five context arms, `extract`, and feedback naming
  its reference. 70 turns of 140, no escalation (the brief's plain-command note held). **The real-target check,
  run on the host before the merge** (`lattice prompts` on sqlite-vector at `0c2223a` with the graph, the clang key
  and the intrinsic index): 465 rows. The median user turn is C-0 4.3k characters (≈1.2k tokens), C-1 5.4k, C-2 8.4k,
  C-3 9.4k and C-4 8.9k, the sizes E1 was priced on. C-2 carries 3 shots on 90 cells and 1 on the 3 `bit1` cells,
  all 273 by `pairing`. C-4 reaches its line match on all 93. No arm's user turn holds another cell's gold body
  (bodies of 3+ lines) in C-0 or C-1, and no prompt holds its own. All 186 real cells, NEON and RVV included,
  round-trip through `extract`. The doer's departures, accepted: `Shot`/`Control` named tuples, a `callers` key
  beside `facts`, and no fallback on the ISA axis (E1-a fixes one crossing per ISA; the design did not say, and
  the doer's reading is the right one). Host: 291 passed, 20 skipped (E0's clang tests; none new).
