# Harness session `S-20260917T172122Z-77da`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.40-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Build ADR-129's mint: **where lane A's parse lost a C or C++ definition to ERROR nodes, lane B's definition row becomes the graph symbol, so the calls scip-clang already resolved there draw instead of (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T172122Z-77da/brief.md`, task sha256 `66bd72551206`)
- **Parent:** `3acd96ff5415` of `hobbes_public`
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 95 of 140; result `success`; reported cost $8.9214 (the envelope's figure, on the subscription); wall 957.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 56 exec decision(s) — `allow`×52, `escalate`×4; escalated: `/bin/sh -c cd pipeline && sed -i 's/print(row\["file"\], row\["tail"\])/print(row)/' /tmp/probe4.py && /work/pipeline/.venv/bin/python3 /tmp/probe4.py 2>&1 | ta`; `/bin/sh -c cd pipeline && sed -i 's/print(row\["file"\], row\["tail"\])/print(row)/' /tmp/probe4.py && /work/pipeline/.venv/bin/python3 /tmp/probe4.py 2>&1 | ta → expired`; `/bin/sh -c cd /work && git commit -q -F - <<'EOF'
feat(extract): ADR-129's mint — a C/C++ definition lane A's parse lost is read from the index

Where a C or C+`; `/bin/sh -c cd /work && git commit -q -F - <<'EOF'
feat(extract): ADR-129's mint — a C/C++ definition lane A's parse lost is read from the index

Where a C or C+ → expired`; records: stream opened→closed
- **Edits:** 18 edit(s) to 10 file(s); first at 7.0 min after launch; files: `/tmp/commit-msg.txt`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/csource.py`, `pipeline/src/hobbes/extract/minted.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_csource.py`, `pipeline/tests/test_laneacache.py`, `pipeline/tests/test_minted.py`, `pipeline/tests/test_scipsource_c.py`
- **Branch:** `hobbes/S-20260917T172122Z-77da`, 1 commit(s); files: `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/csource.py`, `pipeline/src/hobbes/extract/minted.py`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_csource.py`, `pipeline/tests/test_laneacache.py`, `pipeline/tests/test_minted.py`, `pipeline/tests/test_scipsource_c.py`
- **Gate:** **clear** at `3acd96ff5415` (gate v2, grounder v4, record `a23824c559f0`); unknown 0; map over 90 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 903, regressions 0; {'skip': 17, 'P2P': 857, 'new-pass': 28, 'uncollected': 1}

**The doer's closing words:**

> Committed on `hobbes/S-20260917T172122Z-77da` as `f951d4a`.
> 
> ## What I changed
> 
> **`pipeline/src/hobbes/extract/minted.py`** (new) — the whole rule. `mint(repo_root, definitions, lossy_files, symbols, module_of_path) -> (symbols, counts)`, pure but for reading each file's text for rule 6. `read_moniker` reads scip-clang's descriptor grammar (the one `scip/index.mjs`'s `classify`/`terminalName` spell, backticks and `(hash)` included); `shows_body` is the token read (`//` dropped, `{` before `;` at bracket depth 0 over `(`/`[`, 40 lines). Module docstring carries the six refusals with their measured reasons and P6.
> 
> **`extract/__init__.py`** — collects each facts document's C/C++ `definitions` rows as they stream, then mints after `ev.join` and before `scipsource.project`, extending `graph["symbols"]` (re-sorted by id) and writing `graph["minted"]`. `project` is unchanged.
> 
> **`cppsource.py` / `csource.py`** — the layer dicts gain `lossy_files`, collected from `had_error` at the walk (not from the record's message text). `laneacache.py` needed no change: `cached_parse` already returns `had_error` in its triple, so a hit reports it — I added a test pinning that.
> 
> **Tests** — `tests/test_minted.py` (33 cases: one per minted shape, one per refusal, the body read, the id collisions, and six ingest-level cases with hand-fed facts over a repo whose header really loses its definitions — checked with the real parser, not assumed); a `lane_b`-marked case in `test_scipsource_c.py` on a **copy** of `minicpp` (the fixture is untouched); the `lossy_files` assertions in `test_cppsource.py`, `test_csource.py`, `test_laneacache.py`.
> 
> ## What I ran
> 
> - `tests/test_minted.py test_cppsource.py test_csource.py test_scipsource.py test_scipsource_c.py test_laneacache.py test_tail.py -q` → **425 passed, 6 skipped**
> - whole suite, `-q` → **1,774 passed, 14 skipped** (1,752 + 36 new, one of which is the `lane_b` skip)
> 
> The `lane_b` case and the live tests skip in this sandbox — their first real run is the developer's, in the image.
> 
> ## Deviations
> 
> - **`several-monikers` counts refused rows, not lines.** The ADR's fmt tally says "28 lines"; here a line carrying two monikers reads 2. Every other reason is per row, and a mixed counter reads wrong.
> - **Rule 4 is read over every definition row at the line, including kinds rule 2 refuses** (the literal reading, and it mints less): a `term` beside a `method` blocks the mint. The two rows then count once each under `kind` and `several-monikers`.
> - **"A lane A symbol starts at that line" is checked before every counter**, ahead of rule 1 — that is the only way it is "counted nowhere", and it keeps a clean file's ordinary rows out of the `clean-file` bucket.
> - **`unreadable` is checked before rule 3**, because the owner-chain rule needs the parsed chain.
> - **A file whose text will not read shows no body** and counts `declaration` — minting less, never more.
> - **Only the id takes `~b2` on a collision**; `qualname` keeps the compiler's spelling, per the brief's wording. Lane A ids and qualnames do not move.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: merged no-ff (`5a6d3e5`). On the host: 146 passed over the three files, all 9 `lane_b` tests (the new one on a copy of `minicpp`). Regraded against the stored keys (§10.13): fmt 6,527 confirmed at 0 contradicted, args 2,062 at 0, no standing confirmed row lost, both C exports identical. The regrade found two shapes the brief did not name, because the step-0 probe only saw definitions some call targets: scip-clang's `$anonymous_type_…` names minted as symbols (every C mint, 21 on fmt) and a type lane A already names at another line (`typedef struct cJSON {…} cJSON;`, fmt's `#if` arms of `day`). Two refusals, `anonymous` and `lane-a-has-type`, added on top by the developer with four tests; fmt then reads 6,510 at 0. The doer's six deviations all mint less and stand. ADR-130 (R-arity) follows before the version moves: the hand read of the unjudged rows found 35 wrong edges this mint surfaces (C-153).
