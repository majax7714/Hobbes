# Harness session `S-20260916T230256Z-5261`

*2026-09-16 · `hobbes dispatch` v1 · Hobbes 0.2.33-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Fix H-32, the oracle's half of C-155: O10's reader keys a `CallExpr` that sits inside an **unevaluated operand** — `sizeof(f())`, `alignof`, `noexcept(f())`, a requires-expression's requirements — as  (brief `/home/mmarrujo/.hobbes/sessions/S-20260916T230256Z-5261/brief.md`, task sha256 `7eb0f78571f9`)
- **Parent:** `bb52053ac751` of `hobbes_public`
- **Doer:** 2.1.273 (Claude Code); model claude-opus-5; turns 57 of 100; result `success`; reported cost $4.0887 (the envelope's figure, on the subscription); wall 481.9 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `static.rust-lang.org:443`×2
- **Policy:** 30 exec decision(s) — `allow`×18, `escalate`×12; escalated: `/bin/sh -c mkdir -p /tmp/probe && cat > /tmp/probe/p.cpp <<'EOF'
int f(int);
constexpr bool g() { return true; }
struct P { virtual ~P(); };
P& p();

int a = si`; `/bin/sh -c mkdir -p /tmp/probe && cat > /tmp/probe/p.cpp <<'EOF'
int f(int);
constexpr bool g() { return true; }
struct P { virtual ~P(); };
P& p();

int a = si → expired`; `/bin/sh -c printf 'int f(int);\nint a = sizeof(f(1));\n' > /tmp/p1.cpp && echo wrote && ls -l /tmp/p1.cpp`; `/bin/sh -c printf 'int f(int);\nint a = sizeof(f(1));\n' > /tmp/p1.cpp && echo wrote && ls -l /tmp/p1.cpp → expired`; `/bin/sh -c cd /work/bench/oracle/testdata/cppclang && clang++ -std=c++20 -fsyntax-only -Xclang -ast-dump=json uneval.cpp 2>/dev/null | python3 -c '
import json,`; `/bin/sh -c cd /work/bench/oracle/testdata/cppclang && clang++ -std=c++20 -fsyntax-only -Xclang -ast-dump=json uneval.cpp 2>/dev/null | python3 -c '
import json, → expired`; `/bin/sh -c cd bench/oracle/testdata/cppclang && clang++ -std=c++20 -fsyntax-only -Xclang -ast-dump=json uneval.cpp 2>&1 | head -c 200`; `/bin/sh -c cd bench/oracle/testdata/cppclang && clang++ -std=c++20 -fsyntax-only -Xclang -ast-dump=json uneval.cpp 2>&1 | head -c 200 → expired`; `/bin/sh -c cd bench/oracle && go test ./internal/clang/ -count=1 -v 2>&1 | grep -E '^(=== RUN|--- (PASS|SKIP|FAIL))' | sed 's/(.*//' | paste - - | head -40`; `/bin/sh -c cd bench/oracle && go test ./internal/clang/ -count=1 -v 2>&1 | grep -E '^(=== RUN|--- (PASS|SKIP|FAIL))' | sed 's/(.*//' | paste - - | head -40 → expired`; records: stream opened→closed
- **Edits:** 16 edit(s) to 5 file(s); first at 3.0 min after launch; files: `bench/oracle/internal/clang/cpp_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/shard.go`, `bench/oracle/testdata/cppclang/uneval.cpp`
- **Branch:** `hobbes/S-20260916T230256Z-5261`, 1 commit(s); files: `bench/oracle/internal/clang/cpp_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/shard.go`, `bench/oracle/testdata/cppclang/uneval.cpp`
- **Gate:** **clear** at `bb52053ac751` (gate v2, grounder v4, record `43355f830c53`); unknown 0; map over 14 file(s), 12.1% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 36, regressions 0; {'P2P': 32, 'new-pass': 2, 'skip': 2}; build {'P2P': 2}

**The doer's closing words:**

> H-32 is fixed and committed (`062d60c`).
> 
> **What changed** (5 files, all in the partition, `bench/` only — no version move):
> 
> - `internal/clang/dump.go` — the `reader` carries `uneval` (a depth) and `unevaluated` (a count). `walkNode`'s `"inner"` case increments the depth around `walkChildren` for `UnaryExprOrTypeTraitExpr`, `CXXNoexceptExpr` and `RequiresExpr` (new `isUnevaluatedKind`, `CXXTypeidExpr` deliberately absent); the subtree is still walked, and the callee is still peeled. `record` and `recordConstruct` record nothing while the depth is positive and count the dropped site instead, one per call or construct, after the in-repo check. `ReadDump` puts the count on the shard.
> - `internal/clang/shard.go` — `Shard.Unevaluated int` (`json:"unevaluated,omitempty"`), documented.
> - `internal/clang/merge.go` — `cov["sites_unevaluated"]`, summed over shards beside `sites_tu_split`.
> - `testdata/cppclang/uneval.cpp` — the new fixture, `macroqual.cpp`'s comment style.
> - `internal/clang/cpp_test.go` — two tests: `TestUnevaluatedOperandMakesNoSiteInAHandWrittenDump` (a dump fragment, no clang++ needed: a `sizeof` over a call and an evaluated sibling → one site, count 1) and `TestCppUnevaluatedOperandIsNotASite` (the fixture through `clang++ -std=c++20`, asserting by position).
> 
> **Counts, measured not guessed** — `clang++` 18 *is* on this box's PATH, so the fixture test ran here rather than skipping, and I probed the real dump before fixing the numbers. Fixture: **5 dropped** (`sizeof(f(1))` 27:27, `sizeof f(1)` 28:27, the `noexcept(f(1))` initialiser 40:20, the requires-expression's requirement 45:21, and the `sizeof`'s call at 61:33 sharing a line with the evaluated `f(3)`) and **4 kept** sites (`typeid(f(1))` 52:35, `typeid(p())` 53:35, `static_assert(g(), "")` 57:15, `f(3)` 61:41). Before the change the same unit keyed 9 sites; after, 4 and `sites_unevaluated:5`.
> 
> **What I ran:** `go test ./internal/clang/ -count=1` (pass; `TestCppclangFixture` and `TestCppMacroQualifiedCallSitsAtTheNameTheAuthorWrote` unmoved), `go test ./... -count=1` for the whole oracle module (all pass; the two containment end-to-end tests skip as usual — the fixture Makefile names its sources, so `uneval.cpp` never joins that build), `gofmt -l .` (clean) and `go vet ./internal/clang/`.
> 
> **Deviations**
> 
> - The hand-written dump test lives in `cpp_test.go`, not `dump_test.go` — it is written in `dump_test.go`'s manner (a raw JSON fragment through `ReadDump`) but `dump_test.go` is outside the write partition.
> - The count is `sites_unevaluated` in coverage, per your instruction, rather than ADR-121 §3's spelling `excluded.unevaluated`; same "drop and count" shape.
> - A dropped site carries no position, so the coverage number is a sum over units (a header's `sizeof(f(x))` counts once per unit that reads it), unlike every site-keyed bucket. Said so in `merge.go`.
> - Two fixture shapes turn out to be dropped by clang before the reader sees them, so they count as neither site nor drop: `alignof(decltype(f(1)))` (a type operand) and the `noexcept` **specifier**'s own condition on `void h() noexcept(noexcept(f(1)));` (printed inside the declaration's type, no node). Both stay in the fixture as controls, and the fixture comment says so.
> 
> **One thing for the developer, not a deviation:** `UnaryExprOrTypeTraitExpr` is also C's `sizeof`, so the key now abstains under `sizeof(f())` in C too, while lane A's C walk still records it (ADR-121 §1 leaves C alone deliberately). The C fixture has no such call — `TestCclangFixture` is unmoved — but a C cell that writes `sizeof(f(x))` would now read as a Hobbes contradiction rather than agreement.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Merged no-ff as `fffadbd`; the doer's `062d60c` keeps its
  `hobbes-dispatch` authorship. H-32, the oracle's half of C-155,
  decided in ADR-121 §3 after clang's dump was probed in the image
  before the ADR was written: the dump keeps a call under `sizeof`,
  `noexcept` and `typeid` and never holds a `decltype` operand.

  **The diff is the ADR's §3, with the count where a user reads it.** A
  depth on the reader around the three node kinds, `record` and
  `recordConstruct` dropping and counting while it is positive (the
  callee still peeled, RC-9's rule), `CXXTypeidExpr` deliberately
  absent with the standard's reason, `sites_unevaluated` in coverage
  beside `sites_tu_split` and documented as a per-unit sum, since a
  dropped site carries no position. The doer measured the fixture's
  counts on the real dump rather than guessing them — 9 sites before,
  4 after, 5 dropped — and found that two fixture shapes never reach
  the reader at all (`alignof(decltype(…))`, a `noexcept` specifier's
  condition), which the fixture now says.

  **Verified in the image before merging**, since this host has no
  clang++: all five C++ tests RUN and PASS with `-v` under clang++ 18,
  `TestCppclangFixture` unmoved at 25 sites, `gofmt -l` clean. The
  hand-written dump test runs on the host too.

  **The doer's one-line finding is real and is taken up:**
  `UnaryExprOrTypeTraitExpr` is C's `sizeof` too, so the key now
  abstains under `sizeof(f(x))` in C while lane A's C walk — which
  ADR-121 §1 left alone on purpose — still records it. Not a
  contradiction (the key holds no site on that line, so the row would
  read silent) but an asymmetry between the lane and its key; measured
  on the C cells next and closed in ADR-121's amendment.
