# Harness session `S-20260920T155830Z-d2de`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.59-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — a yarn-v1 install names `corepack` by name when it runs in the image, and by the host's path only on a host run (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T155830Z-d2de/brief.md`, task sha256 `bb531da80f6f`)
- **Parent:** `6c539afc1704` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 19 of 80; result `success`; reported cost $0.9505 (the envelope's figure, on the subscription); wall 149.5 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 4 exec decision(s) — `allow`×4; records: stream opened→closed
- **Edits:** 3 edit(s) to 2 file(s); first at 0.6 min after launch; files: `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/tests/test_scipsource.py`
- **Branch:** `hobbes/S-20260920T155830Z-d2de`, 1 commit(s); files: `pipeline/src/hobbes/extract/scipsource.py`, `pipeline/tests/test_scipsource.py`
- **Gate:** **clear** at `6c539afc1704` (gate v2, grounder v4, record `0ace8771c69c`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 527, regressions 0; {'skip': 15, 'P2P': 506, 'new-pass': 5, 'uncollected': 1}

**The doer's closing words:**

> **What I changed** (`pipeline/src/hobbes/extract/scipsource.py` only):
> 
> - New helper `_yarn1_install_argv()` (`scipsource.py:1272`) builds the classic-yarn argv and picks corepack by where argv[0] will resolve. It asks the containment question the same way `containment.run` does — `not uncontained_requested() and unavailable_reason() is None` — and in that case names `"corepack"` by name, without consulting `_corepack()` at all, so a host with no corepack still provisions in the image. Otherwise it is the host run as before: the resolved absolute path, and `"yarn.lock v1 but corepack is not installed"` when there is none.
> - `_corepack`'s docstring now says it is the host run's only.
> - `provision_node_modules`'s yarn branch is two lines calling the helper. The npm branch and `containment.py` are untouched.
> 
> **Tests** (`pipeline/tests/test_scipsource.py`, new `TestYarn1NamesCorepackWhereItRuns` after `TestProvisionNodeModules`, same commit): contained names `corepack` with `yarn@1.22.22` / `--ignore-scripts` / `--frozen-lockfile` at step `fetch-npm`; contained with `_corepack()` returning `None` still installs with the same argv; a host run names the patched host path; a host run with no corepack returns `(None, "yarn.lock v1 but corepack is not installed")` and never spawns; `HOBBES_UNCONTAINED=1` with the image available is a host run.
> 
> **What I ran:** `/work/pipeline/.venv/bin/python3 -m pytest` in `pipeline/` — 2127 passed, 17 skipped (2144 collected; 5 new tests over the 2,139 in CLAUDE.md). No `VERSION`, CHANGELOG, register or BUILDLOG edits; committed to `hobbes/S-20260920T155830Z-d2de` as `0862afe`, not pushed.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: 19 turns of 80, $0.95, two files, both the partition's. The brief's
  premises were read in the tree and probed first (the bare name provisions
  hono's `benchmarks/jsx` in 8 s in the fetch container). The doer kept to
  the brief: one helper, `_yarn1_install_argv`, asking containment's question
  the way `containment.run` does; `containment.py` and the npm branch
  untouched; five tests in a class of their own. On the host, in a worktree
  of the branch: pytest 2,144 green, the twelve `lane_b` tests green, and a
  real hono ingest with the cache entry cleared — provisioned, three
  extraction errors gone, none new, dependency coverage 0 → 9 of 47, every
  symbol and module edge identical. Merged `--no-ff` (`242b767`).
