# Harness session `S-20260920T193750Z-0bf3`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.63-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-139 amended: a module-level `pytestmark`'s `usefixtures` is followed (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T193750Z-0bf3/brief.md`, task sha256 `ca40e363dae0`)
- **Parent:** `f73d85c6d71a` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 39 of 140; result `success`; reported cost $2.9131 (the envelope's figure, on the subscription); wall 312.4 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 11 exec decision(s) — `allow`×11; records: stream opened→closed
- **Edits:** 14 edit(s) to 6 file(s); first at 1.5 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_fixtures.py`, `pipeline/tests/test_pysource.py`
- **Branch:** `hobbes/S-20260920T193750Z-0bf3`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_fixtures.py`, `pipeline/tests/test_pysource.py`
- **Gate:** **clear** at `f73d85c6d71a` (gate v2, grounder v4, record `a3bdfb8c6ee2`); unknown 0; map over 6 file(s), 0.5% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 845, regressions 0; {'P2P': 811, 'skip': 14, 'new-pass': 15, 'removed': 4, 'uncollected': 1}

**The doer's closing words:**

> Done — ADR-139's amendment built, pipeline half only.
> 
> **What changed**
> 
> - `pipeline/src/hobbes/extract/pysource.py` — `ParsedFile.pytestmark: tuple[Decorator, ...]` now holds each `usefixtures` call a plain module-level `pytestmark` assignment carries, each digested at its own line by a new `_call` split out of `_decorator` (so `dotted`, string `args` and `line` are read exactly as a decorator's are, and a non-string argument is simply not in `args`). `_pytestmark_usefixtures` became `_pytestmark`; the walk's scope is kept — one mark or a list/tuple of marks, module level only — and narrowed to *plain*: an annotated assignment (`pytestmark: list = …`) is now skipped, since its `assignment` node carries a `type` field and would otherwise have been read. `pytestmark_usefixtures` is now the count of recorded marks with no string argument at all.
> - `pipeline/src/hobbes/extract/fixtures.py` — `injections` requests each string of each module mark for every **test** in the module (never a fixture definition), after the test's own and its classes' `usefixtures` decorators and before autouse, through the same `request` closure: same walk, same abstentions, one pair one via. The `usefixtures` count adds the module marks that were followed; `unread["pytestmark"]` keeps only what was not. Both module-docstring paragraphs that said a module `pytestmark` is counted rather than followed now say what is actually left in C-4.
> - `pipeline/src/hobbes/cli.py` — the `fixtures:` clause reads "… in a module-level pytestmark with no string argument" (it was already conditional on a non-zero count); its docstring's "a repo with no `pytestmark`" sentence follows.
> 
> **Tests changed** (found via `tests_guarding hobbes.extract.fixtures`)
> 
> - `test_fixtures.py::TestViaCounts::test_a_module_level_pytestmark_is_counted_and_draws_nothing` → `…_is_followed_and_counted_with_the_marks`, plus a new `…_with_no_string_argument_stays_unread` beside it.
> - `test_cli.py::TestFixturesLine::test_each_optional_clause_is_said_when_there_is_some` — asserts the new wording, and that "not followed" is nowhere on the line.
> - `test_pysource.py::TestPytestmark` rewritten onto the recorded marks: a single mark, a list and a tuple each at their own lines, a `*names` argument counted not followed, another mark not recorded, below-module-level not read, and the annotated/augmented case.
> - New `test_fixtures.py::TestAModulePytestmark`: a plain function and a test-class method both get the injection at the mark's line; a fixture definition in the module does not; an unknown name abstains `not-in-repo`; a test that also names it as a parameter is drawn once with `via == "parameter"`; a test whose own decorator names it keeps the decorator's line; a class naming a base abstains `base-class`; and the counts (`via`, `usefixtures`, `unread["pytestmark"]`).
> 
> **Checked:** `cd pipeline && .venv/bin/python3 -m pytest -q` → 2185 passed, 19 skipped (the `lane_b` cases skip here, `HOBBES_SCIP=0`); that is the tree's 2,193 plus the 11 tests added. Committed as `d44dec5` on `hobbes/S-20260920T193750Z-0bf3`; not pushed.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-139's amendment, the pipeline's half, as briefed. The doer found the
  tests that pinned "counted, not followed" through `tests_guarding` and named each
  one it changed; it narrowed the walk to a *plain* assignment (an annotated
  `pytestmark: list = …` had been read) and said so. On the host, in a worktree of
  the branch: pytest 2,204, 14 `lane_b`. The real cell with the branch's lookup,
  against the `-v` keys: missy **35,770 right, 0 wrong, 0 missed in the repo** (the
  probe's figures; 1,265 missed before), flask 1,238 and attrs 118 unchanged. One
  egress refusal, Claude Code's own `llm` host. Merged `--no-ff`.
