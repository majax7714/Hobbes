# Harness session `S-20260920T191040Z-1527`

*2026-09-20 · `hobbes dispatch` v1 · Hobbes 0.2.62-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-145: a call on the value a fixture constructs is a call on that class (brief `/home/mmarrujo/.hobbes/sessions/S-20260920T191040Z-1527/brief.md`, task sha256 `e11b00c19b07`)
- **Parent:** `9933dd9d10f2` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 81 of 140; result `success`; reported cost $6.7262 (the envelope's figure, on the subscription); wall 725.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×2
- **Policy:** 44 exec decision(s) — `allow`×43, `deny`×1; denied: `/bin/sh -c cd pipeline && env | grep -i "hobbes\|scip" ; python -c " from hobbes.extract import containment print('unavailable:', containment.unavailable_reason`; records: stream opened→closed
- **Edits:** 24 edit(s) to 11 file(s); first at 3.0 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minifixval/pyproject.toml`, `pipeline/tests/fixtures/minifixval/src/minifixval/__init__.py`, `pipeline/tests/fixtures/minifixval/src/minifixval/runner.py`, `pipeline/tests/fixtures/minifixval/tests/conftest.py`, `pipeline/tests/fixtures/minifixval/tests/test_runner.py`, `pipeline/tests/test_fixture_values.py`, … and 1 more
- **Branch:** `hobbes/S-20260920T191040Z-1527`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/fixtures.py`, `pipeline/src/hobbes/extract/pysource.py`, `pipeline/tests/fixtures/minifixval/pyproject.toml`, `pipeline/tests/fixtures/minifixval/src/minifixval/__init__.py`, `pipeline/tests/fixtures/minifixval/src/minifixval/runner.py`, `pipeline/tests/fixtures/minifixval/tests/conftest.py`, `pipeline/tests/fixtures/minifixval/tests/test_runner.py`, `pipeline/tests/test_fixture_values.py`, `pipeline/tests/test_pysource.py`
- **Gate:** **clear** at `9933dd9d10f2` (gate v2, grounder v4, record `1c061ec1105d`); unknown 0; map over 75 file(s), 2.1% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1646, regressions 0; {'P2P': 1578, 'skip': 23, 'new-pass': 44, 'uncollected': 1}

**The doer's closing words:**

> Done — committed as `6ea3524` on `hobbes/S-20260920T191040Z-1527`.
> 
> **What I changed**
> 
> - `pipeline/src/hobbes/extract/pysource.py` — `Symbol.value` (`(name, line)` for a single valued `return`/`yield` of `C(…)` on a bare name) and `Symbol.rebound` (the parameters the own body binds again). New `_own_body` / `_returned_value` / `_rebound` / `_import_binding`; a `yield from` is recorded as a valued exit the rule cannot read, so it refuses. Both facts are a function's — a class gets the defaults.
> - `pipeline/src/hobbes/extract/fixtures.py` — `value_calls(modules, parsed, injected, symbols, symbol_edges)`, the ADR's four conditions in order plus the already-drawn refusal, with `FIXTURE_VALUE`, `PROPERTY_DECORATORS` and `VALUE_REASONS` beside the existing constants. Module docstring now says what the rule follows and what stays C-4's (a value that is not a construction; an inherited method).
> - `pipeline/src/hobbes/extract/__init__.py` — called inside the `fixtures` timing step, right after `_add_injection_edges`; `_add_value_call_edges` merges per `(from, to)` and re-sorts by `_edge_order`. **On the order you asked about:** `_build_symbol_layer` is called at `__init__.py:241` and `collect_tests` at `:258`, so the new edges are in `graph["symbol_edges"]` before the test map reads it — no reordering was needed, and reach does follow them (verified on the fixture).
> - `pipeline/src/hobbes/cli.py` — one clause on the `fixtures:` line, only when `value_calls` is present.
> - Tests: `TestReturnedValue` / `TestRebound` in `test_pysource.py`; new `test_fixture_values.py` (drawn shape, fixture-requesting-fixture, two sites, every refusal one case each, the three non-sites, the join's edge left alone, the `lane_b` end-to-end and its lane-A twin); new `pipeline/tests/fixtures/minifixval/` — kept out of collection by the existing `norecursedirs = ["fixtures"]`, confirmed by `--collect-only`.
> 
> **What I ran**
> 
> - `pipeline/.venv/bin/python -m pytest` in `pipeline/` — **2,172 passed, 19 skipped** (2,146 before; 26 new). The `lane_b` case skips here, as podman is absent.
> - Because podman is missing, `extract_repo` falls back to running scip-python uncontained on this host, so I could still check the `lane_b` case's expectations against a real index: the one `calls` edge `test_runner.test_runner → minifixval.runner.Runner.invoke`, `syntactic`, evidence `(2, "fixture-value")`, nothing else out of the test, `value_calls` = 1 drawn / 1 `no-method` (`runner.close`, on `Base`) / 1 `no-class-edge` (`made.invoke`, a function's return value), and the test record reaching `Runner.invoke` and `minifixval.runner`. All held.
> - A lane-A ingest of this repo: 27 sites looked at, 0 drawn (22 `no-construction`, 5 `no-class-edge`) — 24 once minifixval's own three are taken out, which is the number ADR-145 measured here.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: ADR-145's pipeline half, as briefed. The doer asked the four conditions in
  the ADR's order and counts the first that fails; counted a method in the class's own
  module record because duplicate qualnames collapse in the graph's symbols; read a
  `yield from` as a valued exit the rule cannot read; checked that the new edges land
  before `collect_tests` and said what it found. With podman absent in the sandbox it
  checked the `lane_b` case's expectations against an uncontained index, and said so.
  On the host, in a worktree of the branch: pytest 2,191, all 14 `lane_b` green (the
  new case's first contained run); click regraded with the unit's code, **1,755 → 2,136
  confirmed, 0 contradicted, 382 rows all `syntactic`** — one site fewer than the
  simulation, a call inside a nested `def` the rule rightly refuses; cJSON, jsoup,
  memchr, fzf, fmt, args row-identical pre/post. Merged `--no-ff` (`87da32f`).
  Refusals read: the one policy **deny** was the doer's `env | grep -i "hobbes\|scip"`
  while checking containment — a read of the environment the policy refuses outright,
  rightly, and nothing the task needed; the two egress refusals are Claude Code's own
  `llm` host, as in earlier sessions. That deny was the first any session log carried,
  and it found two defects of the record's own: a multi-line command split the Policy
  line (`dispatch.summarize_flight`, fixed) and the tracker's pattern had no `denied`
  clause (fixed); this log's Policy line was joined onto one line by hand.
