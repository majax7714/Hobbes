# Harness session `S-20260914T161308Z-a848`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.17-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Extend the oracle lane's C oracle (`bench/oracle/internal/clang`, ADR-110, O9) to C++ — O10 — so `oracle c-clang --lang cpp` grades a C++ build root's call edges against clang's own front end. The des (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T161308Z-a848/brief.md`, task sha256 `40ccc893be73`)
- **Parent:** `7a805de00732` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model claude-opus-5; turns 139 of 200; result `success`; reported cost $16.0848 (the envelope's figure, on the subscription); wall 1501.9 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×5
- **Policy:** 63 exec decision(s) — `allow`×45, `escalate`×18; escalated: `/bin/sh -c cd bench/oracle/testdata/cclang && for f in Makefile api.h lib.c main.c tool.c pick.h; do echo "=== $f"; cat -n $f; done`; `/bin/sh -c cd bench/oracle/testdata/cclang && for f in Makefile api.h lib.c main.c tool.c pick.h; do echo "=== $f"; cat -n $f; done → expired`; `/bin/sh -c clang++ --version; which clang++ g++ make bear`; `/bin/sh -c clang++ --version; which clang++ g++ make bear → expired`; `/bin/sh -c clang++ --version`; `/bin/sh -c clang++ --version → expired`; `/bin/sh -c cd bench/oracle && go test ./internal/clang/ -run TestProbeDumpShapes -v 2>&1 | grep -E '"(kind|name|mangledName|referencedMemberDecl|referencedDecl|`; `/bin/sh -c cd bench/oracle && go test ./internal/clang/ -run TestProbeDumpShapes -v 2>&1 | grep -E '"(kind|name|mangledName|referencedMemberDecl|referencedDecl| → expired`; `/bin/sh -c cd bench/oracle && mkdir -p /tmp/cppcell && printf '[{"directory":"%s/testdata/cppclang","file":"main.cpp","arguments":["c++","-I.","-c","main.cpp"]}`; `/bin/sh -c cd bench/oracle && mkdir -p /tmp/cppcell && printf '[{"directory":"%s/testdata/cppclang","file":"main.cpp","arguments":["c++","-I.","-c","main.cpp"]} → expired`; records: stream opened→closed
- **Edits:** 54 edit(s) to 17 file(s); first at 3.8 min after launch; files: `/tmp/commit-msg.txt`, `bench/oracle/cmd/oracle/main.go`, `bench/oracle/internal/clang/clang.go`, `bench/oracle/internal/clang/cpp_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/merge_test.go`, `bench/oracle/internal/clang/run.go`, `bench/oracle/internal/clang/run_test.go`, `bench/oracle/internal/clang/shard.go`, … and 7 more
- **Branch:** `hobbes/S-20260914T161308Z-a848`, 1 commit(s); files: `bench/oracle/cmd/oracle/main.go`, `bench/oracle/internal/clang/clang.go`, `bench/oracle/internal/clang/cpp_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/merge_test.go`, `bench/oracle/internal/clang/run.go`, `bench/oracle/internal/clang/run_test.go`, `bench/oracle/internal/clang/shard.go`, `bench/oracle/internal/export/export.go`, `bench/oracle/internal/export/export_test.go`, `bench/oracle/testdata/cppclang/Makefile`, `bench/oracle/testdata/cppclang/main.cpp`, `bench/oracle/testdata/cppclang/shapes.cpp`, `bench/oracle/testdata/cppclang/shapes.h`, `bench/oracle/testdata/cppclang/tool.cpp`
- **Gate:** **clear** at `7a805de00732` (gate v2, grounder v3, record `9af76078da81`); unknown 0; map over 16 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 52, regressions 0; {'P2P': 41, 'new-pass': 4, 'skip': 7}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `6b9ad20`, everything green.
> 
> ## What changed
> 
> **The reader** (`internal/clang/dump.go`, `shard.go`) — `Decl` gained `Mangled`, `Kind`, `Class`, `Type`, `Virtual`; a per-shard id table (`declByID`) plus a record-id → class-name table, since C++ names a callee by id and an out-of-line definition names its class by `parentDeclContextId`. New sites: `CXXMemberCallExpr` (mode `static`, or `virtual` where the declaration was written virtual), `CXXOperatorCallExpr` (mode `operator`), `CXXConstructExpr`/`CXXTemporaryObjectExpr` (mode `constructor`, no callee — matched after the walk on class + `ctorType`). Every id-named site is finished in a post-walk pass, so a method called above its own declaration resolves; an `isImplicit` constructor or destructor is never a target and its site is dropped. A site inside a method is attributed `Class::name`.
> 
> **The merge** — declarations key on the mangled name where there is one (overloads and a template's specialisations told apart), C's name rules otherwise. A member function's `storageClass: "static"` is *not* internal linkage; an unnamed namespace's declarations are. Coverage gained `sites_virtual`, `sites_operator`, `sites_constructor`, `units_cpp`.
> 
> **The runner / CLI** — `--lang c|cpp` and `--clangxx` on `c-clang` and `c-clang-units`; each unit's front end is picked by its own extension whatever `--lang` says. `export.Exts["cpp"]`.
> 
> **The fixture** `testdata/cppclang` (Makefile, shapes.h, shapes.cpp, main.cpp, tool.cpp), hand-keyed in `cpp_test.go`: **25 sites over 4 units** — 15 static, 4 constructor, 2 virtual, 2 dynamic, 1 operator, 1 macro, 0 tu-split. Overloads land on shapes.cpp:33 and :35; `p->area()` lands on the base's `Shape::area`; the header's inline `twice` is one target from both units; the implicit copy and default constructors are never targets.
> 
> ## Counts
> 
> `cd /work/bench/oracle`: `gofmt -l .` clean; `go test ./internal/clang/ ./internal/export/ ./cmd/...` and `go test ./...` pass — **93 pass / 7 skip** (subtests counted) against a baseline of 89/6 in this sandbox: +4 running tests (`TestCppclangFixture`, `TestCppUnitsPickTheirOwnFrontEnd`, `TestMergeKeysOverloadsByMangledName`, `TestCppExtensions`) and +1 skipping (`TestOracleCppClangEndToEnd`, no podman here — it is the C end-to-end test's C++ twin and runs on the host). The 7 skips are 6 podman-dependent plus 1 needing a nightly Rust toolchain. Every C test is unchanged and green.
> 
> ## Deviations
> 
> - **`Coverage` is `map[string]int`, so a string `lang` cannot go in it.** The export records `units_cpp` (units that really ran under clang++, from the shards) and, for a cell run as `--lang cpp`, `lang_cpp: 1` (the cell's claim).
> - **The fixture Makefile passes `-I.`, not `-Iinclude`** — the partition lists the five fixture files flat under `testdata/cppclang/`, so there is no `include/` directory to point at.
> - **An override that does not repeat the `virtual` keyword reads as mode `static`.** clang's JSON prints `virtual` only where the source writes it, and emits no override information; `Circle::area` therefore gives mode `static` while `Shape::area` gives `virtual`. The target is the same method either way. The fixture keeps the common style and asserts it, rather than hiding the quirk.
> - **A `CXXOperatorCallExpr` whose callee names no declaration is `dynamic`, not `operator`** — a template pattern's dependent `v + v` is an `UnresolvedLookupExpr`, not a call to something. C's "any other callee is dynamic" rule carries over.
> - **A construct site whose unit declares no constructor of that `ctorType` is dropped**, like an implicit one: there is no declaration to name.
> - **A construct expression's class is matched on the type's last `::` component** (after stripping cv- and `struct`/`class`/`union` tags), since a `Decl` records the record's own name; a template-argument-qualified type (`ns::vector<T>::X`) would need the namespace path clang prints and is not exercised by the fixture.
> - **A declaration node writt …

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: O10 as ADR-113 §3 wrote it: the reader gains the four C++ site kinds and a per-shard id table so a member call resolves through `referencedMemberDecl`; the merge keys on the mangled name where there is one; the runner picks each unit's front end by its own extension. Seven deviations, all the dump's own shape (an override without the `virtual` keyword reads `static`; a dependent operator call is `dynamic`; a construct site with no declared constructor is dropped), the kind the cells will price. The cppclang fixture is hand-keyed at 25 sites over 4 units, the implicit constructors never targets. Eighteen escalations, all expired (`cat -n` loops and `which` probes). Five `static.rust-lang.org` refusals from the Rust driver tests under `go test ./...`, which skip. Merged `1f81412`, not squashed; the oracle lane's full suite green on the host, the C++ end-to-end test included. No version move.
