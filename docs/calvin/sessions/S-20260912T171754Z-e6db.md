# Harness session `S-20260912T171754Z-e6db`

*2026-09-12 · `hobbes dispatch` v1 · Hobbes 0.2.0-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Rework C's lane A (`pipeline/src/hobbes/extract/csource.py`, merged from session S-20260912T164904Z-eef8): four defects the developer's review found. Three were measured on a real C repo (DaveGamble/c (brief `/home/mmarrujo/.hobbes/sessions/S-20260912T171754Z-e6db/brief.md`, task sha256 `bfe224dc2bcd`)
- **Parent:** `da3f447d59c7` of `hobbes_public`
- **Doer:** 2.1.269 (Claude Code); model the default; turns 68 of 150; result `success`; reported cost $2.8422 (the envelope's figure, on the subscription); wall 708.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×5; refused `llm`×1
- **Policy:** 16 exec decision(s) — `allow`×16
- **Branch:** `hobbes/S-20260912T171754Z-e6db`, 1 commit(s); files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/fixtures/minic/include/minic/api.h`, `pipeline/tests/fixtures/minic/src/main.c`, `pipeline/tests/fixtures/minic/src/platform.c`, `pipeline/tests/test_csource.py`
- **Gate:** **clear** at `da3f447d59c7` (gate v2, grounder v3, record `70e846ac7937`); unknown 0; map over 7 file(s), 5.6% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 169, regressions 0; {'skip': 3, 'P2P': 153, 'removed': 1, 'new-pass': 12}

**The doer's closing words:**

> Committed as `48684e3` on `hobbes/S-20260912T171754Z-e6db`.
> 
> **What changed** (only the five files the gate allows):
> 1. `linkage_specification` (`extern "C" { ... }`) is now transparent to `_walk_top_level`, same as the `preproc_*` containers — fixes the recall bug (measured 0/341 and 0/9 on Unity/cJSON headers).
> 2. Rank 1 of the fallback now abstains on a same-file tie (function+macro together) instead of picking the last-parsed definition, and never falls through to rank 2.
> 3. `_dedupe_symbols` keeps only the first same-id definition per file and adds one `errors` record per file naming the duplicates (stage `"parse"`).
> 4. `_normalize` returns `None` when a `..` climbs above the repo root, so `#include "../x.h"` from a root file no longer resolves to the root's own `x.h`.
> 5. Module docstring updated for all three rule changes.
> 
> **Fixture moves:** added `include/minic/api.h` (the `extern "C"` idiom with a macro, typedef, an include inside the block, and a static inline function) and `src/platform.c` (`sep()` defined twice across `#ifdef _WIN32`/`#else`); `main.c` got an appended include + two calls without renumbering existing lines. `layer["errors"]` is no longer empty for the fixture — api.h's idiom draws tree-sitter's inherent (unavoidable) syntax-error flag, and platform.c's duplicate `sep` draws the new duplicate-id record — so `test_a_clean_file_gets_no_parse_error_record` was replaced by an assertion naming exactly those two files.
> 
> **Ran:** `pytest -q tests/test_csource.py tests/test_discover.py tests/test_tail.py tests/test_emit.py` (144 passed), then the full suite (1432 passed, 8 skipped, 3 pre-existing failures in `tests/test_ttt_units.py` — confirmed via `git stash` to fail identically on the parent commit, unrelated to C/csource.py).

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear — the diff held nothing a blocking class covers, and review found no error inside one.
- outcome: merged — fast-forward to `48684e3`, keeping the doer's `hobbes-dispatch` authorship.
- notes:
  - **Egress.** The one refusal (`GET llm`) has the same source as in `eef8`: `test_bench.py`'s `http://llm/v1` runtimes under the session's proxy. It was read, and nothing was needed.
  - **The doer's three `test_ttt_units.py` failures** are sandbox-only again. The full suite on `main` after the merge passed 1,443.
  - **Re-measured on cJSON** (`fb16e5c`, host, no lane B):
    - `unity.h`: 341 of 341 function-like macros (was 0); `cJSON.h`: 5 of 5 (was 0).
    - Duplicate ids 0 (was 73), with nine records naming the duplicates.
    - Fallback targets naming a dropped definition: 0 of 3,359.
    - C sites fallback-resolved: 3,363 of 4,292 (was 2,125). Unclassified: 558 (was 1,796). Call edges: 1,761 (was 1,189), all `syntactic`.
    - Five sampled edges read against their lines; all five were right.
  - **The replaced test** (`test_a_clean_file_gets_no_parse_error_record`) is stricter, not weaker. It now names exactly the two fixture files that draw records: `api.h` for the `extern "C"` idiom's syntax-error flag, and `platform.c` for the duplicate `sep`.
  - **A residual, registered rather than reworked:** the dedupe is by name regardless of kind. A struct tag and a function sharing a name in one file (`struct stat` and `stat()`) are one tie, so calls to it abstain. That is conservative and never a wrong edge (C-131).
  - Reviewed by Claude (the developer session), 2026-09-12. Max's review of the merge is pending.
