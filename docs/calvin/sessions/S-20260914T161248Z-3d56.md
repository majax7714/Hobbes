# Harness session `S-20260914T161248Z-3d56`

*2026-09-14 · `hobbes dispatch` v1 · Hobbes 0.2.17-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Add C++ at lane A: a tree-sitter-cpp syntax provider `pipeline/src/hobbes/extract/cppsource.py` on the `csource.py` contract, wired into the extract layer, with its fixture, its tail bucket, and the p (brief `/home/mmarrujo/.hobbes/sessions/S-20260914T161248Z-3d56/brief.md`, task sha256 `9c21e9e7517e`)
- **Parent:** `7a805de00732` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model claude-opus-5; turns 142 of 200; result `success`; reported cost $18.6735 (the envelope's figure, on the subscription); wall 1677.0 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×3
- **Policy:** 40 exec decision(s) — `allow`×34, `escalate`×6; escalated: `/bin/sh -c cd pipeline/tests/fixtures/minic && for f in src/util.h src/util.c src/shapes.c src/main.c; do echo "=== $f"; cat -n $f; done`; `/bin/sh -c cd pipeline/tests/fixtures/minic && for f in src/util.h src/util.c src/shapes.c src/main.c; do echo "=== $f"; cat -n $f; done → expired`; `/bin/sh -c cat > /tmp/probe.cpp <<'EOF'
#include <utility>
#include "util.h"

#define TWICE(x) ((x) * 2)

namespace shapes {

class Base {
public:
    virtual i`; `/bin/sh -c cat > /tmp/probe.cpp <<'EOF'
#include <utility>
#include "util.h"

#define TWICE(x) ((x) * 2)

namespace shapes {

class Base {
public:
    virtual i → expired`; `/bin/sh -c cat > /tmp/probe.cpp <<'EOF'
#include <utility>
#include "util.h"

#define TWICE(x) ((x) * 2)

namespace shapes {

class Base {
public:
    virtual i`; `/bin/sh -c cat > /tmp/probe.cpp <<'EOF'
#include <utility>
#include "util.h"

#define TWICE(x) ((x) * 2)

namespace shapes {

class Base {
public:
    virtual i → expired`; records: stream opened→closed
- **Edits:** 70 edit(s) to 21 file(s); first at 2.4 min after launch; files: `/tmp/dump.py`, `/tmp/dump2.py`, `/tmp/dump3.py`, `/tmp/dump4.py`, `/tmp/probe.cpp`, `/tmp/probe2.py`, `go/internal/knowledge/knowledge.go`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/csource.py`, … and 11 more
- **Branch:** `hobbes/S-20260914T161248Z-3d56`, 1 commit(s); files: `go/internal/knowledge/knowledge.go`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/csource.py`, `pipeline/src/hobbes/extract/tail.py`, `pipeline/tests/fixtures/minicpp/Makefile`, `pipeline/tests/fixtures/minicpp/include/minicpp/shapes.h`, `pipeline/tests/fixtures/minicpp/src/main.cpp`, `pipeline/tests/fixtures/minicpp/src/shapes.cpp`, `pipeline/tests/fixtures/minicpp/src/util.cpp`, `pipeline/tests/fixtures/minicpp/src/util.h`, `pipeline/tests/fixtures/minicpp/tests/test_shapes.cpp`, `pipeline/tests/test_cppsource.py`, `pipeline/tests/test_csource.py`, `pipeline/tests/test_tail.py`
- **Gate:** **clear** at `7a805de00732` (gate v2, grounder v3, record `1c964a061797`); unknown 0; map over 81 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 842, regressions 0; {'P2P': 761, 'skip': 13, 'new-pass': 66, 'removed': 1, 'uncollected': 1}; build {'P2P': 2}

**The doer's closing words:**

> Committed as `7fdb638`. Here's what landed.
> 
> ## What changed
> 
> **`pipeline/src/hobbes/extract/cppsource.py`** (new) — the walk: discovery of the six extensions, the `.h` claim (case (a) no `.c` in the repo; case (b) included by C++ and not by C; both-included headers stay C, with one `cpp-headers` record for a mixed repo), module ids with the `foo.cpp`/`foo.c` collision recorded as a `parse` record, symbols (`function`/`method`/`type`/`macro`, `::`-joined qualnames, constructors and destructors named by the class, a template once at its entity's line, namespaces and lambdas not symbols), the five callee shapes, the fallback (C's three ranks by import, qualified by qualname, ties abstaining as `overload-set`), and the test idioms (gtest/Boost as definitions with a file-local symbol; Catch2/doctest named by their string with a `cpp-tests` record). Where a rule is C's, the module calls `csource`'s.
> 
> **`csource.py`** — `iter_c_files`/`extract_c` take `claimed`; the discovery sentence in the module doc now states ADR-113's rule. Nothing else moved.
> **`__init__.py`** — `extract_cpp` runs before `extract_c` and hands it the claimed headers; layer merged identically; coverage rows the C++ layer owns carry `"language": "cpp"`; the tail gets its qualified sites, its overload sites (merged with Java's) and the per-file language map.
> **`tail.py`** — the six extensions → `cpp`; `language_of(file, row_language)`; C's builtins and `__builtin_*` for `cpp`, `std::` read as builtin from the qualifier; `CLASSES_AVAILABLE["cpp"]`; the three row call sites pass the row's language.
> **`knowledge.go`** — `coverageRow.Language` + a `language()` method the three lookups use; `langByExt` gains the six; `artifactLangBucket` gains `cpp`. Drift test still equal.
> **`tests/fixtures/minicpp/`** — the seven files; `make all test` builds clean under `g++ -Wall`.
> 
> ## What I ran
> 
> - `tests/test_cppsource.py tests/test_csource.py tests/test_tail.py tests/test_graph.py` → **214 passed**.
> - Full pipeline suite → **1,575 passed, 9 skipped** (baseline 1,519 + 65 new: 60 cppsource, 4 tail, 1 csource).
> - `cd go && go build ./... && go test ./internal/knowledge/` → **ok**; `go test ./...` also green; `gofmt -l` clean.
> - `cd pipeline/tests/fixtures/minicpp && make all test` → builds with no warnings (then `make clean`).
> 
> ## Deviations
> 
> - `pipeline/tests/test_extract_graph.py` does not exist in this tree; I ran `tests/test_graph.py` in its place, plus the whole suite.
> - An unqualified `f<int>(x)` is a **plain** call with the template arguments dropped, not a qualified one: with no qualifier there is no qualname to match, so it resolves by the plain ranks.
> - `A(x)` (a temporary) parses as a `call_expression` with a bare identifier, indistinguishable from a plain call, so it is recorded `plain`; `new A(x)` and `A a(x)` are the `construct` shape. The site name is the class either way, so the lanes still meet on it.
> - A construction site is never resolved by lane A (ADR-113 gives the fallback only plain and qualified calls), so it reaches the tail as `unclassified`.
> - A qualified call is matched against the enclosing scope's prefixes before the bare qualname (`Circle::unit()` inside `namespace shapes` means `shapes::Circle::unit`); without that, "the unique symbol whose qualname matches" resolves almost nothing written inside a namespace.
> - A member call written `p->f()` / `this->f()` classifies `unclassified`, not `attr-call`: the tail's shape read looks at the character before the name and `>` is not one of its attribute markers — C's tail has the same gap today, and fixing it would move C's numbers in a C++ unit. Dot-form member calls do classify `attr-call`; neither is ever resolved.
> - The four named casts (`static_cast` and siblings) parse as calls of a template function and are recorded as no site at all.
> - A gtest/Boost test gets a file-local symbol of the test's own name — ADR-113 says a `TEST` body's calls attribute to the test, and a symbol is the only way the join can …

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: The eighth walk, on the fixture and the spec: `cppsource.py` reads ADR-113 §1 rule by rule, reusing C's include resolution, ranks and record shapes by import; the `.h` claim in its three cases; the `cpp` tail bucket with a row-language override held to the proxy's copy by the drift test. Nine deviations, every one the grammar's or the design's own gap, stated plainly (a temporary `A(x)` is a plain call; `p->f()` classes `unclassified` as C's does; the named casts draw no site; an unqualified `f<int>` is plain). Six escalations, all expired: `cat -n` loops over fixture files, and a heredoc to /tmp — the box's questions, read as refusals. Three `llm` egress refusals: nothing reached beyond the endpoint. Merged `3030ac7`, not squashed; pytest 1,583 on main (the tracker's drift test red until this block was filled). The host read on fmt follows in the BUILDLOG. The layer moves to 0.2.18-beta: wired, not supported.
