# Harness session `S-20260915T015439Z-3d1a`

*2026-09-15 · `hobbes dispatch` v1 · Hobbes 0.2.19-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make the helper's C and C++ decode deterministic: a moniker that one file defines at more than one line must decode the same whatever order scip-clang lists its definitions in. The design is ADR-113 s (brief `/home/mmarrujo/.hobbes/sessions/S-20260915T015439Z-3d1a/brief.md`, task sha256 `0edf5308efc5`)
- **Parent:** `3e744777654a` of `hobbes_public`
- **Doer:** 2.1.272 (Claude Code); model claude-opus-5; turns 32 of 80; result `success`; reported cost $2.2278 (the envelope's figure, on the subscription); wall 220.0 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 13 exec decision(s) — `allow`×13; records: stream opened→closed
- **Edits:** 10 edit(s) to 2 file(s); first at 1.2 min after launch; files: `scip/index.mjs`, `scip/test/index.test.mjs`
- **Branch:** `hobbes/S-20260915T015439Z-3d1a`, 1 commit(s); files: `scip/index.mjs`, `scip/test/index.test.mjs`
- **Gate:** **clear** at `3e744777654a` (gate v2, grounder v3, record `b74c25959ff4`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 59, regressions 0; {'P2P': 53, 'new-pass': 5, 'not-run': 1}

**The doer's closing words:**

> Committed as `64c4ec0`.
> 
> **What changed** (`scip/index.mjs`, `scip/test/index.test.mjs` only):
> 
> - `decode`'s definitions loop now collects every definition line a moniker has, per file (`defsOf`), instead of keeping the first occurrence met. After the loop the choice is made by rule: >1 file → `ambiguous`, exactly as before; one file at several lines → smallest line for a `namespace` (every language), abstain when `opts.abstainMultiDefined` is set, else smallest line. An abstained moniker gets no definition, no reference row, and its references stay `in_repo` in `external` (no ADR-111 veto) and are counted.
> - `byFile` (the own-file map, `opts.ownFile`) keeps the smallest line per `(moniker, file)` the same way.
> - `decodeOptions`: `cpp` gains `abstainMultiDefined: true`; `c` and every other language unchanged.
> - `degradations` gains one `scip-decode` record with the moniker and reference counts, carried in `decode`'s return as `multi_defined` / `multi_defined_refs` beside `tu_split`. The rule and its cause are in `decode`'s doc comment.
> - Five new tests: C resolves to line 10 from both listing orders and the two decodes are `deepEqual`; the same pair under `cpp` draws no reference row, is `deepEqual` both orders, is in-repo (no veto), and records 1 moniker / 1 reference; a `cpp` namespace at two lines is kept at the smallest; the own-file map takes that file's smallest line whatever the order; a moniker defined once and one defined in two files decode to fixed values under both configs.
> 
> **What I ran**
> 
> - `cd /work/scip && node --test test/index.test.mjs` — 58 pass, 0 fail (53 before, all unchanged and passing both before and after the test additions; no existing test needed editing).
> - `/work/pipeline/.venv/bin/python3 -m pytest tests/test_scipsource.py tests/test_scipsource_c.py -q` — 110 passed, 4 skipped (the consumer of the helper's `degraded` records, unmoved by the new one).
> - `git status --short` — only the two files in the partition.
> 
> **Deviations** — none.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: The determinism amendment, as written.
  - The definitions loop collects every line per file, and the choice is made by rule: several files → `ambiguous` as before; one file at several lines → a namespace keeps its smallest line; `cpp` abstains (no definition, references in-repo so no ADR-111 veto, counted); otherwise the smallest line.
  - The own-file map keeps the smallest line.
  - The own-file lookup stays gated on cross-file ambiguity, so a C++ multi-defined moniker is never resolved through it.

  No deviations, and no existing test moved. On the host: the scip node suite is 58/58. Three fmt ingests with the branch's helper were identical at the edge level: 9,830 edges each, 0 differing, 3,273 call edges, 240 monikers abstained. Before the fix, three ingests gave 3,308, 3,298 and 3,293 call edges.

  A residue this unit did not touch, and older than it: the tail's `builtin-name` count still wobbled (386 / 386 / 384). Four sites, in `printf.h` and `os-test.cc`, flip between `external` and `builtin-name`; this is measured separately. Merged `f0cd459`, not squashed.
