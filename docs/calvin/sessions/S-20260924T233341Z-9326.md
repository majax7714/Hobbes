# Harness session `S-20260924T233341Z-9326`

*2026-09-24 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E0, part 2: the graders, the containment runner and the self-test (brief `/home/mmarrujo/.hobbes/sessions/S-20260924T233341Z-9326/brief.md`, task sha256 `4e0e014fb2fd`)
- **Parent:** `1d1f60556501` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 87 of 140; result `success`; reported cost $9.7185 (the envelope's figure, on the subscription); wall 1296.2 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 43 exec decision(s) — `allow`×33, `escalate`×10; escalated: `/bin/sh -c cd /tmp && rm -rf lat && mkdir -p lat/stage && cp -r /work/bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src lat/stage/src && mkdir -p la`; `/bin/sh -c cd /tmp && rm -rf lat && mkdir -p lat/stage && cp -r /work/bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src lat/stage/src && mkdir -p la → expired`; `/bin/sh -c mkdir -p /tmp/lat/stage/libs && cp -r /work/bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src /tmp/lat/stage/src && cp -r /work/bench/cal`; `/bin/sh -c mkdir -p /tmp/lat/stage/libs && cp -r /work/bench/calvin/lattice/tests/fixtures/sqlite-vector-kernels/src /tmp/lat/stage/src && cp -r /work/bench/cal → expired`; `/bin/sh -c echo hi; mkdir -p /tmp/lat; echo ok`; `/bin/sh -c echo hi; mkdir -p /tmp/lat; echo ok → expired`; `/bin/sh -c mkdir -p /tmp/lat`; `/bin/sh -c mkdir -p /tmp/lat → expired`; `/bin/sh -c cd /work && git add bench/calvin/lattice && git commit -q -F - <<'MSG' && git log --oneline -1 feat(bench): ADR-151 E0 part 2 — the graders, the cont`; `/bin/sh -c cd /work && git add bench/calvin/lattice && git commit -q -F - <<'MSG' && git log --oneline -1 feat(bench): ADR-151 E0 part 2 — the graders, the cont → expired`; records: stream opened→closed
- **Edits:** 33 edit(s) to 18 file(s); first at 5.4 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/build.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/diff.py`, `bench/calvin/lattice/src/lattice/feedback.py`, `bench/calvin/lattice/src/lattice/grade.py`, `bench/calvin/lattice/src/lattice/hsr.py`, `bench/calvin/lattice/src/lattice/run.py`, `bench/calvin/lattice/src/lattice/selftest.py`, `bench/calvin/lattice/src/lattice/task.py`, … and 8 more
- **Branch:** `hobbes/S-20260924T233341Z-9326`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/build.py`, `bench/calvin/lattice/src/lattice/cli.py`, `bench/calvin/lattice/src/lattice/diff.py`, `bench/calvin/lattice/src/lattice/feedback.py`, `bench/calvin/lattice/src/lattice/grade.py`, `bench/calvin/lattice/src/lattice/hsr.py`, `bench/calvin/lattice/src/lattice/run.py`, `bench/calvin/lattice/src/lattice/selftest.py`, `bench/calvin/lattice/src/lattice/task.py`, `bench/calvin/lattice/tests/test_cli.py`, `bench/calvin/lattice/tests/test_diff.py`, `bench/calvin/lattice/tests/test_feedback.py`, `bench/calvin/lattice/tests/test_grade.py`, `bench/calvin/lattice/tests/test_hsr.py`, `bench/calvin/lattice/tests/test_run.py`, `bench/calvin/lattice/tests/test_selftest.py`, `bench/calvin/lattice/tests/test_task.py`
- **Gate:** **blocked** at `1d1f60556501` (gate v2, grounder v4, record `c2f099282acb`) — blocking: invented; unknown 0; map over 13 file(s), 4.3% of their lines uncaptured; partition checked
  - `invented` bench/calvin/lattice/tests/test_grade.py:136 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:149 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:157 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:178 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:187 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:197 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:208 `needs_clang`
  - `invented` bench/calvin/lattice/tests/test_grade.py:215 `needs_clang`
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 101, regressions 0; {'P2P': 12, 'new-pass': 89}

**The doer's closing words:**

> Committed on `hobbes/S-20260924T233341Z-9326`, working tree clean.
> 
> ## What I changed
> 
> Nine new/edited modules under `bench/calvin/lattice/src/lattice/` and eight test files — exactly the 18 files the gate allows.
> 
> - **`run.py`** — `in_container()` (the two container markers), `NotContained` as its own exception type (P10: not a subclass of `OSError`/`RuntimeError`), and `image_plan()` returning the `podman run` argv as pure data. `allow_host` exists for this package's tests on its own fixture; the CLI never sets it.
> - **`build.py`** — G-compile. The target's `Makefile` flags as data per ISA (plus `-Wno-unused-function`, this package's, for the trimmed fixture), an object cache keyed by source text + flags, and clang's diagnostics parsed to `(file relative to root, line, col, severity, message)`.
> - **`hsr.py`** — G-hsr: the four compiler/linker messages that name an invented name, the three buckets, and the six-class failure ladder.
> - **`diff.py`** — G-diff. The generated C driver reaches kernels **through `dispatch_distance_table`** after the ISA's init, compares each `graded_via` slot against the scalar snapshot, and reports a slot still equal to the reference as *not installed*. splitmix64 per case, 32 bulk + 15 ragged + the per-type specials; `inf`/`nan` printed as strings. The comparison and the tolerance table are Python data.
> - **`grade.py`**, **`feedback.py`**, **`selftest.py`**, **`cli.py`**, **`task.py`** (`prelude_bare`, schema `lattice-task/2`), and the README section per module.
> 
> **One measured deviation, flagged:** the gold does not pass at rtol 1e-6 on `(int8, l2_squared)`. Cause read in the source, not guessed — `int8_distance_l2_impl_cpu` accumulates into a `float` while every SIMD kernel accumulates into int32 and converts once, so past float32's exact integer range the **reference** is the imprecise side. Widened that row and `(int8, l2)` to rtol 1e-5 with the worst observed errors (1.06e-6 and 5.09e-7) in the comment. `uint8`'s rows are left at the floor and commented as **unmeasured** — the fixture has no uint8 row, so I have no evidence to move them; the first run on the real target will say.
> 
> ## What I ran
> 
> - `cd bench/calvin/lattice && .venv/bin/python -m pytest -q` → **127 passed in 17s**, nothing skipped (clang 18 and `avx512f` are both present here).
> - The full self-test over **all 39 native fixture cells** (195 gradings, 29s): every gold `pass`, every one of the four mutants its exact class, 0 mismatches. Worst relative errors: `float32/dot` 4.28e-6 (rtol 1e-4), `int8/l2_squared` 1.06e-6 (rtol 1e-5), `bit1/hamming` 0.
> 
> The image path (`--image`, no `--here`) is covered by a test that asserts the argv and stubs `run_plan`: there is no `podman` in this session, so I could not execute it end to end — the developer's run on the host is its first.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: false-block
- outcome: merged
- notes: ADR-151's E0, part 2, built as briefed, and the code is right. **The block is the gate's.** All 8 `invented` rows
  are `@needs_clang` in `tests/test_grade.py`, where `needs_clang = pytest.mark.skipif(...)` is assigned at module
  level in the same file. Since ADR-146 (0.2.65-beta), lane A reads a decorator as a call of the name it holds, and
  the grounder reuses lane A's calls. A name bound by a module-level assignment is neither a symbol nor a local
  binding there, so it reads as invented, or as a near-miss when a similar name exists. Reproduced with a 14-line
  diff (`needs_x = pytest.mark.skipif(...)`, then `@needs_x`: blocked, `near-miss`). A false block, and the
  harness's second (`f3c1` was the first); the fix is the grounder's, not this unit's. **The real target's
  self-test** (in the image, all 93 native cells, `~/.hobbes/bench/calvin-lattice/selftest/`): 38 mismatches. 363 of
  the 372 mutants get their class, and 29 golds do not pass, for two reasons. (Corrected the same night: this note
  first said 372 of 372, and the review commit's message still does. The other 9 are the uint8 `edge` mutants, read
  `wrong` because the gold's own bulk drift at n = 4096 fails them first. The cause is the uint8 one below.) The instrument's, uint8 at n = 4096: the scalar
  reference accumulates in `float` and drifts ~1.05e-6 to 1.45e-6 (the doer's own int8 finding, on the row it could
  not measure). The target's: on inf and NaN inputs, sqlite-vector's f16 and bf16 SIMD kernels disagree with its
  scalar kernel, and with each other (`disagree.py`: bf16 `dot` on overflow is +inf on SSE2 and NaN on AVX2 and
  AVX-512; AVX-512's f16 `l1`/`l2` agree with the scalar where SSE2's and AVX2's do not). No single semantics
  exists to grade those cases against. The rule taken, built in the next unit: a case with a non-finite input or a
  non-finite scalar result is graded against the cell's own gold, and every other case against the scalar. The
  fixture's 39 golds pass, and pytest on the host is 114 passed and 13 skipped (the clang cases; the session ran
  them).
