# Harness session `S-20260919T202902Z-5587`

*2026-09-19 · `hobbes dispatch` v1 · Hobbes 0.2.54-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Fix oracle defects **H-34** and **H-35** in the TypeScript oracle. Their records are the H-34 and H-35 rows of `docs/oracle/oracle-defects.md` — read both. Both were found in the first triage of the P (brief `/home/mmarrujo/.hobbes/sessions/S-20260919T202902Z-5587/brief.md`, task sha256 `4fcc3d9a6499`)
- **Parent:** `d8b12cbfca1b` of `hobbes_public`
- **Doer:** 2.1.278 (Claude Code); model claude-opus-5; turns 37 of 80; result `success`; reported cost $1.8165 (the envelope's figure, on the subscription); wall 219.9 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `static.rust-lang.org:443`×4
- **Policy:** 15 exec decision(s) — `allow`×11, `escalate`×4; escalated: `/bin/sh -c node ts/tsc-oracle.mjs --repo ./testdata/jsdecl --zone . --no-tsconfig --out /tmp/before.json 2>&1 | tail -5; python3 -c "
import json;d=json.load(op`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ./testdata/jsdecl --zone . --no-tsconfig --out /tmp/before.json 2>&1 | tail -5; python3 -c "
import json;d=json.load(op → expired`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ./testdata/jsdecl --zone . --no-tsconfig --out /tmp/before.json`; `/bin/sh -c node ts/tsc-oracle.mjs --repo ./testdata/jsdecl --zone . --no-tsconfig --out /tmp/before.json → expired`; records: stream opened→closed
- **Edits:** 11 edit(s) to 8 file(s); first at 1.0 min after launch; files: `bench/oracle/README.md`, `bench/oracle/internal/grade/jsdecl_test.go`, `bench/oracle/testdata/jsdecl/lib/shape.d.ts`, `bench/oracle/testdata/jsdecl/lib/shape.js`, `bench/oracle/testdata/jsdecl/lib/typed.js`, `bench/oracle/testdata/jsdecl/lib/use.js`, `bench/oracle/testdata/jsdecl/package.json`, `bench/oracle/ts/tsc-oracle.mjs`
- **Branch:** `hobbes/S-20260919T202902Z-5587`, 1 commit(s); files: `bench/oracle/README.md`, `bench/oracle/internal/grade/jsdecl_test.go`, `bench/oracle/testdata/jsdecl/lib/shape.d.ts`, `bench/oracle/testdata/jsdecl/lib/shape.js`, `bench/oracle/testdata/jsdecl/lib/typed.js`, `bench/oracle/testdata/jsdecl/lib/use.js`, `bench/oracle/testdata/jsdecl/package.json`, `bench/oracle/ts/tsc-oracle.mjs`
- **Gate:** **clear** at `d8b12cbfca1b` (gate v2, grounder v4, record `534b016bd22b`); unknown 0; map over 11 file(s), 17.8% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 30, regressions 0; {'new-pass': 25, 'skip': 5}; build {'P2P': 2}

**The doer's closing words:**

> **Changed**
> 
> - `bench/oracle/ts/tsc-oracle.mjs` — H-34: `external` is `!inRepo(file)` alone, so an in-repo `.d.ts` keys repo-relative and is not external (comment states it now counts as an in-repo pair for recall; the site walk is untouched). H-35: new `jsDocTypeHost(decl)`, applied right after `decl = sig.getDeclaration()`, maps a JSDoc **`@type`** tag's declaration to its host — a function-like declaration, or a single variable with a function literal behind it (the variable declaration, as the binding path would). The comment names what is mapped and what is left.
> - `bench/oracle/testdata/jsdecl/` — the five files as specified, no tsconfig/jsconfig.
> - `bench/oracle/internal/grade/jsdecl_test.go` — `TestJsdeclInRepoDeclarationAndJSDocType` with a sibling `runJSOracleAt` helper that takes a repo path; asserts both rows, `External == false`, kind `function`, `Closure == false`, `missClass == static→function`, and `files` exactly the three `.js`.
> - `bench/oracle/README.md` — two sentences in *The TypeScript oracle*.
> 
> **Measured before the fix** (the test run on the unmodified oracle): `lib/use.js:2` → `/work/bench/oracle/testdata/jsdecl/lib/shape.d.ts:1`, `External:true`; `lib/typed.js:5` → `lib/typed.js:1`, kind `closure`, `static→closure`. Both as the defect records say.
> 
> **Ran** (from `/work/bench/oracle`, all through `mcp__hobbes__exec`):
> - `go test ./internal/grade/ -count=1 -run 'Jsdecl|JSDecl|Minijs|NoTsconfig|Minits|TS' -v` — 7 top-level tests RUN and PASS, no skips: `TestMinitsTSAllConfirmed`, `TestJsdeclInRepoDeclarationAndJSDocType` (+2 subtests), `TestMinijsNoTsconfigResolvesTheJavaScriptShapes`, `TestMinijsNoTsconfigSaysSo`, `TestNoTsconfigRefusals` (+3), `TestMinitsParameterBindingsAreDynamic`, `TestTSFixtureRefusesEveryPoisonedEdge`.
> - `go test ./... -count=1` — all 11 packages ok; with `-v`, **120 PASS / 7 SKIP / 0 FAIL** counting subtests (was 117/7 before this unit; the 3 added are the new test and its two subtests). No existing assertion moved.
> - `go vet ./...` clean; `gofmt -l .` empty.
> 
> Committed as `a0785c1` on `hobbes/S-20260919T202902Z-5587`. Not pushed.
> 
> **Deviations** (one line each):
> - H-35 maps only a JSDoc **`@type`** tag, not any JSDoc whose host is function-like: the literal reading would have caught `minijs`' `lib/apply.js` `@param {(s: string) => string} f` — its JSDoc host *is* the function `apply` — and moved the `f("a")` row off `func-value→parameter`, which the partition forbids. This is the narrower shape and matches the H-35 record's own wording.
> - `node` is escalate-only under the repo policy, so every probe ran through `go test`; the before-state above is that test's failure output on the unmodified oracle rather than a direct oracle invocation.
> - `runJSOracleAt` returns only the parsed export (no raw bytes): this test reads no key outside `OracleExport`.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: H-34 and H-35. Read whole: `external` is `!inRepo(file)` alone; `jsDocTypeHost` maps only a JSDoc `@type` tag to its host function (or a variable with a function literal). **The doer narrowed the brief, rightly:** as worded, "a JSDoc whose host is function-like" would have caught `minijs`'s `@param` callback type and moved `f("a")` off `func-value→parameter`; the `@type`-only rule is the defect as recorded. On the host in a worktree: the TS/JS oracle tests RUN and PASS, the module green, vet and gofmt clean. **Regraded contained, no re-ingest:** Preact 1,201/1,223 contradicted → 2,446/0; every other JS cell and the five TS cells row-identical (hono +5 in-repo pairs, calls into its own `.d.ts`).
