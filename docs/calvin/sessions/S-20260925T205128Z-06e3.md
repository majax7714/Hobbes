# Harness session `S-20260925T205128Z-06e3`

*2026-09-25 · `hobbes dispatch` v1 · Hobbes 0.2.70-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** # Unit — ADR-151 E1: three instrument fixes from E1-g's real rows, and the measured price (brief `/home/mmarrujo/.hobbes/sessions/S-20260925T205128Z-06e3/brief.md`, task sha256 `45d5cfa46624`)
- **Parent:** `5b82cd214c07` of `hobbes_public`
- **Doer:** 2.1.282 (Claude Code); model claude-opus-5; turns 91 of 120; result `success`; reported cost $8.9453 (the envelope's figure, on the subscription); wall 786.7 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused none
- **Policy:** 24 exec decision(s) — `allow`×22, `escalate`×2; escalated: `/bin/sh -c cd /work && git commit -q -F - <<'EOF' feat(calvin): E1's three instrument fixes from E1-g's real rows, and the measured price E1-g's record named th`; `/bin/sh -c cd /work && git commit -q -F - <<'EOF' feat(calvin): E1's three instrument fixes from E1-g's real rows, and the measured price E1-g's record named th → expired`; records: stream opened→closed
- **Edits:** 49 edit(s) to 9 file(s); first at 3.0 min after launch; files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/e1.py`, `bench/calvin/lattice/src/lattice/extract.py`, `bench/calvin/lattice/src/lattice/gmem.py`, `bench/calvin/lattice/src/lattice/report.py`, `bench/calvin/lattice/tests/test_e1.py`, `bench/calvin/lattice/tests/test_extract.py`, `bench/calvin/lattice/tests/test_gmem.py`, `bench/calvin/lattice/tests/test_report.py`
- **Branch:** `hobbes/S-20260925T205128Z-06e3`, 1 commit(s); files: `bench/calvin/lattice/README.md`, `bench/calvin/lattice/src/lattice/e1.py`, `bench/calvin/lattice/src/lattice/extract.py`, `bench/calvin/lattice/src/lattice/gmem.py`, `bench/calvin/lattice/src/lattice/report.py`, `bench/calvin/lattice/tests/test_e1.py`, `bench/calvin/lattice/tests/test_extract.py`, `bench/calvin/lattice/tests/test_gmem.py`, `bench/calvin/lattice/tests/test_report.py`
- **Gate:** **clear** at `5b82cd214c07` (gate v2, grounder v4, record `691fb4473ae9`); unknown 0; map over 9 file(s), 18.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 127, regressions 0; {'P2P': 105, 'new-pass': 22}

**The doer's closing words:**

> **Committed as `c3320e9`** on `hobbes/S-20260925T205128Z-06e3`. All nine files the gate names, and nothing else.
> 
> **What changed**
> 
> - **`extract.py`** — `extract` also returns `params`, the parameter names of the definition it read (in order, `None` where there is no body), and `params(signature)` reads them off any signature: the last identifier of each comma-separated parameter, `(void)` giving none, nested lists not splitting. The docstring names what it reads wrongly (a function-pointer parameter's own name, an unnamed parameter) rather than pretending otherwise.
> - **`e1.py`** — the `param` bucket. Each request now carries the target's signature (`_position`), and a merged row re-buckets any invented name that is one of the model's own parameter names and none of the target's. When every invented name is one, the class becomes `compile` with the reason naming both lists; the graders' own answer stays readable under `row["grade"]`. The retry gets `The signature is …: use its parameter names.` appended, kept whole inside `feedback.LIMIT`. Also `MAX_TOKENS = 2048` (`GMEM_MAX_TOKENS` untouched at 512), `PRICING` at E1-g's measured 8,000/950, and a per-call `COLD_START_SECONDS = 180` in `estimate`'s seconds.
> - **`gmem.py`** — `MIN_EXPECTED_TOKENS = 8` and `has_evidence`, used by `gmem.run` and by `e1.plan`'s probe request. A probe below the floor keeps its row and its score.
> - **`report.py`** — states `max_tokens` from `meta.json` (in the dict and in the table header); the docstring names `param` as a bucket of its own. The bucket grouping already handled a new bucket, and the tests hold that it never counts toward `invented intrinsic`.
> - **`README.md`** — the `param` bucket and why the class becomes `compile`, the new `max_tokens` and why, the evidence floor, where the price comes from, and the order-of-work section updated (E1-g has run; step 3 is done).
> 
> **One deliberate narrowing to flag:** the class flip fires only on a row graded `invented`, the only class that is a claim about names — a `pass` or `wrong` row is left alone whatever its entries say. In `grade` the two always coincide (invented names come from a failed compile or link), so this is inert today and a guard against a future grader.
> 
> **What I ran:** `cd bench/calvin/lattice && .venv/bin/python -m pytest -q` → **385 passed** (was 372 before this unit; 13 new tests). No model call, no Modal run, no container.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: E1's three instrument fixes from E1-g's real rows (Max: "good to proceed with recommended"): the `param`
  bucket, `max_tokens` 2,048, the G-mem evidence floor, and the measured price. 91 turns of 120. The doer's narrowing,
  that only an `invented` row moves, is accepted.
  **The real-target check before the merge:**
  - With the new `_merge` applied to E1-g's 1,734 stored rows (signatures and extracts re-derived), exactly 60 rows
    get a `param` entry and 59 move from `invented` to `compile`. The 60th also invents a real name, matching the one
    row the remap re-grade left `invented`.
  - The retry sentence names the target's signature.
  - The floor marks all 30 wrappers' probes `evidence: False`, and all 63 real bodies keep theirs.

  Host: 364 passed, 21 skipped (no clang).
