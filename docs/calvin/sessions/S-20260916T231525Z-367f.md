# Harness session `S-20260916T231525Z-367f`

*2026-09-16 · `hobbes dispatch` v1 · Hobbes 0.2.33-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make lane A's C walk record **no call site inside a `sizeof` or `_Alignof` operand**, matching the C++ walk and the oracle's key. The decision is ADR-121 §1's amendment of 2026-09-16 ("Amended 2026-09 (brief `/home/mmarrujo/.hobbes/sessions/S-20260916T231525Z-367f/brief.md`, task sha256 `2fa1fbea8238`)
- **Parent:** `baf213e4812a` of `hobbes_public`
- **Doer:** 2.1.273 (Claude Code); model claude-opus-5; turns 29 of 80; result `success`; reported cost $1.3699 (the envelope's figure, on the subscription); wall 208.7 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 9 exec decision(s) — `allow`×7, `escalate`×2; escalated: `/bin/sh -c cd /work/pipeline && .venv/bin/python3 - <<'EOF'
import tempfile, pathlib
from hobbes.extract.csource import extract_c
src = (
 "int f(int);\n"
 "int`; `/bin/sh -c cd /work/pipeline && .venv/bin/python3 - <<'EOF'
import tempfile, pathlib
from hobbes.extract.csource import extract_c
src = (
 "int f(int);\n"
 "int → expired`; records: stream opened→closed
- **Edits:** 5 edit(s) to 2 file(s); first at 1.4 min after launch; files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/test_csource.py`
- **Branch:** `hobbes/S-20260916T231525Z-367f`, 1 commit(s); files: `pipeline/src/hobbes/extract/csource.py`, `pipeline/tests/test_csource.py`
- **Gate:** **clear** at `baf213e4812a` (gate v2, grounder v4, record `4be1af1123af`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 231, regressions 0; {'skip': 5, 'P2P': 224, 'new-pass': 2}

**The doer's closing words:**

> **What changed** — two files, both in the partition.
> 
> `pipeline/src/hobbes/extract/csource.py`
> - `_UNEVALUATED_OPERANDS = {"sizeof_expression", "alignof_expression"}` beside `_PREPROC_CONTAINERS`, with a `#:` comment citing C11 6.5.3.4 and ADR-121 §1's amendment.
> - `_unevaluated(node)` — unbounded walk to the root, true if any ancestor is in that set; docstring names the VLA residual.
> - `_calls` skips a `call_expression` for which it holds. `_callee_shape`, `_registration_form`, the registration walk and the fallback are untouched.
> - The docstring's **Call sites and their shapes** paragraph gains the sentence, the ADR citation and the VLA residual.
> 
> `pipeline/tests/test_csource.py` — two tests under `TestCallSites`, on `tmp_path` repos (the file has no `_write` helper; it writes with `write_text`, so I followed it):
> - `test_no_call_under_a_sizeof_or_alignof_records_a_site_and_the_controls_do` — `sizeof(f(1))`, `sizeof f(1)`, `_Alignof(int[n()])`, an evaluated `f(2)`, and `f(3) + sizeof(f(1))` sharing line 7. Sites are exactly `[(6, "f"), (7, "f")]`, plus a column check that the survivor on line 7 is `f(3)`. Before the change the same file recorded 6 sites.
> - `test_the_vla_residual_is_the_known_miss_a_call_sizing_an_array_records_nothing` — `sizeof(int[n()])` records no site, asserted as current behaviour.
> 
> **Grammar probe** (tree-sitter-c as installed, run before relying on it): `sizeof(f(1))` and `sizeof f(1)` both parse to `sizeof_expression`; `_Alignof(…)` and `__alignof__(…)` both parse to `alignof_expression`. Confirmed as the ADR spells them.
> 
> **Deviations**
> - `_Alignof(f(1))` does not parse: `_Alignof` takes a type name, so the grammar reads `f` as a `type_identifier` and emits an `ERROR` node with no `call_expression` at all — no site there before or after. Dropped that line as the task allows, and used `_Alignof(int[n()])` instead (parses clean, `has_error` false), the only shape in C that puts a real `call_expression` inside an `_Alignof` operand, so the `alignof_expression` half of the constant is still exercised. That line is itself a VLA shape; the separate residual test uses `sizeof(int[n()])` as specified.
> 
> **Checked**: `.venv/bin/python3 -m pytest tests/test_csource.py tests/test_cppsource.py tests/test_tail.py -q` → **217 passed**, including `test_cppsource.py`'s `test_no_shape_of_unevaluated_operand_records_a_site_and_the_controls_do`. Full tree: `.venv/bin/python3 -m pytest -q` → **1,645 passed, 10 skipped**. Committed as `7226149` on `hobbes/S-20260916T231525Z-367f`; not pushed. Register, VERSION and CHANGELOG left alone.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Merged no-ff as `83b60c9`; the doer's `7226149` keeps its
  `hobbes-dispatch` authorship. ADR-121 §1's amendment, decided after the
  oracle unit's doer pointed out that the reader's rule reaches C's
  `sizeof` too; measured at 0 sites on cJSON, sqlite-vector and `minic`
  before the brief was written, so no cell moves.

  **The diff is the C++ predicate one grammar over**, as the amendment
  asks: a two-element constant, an unbounded walk to the root, one skip
  in `_calls`, the docstring's sentence with the VLA residual. The doer
  probed the installed grammar before relying on it and found that
  `_Alignof(f(1))` does not parse at all (`_Alignof` takes a type name),
  so the `alignof_expression` half is exercised through the one shape C
  allows there, `_Alignof(int[n()])` — itself a VLA — and the residual
  test pins `sizeof(int[n()])` as the known miss, so a later change to
  it is deliberate.

  **Verified on the host before merging:** the branch's own run was
  1,645 passed; on `main` after the merge the whole pipeline suite is
  green (its count is in the BUILDLOG entry). The third unit of ADR-121
  and the smallest: 29 turns, $1.37.
