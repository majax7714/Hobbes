# Harness session `S-20260913T200618Z-9cad`

*2026-09-13 · `hobbes dispatch` v1 · Hobbes 0.2.11-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Make `hobbes verify`'s worktrees self-contained, so `git` works inside the verify container (D-r). The fix is decided. Follow it; do not re-decide it. (brief `/home/mmarrujo/.hobbes/sessions/S-20260913T200618Z-9cad/brief.md`, task sha256 `02a2bc368420`)
- **Parent:** `0a561995bcba` of `hobbes_public`
- **Doer:** 2.1.270 (Claude Code); model the default; turns 15 of 80; result `success`; reported cost $0.2914 (the envelope's figure, on the subscription); wall 60.4 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused none
- **Policy:** 3 exec decision(s) — `allow`×3
- **Edits:** 2 edit(s) to 2 file(s); first at 0.4 min after launch; files: `pipeline/src/hobbes/derive/harness.py`, `pipeline/tests/test_harness.py`
- **Branch:** `hobbes/S-20260913T200618Z-9cad`, 1 commit(s); files: `pipeline/src/hobbes/derive/harness.py`, `pipeline/tests/test_harness.py`
- **Gate:** **clear** at `0a561995bcba` (gate v2, grounder v3, record `cc696d78bc62`); unknown 0; map over 2 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 88, regressions 0; {'P2P': 85, 'skip': 1, 'new-pass': 2}

**The doer's closing words:**

> Done.
> 
> **Changed:** `checkout()` in `pipeline/src/hobbes/derive/harness.py` drops `--shared` from the `git clone`, so it hardlinks/copies objects instead of writing a `.git/objects/info/alternates` pointing back at the source; docstring rewritten to explain why (D-r). Confirmed via `who_calls` that both callers (`build_row`, `verify`) need no changes.
> 
> **Tests added** in `pipeline/tests/test_harness.py`: `test_checkout_writes_no_alternates_and_survives_the_source_moving` (no alternates file, and `git rev-parse HEAD`/`git log -1` still work after renaming the source away, standing in for the container) and `test_checkout_replaces_an_existing_dest` (a second `checkout()` into the same dest lands at the new sha).
> 
> **Ran:** `/work/pipeline/.venv/bin/python3 -m pytest tests/test_harness.py tests/test_dispatch.py tests/test_gate.py -q` — 69 passed, 1 skipped. Committed as `0bc451b` on the current branch.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged as `0e9f5b3` (not squashed); released as 0.2.12-beta
- notes: Read the diff (2026-09-13): the brief's one argument and the docstring, and two tests that move the source away rather than needing podman. This session's own verify ran the parent's `checkout()`, still `--shared`; none of its 88 selected tests read git, so D-r did not show there. **The after-check, on the host in the image:** `test_cli.py::TestIngest::test_the_artifact_says_which_hobbes_built_it` passes through the merged `checkout()` (no alternates file), and fails through a `--shared` clone of the same commit, where `built_by()`'s checkout falls back. That is `efc8`'s F2F. Host pytest on merged main: 1,482 passed, with the 4 known `testmap_fixture` warnings.
