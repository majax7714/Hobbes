# Harness session `S-20260916T153010Z-8170`

*2026-09-16 · `hobbes dispatch` v1 · Hobbes 0.2.28-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** Fix two defects in the C/C++ oracle's key: a member call is keyed at its object's start (H-28), and a declaration the front end does not mangle is keyed by its bare name, so two classes' same-named me (brief `/home/mmarrujo/.hobbes/sessions/S-20260916T153010Z-8170/brief.md`, task sha256 `af39f099434f`)
- **Parent:** `7240ebd9355b` of `hobbes_public`
- **Doer:** 2.1.273 (Claude Code); model claude-opus-5; turns 77 of 120; result `success`; reported cost $7.2136 (the envelope's figure, on the subscription); wall 735.8 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×4; refused `static.rust-lang.org:443`×1
- **Policy:** 33 exec decision(s) — `allow`×25, `escalate`×8; escalated: `/bin/sh -c mkdir -p /tmp/p && cd /tmp/p && cat > chain.cpp <<'EOF'
struct Wrapped {
    int v;
    int get() const { return v; }
};

struct Builder {
    int to`; `/bin/sh -c mkdir -p /tmp/p && cd /tmp/p && cat > chain.cpp <<'EOF'
struct Wrapped {
    int v;
    int get() const { return v; }
};

struct Builder {
    int to → expired`; `/bin/sh -c mkdir -p /tmp/p && echo ok`; `/bin/sh -c mkdir -p /tmp/p && echo ok → expired`; `/bin/sh -c cd /tmp && cat > pchain.cpp <<'EOF'
struct Wrapped {
    int v;
    int get() const { return v; }
};

struct Builder {
    int total;
    Builder &op`; `/bin/sh -c cd /tmp && cat > pchain.cpp <<'EOF'
struct Wrapped {
    int v;
    int get() const { return v; }
};

struct Builder {
    int total;
    Builder &op → expired`; `/bin/sh -c clang++ --version`; `/bin/sh -c clang++ --version → expired`; records: stream opened→closed
- **Edits:** 34 edit(s) to 8 file(s); first at 4.1 min after launch; files: `bench/oracle/internal/clang/cpp_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/merge_test.go`, `bench/oracle/testdata/cppclang/extc.c`, `bench/oracle/testdata/cppclang/extc2.c`, `bench/oracle/testdata/cppclang/member.cpp`, `bench/oracle/testdata/cppclang/tmpl.cpp`
- **Branch:** `hobbes/S-20260916T153010Z-8170`, 1 commit(s); files: `bench/oracle/internal/clang/cpp_test.go`, `bench/oracle/internal/clang/dump.go`, `bench/oracle/internal/clang/merge.go`, `bench/oracle/internal/clang/merge_test.go`, `bench/oracle/testdata/cppclang/extc.c`, `bench/oracle/testdata/cppclang/extc2.c`, `bench/oracle/testdata/cppclang/member.cpp`, `bench/oracle/testdata/cppclang/tmpl.cpp`
- **Gate:** **clear** at `7240ebd9355b` (gate v2, grounder v4, record `d20e084d83a8`); unknown 0; map over 9 file(s), 7.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 32, regressions 0; {'P2P': 26, 'new-pass': 4, 'skip': 2}; build {'P2P': 2}

**The doer's closing words:**

> Both defects are fixed and committed on `hobbes/S-20260916T153010Z-8170`.
> 
> **What changed**
> 
> - `dump.go` — `readRangeBegin` → `readRange`, returning `begin` and `end`. A `MemberExpr` callee now carries its range end (the member's own token) as `memberPos`, and `buildCall` takes the site *and* the spelling from it for a member call. `walkNode`'s construct-site `nodeRange` still takes `begin`. `finish` spells a call's `Callee` as `declKey` of the declaration it names, `CalleeName` staying the name as written.
> - `merge.go` — `declKey`'s fallback is `qualify(d.Class, d.Name)`: the mangled name where there is one, else `Class::Name`, else the bare name.
> - Fixtures: `member.cpp` (the `(m << 1\n << 2).str().get()` chain), `tmpl.cpp` (`A<T>::format_as` / `B<T>::format_as`), `extc.c` + `extc2.c` (the bare-name join across two units).
> - Tests: `TestCppMemberCallSitsAtItsMemberToken`, `TestCppTemplatePatternsKeyByTheirClass` and `TestCppBareNameKeysJoinAcrossUnits` in `cpp_test.go`, each with its own compile database; `TestDeclKeySpelling` in `merge_test.go`. The existing 25-site table kept its four sources and its 25 sites; seven member-call **columns** moved onto the member token (e.g. `p->area()` 25:10 → 25:13, `this->step()` 6:24 → 6:30), hand-recomputed from the sources and commented.
> 
> **What I ran** (from `/work/bench/oracle`)
> 
> - `go test ./internal/clang/ -count=1` — ok; 25 tests, the four new ones included, clang++ 18 present so nothing skipped. `gofmt -l .` clean, `go vet` clean.
> - `go test ./... -count=1` — the whole oracle module ok (15 packages), the C++ end-to-end test included.
> - Both defects verified to reproduce: with the member-token line disabled, the new test reports 0 sites at `member.cpp:26` and two member calls parked on the object's line 25; with the class fallback disabled, both patterns key as `format_as` and A's call resolves to `tmpl.cpp:14`, B's member — exactly H-29.
> 
> **Deviations**
> 
> - The member token positions a site only where the `MemberExpr` is a `CXXMemberCallExpr`'s callee, not for every `MemberExpr` callee as the amendment words it. A plain `CallExpr` over a member of function-pointer type (`o.fn(1)`, C's shape) keeps the whole expression's position: moving it re-keys C's sites, it breaks `TestCclangFixture`'s hand-keyed dynamic site at `lib.c:25`, and `clang_test.go` is outside the partition. The amendment says only the two C++ cells are re-run, which reads the same way.
> - `Decl.Mangled`'s and `Call.Callee`'s doc comments in `shard.go` still say the key is "the name otherwise"; `shard.go` is outside the partition, so they are now a word behind `declKey`.
> - Probed: clang gives `extern "C"` and `main` a `mangledName` equal to their own name, in C and in C++ alike, so their key is bare through the mangled branch rather than the fallback. The class-less fallback branch is covered by `TestDeclKeySpelling` instead; `extc.c`/`extc2.c` are run under both front ends (`-x c++` for the second), where `extern "C"` is live and `call_main` mangles.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Merged no-ff as `eec8141`; the doer's `e19f850` keeps its
  `hobbes-dispatch` authorship. The design was decided before the
  dispatch (ADR-113 §3's 2026-09-16 amendment, `7240ebd`), and each
  cause was probed in the image first, which is what kept the brief off
  two wrong hypotheses: a `MemberExpr` has no `loc` (the member token is
  its `range.end`), and a class template's *pattern* members are the
  ones carrying `Class` with no mangling.

  **Verified before merging.** The C++ fixture tests need clang++, which
  is in the image and not on this host, so a host run would have skipped
  them silently — my own pre-unit baseline did exactly that in 1.8 s.
  The package was run inside `hobbes-session:local` with `-v`, and all
  five tests RUN and PASS. The whole oracle module is green (11 ok, 0
  fail), `vet` and `gofmt` clean. One red on the first module run
  (`TestTSFixtureRefusesEveryPoisonedEdge`, "Cannot find module
  'typescript'") was mine, not the doer's: a fresh `git worktree add`
  has no gitignored `bench/oracle/ts/node_modules`. Proved by mounting
  that tree — green — and by the diff touching neither `internal/grade`
  nor `ts/`.

  **The existing hand-keyed table.** §10.8's P41 predicted the 25 sites
  stand. They do: every site and every *line* is unchanged, and seven
  member-call **columns** moved onto the member token. D-O4 carries
  columns for triage and outside the match, so grading is unaffected —
  this is the one place the unit edited existing hand-computed truth,
  and it is the right edit for the right reason.

  **The three deviations, all accepted.** (1) The member token positions
  a site only where the `MemberExpr` is a `CXXMemberCallExpr`'s callee,
  not for every `MemberExpr` callee as the amendment worded it. That is
  correct, not a shortfall: the same shape under a plain `CallExpr` is a
  call through a member of function-pointer type, which stays `dynamic`
  and is C's shape under ADR-110; widening it would re-key C's sites and
  break `TestCclangFixture`, which sits outside the partition. The ADR's
  wording is corrected to match the tree, not the reverse. (2)
  `shard.go`'s `Call.Callee` comment was left a word behind `declKey` —
  outside the partition, so fixed here by the developer. (3) Probed:
  clang gives `extern "C"` and `main` a `mangledName` equal to their own
  name, so they key bare through the *mangled* branch rather than the
  class-less fallback; the fallback is covered by `TestDeclKeySpelling`
  instead, and the ADR now says so.

  H-30 is a separate unit (the matcher); H-31 stays open by design — its
  `os.cc` half did not reproduce under a probe of that shape, so no rule
  was written from a synthetic that disagrees with the cell's evidence.
  The regrade of both C++ cells is the developer's, pre-registered in
  `oracle-grading.md` §10.8.
