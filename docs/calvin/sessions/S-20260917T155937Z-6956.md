# Harness session `S-20260917T155937Z-6956`

*2026-09-17 · `hobbes dispatch` v1 · Hobbes 0.2.39-beta · ADR-107.* The record above the review block is written by the harness; the review block is the developer's.

- **Task:** **C++ lane A reads an unchanged file from a per-file cache.** `cppsource._parse_file(rel, source)` is a pure function of the path, the bytes and the pipeline's code; keep its result under a key of exa (brief `/home/mmarrujo/.hobbes/sessions/S-20260917T155937Z-6956/brief.md`, task sha256 `ff399be8c6be`)
- **Parent:** `dfdc29b34e08` of `hobbes_public` — the working tree had uncommitted changes the session did not see
- **Doer:** 2.1.274 (Claude Code); model claude-opus-5; turns 58 of 80; result `success`; reported cost $3.6094 (the envelope's figure, on the subscription); wall 500.3 s; session exit 0
- **Egress:** allow api.anthropic.com:443; opened `api.anthropic.com:443`×3; refused `llm`×1
- **Policy:** 24 exec decision(s) — `allow`×18, `escalate`×6; escalated: `/bin/sh -c cd pipeline && cat > /tmp/probe.py <<'EOF'
from hobbes.extract import cppsource
src = open('/tmp/probe.cpp','rb').read()
p, err, dup = cppsource._par`; `/bin/sh -c cd pipeline && cat > /tmp/probe.py <<'EOF'
from hobbes.extract import cppsource
src = open('/tmp/probe.cpp','rb').read()
p, err, dup = cppsource._par → expired`; `/bin/sh -c cd pipeline && for f in tests/fixtures/minicpp/src/main.cpp tests/fixtures/minicpp/tests/test_shapes.cpp tests/fixtures/minicpp/src/util.cpp tests/fi`; `/bin/sh -c cd pipeline && for f in tests/fixtures/minicpp/src/main.cpp tests/fixtures/minicpp/tests/test_shapes.cpp tests/fixtures/minicpp/src/util.cpp tests/fi → expired`; `/bin/sh -c id -u`; `/bin/sh -c id -u → expired`; records: stream opened→closed
- **Edits:** 16 edit(s) to 9 file(s); first at 2.4 min after launch; files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/laneacache.py`, `pipeline/src/hobbes/extract/timings.py`, `pipeline/tests/conftest.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_laneacache.py`, `pipeline/tests/test_timings.py`
- **Branch:** `hobbes/S-20260917T155937Z-6956`, 1 commit(s); files: `pipeline/src/hobbes/cli.py`, `pipeline/src/hobbes/extract/__init__.py`, `pipeline/src/hobbes/extract/cppsource.py`, `pipeline/src/hobbes/extract/laneacache.py`, `pipeline/src/hobbes/extract/timings.py`, `pipeline/tests/conftest.py`, `pipeline/tests/test_cli.py`, `pipeline/tests/test_laneacache.py`, `pipeline/tests/test_timings.py`
- **Gate:** **clear** at `dfdc29b34e08` (gate v2, grounder v4, record `bbcc818c2fbd`); unknown 0; map over 89 file(s), 0.0% of their lines uncaptured; partition checked
- **Retention:** the doer's transcript and reasoning are not stored; this file and the session dir keep its output only. Recorded sessions are evaluation rows, never model training data.
- **Verify:** pass; tests 1121, regressions 0; {'skip': 18, 'P2P': 1086, 'new-pass': 16, 'uncollected': 1}

**The doer's closing words:**

> Done — committed as `c45515b`.
> 
> **What changed** (only the nine files in the partition):
> 
> - **`pipeline/src/hobbes/extract/laneacache.py`** (new): `ENABLE_ENV`/`enabled`, `KEEP_DAYS`, `FORMAT`, `store_root` (`<cache>/lanea/cpp`), `entry_path` (two-hex fan-out), a once-per-process `code_fingerprint` (every `hobbes/extract/*.py` by name and bytes, then `tree-sitter` and `tree-sitter-cpp` versions), `key`, the tagged `encode`/`decode` and `encode_record`/`decode_record`, `cached_parse`, `LEDGER`/`reset_ledger`/`summary`, `_sweep_once`/`sweep`. A record is written only if it decodes back **equal** to the object it came from; a bad entry is unlinked and parsed; `OSError` anywhere while storing is swallowed.
> - **`cppsource._read_and_parse`**: one line — `laneacache.cached_parse(rel, source, _parse_file)`. Nothing else in the walk moved.
> - **`extract/__init__.py`**: `laneacache.reset_ledger()` beside indexcache's.
> - **`timings.record`**: `lanea_cache: dict | None = None`, written as `"lanea_cache"`.
> - **`cli._cmd_ingest`**: the `lane A C++ file cache: …; C-160` line under the index-cache line, and the summary passed to `record(...)`.
> - **`tests/conftest.py`**: `HOBBES_LANEA_CACHE=0` unless a test carries the new `lanea_cache` marker, registered with `pytest_configure` (pyproject isn't mine to edit).
> - **Tests**: `test_laneacache.py` (13), plus two in `test_timings.py` and two in `test_cli.py`. No existing assertion moved.
> 
> **Deviations**
> 
> - `code_fingerprint()` returns `None` (caching off for the process) not only when a version cannot be read but also when a `hobbes/extract/*.py` cannot be read — the nearest thing that parses rather than keys on a partial fingerprint.
> - Three tests beyond the listed set: a missing fingerprint parses everywhere; the write-side equality assertion itself keeps nothing; the sweep runs once per process.
> 
> **What I ran** (`/work/pipeline`, its own venv)
> 
> - `tests/test_laneacache.py tests/test_cppsource.py tests/test_timings.py tests/test_cli.py` — **158 passed, 1 skipped** in 1.74 s.
> - Full suite — **1,738 passed, 13 skipped** in 51.59 s.
> 
> The one skip in my files is `test_a_store_that_cannot_be_written_never_fails_the_extraction`: this sandbox runs as root, which writes a 0o500 directory anyway, so it skips here and runs on your box.

## Review

*The developer's, after reading the diff. `gate`: `right-clear` | `right-block` | `false-block` | `missed` — was the verdict right; `missed` is an error the gate's classes cover that it let through. `outcome`: merged, reworked, or discarded.*

- gate: right-clear
- outcome: merged
- notes: Read in full against ADR-128 §4 and merged no-ff. The one deviation (an unreadable `hobbes/extract/*.py` also turns caching off) parses rather than trusts, as the brief asks. Checked: the tags cannot meet a real field (the encoder refuses them as keys); the write-side equality guard; a hit decodes a new object. The developer's checks after the merge: ScummVM lane A (lane B off) through the real store, cold and warm, each byte-identical to the pre-ADR-128 tree's extraction (`cmp`, 217 MB): C++ 144 s cold, 21.7 s warm; lane A 163 s and 39.5 s against 261 s; 19,948 records, 318 MB. fmt through `hobbes ingest` twice: 72 misses then 72 hits, `graph.json` and `tests.json` sha256-identical, the summary line and the timings log field present. Host: full pytest passes once this block is filled, the read-only-store test runs here (skipped as root in the session), `lane_b` 8 passed.
