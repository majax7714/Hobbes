# Template 7af3c18016d6 @ 19bddc9230fb (v0)

## Task

Add a --no-tests flag to `oracle go-rta` so a root too large to load together with its test packages can still be graded from its non-test roots; record it as the fix for defect H-9.

## Anchors — what in the task names repo structure, and how it was found

- `oracle go-rta` → bench/oracle/cmd/oracle/main.usage  *(matcher: literal; backtick → no node id or basename matches the two-token string; the literal is in the usage text at main.go:45)*
- `go-rta` → bench/oracle/cmd/oracle/main.main, bench/oracle/cmd/oracle/main.runGoRTA, bench/oracle/internal/gorta/gorta.Run  *(matcher: literal; a2; not an identifier in the graph (see unresolved) but a literal at main.go:28, main.go:66, gorta.go:195 — whether matcher 5 applies to a backticked non-identifier is a step-2 rule to decide)*
- `go-rta` → **unresolved**; nearest names in the graph (recorded, not used): gorta, runGoRTA, gorta_test, shortSHA
- `no-tests` → **unresolved**; nearest names in the graph (recorded, not used): test, note, load_tests, NotIngested
- `non-test` → **unresolved**; nearest names in the graph (recorded, not used): test, note, context, NotIngested
- `H-9.` → **unresolved**; nearest names in the graph (recorded, not used): H, sh, chat, both

## Already answered (round 1)

- **u1** UNRESOLVED — classify each unresolved term: `{"classes": {"go-rta": "refers", "no-tests": "new", "non-test": "not-code", "H-9.": "not-code"}, "refers_to": {"go-rta": "bench/oracle/cmd/oracle/main.runGoRTA"}}` *(by hand (step 1; what an orchestrator is expected to answer))*
- **c1** ANCHOR_CONFIRM — is `web/src/main` (matched by the bare word 'root') a site this task concerns?: `{"confirm": false}` *(by hand (step 1))*
- **c2** ANCHOR_CONFIRM — is `go/internal/policy/load` (matched by the bare word 'load') a site this task concerns?: `{"confirm": false}` *(by hand (step 1))*
- **c3** ANCHOR_CONFIRM — is `go/internal/knowledge/knowledge` (matched by the bare word 'test') a site this task concerns?: `{"confirm": false}` *(by hand (step 1))*
- **c4** ANCHOR_CONFIRM — is `web/src/lib/graphModel` (matched by the bare word 'packages') a site this task concerns?: `{"confirm": false}` *(by hand (step 1))*

## Holes to fill

Answer each hole by id, in the shape shown. `"unchanged"` is an answer. Holes of a type in ['CALLER_UPDATE', 'MODULE_REGION', 'TEST_EXPECTATION', 'COCHANGE_TOUCH'] may be answered together with `patterns: {"<TYPE>": "unchanged"}`.

### h1 · SIGNATURE — the new signature, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:37-41`
Why this hole: anchor = a2 → callee of main.runGoRTA; edge = main.runGoRTA uses gorta.Options @ main.go:72; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; type = type Options struct

```
37  type Options struct {
38  	Repo   string
39  	Module string
40  	Tags   []string
41  }
```

Answer shape: `"unchanged" | {"signature": "<one line>"}`

### h2 · BODY — code for this symbol span, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:37-41`
Why this hole: anchor = a2; edge = as h1; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
37  type Options struct {
38  	Repo   string
39  	Module string
40  	Tags   []string
41  }
```

Answer shape: `"unchanged" | {"code": "<the whole span, rewritten>"}`

### h3 · SIGNATURE — the new signature, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:47-47`
Why this hole: anchor = a2 → callee of main.runGoRTA; edge = main.runGoRTA calls gorta.Run @ main.go:76; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; type = func Run(o Options) (*edges.OracleExport, error)

```
47  func Run(o Options) (*edges.OracleExport, error) {
```

Answer shape: `"unchanged" | {"signature": "<one line>"}`

### h4 · BODY — code for this symbol span, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:47-215`
Why this hole: anchor = a2; edge = as h3; the literal `go-rta` at gorta.go:195; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
 47  func Run(o Options) (*edges.OracleExport, error) {
 48  	repo, err := filepath.Abs(o.Repo)
 49  	if err != nil {
 50  		return nil, err
 51  	}
 52  	dir := filepath.Join(repo, o.Module)
 53  	cfg := &packages.Config{
 54  		Mode: packages.NeedName | packages.NeedFiles | packages.NeedImports |
 55  			packages.NeedDeps | packages.NeedTypes | packages.NeedSyntax |
 56  			packages.NeedTypesInfo | packages.NeedModule,
 57  		Dir:   dir,
 58  		Tests: true,
 59  	}
 60  	if len(o.Tags) > 0 {
 61  		cfg.BuildFlags = []string{"-tags=" + strings.Join(o.Tags, ",")}
 62  	}
 63  	pkgs, err := packages.Load(cfg, "./...")
 64  	if err != nil {
 65  		return nil, fmt.Errorf("load %s: %w", dir, err)
 66  	}
 67  	var loadErrs []string
 68  	packages.Visit(pkgs, nil, func(p *packages.Package) {
 69  		for _, e := range p.Errors {
 70  			loadErrs = append(loadErrs, e.Error())
 71  		}
 72  	})
 73  	if len(loadErrs) > 0 {
 74  		return nil, fmt.Errorf("load %s: %d error(s), first: %s", dir, len(loadErrs), loadErrs[0])
 75  	}
 76  	prog, _ := ssautil.AllPackages(pkgs, ssa.InstantiateGenerics)
 77  	prog.Build()
 78  
 79  	var roots []*ssa.Function
 80  	var rootNames []string
 81  	for _, p := range prog.AllPackages() {
 82  		if p.Pkg.Name() != "main" {
 83  			continue
 84  		}
 85  		// Root at init as well as main: the runtime calls init, main never
 86  		// does, and the synthesized test main's table of Test functions is
 87  		// address-taken in its init — without it no test is reachable.
 88  		// (x/tools' own `callgraph` command roots the same way.)
 89  		if fn := p.Func("main"); fn != nil {
 90  			roots = append(roots, fn)
 91  			rootNames = append(rootNames, p.Pkg.Path())
 92  			if ini := p.Func("init"); ini != nil {
 93  				roots = append(roots, ini)
 94  			}
 95  		}
 96  	}
 97  	sort.Strings(rootNames)
 98  	if len(roots) == 0 {
 99  		return nil, fmt.Errorf("%s: no main functions (binaries or tests) to root RTA at", dir)
100  	}
101  	res := rta.Analyze(roots, true)
102  
103  	rel := func(pos token.Pos) (edges.Pos, int, bool) {
104  		if !pos.IsValid() {
105  			return edges.Pos{}, 0, false
106  		}
107  		p := prog.Fset.Position(pos)
108  		r, err := filepath.Rel(repo, p.Filename)
109  		if err != nil || strings.HasPrefix(r, "..") {
110  			return edges.Pos{Path: p.Filename, Line: p.Line}, p.Column, false
111  		}
112  		return edges.Pos{Path: filepath.ToSlash(r), Line: p.Line}, p.Column, true
113  	}
114  	target := func(fn *ssa.Function) (edges.Target, bool) {
115  		if fn == nil {
116  			return edges.Target{}, false
117  		}
118  		if o := fn.Origin(); o != nil {
119  			fn = o
120  		}
121  		pos, _, inRepo := rel(fn.Pos())
122  		if !fn.Pos().IsValid() {
123  			return edges.Target{}, false
124  		}
125  		return edges.Target{Pos: pos, Name: fn.String(), External: !inRepo, Closure: fn.Parent() != nil}, true
126  	}
127  
128  	files := map[string]bool{}
129  	for _, p := range pkgs {
130  		for _, f := range p.GoFiles {
131  			if r, err := filepath.Rel(repo, f); err == nil && !strings.HasPrefix(r, "..") && underModule(filepath.ToSlash(r), o.Module) {
132  				files[filepath.ToSlash(r)] = true
133  			}
134  		}
135  	}
136  
137  	type key struct {
138  		pos edges.Pos
139  		col int
140  	}
141  	sites := map[key]*edges.Site{}
142  	seenTarget := map[key]map[edges.Pos]bool{}
143  	for fn, node := range res.CallGraph.Nodes {
144  		if fn == nil || fn.Synthetic != "" || fn.Pkg == nil {
145  			continue
146  		}
147  		if _, _, inRepo := rel(fn.Pos()); !inRepo {
148  			continue
149  		}
150  		bySite := map[ssa.CallInstruction][]*ssa.Function{}
151  		for _, e := range node.Out {
152  			if e.Site == nil {
153  				continue
154  			}
155  			bySite[e.Site] = append(bySite[e.Site], unwind(res.CallGraph, e.Callee.Func, 0)...)
156  		}
157  		for _, b := range fn.Blocks {
158  			for _, ins := range b.Instrs {
159  				call, ok := ins.(ssa.CallInstruction)
160  				if !ok {
161  					continue
162  				}
163  				pos, col, inRepo := rel(call.Pos())
164  				if !inRepo || !underModule(pos.Path, o.Module) {
165  					continue
166  				}
167  				k := key{pos, col}
168  				s := sites[k]
169  				if s == nil {
170  					s = &edges.Site{Pos: pos, Col: col, Caller: fn.String(), Mode: "static"}
171  					common := call.Common()
172  					if common.IsInvoke() {
173  						s.Mode = "dynamic"
174  						if m := common.Method; m != nil {
175  							ip, _, inRepo := rel(m.Pos())
176  							s.Interface = &edges.Target{Pos: ip, Name: qualified(m), External: !inRepo}
177  						}
178  					} else if common.StaticCallee() == nil {
179  						s.Mode = "dynamic"
180  					}
181  					sites[k] = s
182  					seenTarget[k] = map[edges.Pos]bool{}
183  				}
184  				for _, callee := range bySite[call] {
185  					t, ok := target(callee)
186  					if !ok || seenTarget[k][t.Pos] {
187  						continue
188  					}
189  					seenTarget[k][t.Pos] = true
190  					s.Targets = append(s.Targets, t)
191  				}
192  			}
193  		}
194  	}
195  	out := &edges.OracleExport{Oracle: "go-rta", Kind: "reachability", Module: o.Module, Roots: rootNames, Tags: o.Tags}
196  	for f := range files {
197  		out.Files = append(out.Files, f)
198  	}
199  	sort.Strings(out.Files)
200  	for _, s := range sites {
201  		sort.Slice(s.Targets, func(i, j int) bool { return s.Targets[i].Pos.Key() < s.Targets[j].Pos.Key() })
202  		out.Sites = append(out.Sites, *s)
203  	}
204  	sort.Slice(out.Sites, func(i, j int) bool {
205  		a, b := out.Sites[i], out.Sites[j]
206  		if a.Pos != b.Pos {
207  			if a.Pos.Path != b.Pos.Path {
208  				return a.Pos.Path < b.Pos.Path
209  			}
210  			return a.Pos.Line < b.Pos.Line
211  		}
212  		return a.Col < b.Col
213  	})
214  	return out, nil
215  }
```

Answer shape: `"unchanged" | {"code": "<the whole span, rewritten>"}`

### h5 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/cmd/oracle/main.go:65-81`
Why this hole: anchor = a1, a2; edge = calls gorta.Run @ 76; uses gorta.Options @ 72; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
65  func runGoRTA(args []string) error {
    … (16 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### h7 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/internal/gorta/gorta_test.go:41-61`
Why this hole: anchor = a2; edge = gorta_test.TestMinigoExact calls gorta.Run @ gorta_test.go:42; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
41  func TestMinigoExact(t *testing.T) {
42  	o, err := Run(Options{Repo: fixtures + "/minigo", Module: "."})
    … (19 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### h8 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/internal/gorta/gorta_test.go:65-78`
Why this hole: anchor = a2; edge = gorta_test.TestTwomodAppExact calls gorta.Run @ gorta_test.go:66; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
65  func TestTwomodAppExact(t *testing.T) {
66  	o, err := Run(Options{Repo: fixtures + "/twomod", Module: "app"})
    … (12 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### h9 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/internal/gorta/gorta_test.go:82-105`
Why this hole: anchor = a2; edge = gorta_test.TestTwomodAppTwoSitesOneLine calls gorta.Run @ gorta_test.go:83; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
 82  func TestTwomodAppTwoSitesOneLine(t *testing.T) {
 83  	o, err := Run(Options{Repo: fixtures + "/twomod", Module: "app"})
     … (22 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### h10 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/internal/gorta/gorta_test.go:110-135`
Why this hole: anchor = a2; edge = gorta_test.TestTwomodLibDynamic calls gorta.Run @ gorta_test.go:111; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
110  func TestTwomodLibDynamic(t *testing.T) {
111  	o, err := Run(Options{Repo: fixtures + "/twomod", Module: "lib"})
     … (24 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### h11 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/internal/gorta/gorta_test.go:137-141`
Why this hole: anchor = a2; edge = gorta_test.TestNoRootsIsAnError calls gorta.Run @ gorta_test.go:138; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
137  func TestNoRootsIsAnError(t *testing.T) {
138  	if _, err := Run(Options{Repo: fixtures + "/twomod", Module: "lib/nonexistent"}); err == nil {
     … (3 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### h12 · CALLER_UPDATE — does this caller change; how
Span: `bench/oracle/internal/gorta/gorta_test.go:147-162`
Why this hole: anchor = a2; edge = gorta_test.TestGenericInstantiationFoldsToOrigin calls gorta.Run @ gorta_test.go:148; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
147  func TestGenericInstantiationFoldsToOrigin(t *testing.T) {
148  	o, err := Run(Options{Repo: "../../testdata/generic", Module: "."})
     … (14 more lines; a "yes" is shown the whole span next)
```

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the caller span, rewritten>"}`

### t1 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/gorta/gorta_test.go:41-61`
Why this hole: anchor = a2; edge = testmap: bench/oracle/internal/gorta/gorta_test.go::TestMinigoExact reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
41  func TestMinigoExact(t *testing.T) {
    … (20 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t2 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/gorta/gorta_test.go:65-78`
Why this hole: anchor = a2; edge = testmap: bench/oracle/internal/gorta/gorta_test.go::TestTwomodAppExact reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
65  func TestTwomodAppExact(t *testing.T) {
    … (13 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t3 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/gorta/gorta_test.go:82-105`
Why this hole: anchor = a2; edge = testmap: bench/oracle/internal/gorta/gorta_test.go::TestTwomodAppTwoSitesOneLine reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
 82  func TestTwomodAppTwoSitesOneLine(t *testing.T) {
     … (23 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t4 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/gorta/gorta_test.go:110-135`
Why this hole: anchor = a2; edge = testmap: bench/oracle/internal/gorta/gorta_test.go::TestTwomodLibDynamic reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
110  func TestTwomodLibDynamic(t *testing.T) {
     … (25 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t5 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/gorta/gorta_test.go:137-141`
Why this hole: anchor = a2; edge = testmap: bench/oracle/internal/gorta/gorta_test.go::TestNoRootsIsAnError reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
137  func TestNoRootsIsAnError(t *testing.T) {
     … (4 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t6 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/gorta/gorta_test.go:147-162`
Why this hole: anchor = a2; edge = testmap: bench/oracle/internal/gorta/gorta_test.go::TestGenericInstantiationFoldsToOrigin reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
147  func TestGenericInstantiationFoldsToOrigin(t *testing.T) {
     … (15 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t7 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/grade/grade_test.go:34-48`
Why this hole: anchor = a2; edge = testmap: grade_test.go::TestMinigoAllConfirmed reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition: state the expectation, do not rewrite the test

```
34  func TestMinigoAllConfirmed(t *testing.T) {
    … (14 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t8 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/grade/grade_test.go:50-58`
Why this hole: anchor = a2; edge = testmap: grade_test.go::TestTwomodAppAllConfirmed reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition: state the expectation, do not rewrite the test

```
50  func TestTwomodAppAllConfirmed(t *testing.T) {
    … (8 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### t9 · TEST_EXPECTATION — what this test should now expect
Span: `bench/oracle/internal/grade/grade_test.go:63-85`
Why this hole: anchor = a2; edge = testmap: grade_test.go::TestTwomodLibDynamicMiss reaches gorta.Run; tier = semantic
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition: state the expectation, do not rewrite the test

```
63  func TestTwomodLibDynamicMiss(t *testing.T) {
    … (22 more lines; a "yes" is shown the whole span next)
```

Answer shape: `"unchanged" | {"expectation": "<prose>", "code"?: "<the test, rewritten>"}`

### m1 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:1-15`
Why this hole: kind = head; between = ['(file start)', 'Options']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
 1  // Package gorta is the Go reachability oracle (design §4, D-O1): load a
 2  // module with its tests, build SSA, run RTA from every main (binaries
 3  // and the synthesized test mains), and export every in-repo call site
 4  // of every reachable non-synthetic function with the targets RTA
 5  // resolved it to.
 6  //
 7  // Normalisation, as the design demands: synthetic functions (wrappers,
 8  // thunks, bound closures, generic instantiations) are unwound to the
 9  // source declaration before they are reported; closures are identified
10  // by declaration position only; test-variant packages (the same file
11  // compiled twice under `Tests: true`) collapse by position. Package
12  // initialisers are in the graded set — `init` functions are reachable
13  // from every main and their sites are real calls.
14  package gorta
15  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m2 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:16-32`
Why this hole: kind = imports; between = ['(file start)', 'Options']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
16  import (
17  	"fmt"
18  	"go/token"
19  	"go/types"
20  	"os"
21  	"path/filepath"
22  	"sort"
23  	"strings"
24  
25  	"golang.org/x/tools/go/callgraph"
26  	"golang.org/x/tools/go/callgraph/rta"
27  	"golang.org/x/tools/go/packages"
28  	"golang.org/x/tools/go/ssa"
29  	"golang.org/x/tools/go/ssa/ssautil"
30  
31  	"github.com/majax7714/Hobbes/bench/oracle/internal/edges"
32  )
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m3 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:33-36`
Why this hole: kind = gap; between = ['(imports)', 'Options']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
33  
34  // Options selects one cell: the repo root and the module directory
35  // (repo-relative) whose packages are loaded with `./...`. Tags are extra
36  // build tags; the default tag set of the box is always in force.
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m4 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:42-46`
Why this hole: kind = gap; between = ['Options', 'Run']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
42  
43  // Run loads, analyses and exports one cell. Roots are the main and init
44  // functions of every main package in the loaded program — binaries and
45  // the test mains `Tests: true` synthesizes — so a library module is
46  // analysed through its test binaries (the cell's Roots say which).
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m5 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:216-220`
Why this hole: kind = gap; between = ['Run', 'unwind']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
216  
217  // unwind follows synthetic callees (wrappers, thunks, bound closures) to
218  // the source functions they end in; generic instantiations are folded by
219  // the caller through Origin. Depth-bounded: a wrapper chain deeper than
220  // four is not a shape SSA produces.
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m6 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:245-248`
Why this hole: kind = gap; between = ['unwind', 'underModule']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
245  
246  // underModule scopes a cell: sites and loaded files are reported only
247  // under the module directory, while targets may land anywhere in the
248  // repo (a replaced sibling module is inside the program — C-33's join).
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m7 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:253-253`
Why this hole: kind = gap; between = ['underModule', 'qualified']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
253  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m8 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/internal/gorta/gorta.go:260-261`
Why this hole: kind = gap; between = ['qualified', 'Exists']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
260  
261  // Exists reports whether dir is a directory (for CLI argument checks).
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m9 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:1-7`
Why this hole: kind = head; between = ['(file start)', 'main']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
1  // Command oracle is the oracle-grading lane's one binary (ADR-089,
2  // docs/oracle-grading.md): `export` reads a Hobbes graph.json into graded
3  // edges, `go-rta` runs the Go reachability oracle on one module, and
4  // `grade` matches the two and prints the cell's report. A cell is data —
5  // a repo, a module directory, a graph — never a script of its own.
6  package main
7  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m10 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:8-18`
Why this hole: kind = imports; between = ['(file start)', 'main']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
 8  import (
 9  	"encoding/json"
10  	"flag"
11  	"fmt"
12  	"os"
13  
14  	"github.com/majax7714/Hobbes/bench/oracle/internal/edges"
15  	"github.com/majax7714/Hobbes/bench/oracle/internal/export"
16  	"github.com/majax7714/Hobbes/bench/oracle/internal/gorta"
17  	"github.com/majax7714/Hobbes/bench/oracle/internal/grade"
18  )
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m11 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:19-19`
Why this hole: kind = gap; between = ['(imports)', 'main']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
19  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m12 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:40-40`
Why this hole: kind = gap; between = ['main', 'usage']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
40  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m13 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:49-49`
Why this hole: kind = gap; between = ['usage', 'runExport']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
49  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m14 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:64-64`
Why this hole: kind = gap; between = ['runExport', 'runGoRTA']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
64  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m15 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:82-82`
Why this hole: kind = gap; between = ['runGoRTA', 'runGrade']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
82  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m16 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:104-104`
Why this hole: kind = gap; between = ['runGrade', 'read']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
104  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m17 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:115-115`
Why this hole: kind = gap; between = ['read', 'write']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
115  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### m18 · MODULE_REGION — code for this module-level region, or unchanged
Span: `bench/oracle/cmd/oracle/main.go:128-128`
Why this hole: kind = gap; between = ['write', 'splitComma']; anchor = every file the structure pass touches
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

```
128  
```

Answer shape: `"unchanged" | {"code": "<the whole region, rewritten>"}`

### x1 · COCHANGE_TOUCH — is `bench/oracle/internal/grade/grade.go` touched by this change; why
Why this hole: cochange = co-committed with gorta.go ×2 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### x2 · COCHANGE_TOUCH — is `bench/oracle/internal/grade/grade_test.go` touched by this change; why
Why this hole: cochange = co-committed with gorta.go ×2, main.go ×2 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### x3 · COCHANGE_TOUCH — is `bench/oracle/internal/export/export.go` touched by this change; why
Why this hole: cochange = co-committed with main.go ×3 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### x4 · COCHANGE_TOUCH — is `bench/oracle/run-cell.sh` touched by this change; why
Why this hole: cochange = co-committed with main.go ×3 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### x5 · COCHANGE_TOUCH — is `bench/oracle/README.md` touched by this change; why
Why this hole: cochange = co-committed with main.go ×2, gorta.go ×1 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### x6 · COCHANGE_TOUCH — is `bench/oracle/internal/edges/edges.go` touched by this change; why
Why this hole: cochange = co-committed with main.go ×2, gorta.go ×1 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### x7 · COCHANGE_TOUCH — is `bench/oracle/internal/export/export_test.go` touched by this change; why
Why this hole: cochange = co-committed with main.go ×2 in the last 200 commits; anchor = a1, a2
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']; note = outside the write partition unless you say why it must move

Answer shape: `{"decision": "yes"|"no", "reason": "<why>", "body"?: "<the file, rewritten>"}`

### n1 · NEW_SYMBOL — where does the new thing `no-tests` go — a new top-level symbol (name, file, position, body), or covered by another hole's fill?
Why this hole: anchor = u1: `no-tests` declared new
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

Answer shape: `{"name": "<name>", "file": "<path>", "after_symbol"?: "<node id>", "region"?: "<hole id>", "body": "<code>"} | {"covered_by": ["<hole id>", ...]}`

### f1 · FREEFORM — anything the template did not anticipate
Why this hole: anchor = none
Constraints: write_partition = ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']

Answer shape: `"none" | {"code": "<code>", "span": {"path": "<path>", "start": <line>, "end": <line>}} | [<one such object per unanticipated change>, ...]`

## Closed before you (a pruning rule fired)

- **h6** CALLER_UPDATE — partition: bench/oracle/internal/grade/grade_test.go is outside the write partition

## Neighborhood (one hop out — context, not holes)

- `bench/oracle/internal/edges/edges.OracleExport` — the export gorta.Run builds at 195 (fields Oracle, Kind, Module, Roots, Tags, Files); used, not called `bench/oracle/internal/edges/edges.go:70-78`
- `bench/oracle/cmd/oracle/main.write` — runGoRTA's output path `bench/oracle/cmd/oracle/main.go:116-127`
- `bench/oracle/cmd/oracle/main.splitComma` — how --tags is parsed `bench/oracle/cmd/oracle/main.go:129-141`
- `ext:golang.org/x/tools/go/packages` — packages.Config{Tests: …} is what decides whether test packages load (gorta.go:58)

## Constraints on the whole answer

- **write_partition**: ['bench/oracle/cmd/oracle/main.go', 'bench/oracle/internal/gorta/gorta.go', 'bench/oracle/internal/gorta/gorta_test.go']
- **invariants_in_scope**: I-1, I-3, I-7, I-9 (scope '.', soft)
- **guarantees**: never read or write .tfstate; never git push (P10, emitted first, cannot be widened)
- **policy**: read-only, escalate-by-default floor; writes only over the partition

## How to answer

One JSON document:

```
{"fills": {"<hole id>": <fill>, ...}, "patterns": {"<TYPE>": "unchanged", ...}}
```

Answer with `patterns` for every type you leave entirely unchanged and list under `fills` only the holes you change or that take no pattern — a long template answered hole by hole does not fit in one reply. A new file is a NEW_SYMBOL with `file` set to the new path and `region: "eof"`, or a FREEFORM entry with span `{path, 1, 0}`.
